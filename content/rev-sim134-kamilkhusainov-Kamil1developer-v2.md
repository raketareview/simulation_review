https://github.com/Kamil1developer/Simulation  
[Kamil KHusainov]

После первого рефакторинга стало значительно лучше. Но есть над чем поработать.  

Ревью на первую версию программы [тут](https://github.com/raketareview/simulation_review/blob/master/content/rev-sim132-kamilkhusainov-Kamil1developer.md).

## ХОРОШО

+ 👍 Реализовано пауза/пуск во время работы
+ 👍 Алгоритм поиска A*

## ЗАМЕЧАНИЯ

**1. Нейминг**

- Геттер должен называться так же, как возвращаемое им поле + префикс "get"
```java
public int getHP() {
  return countHp;
}

//ПРАВИЛЬНО:
public int getHp() {
  return hp;
}
```

- КОНСТАНТЫ_НУЖНО_ПИСАТЬ_СТИЛЕМ_UPPER_SNAKE
```java
private static final Integer attackPower = 50;

//ПРАВИЛЬНО:
private static final Integer ATTACK_POWER = 50;
```

- Избыточно. Мы без дополнительных уточнений понимаем, что в классе поиска пути метод поиска ищет именно путь
```java
public class PathFinder {
  public Position findPath(GameMap gameMap, Creature creature)  {...}
}

//ПРАВИЛЬНО:
public class PathFinder {
  public Position find(GameMap gameMap, Creature creature)  {...}
}
```

- Для обозначения геометрических размеров, лучше использовать стандартные названия: ширина и высота
```java
public class GameMap {
  private final int rowCount;
  private final int columnCount;
  //...
}

//ЛУЧШЕ:
public class GameMap {
  private final int height;
  private final int width;
  //...
}
```

- Если в проекте есть класс `GameMap`, то все переменные с именем, включающим это название, должны быть экземплярами этого класса.
Если разные концепции называются одним и тем же именем, это приводит к путанице
```java
public class GameMap {
  private Map<Position, Entity> gameMap = new HashMap<>();  
  //...
}

//ПРАВИЛЬНО:
public class GameMap {
  private Map<Position, Entity> entities = new HashMap<>();
  //...
}
```

- Перечни в виде массивов, коллекций и мап, нужно писать во множественном числе:
```java
Map<Position, Entity> gameMapHerbivore

//ПРАВИЛЬНО:
Map<Position, Entity> gameMapHerbivores
```

- Венгерская ноттация.

В названии переменных не пиши тип данных, к которым они относится. И вообще не употребляй венгерскую ноттацию.  
Название переменной должно отвечать на вопрос что хранит переменная, а не как хранит.
```java
Map<Position, Entity> gameMap = new HashMap<>();
Map<Position, Entity> gameMapGrass = new HashMap<>();

//ПРАВИЛЬНО:
Map<Position, Entity> entities = new HashMap<>();  //или positionWithEntities
Map<Position, Entity> grasses = new HashMap<>();  //или positionWithGrasses
```

- Если название говорит, что поле хранит траву, оно должно хранить траву, а не предка травы
```java
Map<Position, Entity> gameMapGrass = new HashMap<>();

//ПРАВИЛЬНО:
Map<Position, Grass> grasses = new HashMap<>();
```

- Если метод что-то проверяет(check), то результат проверки он должен вернуть в виде boolean.  
Если метод не возвращает результат проверки, значит он не должен так называться
```java
void checkCountGrasses()
```

- Название обманывает. 

Координата не может быть путем, потому что путь это последовательность координат между двумя точками.  
Даже если путь состоит из одной координаты, все равно эта координата должны быть в составе коллекции(листа, массива etc)
```java
Position path = pathFinder.findPath(gameMap, (Creature) creature);

//ПРАВИЛЬНО:
Position step = pathFinder.findPath(gameMap, (Creature) creature);
```

- Название переменной не соответствует ее типу данных 
```java
Entity creature = entry.getValue();
Object entity

//ПРАВИЛЬНО:
Entity entity = entry.getValue();
Object object
```
*Oracle Java code conventions, part."Naming conventions"*  
*Мартин, "Чистый код", гл.2*  
*Ютуб, Немчинский "Как называть переменные, методы и классы?"*

**2. record's для карты**

+ 👍 все ок
```java
record Position(int row,int column) 
record Size(int width, int height)
```

**3. class GameMap**

- Если объект во время работы программы не пересоздается, а только меняет свое внутреннее состояние, то он должен быть final
```java
private Map<Position, Entity> gameMap = new HashMap<>();
private Map<Position, Entity> gameMapGrass = new HashMap<>();

//ПРАВИЛЬНО:
private final Map<Position, Entity> gameMap = new HashMap<>();
private final Map<Position, Entity> gameMapGrass = new HashMap<>();
```
Посмотри- там даже IDE подчеркивает желтым- это она подсказывает, что поля можно сделать final.

- Нарушение OCP.

Не должно быть отдельных коллекций для хранения определенного типа существ
```java
private Map<Position, Entity> gameMap = new HashMap<>();  <-- ЭТОГО ДОСТАТОЧНО ДЛЯ ФУНКЦИОНИРОВАНИЯ КАРТЫ
private Map<Position, Entity> gameMapGrass = new HashMap<>();  <-- ЛИШНЕЕ
private Map<Position, Entity> gameMapHerbivore = new HashMap<>();  <-- ЛИШНЕЕ
```
Это нарушение чеклиста ТЗ:
```
Проблемы и ошибки в коде: ...параллельные коллекции для разных типов существ (для каждого типа своя коллекция)
```
Проблема параллельных коллекций состоит в том, что при развитии программы и добавлении новых типов существ, придется менять код в Карте.  
Например, при добавлении в проект класса Птица, придется добавить в карту новую коллекцию для хранения птиц:
```java
private Map<Position, Entity> gameMapBird = new HashMap<>();
```
То есть класс становится открытым для изменений, что нарушает принцип OCP:  
"Класс должен быть закрыт для изменений и открыт для расширения".

- По той же причине не должно быть отдельных методов для работы с отдельными типами существ
```java
public void putGrass(Grass grass) 
public void putHerbivore(Herbivore herbivore)

//ПРАВИЛЬНО:
public void putEntity(Entity enity) 
```
То же самое тут касается многих других методов, например:
```java
public Map<Position, Entity> getGameMapHerbivore()
public Map<Position, Entity> getGameMapGrass()
```

- Нарушение SRP, методы чужих ответственностей. 

Карта должна только хранить существа и обеспечить базовые операции с ними: вставить, выдать одно существо и список всех хранимых существ, удалить.
Если какой-то метод не нужен для обеспечения этого функционала, значит он не принадлежит к ответственности карты, а принадлежит к чужой ответственности.
Здесь методы чужих ответственностей: определить тип существа по координате.

Наверное, для проекта в целом полезно иметь метод, который определяет тип существа по координате.  
Но этот процесс не имеет никакого отношения к единой ответственности карты- хранению существ в себе.  
Для организации хранения/удаления/выдачи существ, карте не нужно определять тип хранимого существа.  

Методы чужих ответственностей должны находиться в тех классах, в интересах которых они работают.  
Если один и тот же метод используют разные классы, то метод нужно вынести в отдельный класс, например, BoardUtils.

- Никогда не возвращай null
```java
private Map<Position, Entity> gameMap = new HashMap<>();

public Entity getEntity(Position position) {
  return gameMap.get(position);  <-- может вернуть null
}
```
Вот так метод может вернуть null:
```java
GameMap gameMap = new GameMap(10, 10);
Position position = new Position(5, 5);
Entity entity = gameMap.getEntity(position);
System.out.printf("Entity in position %s: %s", position, entity);

//РЕЗУЛЬТАТ:
Entity in position Position[row=5, column=5]: null
```
Возврат null повышает риск возникновения NullPointerException в программе.  
*Мартин, "Чистый код", гл.7.7-8*  
*Ютуб, Немчинский "Почему нельзя возвращать NULL?"*

- Здесь удаляется не позиция, а существо, находящееся на позиции. Ключевое здесь- существо
```java
public void removePositionOfEntity(Position position) {
  gameMap.remove(position);
}

//ПРАВИЛЬНО:
public void removeEntity(Position position) {
  gameMap.remove(position);
}
```

- При всех операциях с участием координаты(добавить, выдать, удалить и т.д.), нужно проверять координату на корректность.

Если координата некорректна(находится вне пределов карты), нужно бросать исключение.  
Сейчас, если у карты спросить, свободна ли ячейка с координатой (+100500, -100500), то карта скажет, что свободна
```java
private Map<Position, Entity> gameMap = new HashMap<>();

public boolean isOccupied(Position pos) {
  return gameMap.containsKey(pos);
}
```
А правильный ответ- ячейки с такой координатой в карте нет вообще.

Ближайшая аналогия- стандартные хранилища типа List и массива. 
При попытке обратитья к ним по несуществующему индексу, бросается исключение.

+ 👍 Хорошо, что возвращается копия мапы, а не оригинал. Тем самым соблюдается инкапсуляция и защищается внутреннее устройство класса
```java
public Map<Position, Entity> getMap() {
  return new HashMap<>(gameMap);
}
```

- Избыточный алгоритм. У каждого объекта есть стандартный метод `getClass()`, который возвращает его класс
```java
public Class<?> getType(int row, int column) {
  Position position = new Position(row, column);
  for (Map.Entry<Position, Entity> entry : gameMap.entrySet()) {
    if (position.equals(entry.getKey())) {
      String name = entry.getValue().getClass().getSimpleName();
      switch (name) {
          case "Herbivore":
            return Herbivore.class;
          //...  
        }
      }
    }
    return Optional.class;
}

//ПРАВИЛЬНО:
public Class<?> getType(Position position) {
  //провалидировать координату, если не в пределах карты- кинуть исключение
  Entity entity = gameMap.get(position);
  if(entity == null) {
    //бросить исключение
  }
  return entity.getClass();
}
```
Другое дело, что этого метода в этом классе быть вообще не должно.

- Нарушение DRY, дублирование кода. Два метода, которые делают одно и то же- возвращают мапу с определенным типом существ
```java
public Map<Position, Entity> getGameMapHerbivore() {
  //7 строк кода
}

public Map<Position, Entity> getGameMapGrass() {
  //7 строк кода
}
```
Достаточно сделать универсальный метод:
```java
public<T extends Entity> Map<Position, T> getEntitiesBy(Class<T> clazz) {
  Map<Position, T> result = new HashMap<>();
  for (Map.Entry<Position, Entity> entry : gameMap.entrySet()) {
    if (clazz.isInstance(entry.getValue())) {
      result.put(entry.getKey(), (T) entry.getValue());
    }
  }
  return result;
}
```

**4. class PathFinder**

- Нарушение инкапсуляции. Всегда явно указывай область видимости и final, если переменная final
```java
GameMap gameMap;
Map<Entity, Integer> hScores = new HashMap<>();

//ПРАВИЛЬНО:
private GameMap gameMap;
private final Map<Entity, Integer> hScores = new HashMap<>();
```

- Этот класс не ищет путь.

Путь это последовательность точек от точки старта до точки финиша.  
Этот класс не ищет путь. Его единственный публичный метод возвращает одиночную координату
```java
public Position findPath(GameMap gameMap, Creature creature)
```
Как с помощью этого класса искать путь, совершенно неясно.

Поиск должен искать путь между точками старта и финиша, согласно алгоритма A*
```java
public List<Position> find(GameMap gameMap, Position from, Position to) {...}
```

Или поиск пути должен искать путь от точки старта до точки, соответствующей заданным условиям согласно алгоритма BFS.  
В данном случае- до точки, в которой находится существо нужного класса
```java
public List<Position> find(GameMap gameMap, Position start, Class<? extends Entity> target) {
  //ищет путь на карте world от точки start
  //до точки, где находится существо нужного класса(напр. target == Grass.class)
}
```

Так же, поиск пути по алгоритму A* может использовать ту же сигнатуру, что и BFS.  
Тогда поиск пути в нем интерпритируется не как поиск пути между двумя точками, а как поиск цели и прокладывание пути к ней.  
Например: 
```java
public List<Position> find(GameMap gameMap, Position start, Class<? extends Entity> target) {
  //ищет координату цели
  //ищет путь по алгоритму A* между точкой стартой и точкой цели
}
```
Плюсы- унификация сигнатуры разных алгоритмов поиска для полиморфизма.  
То есть, можно сделать семейство родственных классов поиска, объединенных общим интерфейсом.

- Никогда не возвращай null
```java
private Map.Entry<Position, Integer> sortedFList(...) throws NoSuchElementException {
  //...
  return null;  
}
```

- Нарушение SRP.

Класс не должен знать особенности потомков Entity и как-то учитывать их в своей работе
```java
if (creature instanceof Herbivore) {
  path = findFoodForHerbivore(gameMap, creature);
} else if (creature instanceof Predator) {
  path = findFoodForPredator(gameMap, creature);
}
```
Единственное, что должен знать класс поиска пути это то, что он должен найти путь. 
То есть последовательность координат от точки старта до точки, соотвествующей заданным условиям.  
Эти условия(тип еды) метод поиска должен принимать в аргументы метода, а не определять их самостоятельно путем опроса существ.

Не должно быть отдельных методов поиска пути для разных типов существ
```java
private List<Position> findFoodForHerbivore(GameMap gameMap, Creature creature)
private List<Position> findFoodForPredator(GameMap gameMap, Creature creature)
```
Поиск пути для Хищника и Травоядного отличается только тем, что Хищник ищет Траводяных, а Травоядное ищет Траву.  
То есть, различие только в типе цели.  
Алгоритм при этом должен быть одинаковым.  

Именно для этого поиск пути должен принимать класс цели:
```java
public List<Position> find(..., Class<? extends Entity> target) 
```

**5. class NeighborFinder**

- В классе большое количество дублированного кода.  

В классе для его функционала очень много кода- 245 строк.  
Алгоритм нужно оптимизировать и избавиться от дублирования.  
Я бы подсказал, как именно. Но я просто не могу понять, что делает этот метод:
```java
public List<Map.Entry<Position, Object>> allNeighbors()
```

Но я могу предложить, как убрать семь методов из восьми.  
Нужно просто сделать один универсальный метод
```java
private void checkNeighborUp(Object entity, List<Position> entityList) {
  deltaColumn = ((Position) entity).column();
  deltaRow = ((Position) entity).row() + Direction.UP.getRowDelta();
  //...
}

private void checkNeighborRightUp(Object entity, List<Position> entityList) {
  deltaColumn = ((Position) entity).column() + Direction.RIGHT_UP.getColumnDelta();
  deltaRow = ((Position) entity).row() + Direction.RIGHT_UP.getRowDelta();
  //...
}

//ПРАВИЛЬНО:
private void checkNeighbor(Object entity, List<Position> entityList, Direction direction) {
  deltaColumn = ((Position) entity).column() + direction.getColumnDelta();
  deltaRow = ((Position) entity).row() + direction.getRowDelta();
  //...
}
```

И тогда
```java
//ВМЕСТО:
public List<Map.Entry<Position, Object>> allNeighbors() {
  List<Position> entityList = new ArrayList<>();
  List<Map.Entry<Position, Object>> validNeighbors = new ArrayList<>();
  checkNeighborUp(foodEater, entityList);
  checkNeighborRightUp(foodEater, entityList);
  //....
  addValidNeighbors(foodEater, entityList, validNeighbors);
  return validNeighbors;
}

//БУДЕТ:
public List<Map.Entry<Position, Object>> allNeighbors() {
  List<Position> entityList = new ArrayList<>();
  List<Map.Entry<Position, Object>> validNeighbors = new ArrayList<>();
  for(Direction direction : Direction.values()) {
    checkNeighbor(foodEater, entityList, direction);
  }
  addValidNeighbors(foodEater, entityList, validNeighbors);
  return validNeighbors;
}
```

- Этот и ему подобные методы делает какие-то две разные вещи
```java
private void checkNeighborUp(Object entity, List<Position> entityList) {
  int deltaRow, deltaColumn;
  if (entity instanceof Position) {
    //операция #1
  }
  if (entity instanceof Entity) {
    //операция #2
  }
}
```
Каждый метод должен выполнять только одну операцию.  
Этот метод нужно разделить на два
```java
private void понятноеНазваниеОдин(Position position, List<Position> entityList) {...}
private void понятноеНазваниеДва(Entity entity, List<Position> entityList) {...}
```
*Мартин, "Чистый код", гл.3, "Правило одной операции"*

**6. abstract class Entity**

- Содержит координату. Но координата нужна только тому существу, которое ходит.  
Поэтому entities должны хранить координату только начиная с уровня `Creature`
```java
private Position position;
```

- Входящий аргумент `GameMap` в методе ни в чем не участвует. Сам метод является чем-то вроде обычного сеттера
```java
public void createPosition(int row, int column, GameMap gameMap) {
  position = new Position(row, column);
}

//ПРАВИЛЬНО:
public void setPosition(Position position) {
  this.position = position;
}
```

- Нарушение SRP, зависимость модели от представления- существо хранит спрайт с собственным изображением
```java
private String emoji;
```
Модель(а это модель) не должна зависеть от представления и знать, как ее будут показывать юзеру.  
Потому что в разных средах(консоль, Swing, Android) одна и та же модель может быть показана разными способами- пиксельной картинкой, анимацией etc.  
Спрайты всех существ должны храниться в классе, который распечатывает карту.

- Нарушение OCP.

Класс не должен знать по именам своих потомков и содержать в себе особенную логику для этих потомков
```java
public boolean isObstacle(Class<?> neighbour, Object potentialFoodEater) {
  if ((neighbour == Grass.class) && (potentialFoodEater instanceof Herbivore)) {
    return false;
  }
  if ((neighbour == Herbivore.class) && (potentialFoodEater instanceof Predator)) {
    return false;
  }
  //...
}
```
Этот метод отсюда нужно убрать.  
Отдельно определять препятствия в программе не нужно. Все, что не еда, то- препятствие для существ.  
На уровне Creature нужно хранить поле, в котором будет храниться класс еды.  
Примерно, так:
```java
public abstract class Creature extends Entity {
  private final Class<? extends Entity> food;

  public Creature(Class<? extends Entity> food) {
    this.food = food;
  }  
  //...
}
```

+ 👍 Вот таким должен быть идеальный Entity для этого проекта:
```java
public abstract class Entity {
  //да, тут совсем пусто
}
```

В иерархии классов ентити логика работы с координатами сейчас очень запутана.  
Самым лучшим решением будет вообще не хранить координату в Entity и его потомках.  
Это не только возможно, но код при этом станет проще и понятнее.

**7. abstract class Creature extends Entity**

Нарушение конвенции кода. Конструктор должен стоять выше остальных методов.

**8. class Predator extends Creature**

- При прочих равных, нужно использовать примитивный тип, а не класс-обертку 
```java
private static final Integer attackPower = 50;

//ПРАВИЛЬНО:
private static final int ATTACK_POWER = 50;
```
Для применения класса-обертки над примитивным типом данных, должна быть какая-то причина.  
Здесь использование обертки не дает ничего большего по сравнению с примитивом.  
*Блох "Java. Эффективное программирование", изд.3, гл.9.5*
```
"Предпочитайте примитивные типы упакованным примитивным типам" - Блох.
```

- Не стремись несколько инструкций писать в одну строку.

Иногда код намного лучше читается, когда он написан в несколько строк.  
Приоритет- читаемость, а не экономия строчек
```java
if (tileType == Herbivore.class) {
  new Attack(this, gameMap, (Herbivore) gameMap.getEntity(tile)).execute();
}

//ПРАВИЛЬНО:
if (tileType == Herbivore.class) {
  Herbivore target = (Herbivore) gameMap.getEntity(tile);
  Attack attack = new Attack(this, gameMap, target);
  attack.execute();
}
```

**9. class Attack**

Антипаттерн "Дробление".  
Класс выглядит, как искуственное дробление единой сущности `Predator` на два класса.  
Метод атаки нужно вынести отсюда в класс `Predator`, а этот класс удалить.

**10. class Eat**

То же самое- этот класс нужно соединить с `Herbivore`.

**11. class StepToFood**

То же самое. Если все креатуры ходят по карте, то этот метод нужно вынести в `Creature`.  

**12. class Emoji**, хранит спрайты существ

Для возврата картинки, этому классу нет необходимости хранить существо
```java
public class Emoji {
  public Entity entity;

  public Emoji(Entity entity) {
    this.entity = entity;
  }

  public String getEmoji() {
    switch (entity.getClass().getSimpleName()) {
      case "Herbivore":
        return "\uD83D\uDC2E";
    //...
    }
  }
}

//ПРАВИЛЬНО:
public class Emoji {

  public String get(Entity entity) {
    String name = entity.getClass().getSimpleName();
    switch (name) {
      case "Herbivore":
        return "\uD83D\uDC2E";
    //...
    }
  }
}
```

- В switch-case в default нужно кидать исключение, а не возращать дефолтный спрайт. 

Потому что при добавлении нового существа в проект, можно забыть добавить его спрайт в распечатку.
И тогда существо будет жить на карте своей жизнью, но мы этого не увидим- вместо этого существа будет распечатываться дефолтный спрайт
```java
switch (entity.getClass().getSimpleName()) {
  case "Herbivore":
    return "\uD83D\uDC2E";
    //...
  default:
    return "-";  <-- ДЕФОЛТНЫЙ СПРАЙТ
}

//ПРАВИЛЬНО:
default: //бросить исключение
```

**13. interface Action**

👍 Норм
```java
public interface Action {
  void perform();
}
```

**14. class SpawnEntitiesAction implements Action**

- Совмещение команды и запроса в одном методе.  
Каждый метод должен или выполнить команду, или ответить на запрос. 
Но не то и другое вместе
```java
protected Position generateIndex(Entity entity) {
  Random random = new Random();
  int i = random.nextInt(0, rowCount);
  int j = random.nextInt(0, columnCount);
  entity.createPosition(i, j, gameMap);  <-- ВЫПОЛНЕНИЕ КОМАНДЫ
  return entity.getPosition();  <-- ОТВЕТ НА ЗАПРОС
}
```

Выдели из этого метода только создание случайной пустой координаты
```java
protected Position getRandomEmptyPosition() {
  Random random = new Random();
  while(true) {
    int row = random.nextInt(0, rowCount);
    int column = random.nextInt(0, columnCount);
    Position position = new Position(row, column);
    if(!gameMap.isOccupied(position)) {
      return position;
    }
  }
}
```
И дальше используй в алгоритме:
```java
protected void addEntities(Entity entity) {
  //...
  Position position = getRandomEmptyPosition();
  entity.setPosition(position);
  gameMap.addEntity(entity);
}
```

**15. Дублирование кода в классах AddGrasses и AddHerbivores**

Два класса, которые делают одно и то же- поддерживают количество существ на карте на заданном уровне.  
Это нарушение чеклиста ТЗ:
```
Проблемы и ошибки в коде:
  Иерархия классов Action’s
  Дублирование кода
```
Должен быть один универсальный класс, типа такого:
```java
public class PopulationBalancer бла-бла {
  //...
  public PopulationBalancer(GameMap gameMap, Class<? extends Entity> type, int count, Supplier<Entity> entitySupplier) {
    this.gameMap = gameMap;
    //...
  }

  @Override
  public void perform() {
    if(...)  { //если количество существ класса type на карте > 0
      return;
    }
    for(int i =0; i<count; i++) {
      Position position = getRandomEntityPositin();
      Entity entity = entitySupplier.get(position);
      entity.setPosition(position);
      gameMap.addEntity(entity);
    }
  }
}
```
Использование:
```java
List<Action> turnActions = List.of(
  new PopulationBalancer(gameMap, Grass.class, GRASS_COUNT, ()-> new Grass()),
  new PopulationBalancer(gameMap, Herbivore.class, HERBIVORE_COUNT, ()-> new Herbivore()),
  //oths actions
);
```

**16. class MoveCreaturesAction implements Action**

- Если все креатуры умеют ходить, то именно к этому типу осуществляй приведение 
```java
Entity creature = entry.getValue();
if (creature instanceof Herbivore || creature instanceof Predator) {
  ((Creature) creature).makeMove(path, gameMap);
  //...
}

//ПРАВИЛЬНО:
Entity entity = entry.getValue();
if (entity instanceof Creature creature) {
  creature.makeMove(path, gameMap);
  //...
}
```

**17. class Constants**

Константный классы, как и утилитные, должны быть `final` и иметь приватный конструктор.  
Не должно быть возможности унаследоваться от такого класса или сделать его экземпляр.  
*Блох, "Java. Эффективное программирование", изд.3, гл.2.4*

**18. class Renderer**

+ 👍 Спрайты существ беруться из специального класса, это хорошо.  
Поэтому нет смысла хранить в Entity поле `String emoji`.

- Сложная система получения спрайта из класса.

Среди прочего, каждый раз пересоздается объект `Emoji`, что явно избыточно.  
Как я писал выше, нужно переделать способ получения спрайта из класса Emoji
```java
sb.append(new Emoji(gameMap.getEntity(position)).getEmoji());  <-- Emoji создается каждый раз

//ПРАВИЛЬНО:
private final Emoji emoji = new Emoji();  <-- Emoji создается один раз

//...
Entity entity = gameMap.getEntity(position);
sb.append(emoji.get(entity));
```

**19. class Simulation**

👍 В целом, норм.

**20. class Task implements Runnable**

Не используй try-catch для ветвления логики.  
Бизнес логика вперемешку с обработками ошибок читается плохо.  
По возможности, изолируй try-catch во вспомогательных методах
```java
while(...) {
  try {
    int command = sc.nextInt();
    //миллион строк бизнес логики
  } catch (InputMismatchException e) {
    sc.next();
    System.out.printf("Нажмите число!%n");
  }
}

//ПРАВИЛЬНО:
while(...) {
  String s = sc.nextLine();
  if(!isNumber(s)) {
    System.out.printf("Нажмите число!%n");
    continue;
  }
  int command = Integer.parseInt(s);
  //миллион строк бизнес логики
}

private boolean isNumber(String s) {
  try() {
    Integer.parseInt(s);
    return true;
  } catch(NumericFormatException e) {
    return false;
  }
}
```
*"ЧК", гл.3*

- В остальном ок. Но не забывай про инкапсуляцию
```java
class Task implements Runnable {
  Simulation simulation;
  ExecutorService executorService;
  //...
}

//ПРАВИЛЬНО:
class Task implements Runnable {
  private final Simulation simulation;
  private final ExecutorService executorService;
  //...
}
```

**21. class Main**, содержит точку входа main

👍 Только создает и запускает Симуляцию, это хорошо.

## АРХИТЕКТУРА

Необязательно хранить координаты существ одновременно в классе Карта и в самих существах.  
Даже наоборот, хранение координаты и в Карте и в Существе имеет свои недостатки- это дублирование данных.  

Хранить координату в существах можно, но только если это упрощает код, как сделано у Сергея в Шахматах. 
Там нарушение DRY обосновывается упрощением алгоритма игры.

Но у тебя получилось все совсем иначе- операции с координатами настолько избыточны и переплетены в логике программы, 
что это делает алгоритм не проще, а сильно сложнее.  
И как ты можешь это починить *не удаляя координату из существ*, я не вижу.

Поэтому я тебе советую убрать хранение координаты из всех существ- не только из базового Entity, но из всех его потомков.

Координата нужна только на уровне `Creature` для совершения хода.  
Лучшее решение тут будет запрашивать свою координату из Карты и использовать эту координату при совершении хода.  
Примерно, так:
```java
public abstract class Creature extends Entity {
  private final static PathFinder PATH_FINDER = new PathFinder(); 
  private final Class<? extends Entity> food; 
  //...

  public Creature(Class<? extends Entity> food, ...) {...}

  public void makeMove(GameMap gameMap) {
    Position current = gameMap.getPosition(this);
    List<Position> path = PATH_FINDER.find(gameMap, current, food);
    //пройти по пути path
    //если хватит запаса хода- съесть еду
  }
}
```
Это приведет к изменению во многих классах- в Карте, в Экшенах.  
Но эти изменения приведут к упрощению архитектуры, что очень хорошо.

Например, вот так без хранения в себе координат и спрайтов, будет выглядить базовый Entity и его простые неходячие наследники:
```java
public abstract class Entity {
}

public class Tree extends Entity {
}
```

## ВЫВОД

По сравнению с первой версией, заметен прогресс.  
Замечаний все еще много, но это даже хорошо- в процессе рефакторинга растет мастерство.

n.134(292)  
#ревью #симуляция 