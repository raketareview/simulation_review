https://github.com/haushekmiva/simulation-project  
[artemon]

Есть над чем поработать.

## НЕДОСТАТКИ РЕАЛИЗАЦИИ

1. Нет паузы/пуск во время работы. Хотя это требование есть в ТЗ.

## ХОРОШО

+ 👍 Координаты существ не хранятся в самих существах(мне так больше нравится)

## ЗАМЕЧАНИЯ

**1. Нейминг**

- Не называй класс только большими буквами, даже если это аббревиатура- встречаясь в коде, это будет путать с константой
```java
class BFS

//ПРАВИЛЬНО:
class Bfs
```

- В названии переменных не пиши тип данных, к которым они относится. И вообще не употребляй венгерскую ноттацию.  
Название переменной должно отвечать на вопрос что хранит переменная, а не как хранит
```java
private Map<Coordinates, Entity> entityMap = new HashMap<>();
private Map<Entity, Coordinates> coordinatesMap = new HashMap<>();

//ПРАВИЛЬНО:
private Map<Coordinates, Entity> entities = new HashMap<>(); //или: entitiesWithCoordinates
private Map<Entity, Coordinates> coordinates = new HashMap<>(); //или: coordinatesWithEntities 
```

- Название класса должно быть существительным. "Render" это процесс, а Renderer- тот, кто выполняет этот процесс
```java
class Render
```

- Только конструктор класса может называться так же, как сам класс. 
Остальные методы должны называться по-другому хотя бы потому, что название класса должно быть существительным, а метода- глаголом
```java
public class Render {
  public static void render(...) 
}

//ПРАВИЛЬНО:
public class Renderer {
  public static void render(...) 
}
```
*Oracle Java code conventions, part."Naming conventions"*  
*Мартин, "Чистый код", гл.2*  
*Ютуб, Немчинский "Как называть переменные, методы и классы?"*

**2. Комментарии**

Комментарии здесь в основном не несут полезной нагрузки, а констатируют очевидное
```java
// This variable is used as empty coordinates for cases where needed
public static final Coordinates EMPTY = new Coordinates(-1, -1);
```
Когда в проекте много каментов, это плохо- пользы от них практически нет, они только забивают пространство и мешают читать код.  
В идеале, комментариев вообще не должно быть, код должен объяснять сам себя через правильный нейминг и лаконичный код.  
*Мартин, "ЧК", гл.4*

**3. Нарушение конвенции кода**. 

В любой ситуации выделяй тело блока скобочками, даже если тело состоит из одной строки. 
Исключение- метод equals(), там можно после if не выделять блоки скобочками
```java
if (cell != null && cell.getType() == goal) return currentPosition;
if (finalPoint.equals(currentPosition))
  return generateRoad(currentPosition, startPoint, parentMap);


//ПРАВИЛЬНО:
if (cell != null && cell.getType() == goal) {
  return currentPosition;
}
if (finalPoint.equals(currentPosition)) {
  return generateRoad(currentPosition, startPoint, parentMap);
}
```

**4. Форматирование строк**

Если нужно печатать или создавать строку с более, чем одним подстановочным значением или значение вставляется внутрь сообщения, используй форматирование- тогда сразу будет виден весь шаблон
```java
System.out.println("Somebody attacked somebody. The victim has " + victim.getHealth() + " now");

//ПРАВИЛЬНО:
System.out.printf("Somebody attacked somebody. The victim has %d now \n", victim.getHealth());
```

**5. Пакет utils**

Содержит не только утилиты.

Утилитные классы должны содержать только статические методы, быть `final` и иметь приватный конструктор.  
Не должно быть возможности унаследоваться от утилиты или сделать ее экземпляр.  
*Блох, "Java. Эффективное программирование", изд.3, гл.2.4*

**6. Пакет models**

Не все модели в проекте лежат в этом пакете.  
Например, эти модели находятся в других пакетах: Coordinates, BFS etc.  
Если пакет называется "модели", то это завявка на то, что в нем находятся все модели программы. 
Если это не так, то называй пакет по-другому.  
В данном случае лучше будет "entities".

**7. class Coordinates**

- Нарушение конвенции кода. Константы должны стоять самыми первыми.

- Хранить эту константу здесь не надо, это уменьшает универсальность класса
```java
public static final Coordinates EMPTY = new Coordinates(-1, -1);
```
В других проектах с участием координат, такая кордината [-1, -1] может быть валидной. 
А значит хранение в ней константы EMPTY с такими значениями будет некорректным.

Даже если ты никогда больше не планируешь делать программ, которые используют координаты, сути дела это не меняет:  
Классы "общего назначения" нужно проектировать максимально независимыми от контекста конкретной прикладной задачи.

Если по какой-то причине *конкретно в этой программе* нужна константа `Coordinates EMPTY`, то сделай такую константу.
Но храни ее не в самом классе `Coordinates`.

- Класс может быть преобразован в record без потери функционала.

**8. class WorldMapConfig**

- Используеся как объект-аргумент для класса карты мира WorldMap: *"ЧК", гл.3, "Оъекты как аргументы"*.  

Содержит информацию, которая в большинстве своем не имеет отношение к ответственности, которая *должна быть* в нормально спроектированной Карте.
Если сделать карту так, чтобы она имела только одну ответственность, то WorldMapConfig будет не нужен.

- Эти поля стоит сделать константами
```java
private final int width = 25;

//ПРАВИЛЬНО:
private static final int WIDTH = 25;
```

**9. class WorldMap**

- Нарушение SRP, методы чужих ответственностей. 

Карта должна только хранить существа и обеспечить базовые операции с ними: вставить, выдать одно существо и список всех хранимых существ, удалить.
Если какой-то метод не нужен для обеспечения этого функционала, значит он не принадлежит к ответственности карты, а принадлежит к чужой ответственности.
Здесь методы чужих ответственностей: поддерживать популяцию существ на заданном уровне, установить уровень популяции.

Наверное, для проекта в целом полезно иметь метод, который поддерживает популяцию зайцев. 
Но этот процесс не имеет никакого отношения к единой ответственности карты- хранению существ в себе.  
Для организации хранения/удаления/выдачи существ, карте не нужно управлять заячьей популяцией.  

Методы чужих ответственностей должны находиться в тех классах, в интересах которых они работают.
Если один и тот же метод используют разные классы, то метод нужно вынести в отдельный класс, например, WorldMapUtils.

Поддержание популяции существ должно происходить в отдельном `turn action'e`.

- Избыточно

Нет необходимости иметь две хешмапы, которые хранят записи координата-существо и существо-координата.
Достаточно иметь только хешмапу координата-существо
```java
private Map<Coordinates, Entity> entityMap = new HashMap<>(); <-- ЭТОГО ДОСТАТОЧНО
private Map<Entity, Coordinates> coordinatesMap = new HashMap<>();  <-- ЭТО ИЗБЫТОЧНО
```
Две хешмапы могут улучшить время выполнения некоторых операций. Но они усложняют код и делают класс более громоздким.  
В таких случаях нужно выбирать простоту.  
Преждевременная оптимизация является антипаттерном.

- При всех операциях с участием координаты(добавить, выдать, удалить и т.д.), нужно проверять координату на корректность.  
Если координата некорректна(находится вне пределов карты), нужно бросать исключение
```java
public boolean isCellEmpty(Coordinates cell) {  <-- Какой результат вернет, если придет Coordinates.EMPTY?
  return !entityMap.containsKey(cell);
}
```
Сейчас, если у карты спросить, свободна ли ячейка с координатой (+100500, -100500), то карта скажет, что свободна. 
А правильный ответ- ячейки с такой координатой в карте нет вообще.

Ближайшая аналогия- стандартные хранилища типа List и массива. 
При попытке обратитья к ним по несуществующему индексу, бросается исключение.

- Никогда не возвращай null
```java
private Map<Coordinates, Entity> entityMap = new HashMap<>();

public Entity getEntity(Coordinates coordinates) {
  return entityMap.get(coordinates);  <-- может вернуть null
}
```
Возврат null повышает риск возникновения NullPointerException в программе.  
*Мартин, "Чистый код", гл.7.7-8*  
*Ютуб, Немчинский "Почему нельзя возвращать NULL?"*

**10. class CoordinatesUtils**

- Как я писал выше, утилитный класс должен быть `final` и иметь закрытый конструктор.

- Это скорее утилита Карты Мира, а не координат. 
Потому что координаты используются в контексте работы с Картой Мира
```java
public static boolean isInBoundaries(int x, int y, WorldMap world)
```

- Работай с координатой целиком, а не по запчастям
```java
public static List<Coordinates> determineDirections(int x, int y)

//ПРАВИЛЬНО:
public static List<Coordinates> determineDirections(Coordinates coordinates)
```
*Макконнелл "Совершенный код", гл.7*
```
"Передавайте в метод те переменные или объекты, которые нужны ему для поддержания абстракции интерфейса" - Макконнелл.
```

+ 👍 В целом, не имею ничего против существования этого класса.

**11. enum EntityType** 

Енам существует для дополнительной типизации классов
```java
public enum EntityType {
  HERBIVORE, PREDATOR, ROCK, TREE, GRASS
}
```
Не используй дополнительную типизацию классов, потому что это дублирует стандартные способы.  
Используй стандарные способы определять принадлежность объектов к типам:
```java
EntityType type = EntityType.HERBIVORE;
Entity entity = entityFactory.createEntity(type, config, bfs);

//ПРАВИЛЬНО:
Entity entity = entityFactory.createEntity(Herbivore.getClass(), config, bfs);
```

**12. class EntityFactory**

+ 👍 Фабрика существ- почему бы и нет.

- Чисто логически- зачем Фабрике Существ принимать в свой метод конфигурацию *другого класса?*.
Это всё ещё тянутся последствия плохо спроектированного класса WorldMap
```java
public Entity createEntity(EntityType type, WorldMapConfig config, BFS bfs)
```
Задача класса фабрики- просто создать нужное существо.  
Для этого классу вообще не нужно знать про существование класса `WorldMap` и вспомогательных ему классов.

**13. class BFS**

- Сложные правила пользования классом

Сейчас в классе есть два публичных метода
```java
public class BFS {
  public Coordinates searchGoal(Coordinates currentPosition, WorldMap world, EntityType goal) {...}
  public List<Coordinates> searchPath(Coordinates startPoint, Coordinates finalPoint, WorldMap world) {...}
  //private methods
}
```

Для использования этого класса, нужно сначала вызвать один метод и получить координаты цели.  
Потом вызвать второй метод, передать в него координаты цели и получить путь.

Нет никаких оснований, чтобы класс был таким сложным для пользования.  
Сделай только один публичный метод, который предназначени для пользования клиентом.  
Например, такой:
```java
public List<Coordinates> searchPath(WorldMap world, Coordinates startPoint,  Class<? extends Entity>  target) {
  //ищет путь на карте от точки start
  //до точки, где находится существо нужного класса(напр. target == Grass.class)
}
```
Если для работы этого метода понадобится предварительно найти координату цели, 
то пусть метод поиска координаты цели будет приватным.  
И вызывать его будет не клиент, а `List<Coordinates> searchPath(...)`.

*Вайсфельд "Объектно-ориентированный подход", гл.5, "Минимальный открытый интерфейс"*

Хотя алгоритм BFS вообще не требует предворительного нахождения цели. Этого требует только A*.  

- Мапа координата-координата обычно при реализации поиска используется как эрзац-заменитель связного списка
```java
Map<Coordinates, Coordinates> parentMap = new HashMap<>();
```
Вместо мапы координата-координата сделай класс связного списка, алгоритм тогда будет выглядеть понятнее.
Например, так:
```java
class Node {
  private Coordinate coordinate; 
  private Node parent;
  //...
  public Coordinates getCoordinates() {...}
  public Node getParent() {...}
}
```

- Вместо комментариев, создавай вспомогательные методы, которые своим названием сами все объяснят
```java
// if the cell is empty, then we do not search its neighbors (except when it's the start position)
if (!world.isCellEmpty(currentPosition) && !currentPosition.equals(startPoint)) continue;

//ПРАВИЛЬНО:
if (isНазваниеКотороеВсеОбъясняет(...)) {
  continue;
}
```

**14. abstract class Entity**

- Нарушение SRP, зависимость модели от представления- существо хранит спрайт с собственным изображением
```java
protected String sign;
```
Модель(а это модель) не должна зависеть от представления и знать, как ее будут показывать юзеру.
Потому что в разных средах(консоль, Swing, Android) одна и та же модель может быть показана разными способами.
Спрайты всех существ должны храниться в классе, который распечатывает карту.

- Внешняя типизация, не нужно этого
```java
public EntityType getType() {
  return type;
}
```

- Вот так здесь должен выглядеть идеальный Entity:
```java
public abstract class Entity {
}
```

**15. class Grass extends Entity**

Этот класс не должен иметь никакого отношения к конфигурации Карты
```java
public Grass(WorldMapConfig config) {
  this.energy = config.getGrassEnergy();
  //...
}

//ПРАВИЛЬНО:
public Grass(int energy, ...) {
  this.energy = energy;
  //...
}
```

**16. class PredatorOnArrive implements OnArrive**

Нарушение SRP, чужая ответственность, зависимость модели от представления.

Модель(а это модель) не должна ничего печатать в консоль
```java
System.out.println("Somebody attacked somebody. The victim has " + victim.getHealth() + " now");
```
Иначе модель перестает быть универсальной и становится заточенной под конкретную среду и конкретное представление себя в этой среде- в данном случае, консоль.
В других средах нужно будет менять код в этом классе, чтобы вывод осуществлялся по правилам этой среды, то есть, это лишняя причина для изменения класса.
В других средах(напр. Андроид) эту модель нельзя будет использовать- она там просто не скомпилируется. 
Другое представление для модели(напр. если одну и ту же модель нужно в программе показвать по-разному) нельзя будет сделать, или придется делать через костыль.

Если модель должна что-то сообщить миру, она может это сделать через паттерн [CallBack](https://t.me/zhukovsd_it_chat/53243/139594)

Например, вот к чему приводит зависимость модели от представления: карта рисуется в виндовый интерфейс, а модель [пишет в консоль](https://github.com/raketareview/simulation_review/blob/master/content/rev-sim064-davidtagirov-DavidTagirov.md)  

Если модель должна что-то сообщить миру, она может это сделать через паттерн CallBack.  
[CallBack луноход](https://t.me/zhukovsd_it_chat/53243/139594)  
[CallBack разрушение](https://t.me/zhukovsd_it_chat/53243/184749)

**17. Разделение одной сущности на несколько**

В проекте есть несколько пар классов: Predator и PredatorOnArrive, Herbivore и HerbivoreOnArrive.
Эти пары классов выглядят как необоснованное разделение единых сущностей на два класса.

Абстрактный метод `onArrive(...)` можно перенести из `interface OnArrive` в `class Creature`. 
А реализации этого метода, соответственно, перенести из `xxxOnArrive` в Predator и Herbivore.

**18. class Predator extends Creature**

Нарушение дизайна ООП в наследовании. 

При создании экземпляра класса, значения обязательных полей класса должны устанавливаться через конструктор, а не сетиться
```java
public class Predator extends Creature {
  private final int power;

  public Predator(WorldMapConfig config, BFS bfs) {
    super(config, bfs);
    this.type = EntityType.PREDATOR;
    this.health = config.getPredatorHealth();
  }
}

//ПРАВИЛЬНО:
public class Predator extends Creature {
  private final int power;

  public Predator(WorldMapConfig config, BFS bfs) {
    super(config, bfs, EntityType.PREDATOR, config.getPredatorHealth(), ...);
  }
}
```
Да, в этом классе вообще неправильно использовать `WorldMapConfig config`.  
Но я сейчас говорю о другом- о способе инициализации данных в предке.  
*Эккель "Философия Java", гл.5, "Конструктор гарантирует инициализацию"* 

**19. class Actions**

- Нарушение конвенции кода. Вспомогательный приватный метод не должен стоять выше главного по отношению к нему публичного метода.

- Этот метод не инициализирует действия. Этот метод создает экземпляр `WorldMap`
```java
public WorldMap initActions(WorldMapConfig config) {
  return new WorldMap(config);
}
```

- Идея `Action` здесь не осмыслена. 

Идея состоит в том, что нужно создать семейство родственных классов, объединенных общим интерфейсом.  
Каждый из этих классов должен делать что-то свое с картой: одна акция должна заселять карту существами, другая делать ходы и т.д.  
Action'ы, изложенные в ТЗ, это вариант реализации паттерна Command. 

Сейчас `Actions` у тебя это просто сборник функций, сделанный в стиле процедурного программирования
```java
public class Actions {
  public WorldMap initActions(WorldMapConfig config) {...}
  public void turnActions(WorldMap world) {...}
}
```
Сейчас этот класс делает несколько разных дел, каждое из которых должен делать отдельный класс-экшен.  
Например, совершает движение существ и поддерживает популяцию существ на карте.

Смысл Action'ов состоит в том, что должен быть общий класс/интерфейс Action и его наследники.  
В каждом экшене должен быть только один публичный метод(не публичных может быть сколько угодно).  
Это вариация паттерна Command- экшены должны быть родственны и одинаково использоваться через полиморфизм. Примерно так
```java
interface Action{
 void execute(Карта карта);
}

class ХодитьAction реализует Action {
  void execute(Карта карта) {
    //обойти всю карту
    //найти каждую креатуру
    //и дать ей пинка чтоб побежала
  }
}

class ДатьСигаретуВсемЗайцамAction реализует Action {
  void execute(Карта карта) {
    //обойти всю карту
    //найти всех зайцев и дать им по сигарете 
  }
}

List<Action> actions = List.of(new ДатьСигаретуВсемЗайцамAction(), new ХодитьAction());
for(Action a: actions) {
  a.execute(карта);
}

/*
Результат: программа обойдет всю карту, найдет всех креатур и даст им пинка, чтобы они побежали.
А каждому зайцу предварительно даст сигарету.
*/
```

**20. class Render**

- Спрайты существ должны храниться здесь, а не браться из самих существ.

- Снова нарушения конвенции кода, единственный публичный метод стоит самым последним.

## ВЫВОД

Доделать функционал по ТЗ: пауза/старт во время работы симуляции.  
Подробнее разберись с Action'ами, посмотри ролики на ютубе про паттерн "Command", чтобы получить о нем общее представление.

Для лучшего понимания декомпозиции ООП посмотреть ролики Сергея про шахматы.  
Посмотреть ролики Немчинского про SOLID.

n.115(263)  
#ревью #симуляция 