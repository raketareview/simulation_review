https://github.com/codleto/Simulation  
[Boris]

Есть очень-очень много, над чем нужно вдумчиво поработать.  
Используется процедурный стиль программирования.

## ХОРОШО

+ 👍 Реализовано пауза/пуск во время работы
+ 👍 Возможность конфигурирования карты через меню
+ 🚀 Меню для создания карт разного размера и разным количеством существ

## ЗАМЕЧАНИЯ

**1. Нейминг**

- В названии переменных не пиши тип данных, к которым они относится. И вообще не употребляй венгерскую ноттацию.  
Название переменной должно отвечать на вопрос что хранит переменная, а не как хранит
```java
HashMap<Coordinates, Entity> map = new HashMap<>();

//ПРАВИЛЬНО:
Map<Coordinates, Entity> entities = new HashMap<>();
```

- Для одной концепции давай одно название
```java
public static void addEntity(Coordinates coordinates, Entity entity) {
  //добавляет существо на конкретную координату  
}

public static void spawnEntity(Entity entity) {
  //добавляет существо на случайную координату
}

//ПРАВИЛЬНО:
public static void addEntity(Coordinates coordinates, Entity entity) 

public static void addEntityAtRandomCoordinates(Entity entity) 
```

- Название обманывает. Метод возвращает существо, а не мапу
```java
Entity getMap(int vertical, int horizontal)
```

- Название класса должно быть существительным. Это название- глагол
```java
class Eat
```

- Название класса обманывает и сильно путает. Метод не ест, метод определяет еду
```java
public boolean eat(Entity entity) {
  return entity instanceof Herbivore;
}

//ПРАВИЛЬНО:
public boolean isFood(Entity entity) {
  return entity instanceof Herbivore;
}
```

- Названия пакетов нужно писать стилем lowercase 
```java
package Simulation.Entity.StaticObject;

//ПРАВИЛЬНО:
package simulation.entity.etaticobject;
```

*Oracle Java code conventions, part."Naming conventions"*  
*Мартин, "Чистый код", гл.2*  
*Ютуб, Немчинский "Как называть переменные, методы и классы?"*

**2. Используй классы через их интерфейсы**
```java
HashMap<Coordinates, Entity> map = new HashMap<>();
ArrayList<Creature> predatorArrayList = new ArrayList<>();

//ПРАВИЛЬНО:
Map<Coordinates, Entity> map = new HashMap<>();
List<Creature> predatorArrayList = new ArrayList<>();
```
Общее правило: ArrayList нужно использовать через List, HashMap- через Map и т.д.  
Это позволяет пользоваться преимуществами полиморфизма.

Да, бывают ситуации, когда, например, с LinkedList нужно работать именно как с LinkedList, а не с List. 
Но это уже нюансы.  
*"Java. Эффективное программирование", изд.3, гл.9.8*
```
"Если вы выработаете привычку использовать в качестве типов интерфейсы, ваша программа будет гораздо более гибкой" - Блох.
```

**3. Нарушение DRY**, магические буквы, числа, слова. Вводи константы 
```java
System.out.println("""
  Карта 4x4 - жми 1
  Карта 5х5 = жми 2
  Карта 10х10 - жми 3""");

System.out.println("Введи от 1, 2 или 3");

//ПРАВИЛЬНО:
private final static int SMALL = 1;
private final static int MEDIUM = 2;
private final static int LARGE = 3;

System.out.println("Карта 4x4 - жми " + SMALL);
System.out.println("Карта 5х5 - жми " + MEDIUM);
System.out.println("Карта 10х10 - жми " + LARGE);

System.out.printf("Введи %d - %d   \n", SMALL, LARGE);
```
*Фаулер, "Рефакторинг", гл.8, "Замена магического числа символической константой"*  
*refactoring.guru "Замена магического числа символьной константой"*

**4. class Coordinates**

+ 👍 Нет ничего лишнего, это хорошо.

- Если значение поля не изменяется во время работы программы, это поле должно быть final
```java
public Integer vertical;

//ПРАВИЛЬНО:
public final Integer vertical;
```

- В качестве наименоввания осей, в координатах традицонно принято использовать x/y.  
Можно также назвать row/column- так в java принято называть позицию ячейки в таблице
```java
public Integer vertical;
public final Integer horizontal;

//ПРАВИЛЬНО:
public Integer y;  //row
public final Integer x;  //column
```
Старайся минимизировать изменяемость классов.  
*Блох "Java. Эффективное программирование", изд.3, гл.4.3.*

- При прочих равных, нужно использовать примитивный тип, а не класс-обертку
```java
public Integer vertical;
public final Integer horizontal;

//ПРАВИЛЬНО:
public final int vertical;
public final int horizontal;
```
Для применения класса-обертки над примитивным типом данных, должна быть какая-то причина. 
Здесь использование обертки не дает ничего большего по сравнению с примитивом.  
*Блох "Java. Эффективное программирование", изд.3, гл.9.5*
```
"Предпочитайте примитивные типы упакованным примитивным типам" - Блох.
```

Среди прочего, это упростит `equals()`, хотя это далеко не главная причина для использования примитивов вместо оберток
```java
public Integer vertical;
public final Integer horizontal;

public boolean equals(Object o) {
  //...  
  return vertical.equals(that.vertical) && Objects.equals(horizontal, that.horizontal);
}

//ПРАВИЛЬНО:
public final int row;
public final int column;

public boolean equals(Object o) {
  //...  
  return row == that.row && column == that.column;
}
```

- Класс может быть преобразован в record без потери функционала.

**5. class Map**

- Дублирование названия стандартного класса/интерфейса

Следовать ТЗ это хорошо. Называть свои пользовательские классы так же, как стандартные классы и интерфейсы- плохо.  
Второе приводит к постоянной путанице в коде. `Мар` это название стандартного интерфейса.  
Поэтому меньшим злом является дать другое название прикладному классу: Board, GameMap или что-то подобное.

В частности, здесь это приводит к тому, что внутри пользовательского класса `Map` нельзя использовать стандартный интерфейс `Map`(на самом деле конечно можно, но выглядеть это будет криво).
Поэтому вместо использования интерфейса `Map`, приходится использовать конкретную реализацию `HashMap` 
```java
private static final HashMap<Coordinates, Entity> map = new HashMap<>();
```

- Карта содержит в себе фиксированные размеры, поэтому становится неуниверсальной
```java
public static int mapSize = 5;
```
Фиксированные размеры могут быть у шахматной доски(8x8) или игры крестики-нолики(3x3).  
Здесь размеры должны приниматься в конструктор, чтобы можно было создавать карту произвольных размеров.

- Нарушение ООП дизайна.

Эти поля не должны быть static
```java
private static final HashMap<Coordinates, Entity> map = new HashMap<>();
public static ArrayList<Creature> predatorArrayList = new ArrayList<>();
```
Статические поля в ООП программе нужно применять очень осторожно. 
Потому что сама идея классов состоит в том, что у класса может быть несколько экземпляров одновременно. 
И при этом все они должны корректно работать.

В данном случае если в проекте будет существовать несколько экземпляров класса одновременно, они будут работать некорректно. 
Потому что будут одновременно вносить изменения в одно и то же статическое поле.

И тут вопрос не в том, должно ли *конкретно в этом* проекте быть два экземпляра одного класса, или не должно. 
Если класс не утилитный, не синглтон и можно создать несколько его экземпляров, то по умолчанию мы признаем, что его экземпляров в проекте может быть более одного.

- Нарушение OCP.

Не должно быть отдельных коллекций для хранения определенного типа существ
```java
public static ArrayList<Creature> predatorArrayList = new ArrayList<>();
public static ArrayList<Creature> herbivoreArrayList = new ArrayList<>();
```
Это нарушение чеклиста ТЗ:
```
Проблемы и ошибки в коде: ...параллельные коллекции для разных типов существ (для каждого типа своя коллекция)
```

- Процедурный стиль программирования

Этот класс сделан в процедурном стиле программирования- все поля и методы в нем статические.  
По сути, это что-то вроде утилитного класса.  

В утилитных классах нет ничего плохого, когда они применяются в уместных ситуациях.  
Но строить игровую карту на принципах утилиты это неправильно.

**На этом можно было остановить разбор проекта, потому что дальше ничего хорошего быть уже не может. Нужно переделывать.**

Неправильно выбранный подход,- предпочтение утилит нормальным объектам,- может привести только к плохой и неправильной архитектуре в ООП программе.

- Нарушение SRP, методы чужих ответственностей. 

Карта должна только хранить существа и обеспечить базовые операции с ними: вставить, выдать одно существо и список всех хранимых существ, удалить.
Если какой-то метод не нужен для обеспечения этого функционала, значит он не принадлежит к ответственности карты, а принадлежит к чужой ответственности.
Здесь методы чужих ответственностей: определяет есть ли доступная еда, находит случайную координату, возвращает спрайт существа etc. 

Наверное, для проекта в целом полезно иметь метод, который определяет, есть ли доступная еда. 
Но этот процесс не имеет никакого отношения к единой ответственности карты- хранению существ в себе.  
Для организации хранения/удаления/выдачи существ, карте не нужно определять доступность еды(для кого-то).  

Методы чужих ответственностей должны находиться в тех классах, в интересах которых они работают.
Если один и тот же метод используют разные классы, то метод нужно вынести в отдельный класс, например, BoardUtils.

- При всех операциях с участием координаты(добавить, выдать, удалить и т.д.), нужно проверять координату на корректность. 

Если координата некорректна(находится вне пределов карты), нужно бросать исключение.  
Сейчас можно вставит существо на координату, которая находится вне пределов карты
```java
public static void addEntity(Coordinates coordinates, Entity entity) {
  map.put(coordinates, entity);
}
```
Ближайшая аналогия- стандартные хранилища типа List и массива. 
При попытке обратитья к ним по несуществующему индексу, бросается исключение.

- Никогда не возвращай null
```java
private static final HashMap<Coordinates, Entity> map = new HashMap<>();

public static Entity getEntity(Coordinates coordinates) {
  return map.get(coordinates);  <-- может вернуть null
}
```
Возврат null повышает риск возникновения NullPointerException в программе.  
*Мартин, "Чистый код", гл.7.7-8*  
*Ютуб, Немчинский "Почему нельзя возвращать NULL?"*

- Метод совершения хода в карте- нарушение SRP.

Должна ли карта учитывать логистику зайцев? Если да, то как?  
```java
public static void moveCreature(Coordinates from, Coordinates to, Creature mover) {
  //20 строк логики перемещения существа  
}
```
Сейчас метод совершения хода содержит 20 строк логики, которая описывает правила логистики.  
Таким образом, карта кроме всего прочего, берет на себя часть чужой ответственности- знает правила общеигровой логики.

Карта не должна иметь в себе метод телепортации.

- Нарушение Low Coupling

Карта вносит изменения в поле класса, про который карта вообще не должна знать- Simulation 
```java
public static void moveCreature(Coordinates from, Coordinates to, Creature mover) {
  //...
  Simulation.moveCount++;
}
```
Минимизируй связи между классами, иначе это приводит к спагетти-коду и циклическим зависимостям: 
`class Simulation` использует `class Map`, а тот в свою очередь использует `class Simulation`.

Соблюдение принципов SOLID и GRASP помогает минимизировать связи между классами и делать программу более простой и понятной.

Здесь *данное* нарушение Low Coupling возникает из-за предыдущего нарушения SRP- выполнения классом чужой ответственности по перемещению существ на карте.

- Неправильная перегрузка методов
```java
public static Entity getMap(Coordinates coordinates) {
  return map.get(coordinates);
}

public static Entity getMap(int vertical, int horizontal) {
  return map.get(new Coordinates(vertical, horizontal));
}

//ПРАВИЛЬНО:
public static Entity getMap(Coordinates coordinates) {
  return map.get(coordinates);
}

public static Entity getMap(int vertical, int horizontal) {
  return getMap(new Coordinates(vertical, horizontal));
}
```

**6. abstract class Entity**

- Содержит координату. Но координата нужна только тому существу, которое ходит. 
Поэтому entities должны хранить координату только начиная с уровня `Creature`.

- Нарушение SRP, зависимость модели от представления- существо хранит спрайт с собственным изображением
```java
private String skin;
```
Модель(а это модель) не должна зависеть от представления и знать, как ее будут показывать юзеру.  
Потому что в разных средах(консоль, Swing, Android) одна и та же модель может быть показана разными способами- пиксельной картинкой, анимацией etc.  
Спрайты всех существ должны храниться в классе, который распечатывает карту.

- Нарушение инкапсуляции. 

Все операции с хранимыми данными должны выполняться клиентами не напрямую, а через методы.  
Например, через геттеры и сеттеры
```java
public Coordinates coordinates;
```
*Блох "Java. Эффективное программирование", изд.3, гл.4.2.*
```
"Используйте в открытых классах методы доступа, а не открытые поля" - Блох.
```

- Минимизируй изменяемость в классах.

После установки спрайта существа, нет никаких оснований его менять- на протяжении работы программы, изображение существа не меняется
```java
public void setSkin(String skin)
```

**7. Нарушение дизайна ООП в наследовании Entity**

Наследники Entity инициализируют поля в предке неправильно.  
При создании экземпляра класса, значения обязательных полей класса должны устанавливаться через конструктор, а не сетиться
```java
public class Rock extends Entity {
  public Rock() {
    super.setSkin("\uD83E\uDEA8");
  }
}

//ПРАВИЛЬНО:
public Rock() {
  super("\uD83E\uDEA8");
}
```
Да, в Entity вообще неправильно хранить спрайт существа.  
Но я сейчас говорю о другом- о способе инициализации данных в предке.  
*Эккель "Философия Java", гл.5, "Конструктор гарантирует инициализацию"* 

**8. abstract class Creature extends Entity**

- Очередное нарушение инкапсуляции- публичные поля
```java
public int hp = 100;
public int speed = 1;
public int attack = 1;
```

- Магические числа.

Число "100" в этих двух случаях используется в одном и том же контексте?  
Тогда это должно быть синхронизировано
```java
public int hp = 100;

public void satisfiedItsHunger() {
  this.hp = 100;
}

//ПРАВИЛЬНО:
private static final int MAX_HP = 100;

public int hp = MAX_HP;

public void satisfiedItsHunger() {
  this.hp = MAX_HP;
}
```

**9. class Herbivore/Predator extends Creature**

- Нарушение инкапсуляции.

Всегда явно указывай область видимости и final, если переменная final
```java
TurnAction turnAction = new TurnAction();

//ПРАВИЛЬНО:
private final TurnAction turnAction = new TurnAction();
```

- Нарушение Low Coupling.

Неправильное понимание идеи классов Action's.  
Сейчас существа содержат в себе экземпляр экшена и используют его
```java
public class Predator extends Creature {
  TurnAction turnAction = new TurnAction();
  //...
}
```
Но идея экшенов состоит в прямо противоположном:  
Существа не должны ничего знать про экшены и не должны их использовать.  
А экшены, наоборот, должны работать с картой и существами.

- Нарушение DRY, дублирование логики.

Заяц и Волк в методе `makeMove()` частично дулируют код друг друга.  
Общий код выноси во вспомогательный метод предка. 

- Нарушение DRY, дублирование данных.

Заяц и Волк хранят в себе одинаковое поле
```java
public class Herbivore extends Creature {
  TurnAction turnAction = new TurnAction();
  //...
}

public class Predator extends Creature {
  TurnAction turnAction = new TurnAction();
  //...
}
```
Общие данные выноси в предка.

**10. Реализация Action's**

Идея `Action` здесь не осмыслена. 

Идея состоит в том, что нужно создать семейство родственных классов, объединенных общим интерфейсом.  
Каждый из этих классов должен делать что-то свое с картой: одна акция должна заселять карту существами, другая делать ходы и т.д.  
Action'ы, изложенные в ТЗ, это вариант реализации паттерна Command. 

Сейчас `Actions` это два класса, просто контейнеры функций, сделанные в стиле процедурного программирования.  
Это тоже почти утилитные классы.  
"Почти" потому что по сравнению с нормальными утилитными классами, они еще содержат изменяемые статические поля.

Между `InitAction` и `TurnAction` нет никакой родственной связи и у них нет ничего общего, кроме названия.  
У этих классов нет ничего общего не только с экшенами из ТЗ, но с ООП в целом
```java
public class InitAction {
  static int sheepCount;

  public static void chooseMapSize() {...}

  public static void configureSheepCount() {...}

  public static void initializeWorld() {...}

  public static void setWolfAttackPower() {...}

  public static void setWolfSpeed() {...}

  public static int readNumber() {...}
}

public class TurnAction {
  public ArrayList<Coordinates> visitedCells = new ArrayList<>();
  public HashMap<Coordinates, Coordinates> cameFrom = new HashMap<>();
  public ArrayList<Coordinates> pathToFood = new ArrayList<>();
  public static ArrayList<Integer> movesThisTurn = new ArrayList<>();
  public ArrayList<Integer> foodIndicators = new ArrayList<>();

  public void clear() {...}

  public boolean isCellNotVisited(Coordinates coordinates) {...}

  public void searchForFood(Coordinates coordinates, Creature creature) {...}

  public void findPathToFood(Coordinates coordinates) {...}

  public static void nextTurn() {...}
}
```

Смысл Action'ов состоит в том, что должен быть общий класс/интерфейс Action и его наследники.  
В каждом экшене должен быть только один публичный метод(не публичных может быть сколько угодно).  
Это вариация паттерна Command- экшены должны быть родственны и одинаково использоваться через полиморфизм.  
Примерно так:
```java
interface Action{
 void execute(Карта карта);
}

class ХодитьAction реализует Action {
  void execute(Карта карта) {
    //обойти всю карту
    //найти каждую креатуру
    //и дать ей пинка чтоб побежала
  }
}

class ДатьСигаретуВсемЗайцамAction реализует Action {
  void execute(Карта карта) {
    //обойти всю карту
    //найти всех зайцев и дать им по сигарете 
  }
}

List<Action> actions = List.of(new ДатьСигаретуВсемЗайцамAction(), new ХодитьAction());
for(Action a: actions) {
  a.execute(карта);
}

/*
Результат: программа обойдет всю карту, найдет всех креатур и даст им пинка, чтобы они побежали.
А каждому зайцу предварительно даст сигарету.
*/
```

Подробно рассматривать код в `InitAction` и `TurnAction` я не вижу смысла.  
Код там плохой не потому, что он плохой сам по себе, хотя он там в том числе плохой сам по себе.  

Код в этих классах плохой прежде всего из-за неправильной архитектуры экшенов, потому что они сделаны утилитами.  
Сначала переделай экшены в стиле паттерна Command, потом можно будет обсуждать их содержание.

**11. Нарушения ТЗ**

В проекте есть многочисленные нарушения ТЗ.  
Всегда внимательно читай ТЗ, не менее 3-х раз.  
Соблюдай требования ТЗ- это обязательная часть работы программиста.

В ТЗ есть раздел "Чеклист для самопроверки"- перед публикацией реализации, чекай его.

Например, там есть такой пункт:
```
Проблемы и ошибки в коде: Реализация алгоритма поиска пути внутри классов существ. Следует вынести это в отдельный класс
```

Имеется ввиду, что алгоритм поиска пути должен находиться в отдельном классе от всех прочих классов, в том числе Action и Entity.  
Создай *отдельный* класс поиска пути, который будет реализовывать вот такой интерфейс:
```java
public interface PathFinder {
  //ищет путь на карте от точки start
  //до точки, где находится существо нужного класса(напр. target == Grass.class)  
  List<Coordinates> find(Coordinate start, GameMap gameMap, Class<? extends Entity> target);  
}
```

**12. class Simulation**

Подробно рассматривать класс не вижу смысла. 
Все предыдущие ошибки архитектуры приводят к тому, что он плохой.

## АРХИТЕКТУРА

Фактически, проект написан в процедурном стиле.  
Вместо нормальных объектов, архитектура строится на утилитах.

Пока не разобрался с ООП на базовом уровне, не используй в своих ООП-программах утилитные классы.
То есть такие классы, которые содержат статические публичные методы.

Все пользовательские классы  ты на *этом этапе* должен в своих программах создавать через `new` и тем самым работать с ними как с экземплярами классов, а не классами-утилитами.

Статические изменяемые поля в классах это вообще абсолютное зло в ООП программе.  
В твоих объектно-ориентированных программах должен быть только один вид статических полей- константы.

Другие проблемы кода: спагетти-код из-за чрезмерного количества зависимостей между классами.

Есть многочисленные циклические зависимости.  
Например, class TurnAction использует Creature, а тот в свою очередь использует TurnAction
```java
public class TurnAction {

  public static void nextTurn() {
    //...
      herbivore.makeMove();  <-- TurnAction зависит от Herbivore
  }
}

public class Herbivore extends Creature {
  private final TurnAction turnAction = new TurnAction();
  
  public void makeMove() {
    turnAction.searchForFood(this.coordinates, this);
    //...
    TurnAction.movesThisTurn.add(1);  <-- Herbivore зависит от TurnAction
  }
}
```

## ВЫВОД

Не видно понимания ООП даже на самом базовом уровне: зачем нужны геттеры, сеттеры, конструкторы.  
Что такое инкапсуляция, полиморфизм, наследование и как их использовать в программе.  
Пока не разобрался с этой базой, дальше идти нельзя.

Почитай статьи, посмотри видосики на ютубе.  
Возможно, сделай пару простых упражнений на наследование и полиморфизм.

Подробнее разберись с Action'ами, посмотри ролики на ютубе про паттерн "Command", чтобы получить о нем общее представление.

Не видно и понимания декомпозиции- как делить программу на классы.  
Для лучшего понимания декомпозиции ООП посмотреть ролики Сергея про шахматы.  
Посмотреть ролики Немчинского про SOLID.

Сравнить различия между ООП и процедурным стилями:  
Стрим Сергея [Крестики-нолики в процедурном стиле](https://www.youtube.com/watch?v=PPikj1qHxrA)  
Мой стрим [Крестики-нолики в ООП стиле](https://t.me/zhukovsd_it_chat/53243/187097)

n.121(270)  
#ревью #симуляция  