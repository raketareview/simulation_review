https://github.com/AquaProdigy/Simulation  
[распознаётся графически]

В целом, норм.

## ХОРОШО

+ 👍 Спрайты существ не хранятся в самих существах
+ 👍 Координаты существ не хранятся в самих существах(мне так больше нравится)
+ 👍 Реализованы пауза/пуск во время работы
+ 👍 Разумно применяются интерфейсы

## ЗАМЕЧАНИЯ

**1. Нейминг**

- Для избежания путаницы не называй пакеты, классы, интерфейсы так же, как называются классы и интерфейсы стандартной библиотеки.  
Есть стандартный интерфейс `Map`, не должно быть кастомного пакета с таким же названием
```java
package org.example.map;

//ПРАВИЛЬНО:
package org.example.gamemap;
```

- Избыточно. Мы и так понимаем, что метод поиска в интерфейсе поиска пути ищет именно путь, а не что-то иное
```java
public interface PathFinder {
  List<Coordinates> findPath(...);
}

//ПРАВИЛЬНО:
public interface PathFinder {
  List<Coordinates> find(...);
}
```

- Венгерская ноттация.

В названии переменных не пиши тип данных, к которым они относится. И вообще не употребляй венгерскую ноттацию.  
Название переменной должно отвечать на вопрос что хранит переменная, а не как хранит.
```java
List<Coordinates> listCoordinatesToTarget = pathFinder.findPath(...);

//ПРАВИЛЬНО:
List<Coordinates> path = pathFinder.findPath(...);
```

- Не называй метод "run" если он не переопределяет `Runnable.run()`.

Название этого метода ассоциируется с этим стандартным интерфейсом и применением потоков.  
Здесь этот метод не имеет отношения к `Runnable.run()`
```java
public class DialogSimulation {
  public void run() {...}
}
```
*Oracle Java code conventions, part."Naming conventions"*  
*Мартин, "Чистый код", гл.2*  
*Ютуб, Немчинский "Как называть переменные, методы и классы?"*

**2. Используй классы через их интерфейсы**
```java
HashMap<Coordinates, Entity> entities = new HashMap<>();
HashMap<Coordinates, Coordinates> parents = new HashMap<>();

//ПРАВИЛЬНО:
Map<Coordinates, Entity> entities = new HashMap<>();
Map<Coordinates, Coordinates> parents = new HashMap<>();
```
Общее правило: ArrayList нужно использовать через List, HashMap- через Map и т.д.  
Это позволяет пользоваться преимуществами полиморфизма.

Да, бывают ситуации, когда, например, с LinkedList нужно работать именно как с LinkedList, а не с List. 
Но это уже нюансы.  
*"Java. Эффективное программирование", изд.3, гл.9.8*
```
"Если вы выработаете привычку использовать в качестве типов интерфейсы, ваша программа будет гораздо более гибкой" - Блох.
```

**3. record Coordinates(int x, int y)**

+ 👍 Record для координаты- идеально.

- Нарушение SRP.

При создании  экземпляра класса есть ограничения- нельзя создать экземпляр с отрицательными значениями осей
```java
if (y < 0 || x < 0) {
  throw new IllegalArgumentException("Coordinates coordinates cannot be negative");
}
```
Таким образом, класс знает не только про свою единую ответственность- как идентифицировать точку на плоскости.  
Класс еще знает про другие правила игровой логики- о том, что в проекте не должно быть отрицательных координат.  
Отрицательных координат в программе действительно может не быть, но сама координата об этом думать не должна.

Один из базовых принципов ООП состоит в том, что классы нужно делать универсальными.  
Это позволяет переиспользовать классы в других проектах, где нужны те же концепции.  
Например, концепция координаты.

Из школьной математики мы знаем, что в декартовой системе могут быть отрицательные координаты.  
Поэтому ограничение на положительные координаты это ограничение конкретного проекта, а не ограничение координаты как базовой концепции.  

Координата должна быть универсальной для повторного переиспользования в других проектах и иметь возможность быть отрицательной.

**4. class GameMap**

+ 👍 Геттеры не возвращают null, они возвращают Optional. Это хорошо.

- Неправильная реакция на ошибки.

Есть три основных реакции на ошибки: игнорирование, исправление, падение.  
Здесь выбрано игнорирование- при попытке вставить существо на несуществующую координату, операция не выполняется
```java
public void addEntity(Coordinates coordinates, Entity entity) {
  if (isCoordinatesInBounds(coordinates)) {
    entities.put(coordinates, entity);
  }
}
```

Правильная реакция должна быть другой, нужно сделать быстрое падение- кинуть исключение.  
Потому что попытка вставить существо за пределами карты означает, что в алгоритме есть баг, который нужно выявить и устранить.  
Попытка игнорировать проблему и замести ее под коврик приведет только к тому, что программа будет работать неправильно и мы это заметим не сразу.

Например представим, что в алгоритме действительно есть баг и программа будет пытаться передвинуть зайца за пределы карты.
Тогда при попытке это сделать, заяц будет удален со своей старой координаты и не записан на новую координату.  
То есть, заяц просто тихо исчезнет из карты- это глюк.

+ 👍 Метод норм. Хотя я бы возвращал не мапу, а только `List<T>`
```java
private <T extends Entity> Map<Coordinates, T> getEntitiesByType(Class<T> type)
```

- Нарушение OCP. 

Карта должна работать со всеми хранимыми существами одинаково.  
И не должна работать как-то по-особому с конкретным классом представителей и не должна даже знать по именам наследников Entity
```java
public Map<Coordinates, Creature> getAllCreatures() 
```
Если карта будет знать по именам наследников Entity и иметь для них персональные методы, то класс будет открыт для изменений.  
Например, при добавлении в проект класса Птица, понадобится изменить класс Карта и добавить в него метод `getAllBirds()`.

В этом классе уже есть универсальный метод `getEntitiesByType(Class<T> type)`, пусть клиент его юзает для получения всех креатур из карты.

- Сообщения эксепшенов делай информативными, а не формальными:
```java
throw new IllegalArgumentException("Coordinates out of bounds");

//ПРАВИЛЬНО:
throw new IllegalArgumentException("Coordinates out of bounds: " + coordinates);
```

+ 👍 В целом, с точки зрения SRP класс хороший.

**5. interface PathFinder**

+ 👍 Интерфейс поиска пути это хорошо. Теперь можно делать разные реализации поиска: BFS, A* и т.д.

+ 👍 Сигнатура метода поиска ок. Сразу понятно, как пользоваться классом
```java
List<Coordinates> findPath(GameMap map, Coordinates from, Class<? extends Entity> target);
```

**6. class BfsFinder implements PathFinder**

- Мапа координата-координата при реализации поиска обычно используется как эрзац-заменитель связного списка
```java
HashMap<Coordinates, Coordinates> parents = new HashMap<>();
```
Вместо мапы координата-координата сделай класс связного списка, алгоритм тогда будет выглядеть понятнее.
Например, так:
```java
class Node {
  private Coordinates coordinates; 
  private Node parent;
  //...
  public Coordinates getCoordinates() {...}
  public Node getParent() {...}
}
```

+ 👍 В целом, класс хороший.

**7. abstract class Entity и его простые наследники Tree/Rock/Grass**

👍 Идеально
```java
public abstract class Entity {
}

public class Tree extends Entity {
}
```

**8. Пакет valueobjects**

Здесь находятся три фактически одинаковых класса
```java
record Health(int value)
record AttackPower(int value)
record Speed(int value)
```

Каждый из них это сущность, задача которой- хранить только положительное число
```java
public record Speed(int value) {
  public Speed {
    if (value <= 0) {
      throw new IllegalArgumentException("Speed cannot be negative");
    }
  }
}

public record AttackPower(int value) {
  public AttackPower {
    if (value <= 0) {
      throw new IllegalArgumentException("Attack power value must be positive");
    }
  }
}
```
Если тебе в программе нужен класс, который хранит только положительное число, сделай такой класс: `class UnsignedInt`.  
А не три таких класса.

**9. public class Creature extends Entity**

- Тип еды, здоровье и скорость это равноценная информация.  
Нет объективных причин одно из них хранить в поле, а другое- получать из абстрактного метода
```java
protected Health health;
protected Speed speed;

public abstract Class<? extends Entity> getTargetClass();

//ПРАВИЛЬНО:
protected Health health;
protected Speed speed;
protected final Class<? extends Entity> food;
```

- Не вижу смысла в использовании специальных классов вместо примитива `int` для хранения скорости и здоровья
```java
protected Health health;
protected Speed speed;
```
При использованиии `int`, бросание исключения при отрицательных значениях можно сделать с помощью простого метода внутри класса `Creature`.  
Преимущества такого подхода будет в его простоте и наглядности.

- Нарушение инкапсуляции.  

Метод атаки является подробностью внутреннего устройства класса и используется только внутри своего класса и его наследников.  
Он не предназначен для использование клиентами, поэтому должен быть protected  
```java
public abstract void attack(GameMap gameMap, Coordinates targetCoordinates);
```
Клиент должен видеть только те методы, которые ему нужны для выполнения поставленной задачи.  
*Вайсфельд "Объектно-ориентированный подход", гл.5, "Минимальный открытый интерфейс"*

**10. class Herbivore/Predator extends Creature**

Нарушение DRY. Дублирование кода в `makeMove(...)` у Зайца и Волка
```java
Optional<Entity> targetEntity = gameMap.getEntity(targetCoordinates);
if (targetEntity.isEmpty()) {
  return;
}
if (!this.getTargetClass().equals(targetEntity.get().getClass())) {
  return;
}
```
Общий код выноси в общего предка.

**11. interface Action**

👍 Идеально
```java
public interface Action {
  void execute(GameMap map);
}
```

**12. class InitWorld implements Action**

- Почему класс называется "InitWorld" хотя он заселяет существ в "GameMap"?

- Нарушение DRY. 

Куча методов, которые делают одно и то же- заселяют в карту существ определенного типа
```java
  private void spawnGrass(int count, GameMap map) {
    for (int i = 0; i < count; i++) {
      map.addEntity(createRandomCoordinates(map), new Grass());
    }
  }

  private void spawnRock(int count, GameMap map) {
    for (int i = 0; i < count; i++) {
      map.addEntity(createRandomCoordinates(map), new Rock());
    }
  }
```

Сделай один универсальный метод. Например:
```java
public void execute(GameMap gameMap) {
  spawn(gameMap, ()-> new Заяц(1, 2), КОЛИЧЕСТВО_ЗАЙЦЕВ);
  spawn(gameMap, ()-> new Волк(2, 3, 4), КОЛИЧЕСТВО_ВОЛКОВ);
  //...
}

private void spawn(GameMap gameMap, Supplier<Entity> entitySupplier, int count) {
  for(....) {
    Coordinates coordinates  = getRandomEmptyCoordinates(gameMap);
    Entity entity = entitySupplier.get();
    gameMap.setEntity(coordinates, entity);
  }  
}
```

**13. class MoveCreatures implements Action**

👍 Всё ок.

**14. interface Renderer**

👍 Интерфейс рендерера это хорошо.  
Теперь можно делать разные рендереры для разных визуальных сред(консоль, интерфейс виндовс, http, матричный принтер etc)  
и разного отображения информации(цветной, черно-белый и пр.)

**15. class ConsoleRenderer implements Renderer**

- В switch-case в default нужно кидать исключение, а не возращать дефолтный спрайт. 

Потому что при добавлении нового существа в проект, можно забыть добавить его спрайт в распечатку.
И тогда существо будет жить на карте своей жизнью, но мы этого не увидим- вместо этого существа будет распечатываться дефолтный спрайт
```java
return switch (entity) {
  case Grass g -> "\uD83C\uDF3F";
  case Rock r -> "\uD83E\uDEA8";
  //...
  default -> EMPTY_CELL;  <-- ТУТ НУЖНО КИНУТЬ ИСКЛЮЧЕНИЕ
};
```

+ 👍 В целом, класс норм.

**16. class Simulation**

- Нарушение конвенции кода. Константы должны стоять выше остальных полей.

- Action'ы объявляются индивидуально
```java
private final InitWorld initWorld;
private final MoveCreatures moveCreatures;
```

На самом деле экшены нужно объявлять не индивидуально, а группой и дальше использовать через полиморфизм.  
Примерно так:
```java
private final List<Action> initActions = List.of(new UnoAction());
private final List<Action> turnActions = List.of(new DosAction(), new TresAction(), new MoveAction());

public Simulation(...) {
  //...
  executeActions(initActions);  //выполнение экшенов на старте
}

public void nextTurn() {
  //...
  executeActions(turnActions);  //выполнение экшенов на каждом ходе
}

private void executeActions(List<Action> actions) {
  for(Action a : actions) {
    a.execute(карта);
  }
}
```
Даже если сейчас есть только один init-экшен и один turn-экшен, все равно их нужно хранить группой- например, в листе.  
Это позволит при необходимости быстро добавить в эти группы новые экшены, которые так же будут использоваться через полиморфизм.

**17. class Main**, содержит точку входа main

+ 👍 Только создает и запускает симуляцию, это хорошо.

+ 👍 Правильно формирует зависимости(через использование интерфейсов) и инжектит их в симуляцию.

## ВЫВОД

В целом, норм 👍

n.139(299)  
#ревью #симуляция 