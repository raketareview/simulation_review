https://github.com/dymonwesa/simulation-project-2-7-zhukovs-pet-project  
[распознаётся графически]

Есть много над чем нужно вдумчиво поработать. В целом удручающе.

## НЕДОСТАТКИ РЕАЛИЗАЦИИ

1. В программе не реализована работа с потоками и выполнение старт/стоп во время работы симуляции.  
Хотя это требование тоже есть в ТЗ.

## ХОРОШО

+ 👍 Работает. 

## ЗАМЕЧАНИЯ

**1. Нейминг**

- Названия пакетов нужно писать стилем alllowercase 
```java
package WorldMap;

//ПРАВИЛЬНО:
package worldmap;
```

- Аббревиатуры в названиях лучше писать так же, как и другие слова
```java
class UIClass

//ЛУЧШЕ:
class UiClass
```
Например, какое название понятнее: HTTPURL или HttpUrl?  
*Блох "Java. Эффективное программирование", изд.3, гл.9.12.*

- Никогда в названия классов и интерфейсов не пиши слова "класс" и "интерфейс"
```java
class UIClass

//ПРАВИЛЬНО:
class Ui
```

- Избыточно
```java
public class WorldMap {
  private int widthOfMap;
  private int heightOfMap;
  private int sizeOfMap;
  //...
}

//ПРАВИЛЬНО:
public class WorldMap {
  private int width;
  private int height;
  private int size;
  //...
}
```

- Придерживайся единообразия
```java
public Position getNextPosition(Position p, WorldMap worldMap) {...}
public void makeStep(Position position, WorldMap worldMap) {...}

//ПРАВИЛЬНО:
public Position getNextPosition(Position position, WorldMap worldMap) {...}
public void makeStep(Position position, WorldMap worldMap) {...}
```

*Oracle Java code conventions, part."Naming conventions"*  
*Мартин, "Чистый код", гл.2*  
*Ютуб, Немчинский "Как называть переменные, методы и классы?"*

**2. Нарушение DRY**

- Магические буквы, числа, слова. Вводи константы 
```java
System.out.println("1. Симуляция n раз.");
System.out.println("2. Симуляция до истребления травоядных.");
System.out.println("0. Выход.");

case 1:  //...
case 2:  //...
case 0:  //...


//ПРАВИЛЬНО:
private static final int UNO_SOME_NAME = 1;
private static final int DOS_SOME_NAME = 2;
private static final int TRES_SOME_NAME = 0;
private static final 

System.out.println(UNO_SOME_NAME + ". Симуляция n раз.");
System.out.println(DOS_SOME_NAME + ". Симуляция до истребления травоядных.");
System.out.println(TRES_SOME_NAME + ". Выход.");

case UNO_SOME_NAME:  //...
case DOS_SOME_NAME:  //...
case TRES_SOME_NAME:  //...
```
*Фаулер, "Рефакторинг", гл.8, "Замена магического числа символической константой"*  
*refactoring.guru "Замена магического числа символьной константой"*

- Дублирование кода. Вводи вспомогательные методы.

```java
public class WorldMap {
  //...

  public void generateRandomObjectsOnMap() {
    fillEmptyMap();
    fillRandomPredatorOnMap(5);
    fillRandomHerbivoreOnMap(10);
    fillRandomGrassOnMap(20);
    fillRandomRockOnMap(30);
  }

  public void fillRandomHerbivoreOnMap(int number) {
    //одинаковый код
    cells.put(new Position(columns, rows), new Herbivore());
    //тоже одинаковый код
  }

  public void fillRandomPredatorOnMap(int number) {
    //одинаковый код
    cells.put(new Position(columns, rows), new Predator());
    //тоже одинаковый код
  }
  //...
}

//ПРАВИЛЬНО:
public class WorldMap {
  //...

  public void generateRandomObjectsOnMap() {
    fillEmptyMap();

    fillRandomEntityOnMap(5, () -> new Predator());
    fillRandomEntityOnMap(10, () -> new Herbivore());
    //...
  }

  public void fillRandomEntityOnMap(int count, Supplier<Entity> supplier) {
    //some code
    Position position = new Position(columns, rows);
    Entity entity = supplier.get();
    
    cells.put(position, entity);
    //oths code
  }
}
```

**3. Форматирование**

- Мысленные преобразования.

Не вставляй перевод строки в середину строк при распечатке.  
Иначе при чтении кода приходится мысленно "разворачивать" одну строку в несколько.  
Если нужно распечатать несколько строк, то печатай их или многострочными строками(""") или отдельными инструкциями печати.  
Тогда сразу будет видно, как текст будет выглядеть на печати:
```java
System.out.println("\n\nВведите цифру:");

//ПРАВИЛЬНО:
System.out.println();
System.out.println();
System.out.println("Введите цифру:");
```

**4. Используй классы через их интерфейсы**
```java
private HashMap<Position, Entity> cells;
public HashMap<Position, Herbivore> getHerbivorePosition()

//ПРАВИЛЬНО:
private Map<Position, Entity> cells;
public Map<Position, Herbivore> getHerbivorePosition()
```
Общее правило: ArrayList нужно использовать через List, HashMap через Map, HashSet через Set и т.д.  
Это позволяет пользоваться преимуществами полиморфизма.

Да, бывают ситуации, когда, например, с LinkedList нужно работать именно как с LinkedList, а не с List. 
Но это уже нюансы.  
*"Java. Эффективное программирование", изд.3, гл.9.8*
```java
"Если вы выработаете привычку использовать в качестве типов интерфейсы, ваша программа будет гораздо более гибкой" - Блох.
```

**4. class Position**

+ 👍 Нет ничего лишнего, это хорошо. Класс только хранит значения x,y. 

- Поля x,y тут должны быть final.

При прочих равных простые делай классы `immutable`.
```java
"Классы должны быть неизменяемыми, если только нет очень важной причины, чтобы сделать их изменяемыми",
"Вы всегда должны делать объекты с небольшими значениями, такими как PhoneNumber или Complex, неизменяемыми" - Блох. - Блох.
```
*Блох "Java. Эффективное программирование", изд.3, гл.4.3.*

- Класс может быть преобразован в `record` без потери функционала.

Record'ы по умолчанию умеют правильно делать `hashCode()`, `equals()` и `toString()`.  
Про возможности рекордов почитай [тут](https://docs.oracle.com/en/java/javase/21/docs/api/java.base/java/lang/Record.html).

**5. class WorldMap**

- Нарушение SRP, божественный класс, методы чужих ответственностей. 

Карта должна только хранить существа и обеспечить базовые операции с ними:  
Вставить, выдать одно существо и список всех хранимых существ, удалить.  
И методы, которые напрямую не управляют размещением существ, но необходимы для этого функционала:  
Сказать ширину/высоту карты и т.д.

Если какой-то метод не нужен для обеспечения хранения существ в карте, 
значит он не принадлежит к ответственности карты, а принадлежит к чужой ответственности.  

Здесь методы чужих ответственностей:  
🔸 Распечатать карту  
🔸 Выполнить ход существами  
🔸 Выполнить первоначальное заселение карты существами на случайные позиции  
🔸 Рассчитать свою площадь  
etc

Наверное, для проекта в целом полезно иметь метод, который распечатывает карту.  
Но этот процесс не имеет никакого отношения к единой ответственности карты- хранению существ в себе.  
Для организации хранения/удаления/выдачи существ, карте не нужно себя распечатывать.

Методы чужих ответственностей должны находиться в тех классах, в интересах которых они работают.  
Если один и тот же метод используют разные классы, то метод нужно вынести в отдельный класс, например, "BoardUtils".

Распечаткой карты должен заниматься отдельный класс. Например, `WorldMapRenderer`.

- Нарушение SRP, чужая ответственность, зависимость модели от представления.

Модель(а это модель) не должна ничего печатать в консоль
```java
public class WorldMap {
  //...
  
  public void printWorldMap() {
    //карта печатает сама себя в консоль
  }
}
```
Иначе модель перестает быть универсальной и становится заточенной под конкретную среду и конкретное представление себя в этой среде- в данном случае, консоль.
В других средах нужно будет менять код в этом классе, чтобы вывод осуществлялся по правилам этой среды, то есть, это лишняя причина для изменения класса.
В других средах(напр. Андроид) эту модель нельзя будет использовать- она там просто не скомпилируется. 
Другое представление для модели(напр. если одну и ту же модель нужно в программе показывать по-разному) нельзя будет сделать, или придется делать через костыль.

Подробнее необходимость отделения модели от представления обоснована в архитектуре MVC(model-view-controller).  
Но необходимость отделения модели от представления появляется не на уровне MVC. Оно появляется уже на уровне SOLID.  
Просто в MVC это требование более явно формализовано.

Модель, которая зависит от представления, перестает быть моделью.

- Карта не должна сама себя заселять существами.

Иначе она становится неуниверсальной и нельзя будет создать несколько конфигураций игры с разными начальными комбинациями существ на карте
```java
public void generateRandomObjectsOnMap() {
  //начальное заселение карты существами  
}
```
Объект не должен сам себя конструировать.  
*Мартин, "ЧК", гл.11, "Отделение конструирования системы от ее использования"*

- Карта не должна хранить пустые сущности `EmptyCell`.

Иначе теряется  все преимущество, которое дает `HashMap` для хранения сущностей
```java
public void fillEmptyMap() {
  for (int columns = 1; columns <= getWidthOfMap(); columns++) {
    for (int rows = 1; rows <= getHeightOfMap(); rows++) {
      putEntityAt(new Position(columns, rows), new EmptyCell());
    }
  }
}
```

Фактически, тут `Map` становится аналогом обычного массива, только хуже, бесполезно забивая память.  
Например, есть игровая доска 100x100 и в ней одномоментно находится общим числом 150 разных существ: камней, травы, зайцев и т.д.
Тогда при нормальном хранении этих данных в мапе с помощью ключ-значение, потребуется 150 записей.
Если в этой же мапе кроме этих значимых данных хранить еще пустые сущности, то потребуется уже 10.000 записей.  
То есть, эта карта будет хранить 98.5% ненужной информации.

`Map` должна хранить только значимые записи координата-entity.  
Пустое место на карте(земля) должно кодироваться отсутствием координаты в мапе.  
То есть, если какой-то координаты нет в Map'e, то это должно значить, что в Карте по этой координате не находится никаких существ.

- Нарушение OCP. 

Карта должна работать со всеми хранимыми существами одинаково.  
И не должна работать как-то по-особому с конкретными классами-наследниками Entity
```java
public HashMap<Position, Herbivore> getHerbivorePosition() {...}

//ПРАВИЛЬНО ТАК:
public List<Entity> getAll() //и тогда пусть клиент сам отбирает отсюда креатур

//ИЛИ ТАК:
public <T extends Entity> List<T> getEntitiesBy(Class<T> type)  //вернуть существ класса, указанного клиентом 
```
Если карта будет знать по именам наследников Entity и иметь для них персональные методы, то класс будет открыт для изменений.  
Например, при добавлении в проект класса Птица, понадобится изменить класс Карта и добавить в него метод `getBirds()`.

- Никогда не возвращай null
```java
private HashMap<Position, Entity> cells;
 
public Entity getEntityAt(Position position) {
  return cells.get(position);  <-- вернет null если position нет в cells
}

public Position getPosition(Entity entity) {
  //...
  return null;  <-- возвращает null
}
```
Возврат null повышает риск возникновения NullPointerException в программе.  
*Мартин, "Чистый код", гл.7.7-8*  
*Ютуб, Немчинский "Почему нельзя возвращать NULL?"*

- При всех операциях с участием координаты(добавить, выдать, удалить и т.д.), нужно проверять координату на корректность  
```java
public void putEntityAt(Position position, Entity entity) {
  cells.put(position, entity);
}

//ПРАВИЛЬНО:
public void putEntityAt(Position position, Entity entity) {
  validate(position);  <-- Если координата не в пределах карты, то бросает исключение  
  cells.put(position, entity);
}
```
Ближайшая аналогия- стандартные хранилища типа List и массива.  
При попытке обратиться к ним по несуществующему индексу, бросается исключение.

- В классе куча дублированного кода. Вводи вспомогательные методы.

**6. abstract class Entity**

- Нарушение SRP.

Существа не должны вести сами вести подсчет своей популяции
```java
private static int entityCounter = 0;

public Entity() {
  entityCounter++;
}
```
Если нужно вести учет существ, это должен делать какой-то отдельный класс.

- Нарушение SRP, зависимость модели от представления- существо хранит спрайт с собственным изображением
```java
public abstract String toCell();  <-- возвращает свое изображение 
```
Модель(а это модель) не должна зависеть от представления и знать, как ее будут показывать юзеру.  
Потому что в разных средах(консоль, Swing, Android) одна и та же модель может быть показана разными способами- пиксельной картинкой, анимацией etc.  
Спрайты всех существ должны храниться в классе, который распечатывает карту.

**7. class Creature extends Entity**

- Неправильный ООП дизайн в наследовании. 

Отсутствует конструктор, поэтому общие для потомков поля приходится инжектить не через конструктор, а через сеттеры
```java
public class Creature extends Entity {
  private int speed;
  //...

  //используется конструктор по умолчанию

  public void setSpeed(int speed) {
    this.speed = speed;
  }
  //....
}

//ПРАВИЛЬНО:
public class Creature extends Entity {
  private final int speed;
  //...

  public Creature(int speed, ...) {
    this.speed = speed;
    //...
  }

  //....
}
```

- Не делай пустые методы. Делай абстрактные методы
```java
public void makeMove() {
}

//ПРАВИЛЬНО:
public abstract void makeMove();
```

- Нарушение OCP.

Класс не должен знать про своих потомков и как-то учитывать различия своих потомков в логике своей работы
```java
public Boolean isMoveable(Position position, WorldMap worldMap) {
  if (worldMap.getEntityAt(position) instanceof EmptyCell || worldMap.getEntityAt(position) instanceof Grass) {  <-- ПРИМ.*
    return true;
  }
  return false;
}

*ПРИМ.: Класс знает и учитывает особенности поведения своих наследников Зайца и Волка
```
Потому что при добавлении новых потомков, придется переписывать предка и тем самым нарушать правило "класс должен быть закрыт для изменений".

Класс должен в себе содержать только общий ДЛЯ ВСЕХ потомков код.

- Сложные правила пользования классом.

В классе куча ПУБЛИЧНЫХ методов и поэтому пользоваться классом трудно.  

Этот класс должен быть более простым в использовании.  
Кроме геттеров и некоторых вспомогательных методов типа определить умерло существо или нет, тут должен быть только один публичный метод: `makeMove(...)`.  
Логика его работы должна быть примерно такая:
```java
//...
private final Class<? extends Entity> food;  //класс еды, которую ест существо

public Creature(int speed, Class<? extends Entity> food, ...) {...}

public void makeMove(GameMap gameMap) {
  Position current = gameMap.getPosition(this);
  List<Position> path = PathFinder.find(gameMap, current, food);
  //Пройти по пути path до еды
  //если хватит запаса хода- съесть еду
}
```

- Нарушение SRP.

Алгоритм поиска пути к цели нужно вынести в отдельный класс.  

Класс поиска пути должен просто искать путь от точки старта до точки, соответствующей заданным условиям согласно алгоритма BFS или A*.  
В случае алгоритма BFS- до точки, в которой находится существо нужного класса.  
Класс поиска пути должен выглядеть примерно так:
```java
public class BfsPathFinder {
  public List<Coordinates> find(Карта карта, Coordinates start, Class<? extends Entity> target) {
  //ищет путь на карте от точки start
  //до точки, где находится существо нужного класса(напр. target == Grass.class)
  }
}
```

**8. Наследники Creature: Заяц и Волк**

- Нарушение DRY.

У зайца и Волка куча общего кода.  
Нарушение чеклиста ТЗ: 
```java
Чеклист для самопроверки #
Дублирование кода между классами Herbivore и Predator
```

- Магические цифры: 50, 30 etc.

**9. class Simulation**

- Нарушение ТЗ, нет указанных для этого класса методов 
```java
Simulation #
Главный класс приложения, включает в себя:
...
nextTurn() - просимулировать и отрендерить один ход
startSimulation() - запустить бесконечный цикл симуляции и рендеринга
pauseSimulation() - приостановить бесконечный цикл симуляции и рендеринга
```

**10. Нарушение ТЗ. Отсутствуют классы и методы**

В этой реализации системно нарушается ТЗ, отсутствуют указанные в нём классы и методы.  
Нет семейства классов Action, нет класса поиска пути. 

Для других классов отсутствуют указанные для них в ТЗ методы.

Все это приводит к тому, что архитектура проекта становится хуже, нарушается SOLID, ответственности сущностей перемешиваются между собой.

## ВЫВОД

Программа сделана частично.

Работа программы не соответствует ТЗ: во время работы нельзя сделать паузу, один ход и бесконечную симуляцию.  
Нет указанных в ТЗ классов и методов.

Придерживайся ТЗ.  
Если в нем указано, что нужно сделать класс- делай его.  
Соблюдение ТЗ это важный навык для работы.

После написания проекта выполняй самопроверку по чеклисту ТЗ.

Доделать программу: сделать паузу/пуск во время работы программы.  
Это важно для отработки азов работы с многопоточностью.

Подробнее разберись с Action'ами, посмотри ролики на ютубе про паттерн "Command", чтобы получить о нем общее представление.

Не видно понимания SOLID и ООП на базовом уровне- зачем нужны конструкторы и т.д.  
Для лучшего понимания декомпозиции ООП посмотреть ролики Сергея про шахматы.  
Посмотреть на ютубе ролики Немчинского про SOLID- по одному ролику на каждый принцип.

Эталонная версия Симуляции с объяснениями есть у Сергея в расширенных материалах.

n.163(347)  
#ревью #симуляция 