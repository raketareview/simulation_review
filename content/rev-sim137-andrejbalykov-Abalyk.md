https://github.com/Abalyk/Simulation  
[Андрей Балыков]

Есть над чем поработать.

## ХОРОШО

+ 👍 Спрайты существ не хранятся в самих существах
+ 👍 Реализовано пауза/пуск во время работы

## ЗАМЕЧАНИЯ

**1. Нейминг**

- Геттер должен называться так же, как возвращаемое им поле + префикс "get"
```java
public Map<Cell, Entity> getWorldEntities() {
  return entities;
}

//ПРАВИЛЬНО:
public Map<Cell, Entity> getEntities() {
  return entities;
}
```

- Название должно объяснять, что делает метод. Этот метод не просто возвращает пустые координаты, он их еще и перемешивает
```java
List<Cell> getEmptyCells()

//ПРАВИЛЬНО:
List<Cell> getShuffledEmptyCells()
```

- Карта не может быть живой или неживой, она же карта
```java
public class World {
  //...
  public boolean isAlive() {
    return needRender;
  }
}

//ПРАВИЛЬНО:
public class World {
  //...
  public boolean isNeedRender() {
    return isNeedRender;
  }
}
```
*Oracle Java code conventions, part."Naming conventions"*  
*Мартин, "Чистый код", гл.2*  
*Ютуб, Немчинский "Как называть переменные, методы и классы?"*

**2. Нарушение DRY**, магические буквы, числа, слова. Вводи константы. 

Если к одной и той же магической штуке обращаются два разных класса, то делай ее константой в одном из этих двух классов и читай их отттуда.  
Либо перенеси ее в третий класс- эти данные должны быть синхронизированы между собой
```java
public class WorldRenderer {
  public static void renderMainMenu() {
    System.out.println("1 - Для начала бесконечной генерации мира без добавления новых объектов.");
    System.out.println("2 - Для начала бесконечной генерации мира с добавлением новых объектов.");
    System.out.println("3 - Генерация мира по ходам.");
    System.out.println("0 - Выход.");
  }
}

public enum SimulationMenu {
  EXIT("0"),
  INFINITE_NO_SPAWN("1"),
  INFINITE_WITH_SPAWN("2"),
  STEP_BY_STEP("3");
}
```
*Фаулер, "Рефакторинг", гл.8, "Замена магического числа символической константой"*  
*refactoring.guru "Замена магического числа символьной константой"*

**3. Нарушение конвенции кода**

В любой ситуации выделяй тело блока скобочками, даже если тело состоит из одной строки.  
Исключение- метод equals(), там можно после if не выделять блоки скобочками
```java
if (closedSet.contains(cell)) continue; //Пропускаем клетки из закрытого сета
if (!cell.equals(goal) && entities.get(cell) != null) continue; //Пропускаем клетки с препятствиями

//ПРАВИЛЬНО:
if (closedSet.contains(cell)) {
  continue; 
}
if (!cell.equals(goal) && entities.get(cell) != null) {
  continue; 
}
```
*"Oracle Java code conventions"*  

**4. class Cell**

+ 👍 Нет ничего лишнего, это хорошо. 
- Класс может быть преобразован в record без потери функционала.

**5. class World**

- Если объект в ходе работы не пересоздается, делай его final.  
Посмотри- там даже Idea  дает посказку желтым подчеркиванием
```java 
private Map<Cell, Entity> entities = new HashMap<>();

//ПРАВИЛЬНО:
private final Map<Cell, Entity> entities = new HashMap<>();
```

- Нарушение SRP, методы чужих ответственностей. 

Карта должна только хранить существа и обеспечить базовые операции с ними: вставить, выдать одно существо и список всех хранимых существ, удалить.  
Если какой-то метод не нужен для обеспечения этого функционала, значит он не принадлежит к ответственности карты, а принадлежит к чужой ответственности.
Здесь методы чужих ответственностей:  
Создает список пустых перемешанных координат, сообщает когда нужно распечатать карту, хранит фабрику существ.

Наверное, для проекта в целом полезно иметь метод, который сообщает, когда нужно распечатать карту.  
Но этот процесс не имеет никакого отношения к единой ответственности карты- хранению существ в себе.  
Для организации хранения/удаления/выдачи существ, карте не нужно знать, когда ее нужно печатать.  
Тем более, что одну и ту же карту в разных визуальных средах(консоль, swing, android) нужно печатать по разным правилам.

Методы чужих ответственностей должны находиться в тех классах, в интересах которых они работают.  
Если один и тот же метод используют разные классы, то метод нужно вынести в отдельный класс, например, BoardUtils.

- Учет необходимости распечатывания карты.

Принцип SRP гласит:
```
У класса должна быть только одна причина для изменения.
```
Здесь карты хранит правила, по которым нужно себя распечатывать.  
Например, при удалении существа из карты ставится флаг, что карту нужно распечатать:
```java
public void removeEntity(Cell cell) {
  if (entities.get(cell) != null) {
    entities.remove(cell);
    needRender = true;  <-- флаг необходимости распечатки карты
  }
}
```
Эти правила распечатки карты- лишняя причина для изменения класса, то есть, нарушение SRP.

Карта может сообщать миру о том, что в ней произошли какие-то перестановки хранимых существ.  
Но, во-первых, сообщать она должна именно о факте того, что в ней произошла перестановка существ.  
А не то, что нужно распечатать карту- это концептуально важная разница.  

Во-вторых, делать она карта должна через паттерн Callback  
[CallBack луноход](https://t.me/zhukovsd_it_chat/53243/139594)  
[CallBack разрушение](https://t.me/zhukovsd_it_chat/53243/184749)

Но я тебе предлагаю пока этим не заморачиваться и просто распечатывать карту на каждом ходе.

- Нарушение инкапсуляции, класс предоставляет клиентскому коду подробности внутреннего устройства и доступ к нему
```java
private Map<Cell, Entity> entities = new HashMap<>();

public Map<Cell, Entity> getWorldEntities() {
  return entities;
}
```
В данный момент клиентский код может выполнять любые операции с HashMap Карты напрямую, игнорируя допустимые картой способы. 
Например так
```java
Карта карта = new Карта();
<заселить карту существами>
карта.getWorldEntities().clear(); //геноцид- удаление из карты всех существ, минуя дозволенные картой механизмы
```
Из-за этого класс является гибридом со всеми вытекающими последствиями: *"Чистый код", гл.6*

Тут есть два варианта, как сделать правильно.  
Либо вернуть не оригинал мапы, а ее копию: 
```java
public Map<Coordinates, Entity> getEntities() {
  return new HashMap(entities);
}
```
Либо возвращать существ персонально:
```java
public Entity get(Coordinates coordinates)
public Coordinates getCoordinates(Entity entity)
```
Лично мне больше нравится второй вариант, когда существ возвращают персонально. 

- При всех операциях с участием координаты(добавить, выдать, удалить и т.д.), нужно проверять координату на корректность. 
Если координата некорректна(находится вне пределов карты), нужно бросать исключение.

Потому что сейчас можно поместить существо на координату, которая будет выходить за пределы карты:
```java
public void addEntity(Cell position, Entity entity) {  <-- Что, если передадут new Cell(+100500, -100500)?
  if (!entities.containsKey(position)) {
    entities.put(position, entity);
  }
}
```
Ближайшая аналогия- стандартные хранилища типа List и массива. 
При попытке обратитья к ним по несуществующему индексу, бросается исключение.

**6. class PathFinder**

- Соблюдай требования утилитных классов. 

Это утилитный класс, он содержит только статические методы.  
Утилитные классы должны быть `final` и иметь приватный конструктор.  
Не должно быть возможности унаследоваться от утилиты или сделать ее экземпляр.  
*Блох, "Java. Эффективное программирование", изд.3, гл.2.4*

- Нарушение SRP.

Этот класс должен только находить и вовзращать путь.  
Путь это последовательность точек от точки старта до дочки финиша.  
Поэтому в классе должен быть только один публичный метод- метод поиска пути.

Но кроме поиска пути, класс делает много разного.  
Например, в интересах класса `Creature` определяет, есть ли цель по соседству.  
Определение цели по соседству это не поиск пути и не не должно находиться в классе поиска пути.

- Без особой нужды не используй экзотические типы данных для возвращаемых значений.  
Используй простые и понятные всем типы данных
```java
public static Deque<Cell> findPath(...) 

//ПРАВИЛЬНО:
public static List<Cell> findPath(...) 
```

+ 👍 Сигнатура метода поиска по алгоритму А* в целом пойдет, за исключением типа возвращаемого значения
```java
public static Deque<Cell> findPath(Cell start, Cell goal, World world) 
```
Но имей ввиду, что если ты SRP поиска определяешь как поиск пути между точкой старта и точкой финиша,
то метод поиска координаты цели не должен находиться в этом классе.

Если ты SRP поиска определяешь как поиск пути между точкой старта и точкой соответствующей заданным условиям(там находится еда), 
тогда нужно поменять сигнатуру метода поиска.  
И тогда при этом поиск пути и поиск цели могут находиться в одном классе:
```java
public static List<Cell> findPath(World world, Cell from, Class<? extends Entity> target) {
    Coordinates to = getTargetCoordinates(world, from, target);
    //найти путь между from и to
} 

private static Coordinates getTargetCoordinates(World world, Cell from, Class<? extends Entity> target) {...}
```

- Два вида поиска в одном классе это что-то не то.  
Сейчас в классе, судя по каментам, есть два вида поиска- BFS и A*  
```java
//поиск цели через BFS
public static Cell findNearestTarget(Cell start, Class<? extends Entity> targetType, World world) {...}

//Поиск кратчайшего пути через алгоритм A*
public static Deque<Cell> findPath(Cell start, Cell goal, World world) {...}
```
BFS используется для поиска координаты цели, а A*- для поиска пути от старта до координаты цели вот так:
```java
targetCell = PathFinder.findNearestTarget(currentCell, SimulationConstants.ENTITY_TARGET.get(this.getClass()), world);
path = PathFinder.findPath(currentCell, targetCell, world);
```
Это двойная работа, потому что сейчас поиск проводится два раза подряд- сначала по BFS, потом- по A*.

Или просто используй BFS для поиска пути.  
Или придумай более оптимальный способ нахождения целей для A*, чем через использование BFS.

**7. abstract class Entity и его простые наследники**

Избыточно
```java
public abstract class Entity {
  public Entity() {
  }
}

public class Grass extends Entity {
  public Grass() {
    super();
  }
}

//ПРАВИЛЬНО:
public abstract class Entity {
}

public class Grass extends Entity {
}
```

**8. class Creature extends Entity**

- Циклическая зависимость.

Карта хранит в себе креатуры, а креатуры хранят в себе карту
```java
public class Creature extends Entity {
  //...  
  protected World world;
}
```
Чисто логически- если заяц хранит в себе карту, значит между этими классами отношение HAS-A.  
То есть Заяц включает в себя Карту как свою часть.   
Но карта не может быть частью зайца.  
Тут все должно быть ровно наоборот- Карта должна включать в себя всех наличных Зайцев в качестве типа связи "Агрегация".

Аналогия: 
Человек не хранит планету Земля в себе как часть себя.  
Планета Земля хранит в себе человека как часть себя.  
А когда человек улетает на Луну- Земля перестает его хранить, как часть себя.

Короче, в этом классе не храни Word как поле, просто получай его в метод совершения хода:
```java
public void makeMove(Cell currentCell) 

//ПРАВИЛЬНО:
public void makeMove(World world) 
```

- Креатура должна сама искать свои текущие координаты
```java
public void makeMove(Cell currentCell)

//ПРАВИЛЬНО:
public void makeMove(World world) {
  Cell current = world.getCell(this);
  //найти и пройти путь от current до цели
}
```
Если креатура будет принимать свое текущее местоположение извне, а не получать самостоятельно из карты, то это потенциально может привести к багам.  
Потому что креатура не может гарантировать, что пришедшая извне координата *действительно* соответствует ее текущему местоположению.  
Слепая вера клиенту тут вредна.

- Нарушение инкапсуляции, нарушение принципа минимального открытого интерфейса.

Куча публичных методов, которые используются только в самом классе и в его потомках. 
Такие методы должны быть private или protected.  

Клиент должен видеть только те методы, которые ему нужны для выполнения поставленной задачи.  
*Вайсфельд "Объектно-ориентированный подход", гл.5, "Минимальный открытый интерфейс"*

- Нарушение OCP.

Класс не должен знать своих потомков и как-то учитывать их в логике своей работы
```java
if (i != 0 && this.getClass() == Herbivore.class) //Травоядный может тратить ход либо на движение, либо на еду.
```
А тут `Creature` тоже что-то проверяет в интересах своего наследника- травоядного:
```java
if (target instanceof Grass) 
```
Потому что при добавлении новых потомков, придется переписывать предка и тем самым нарушать правило 
"класс должен быть закрыт для изменений и открыт для расширения".

Функционал, специфический для Травоядного, должен находиться в классе Травоядного, а не в его общем с Хищником предке.

**9. class Herbivore extends Creature**

- Нарушение дизайна ООП.  
При создании экземпляра класса, значения обязательных полей предка должны устанавливаться через конструктор, а не сетиться  
```java
public class Creature extends Entity {
  protected int speed;
  protected World world;
  //...
}

public class Herbivore extends Creature {
  public Herbivore(World world) {
    super(world);
    speed = SimulationConstants.HERBIVORE_SPEED;
  }
}

//ПРАВИЛЬНО:
public class Herbivore extends Creature {
  
  public Herbivore(World world) {
    super(world, SimulationConstants.HERBIVORE_SPEED;);
  }
}
```
*Эккель "Философия Java", гл.5, "Конструктор гарантирует инициализацию"*

- Нарушение дизайна ООП.  
Если поле в Creature является общим для всех наследников, показывай намерения более явно
```java
public class Herbivore extends Creature {
  public Herbivore(World world) {
    super(world);
    speed = SimulationConstants.HERBIVORE_SPEED;
  }
}

public class Predator extends Creature {
  public Predator(World world) {
    super(world);
    speed = SimulationConstants.PREDATOR_SPEED;
    attackPower = SimulationConstants.PREDATOR_ATTACK;
  }
}

//ПРАВИЛЬНО:
public class Herbivore extends Creature {
  public Herbivore(World world) {
    super(world, 
        SimulationConstants.HERBIVORE_SPEED, 
        SimulationConstants.HERBIVORE_ATTACK_POWER 
    );
  }
}

public class Predator extends Creature {
  public Predator(World world) {
    super(world,
        SimulationConstants.PREDATOR_SPEED,
        SimulationConstants.PREDATOR_ATTACK
    );    
  }
}
```

- Константный класс в программе используется неправильно.

Этот класс напрямую лезет в константый класс, а должен *только* получать необходимые данные в конструктор
```java
public class Herbivore extends Creature {
  public Herbivore(World world) {
    super(world);
    speed = SimulationConstants.HERBIVORE_SPEED;
  }
}

//ПРАВИЛЬНО:
public class Herbivore extends Creature {
  public Herbivore(World world, int speed) {
    super(world, speed);
  }
}
```
Это делает классы неуниверсальными и зависимыми от константного класса.  

Условные примеры:
```java
public class Settings {
  public static final int ROOM_NUMBERS = 5;
}

//ПЛОХО:
public static void main(String[] args) {
  House house = new House();
  //oth code
}

public class House {
  private final int roomNumbers;

  public House() {
    this.roomNumbers = Settings.ROOM_NUMBERS;
  }

  public int getRoomNumbers() {
    return roomNumbers;
  }
}

//ХОРОШО:
public static void main(String[] args) {
  House house = new House(Settings.ROOM_NUMBERS);
  //oth code
}

public class House {
  private final int roomNumbers;

  public House(int roomNumbers) {
    this.roomNumbers = roomNumbers;
  }

  public int getRoomNumbers() {
    return roomNumbers;
  }
}
```
Про классы констант, конфигурации и их использование я писал [ТУТ](https://t.me/zhukovsd_it_chat/53243/176984)

Да, класс может получать в конструктор не отдельные поля, а целый объект с набором этих полей.  
Но там должна быть только нужная классу информация.  
*"ЧК", гл.3, "Объекты как аргументы"*

**10. class EntityFactory**

+ 👍 Фабрика существ это хорошо.

+ 👍 Удачно используется стандартный интерфейс Supplier
```java
private void register(Class<? extends Entity> type, Supplier<Entity> factory)
```

- Фабрика существ не является существом, поэтому не должна находиться в пакете `entity`.

- Текст в exception всегда должен быть на английском языке.
```java
throw new IllegalArgumentException("Невозможно создать объект: " + type);
```
Исключение это не просто телеграмма, которая летит сквозь слои. 
У exception особое назначение- если исключение вылетит и не будет перехвачено внутри программы, то аварийно прекратит выполнение программы.
Тогда на экране будет распечатано сообщение эксепшена, и это сообщение должно быть понятно сисадмину в любой точке планеты. 
А значит, сообщение должно быть на английском. 
Интерпритация исключения и перевод его на локальный язык должно происходить там, где это соответствует архитектуре программы или не происходить вовсе, если исключение не планируется перехватывать.

**11. abstract class Action**

- Всегда явно указывай область видимости полей и методов
```java
World world;

//ПРАВИЛЬНО:
protected final World world;
```

- Из чисто практических соображений, можно сделать удобнее 
```java
public abstract class Action {
  protected final World world;

  public Action(World world) {
    this.world = world;
  }
  abstract public void execute();
}

//ЛУЧШЕ:
public abstract class Action {
  abstract public void execute(World world);
}
```

**12. class EntityGenerateAction extends Action**

- Нарушение SRP, чужая ответственность, зависимость модели от представления.

Модель(а бизнес логика экшенов это модель) не должна ничего печатать в консоль
```java
if (emptyCells.isEmpty()) {
  System.err.println("Нет свободных клеток для " + entityType);
  return;
}
```
Иначе модель перестает быть универсальной и становится заточенной под конкретную среду и конкретное представление себя в этой среде- в данном случае, консоль.
В других средах нужно будет менять код в этом классе, чтобы вывод осуществлялся по правилам этой среды, то есть, это лишняя причина для изменения класса.
В других средах(напр. Андроид) эту модель нельзя будет использовать- она там просто не скомпилируется. 
Другое представление для модели(напр. если одну и ту же модель нужно в программе показвать по-разному) нельзя будет сделать, или придется делать через костыль.

В данном случае нужно просто бросить исключение в том случае, если нет свободных клеток.

- Не пиши сложную логику в условии if'ов- это очень плохо читается. Вводи поясняющие переменные
```java
for (int i = 0; i < ((world.getWidth() * world.getHeight()) * spawnRate) / 100; i++) {...}

//ПРАВИЛЬНО:
int названиеКотороеВсеОбъясняет = ((world.getWidth() * world.getHeight()) * spawnRate) / 100;
for (int i = 0; i < названиеКотороеВсеОбъясняет; i++) {...}
```
*Фаулер, "Рефакторинг", гл.6, "Введение поясняющей переменной"*

- Что-то магическое: 100.

**13. class CreaturesMoveAction extends Action**

👍 Норм.

**14. enum SimulationMenu**

Никогда не возвращай null
```java
public static SimulationMenu fromValue(String value) {
  //...  
  return null;
}
```

**15. class WorldRenderer**

- Что-то магическое: 
```java
System.out.print("\033[H\033[2J");
```

**16. class WorldRenderer**

- Название класса не соответствует тому, что он делает.  
Клас не только распечатывает World, но и много чего еще.  
Например, меню:
```java
public static void renderMainMenu() {
  System.out.println("Добро пожаловать в 2д симуляцию! Выберите вариант:");
  System.out.println("1 - Для начала бесконечной генерации мира без добавления новых объектов.");
  //...
}
```
Например, сообщения об ошибках:
```java
public static void printError(String error) {
  System.out.println(error);
}
```

- Неведомая магия
```java
System.out.print("\033[H\033[2J");
```

- Нарушение Low Coupling и правила одной операции, метод делает разное.

Метод называется "распечатать World", но почему-то принимает в себя не World, а Simulation
```java
public static void renderWorld(Simulation simulation) {
  World world = simulation.getWorld();  
  //...
}

//ПРАВИЛЬНО:
public static void renderWorld(World word) {...}
```
Кроме самой карты, метод распечатывает много всего разного: номер хода, пункт меню etc.


**17. class Simulation**

- Нарушение DI. Класс не должен инициализировать сам себя.  
В данном случае класс инициализирует сам себя тем, что сам создает карту, а не принимает ее в конструктор
```java
private void initWorld() {
  world = new World(SimulationConstants.WORLD_WIDTH, SimulationConstants.WORLD_HEIGHT);
  //...
}

//ПРАВИЛЬНО:
public Simulation(World word) {
  this.word = word;  
}
```
Таким образом нельзя создать несколько игровых конфигураций с разными картами.  
*Мартин, "ЧК", гл.11, "Отделение конструирования системы от ее использования"*

- Этот метод делает какие-то две разные вещи.  
Каждый метод должен выполнять только одну операцию
```java
private void initGenerateActions(World world) {
  for (var entityClass : SimulationConstants.entitiesToSpawn) {
    Action action = new EntityGenerateAction(world, entityClass);
    spawnActions.add(action);  <-- ПЕРВАЯ ОПЕРАЦИЯ, СОЗДАНИЕ ЭКШЕНА
    action.execute();  <-- ВТОРАЯ ОПЕРАЦИЯ, ВЫПОЛНЕНИЕ ЭКШЕНА
  }
}
```
*Мартин, "Чистый код", гл.3, "Правило одной операции"*

- Создание объекта и его использование делай разными операциями
```java
new EntitySpawnAction(world, entityClass).execute();

//ПРАВИЛЬНО;
Action action = new EntitySpawnAction(world, entityClass);
action.execute();
```

- Какая-то магия, используй константы
```java
if (decision.equalsIgnoreCase("e")) {
if (decision.equalsIgnoreCase("s")) {
WorldRenderer.printError("Неизвестная команда! Используйте: число, 'q' или 's'");
```

- Нарушение ТЗ.
```java
public void startNewSimulation()

//ПО ТЗ:
public void startSimulation()
```

- Нарушение ТЗ, указанный в нем метод отстутствует:
```java 
pauseSimulation()
```

**18. class Main**, содержит точку входа main

👍 Только создает и запускает Симуляцию, это хорошо.

## ВЫВОД

Проработать тему "Наследование в ООП":  
почему потомки должны инжектить данные в конструктор, а не сетить, и на каких уровнях иерархии классов нужно разполагать поля.  
Разобраться с принципом SRP.

n.137(296)  
#ревью #симуляция 