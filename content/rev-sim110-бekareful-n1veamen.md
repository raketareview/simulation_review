https://github.com/n1veamen/simulation2try  
[бekareful]

Есть много над чем поработать. Прогрмма не соответствует ТЗ.

## НЕДОСТАТКИ РЕАЛИЗАЦИИ

1. После запуска программы, происходит одновременная распечатка всех ходов.

То есть, это работает не как симуляция, а как комикс.  
Когда делаешь программу, нужно представлять, как ей будет пользоваться юзер.  
Юзеру(мне) не сильно интересно вручную прокручивать кучу картинок.

Программа должна через заданный промежуток времени, например 1 секунду, делать ход и распечатывать карту.

2. Исходя из этого, в программе не реализована работа с потоками и выполнение старт/стоп во время работы симуляции.  
Хотя это требование тоже есть в ТЗ.

## ХОРОШО

+ 👍 Работает 

## ЗАМЕЧАНИЯ

**1. Нейминг**

- Если метод называется "проверить", то он должен выполнить проверку, а результат вернуть в виде `boolean`
```
public Point checkUp() 
```

- Названия полей должны как можно лучше объяснять, что они хранят
```
GameMap.x
GameMap.y 

//ПРАВИЛЬНО:
GameMap.width
GameMap.height
```

- Однобуквенное название "e" принято для обозначения Exception'ов
```
Entity e

//ПРАВИЛЬНО:
Entity entity
```

- Но `Entity e` в определенном смысле было логично- это первая буква слова. Здесь почему оно "n"?
```
Entity n
```

- Допустимы только общепринятые сокращения-аббревиатуры: Ui, Url, Http etc. Это сокращение вообще не понятно 
```
int obstacleQty
```

- Не используй нестандартные аббревиатуры
```
PathFinder pf = new PathFinder(gameMap);

//ПРАВИЛЬНО:
PathFinder pathFinder = new PathFinder(gameMap);
```

- Хуже венгерской ноттации только венгерская ноттация, которая обманывает. 
Тип этой переменной- не `Map` и не `GameMap`
```
private String[][] map;
```
*Oracle Java code conventions, part."Naming conventions"*  
*Мартин, "Чистый код", гл.2*  
*Ютуб, Немчинский "Как называть переменные, методы и классы?"*  

**2. class Point**, координата

- Если поля в классе не изменяются, а они тут не изменяются, то такие поля должны быть final:
```
private int x;
private int y;

//ПРАВИЛЬНО:
private final int x;
private final int y;
```

- Этот класс *уже* представляет из себя координату
```
public class Point {  <-- КООРДИНАТА
  //...
  public int[] getCoordinate() {  <-- КООРДИНАТА
    return new int[]{this.x, this.y};
  }
}

//ПРАВИЛЬНО:
public class Point {
  //...
  public getX() {...};
  public getY() {...};
}
```

- Нарушение SRP

Это должен быть простейший класс, который должен содержать только два числа, обозначающие положение точки на плоскости: x,y или row,column.
Лучше даже, если этот класс будет record'ом.
Здесь же класс отвечает не только за хранение координаты, но выполняет другие действия, тем самым нарушая SRP: создает соседние координаты
```
public Point checkUp() {
  return new Point(this.x - 1, this.y);
}

public Point checkDown() {
  return new Point(this.x + 1, this.y);
}
//checkLeft(), checkRight()
```

**3. class GameMap**

- Нарушение конвенции:
  - некоторые поля объявлены ниже методов
  - конструктор стоит ниже других методов

- Можно возвращать размеры карты двумя отдельными гетерами- это самый простой и логичный вариант. 
Если по какой-то причине ты не хочешь так делать, но хочешь возвращать размер одним запросом, 
то возвращай размеры не в виде массива интов(это процедурный приём), а в виде объекта
```
public int[] getMapSize() {
  return new int[]{this.x, this.y};
}

//ПРАВИЛЬНО:
public Size getSize() {
    return new Size(x, y);
}

public record Size(int width, int height) {
};
```

- Оригинальный способ хранения существ- в связном списке 
```
private LinkedList<Entity> allEntities = new LinkedList<>();
```
Идея тут в том, что все существа хранят в себе свою координату. 
Ну а поэтому, зачем платить больше и содержать координату еще и в классе Карта?

Логика понятна, но она должны быть прямо противоположной: 
если хочешь держать координату *только в одном месте*, то держать её должен именно в Карте, а не в Entity.

Этот класс называется "Карта". А принцип работы карты, как явления реального мира, состоит в том, что мы находим что-то по координатам.  
То есть, первичное в карте это имено координата. 

Если карта работает по принципу "перебрал все существа и взял из них координату", то это не карта, а что-то иное. 
Мешок, Коробка или что-то типа того.

Карта должна хранить и существа и их координаты. 
Примерно, так:
```
private final Map<Point, Entity> entities
```

- Из предыдущего пункта вытекают разные другие проблемы, например, громоздкий метод определения пустой координаты:
```
public boolean isFree(Point point) {
  if (!allEntities.isEmpty()) {
    for (Entity e : allEntities) {
      if (!(e.getPos() == null) && e.getPos().equals(point)) {
        return false;
      }
    }
  }
  return true;
}
```

+ 👍 Этот метод норм. По крайней мере, на уровне его сигнатуры
```
public <T extends Entity> List<T> getAllEntities(Class<T> type)
```

- При всех операциях с участием координаты(добавить, выдать, удалить и т.д.), нужно проверять координату на корректность. 

Если координата некорректна(находится вне пределов карты), нужно бросать исключение
```
public void addEntity(Entity e) {
  allEntities.add(e);
}
```
Ближайшая аналогия- стандартные хранилища типа `List` и массива. При попытке обратитья к ним по несуществующему индексу, бросается исключение.

Здесь только существа хранят в себе свои координаты.  
Но кто сказал, что эти координаты обязательно будут находиться в пределах карты в тот момент, когда эти существа попытаются вставить в карту?

+ 👍 В целом, по своему функционалу, класс не нарушает SRP.

**4. class PathFinder**

Не забывай про инкапсуляцию
```
GameMap gameMap;

//ПРАВИЛЬНО:
private final GameMap gameMap;
```

+ 👍 Входящие аргументы поиска ок
```
public LinkedList<Point> findTarget(Point start, Class<? extends Entity> targetType)
```

- Используй классы через их интерфейсы
```
public LinkedList<Point> findTarget(Point start, Class<? extends Entity> targetType)

//ПРАВИЛЬНО:
public List<Point> findTarget(Point start, Class<? extends Entity> targetType)
```

- Процедурный стиль
```
if (next.getCoordinate()[0] >= gameMap.getMapSize()[0])

//ПРАВИЛЬНО:
if (next.getX() >= gameMap.getWidth())
```

- Мапа координата-координата обычно при реализации поиска используется как эрзац-заменитель связного списка
```
Map<Point, Point> parent = new HashMap<>();
```
Вместо мапы координата-координата сделай класс связного списка, алгоритм тогда будет выглядеть понятнее.
Например, так:
```
class Node {
  private Point; 
  private Node parent;
  //...
  public Point getPoint() {...}
  public Node getParent() {...}
}
```

- Никогда не возвращай null
```
public LinkedList<Point> findTarget(...) {
 //...
  return null;
}
```
Возврат null повышает риск возникновения NullPointerException в программе.  
*Мартин, "Чистый код", гл.7.7-8*  
*Ютуб, Немчинский "Почему нельзя возвращать NULL?"*

**5. class Actions**

- Идея `Action` здесь не осмыслена. 

Идея состоит в том, что нужно создать семейство родственных классов, объединенных общим интерфейсом.  
Каждый из этих классов должен делать что-то свое с картой: одна акция должна заселять карту существами, другая делать ходы и т.д.  
Action'ы, изложенные в ТЗ, это вариант реализации паттерна Command. 

Сейчас `Actions` у тебя это просто сборник функций, сделанный в стиле процедурного программирования.
Более того, утилитный класс. А значит, что он даже теоретически не предполагает полиморфизм
```
public class Actions {
  public static void initActions (GameMap gameMap) {...}
  public static void turnActions(GameMap gameMap, Renderer renderer, int counter) {...}
  public static void grassSpawner(GameMap gameMap) {...}
}
```

Смысл Action'ов состоит в том, что должен быть общий класс/интерфейс Action и его наследники.  
Это вариация паттерна Command- экшены должны быть родственны и одинаково использоваться через полиморфизм. Примерно так
```
interface Action{
 void execute(Карта карта);
}

class ХодитьAction реализует Action {
  void execute(Карта карта) {
    //обойти всю карту
    //найти каждую креатуру
    //и дать ей пинка чтоб побежала
  }
}

class ДатьСигаретуВсемЗайцамAction реализует Action {
  void execute(Карта карта) {
    //обойти всю карту
    //найти всех зайцев и дать им по сигарете 
  }
}

List<Action> actions = List.of{new ДатьСигаретуВсемЗайцамAction(), new ХодитьAction()};
for(Action a: actions) {
  a.execute(карта);
}

/*
Результат: программа обойдет всю карту, найдет всех креатур и даст им пинка, чтобы они побежали.
А каждому зайцу предварительно даст сигарету.
*/
```

- Нарушение DRY
```
for (int k = 0; k < obstacleQty; k++) {
  gameMap.addEntity(new Rock());
}
for (int i = 0; i < hebrivoreQty; i++) {
  gameMap.addEntity(new Hebrivore());
}

//ПРАВИЛЬНО:
spawnEntities(obstacleQty, () -> new Rock());
spawnEntities(hebrivoreQty, () -> new new Hebrivore());

private void spawnEntities(int numbers, Supplier<Entity> supplier) {...};
```

**6. abstract class Entity**

- Нарушение SRP, чужая ответственность, зависимость модели от представления.
Модель(а это модель) не должна хранить в себе спрайт своего изображения и вообще знать, как ее будут показывать
```
private String type;
```
Иначе модель перестает быть универсальной и становится заточенной под конкретную среду и конкретное представление себя в этой среде- в данном случае, консоль.
В других средах(напр. Андроид) модель нельзя будет использовать. 
Другое представление для модели(напр. если одну и ту же модель нужно в программе показвать по-разному) нельзя будет сделать, или придется делать через костыль.

Спрайты существ нужно хранить в Рендерере.

- Нарушение SRP

Существа не должны искать пустые координаты
```
Point getRandomFreePos(GameMap gameMap)
```
Поиск пустых координат в карте нужен для заселений в карту существ, но не для выполнения существми своей единой ответственности.

**7. class Creature extends Entity**

- Нарушение конвенции кода, конструктор объявлен где-то среди других методов.

- Придерживайся единообразия
```
public void setHP(int hp) {
  this.hp = hp;
}

public static void setSpeed(int s) {
  speed = s;
}

//ПРАВИЛЬНО:
public static void setSpeed(int speed) {
  this.speed = speed;
}
```

- Нарушение инкапсуляции.

Если метод предназначен для использования только внутри линии наследников, он должен быть protected
```
public abstract void attackTarget(LinkedList<Point> path, GameMap gameMap)
public void killFrom(GameMap gameMap)
```

**8. class Hebrivore extends Creature**

- Неправильное использование конфигурационного класса

```
public Hebrivore() {
  //...
  setHP(Config.HERBIVORE_HP);
}

//ПРАВИЛЬНО:
public Hebrivore(int hp, ...) {
  //...
}
```

- Нарушение ООП в наследовании
```
public Hebrivore() {
  super("\uD83D\uDD35");
  setHP(Config.HERBIVORE_HP);
  setSpeed(Config.HERBIVORE_SPEED);
}

//ПРАВИЛЬНО:
public Hebrivore(String sprite, int hp, int speed) {
  super(sprite, hp, speed);
}
```

- Объясняй магию. Что здесь значит это число?
```
if (path.size() == 2)
```

- Нарушение DRY. У зайца и Волка общий код в методе
```
public void makeMove(GameMap gameMap)
```
Общий код выноси в предка.

**9. class Config**

Конфигурационный класс в программе используется неправильно.

Другие классы напрямую лезут в конфиг за своими данными, а должны только получать эти данные в свой конструктор.  
Это делает классы неуниверсальными и зависимыми от конфигурационного класса.  

Условный пример:
```
public class Settings {
  public static final int ROOM_NUMBERS = 5;
}

//ПЛОХО:
public static void main(String[] args) {
  House house = new House();
  //oth code
}

public class House {
  private final int roomNumbers;

  public House() {
    this.roomNumbers = Settings.ROOM_NUMBERS;
  }

  public int getRoomNumbers() {
    return roomNumbers;
  }
}

//ХОРОШО:
public static void main(String[] args) {
  House house = new House(Settings.ROOM_NUMBERS);
  //oth code
}

public class House {
  private final int roomNumbers;

  public House(int roomNumbers) {
    this.roomNumbers = roomNumbers;
  }

  public int getRoomNumbers() {
    return roomNumbers;
  }
}
```
Про классы констант, конфигурации и их использование я писал [ТУТ](https://t.me/zhukovsd_it_chat/53243/176984)  
*"ЧК", гл.3, "Объекты как аргументы"*

**10. class Simulation**

Не реализует функционал согласно ТЗ.

## ВЫВОД

Есть простейшие нарушения конвенции кода, почитай *"Oracle Java code conventions"*.  
Подробнее разберись с Action'ами, посмотри ролики на ютубе про паттерн "Command", чтобы получить о нем общее представление.

Для лучшего понимания декомпозиции ООП посмотреть ролики Сергея про шахматы.  
Посмотреть ролики Немчинского про SOLID.


n.110(251)  
#ревью #симуляция 