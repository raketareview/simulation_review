https://github.com/Kirillzhukov737/simulation  
[Kirill Zhukov]

В целом, норм.

## НЕДОСТАТКИ РЕАЛИЗАЦИИ

1. В качестве хищников- тигры, травоядные- слоны. 
Не слышал, чтобы в реальности тигры охотились на слонов. 

2. Хищники на протяжении десятков ходов никак не могут догнать и съесть травоядных, все время отставая от них на шаг  
![pic](https://github.com/raketareview/simulation_review/blob/master/content/resources/rev-sim119/img0.png) 

## ХОРОШО

+ 👍 Спрайты существ не хранятся в самих существах
+ 👍 Реализовано пауза/пуск во время работы
+ 👍 Дополниительная сущность- Вода. 

## ЗАМЕЧАНИЯ

**1. Нейминг**

- В названии переменных не пиши тип данных, к которым они относится. И вообще не употребляй венгерскую ноттацию.  
Название переменной должно отвечать на вопрос что хранит переменная, а не как хранит
```java
private final Map<Coordinate, Entity> map = new HashMap<>();

//ПРАВИЛЬНО:
private final Map<Coordinate, Entity> entities = new HashMap<>();
```

- В названия не нужно вставлять частицы "Of", "The" и т.д. 
Если конечно там "Of" не используется в контексте valueOf()
```java
List<T> getEntitiesOfType(Class<T> type)

//ПРАВИЛЬНО:
List<T> getEntitiesBy(Class<T> type)
```

- Аббревиатуры предпочтительнее писать так же, как другие названия 
```java
class BFSPathFinding

//ЛУЧШЕ:
class BfsPathFinding
```
Тогда сразу видно, где заканчивается одно слово и начинается другое, например: HTTPURL и HttpUrl.

- Лучше называть сущности более персонализировано. Примеры: Reader, а не Reading; Manager, а Menagement; Renderer, а не Rendering
```java
class BFSPathFinding

//ПРАВИЛЬНО:
class BFSPathFinder
```

- Давай понятные, но лаконичные названия. 

Здесь в названии метода избыточно дополнительное уточнение, что в рамках класса поиска пути по алгоритму BFS происходит не просто поиск пути, но строительство пути к ближайшей(точке).
По умолчанию мы и так понимаем, что поиск пути это всегда поиск кратчайшего(или оптимального) пути, если специально не заявлено иное 
```java
public final class BFSPathFinding {
  public static List<Coordinate> buildPathToNearest(...) {...}
}

//ПРАВИЛЬНО:
public final class BFSPathFinding {
  public static List<Coordinate> find(...) {...}
}
```

- Этот интерфейс декларирует одно действие, а не много
```java
public interface Actions {
  void perform(GameMap map);
}

//ПРАВИЛЬНО:
public interface Action
```

- Только конструктор может называться так же, как его класс, остальные методы нет. 
Хотя бы потому, что название класса должно быть существительным, а метода- глаголом.
Здесь "render" это процесс, а "Renderer"- тот, кто выполняет этот процесс
```java
public class Render {
  public void render(GameMap map) {...}
}

//ПРАВИЛЬНО:
public class Renderer {
  public void render(GameMap map) {...}
}
```

- Название метода должно быть глаголом в повелительном наклонении, а это название- существительное
```java
void game()
```
*Oracle Java code conventions, part."Naming conventions"*  
*Мартин, "Чистый код", гл.2*  
*Ютуб, Немчинский "Как называть переменные, методы и классы?"*

**2. Нарушение конвенции кода**

Переменные, поля, константы нельзя объявлять через запятую
```java
private static final int HERBIVORE_COUNT = 3,
    PREDATOR_COUNT = 2,
    STATIC_OBJECT_COUNT = 2;

//ПАРВИЛЬНО:
private static final int HERBIVORE_COUNT = 3;
private static final int PREDATOR_COUNT = 2;
private static final int STATIC_OBJECT_COUNT = 2;
```

**3. Нарушение DRY**, магические буквы, числа, слова. Вводи константы 
```java
out.println(" Нажмите ENTER - для старта, P - для паузы, Q - для выхода ");

case "Q":  //...
case "P":  //...

//ПРАВИЛЬНО:
private final static String PAUSE = "P";
private final static String QUIT = "Q";

out.printf(" Нажмите ENTER - для старта, %s - для паузы, %s - для выхода   \n", PAUSE, QUIT);

case QUIT:  //...
case PAUSE:  //...
```
*Фаулер, "Рефакторинг", гл.8, "Замена магического числа символической константой"*  
*refactoring.guru "Замена магического числа символьной константой"*  

**4. Пакет model**

- Не все модели проекта находятся в этом пакете.

Экшены это модели, но они не находятся в пакете model.  
Экшены являются моделями потому что там находится бизнес логика, которая не осуществляет взаимодействие с представлением- ничего не печатает в консоль.

- Не все классы в пакете являются моделями.

Класс Render осуществляет взаимодействие с представлением- печатает в консоль.  
Следовательно, он не модель.

**5. Пакет util**

👍 Единственный класс в нем `BFSPathFinding`, и это действительно утилита.

**6. record Coordinate(int x, int y)**

👍 Нет ничего лишнего, это хорошо. Record для координаты- идеально.

**7. class GameMap**

- Нарушение SRP, методы чужих ответственностей. 

Карта должна только хранить существа и обеспечить базовые операции с ними: вставить, выдать одно существо и список всех хранимых существ, удалить.
Если какой-то метод не нужен для обеспечения этого функционала, значит он не принадлежит к ответственности карты, а принадлежит к чужой ответственности.
Здесь методы чужих ответственностей: найти соседние координаты, найти случайную пустую координату.

Наверное, для проекта в целом полезно иметь метод, который находит соседние координаты. 
Но этот процесс не имеет никакого отношения к единой ответственности карты- хранению существ в себе.  
Для организации хранения/удаления/выдачи существ, карте не нужно искать соседние координаты относительно заданной.  

Методы чужих ответственностей должны находиться в тех классах, в интересах которых они работают.
Если один и тот же метод используют разные классы, то метод нужно вынести в отдельный класс, например, GameMapUtils.

Поиск соседних координат относится к отвественности совершения хода/поиска пути, поиск случайной координаты- к ответственности первоначального заселения существ на карту.

Например, сейчас метод поиска случайной пустой координаты в карте нужен для начального заселения существ на случайные позиции на карте.
Если концепция изменится и будет принято решение существ поселять на карту не на случайные позиции, а на фиксированные,
тогда метод вообще станет не нужен. Карта при этом во всех случаях будет выполнять свое назначение- хранить существа и обеспечивать к ним доступ.  

Следовательно, наличие или отсутствие метода поиска случайной пустой координаты никак не влияет на функционирование карты.  
А значит, не имеет к ней прямого отношения. 

- Никогда не возвращай null
```java
private final Map<Coordinate, Entity> map = new HashMap<>();

public Entity getEntity(Coordinate coordinate) {
  return map.get(coordinate);  <-- может вернуть null
}

public Coordinate getRandomEmptyCoordinate() {
  //...
  return null;
}
```
Возврат null повышает риск возникновения NullPointerException в программе.  
*Мартин, "Чистый код", гл.7.7-8*  
*Ютуб, Немчинский "Почему нельзя возвращать NULL?"*

- При всех операциях с участием координаты(добавить, выдать, удалить и т.д.), нужно проверять координату на корректность. 
Если координата некорректна(находится вне пределов карты), нужно бросать исключение
```java
public boolean isCellEmpty(Coordinate coordinate) {
  return !map.containsKey(coordinate);
}
```
Сейчас, если у карты спросить, свободна ли ячейка с координатой (+100500, -100500), то карта скажет, что свободна. 
А правильный ответ- ячейки с такой координатой в карте нет вообще.

Ближайшая аналогия- стандартные хранилища типа List и массива. 
При попытке обратитья к ним по несуществующему индексу, бросается исключение.

+ 👍 Хороший метод 

```java
public <T extends Entity> List<T> getEntitiesOfType(Class<T> type)
```

- ООП подход: работай с сущностью в виде объекта, а не с сущностью в виде ее отдельных составляющих
```java
public List<Coordinate> getNeighbours(Coordinate c) {
  List<Coordinate> neighbours = new ArrayList<>();
  int[][] deltas = {{-1, 0}, {1, 0}, {0, -1}, {0, 1}};

  for (int[] d : deltas) {
    int nx = c.x() + d[0];
    int ny = c.y() + d[1];
    //...
    neighbours.add(new Coordinate(nx, ny));
  }
}

//ПРАВИЛЬНО:
private final static List<Coordinate> SHIFT_COORDINATES = List.of(new Coordinate(-1, 0), ...);

public List<Coordinate> getNeighbours(Coordinate c) {
  List<Coordinate> neighbours = new ArrayList<>();

  for (Coordinate shift : SHIFT_COORDINATES) {
    Coordinate neighbourCoordinate = sum(c, shift)
    neighbours.add(neighbourCoordinate);
  }
}
```

+ 👍 В целом, с точки зрения SRP, в классе нет существенных нарушений.

**8. class BFSPathFinding**

+ 👍 Сигнатура поиска- идеально. Как пользоваться классом, предельно понятно
```java
List<Coordinate> buildPathToNearest(GameMap map, Coordinate from, Class<? extends Entity> targetType)
```

- Мапа координата-координата обычно при реализации поиска используется как эрзац-заменитель связного списка
```java
Map<Coordinate, Coordinate> parentMap = new HashMap<>();
```
Вместо мапы координата-координата сделай класс связного списка, алгоритм тогда будет выглядеть понятнее.
Например, так:
```java
class Node {
  private Coordinates coordinates; 
  private Node parent;
  //...
  public Coordinates getCoordinates() {...}
  public Node getParent() {...}
}
```

+ 👍 В целом, класс хороший.

**9. abstract class Entity**

- Содержит координату. Но координата нужна только тому существу, которое ходит.  
Поэтому entities должны хранить координату только начиная с уровня `Creature`.

- Дискуссионно. 

Если нужно разделять существа на препятствия и не препятствия, лучше создай маркерный интерфейс и меть им препятствия
```java
public abstract class Entity {
  //...
  public abstract boolean isObstacle();
}

public class Rock extends Entity implements PassiveObject {
  public Rock(Coordinate coordinate) {...}

  @Override
  public boolean isObstacle() {
    return true;
  }
}

//ЛУЧШЕ
public interface Obstacle {
}

public class Rock extends Entity implements PassiveObject, Obstacle {
  public Rock(Coordinate coordinate) {...}
}
```
Иначе возникает вопрос- почему в Entity есть метод `isObstacle()`, но нет `isPassiveObject()`?

**8. abstract class Creature extends Entity implements AliveObject**

👍 Всё ок.

**10. Дублирование логики и данных в наследниках Creature**

- Общие данные наследников выноси в предка.

Herbivore и Predator едят еду. Это их общая родственная черта, а значит эта информация должна храниться в предке.  
Например, так:
```java
public class Herbivore extends Creature {
  private static final Class<? extends Entity> FOOD = Grass.class;

  public Herbivore(...) {
    super(FOOD, ...);
  }
  //...
}

public class Predator extends Creature {
  private static final Class<? extends Entity> FOOD = Herbivore.class;

  public Predator(...) {
    super(FOOD, ...);
  }
}
```

- Общую логику наследников выноси в предка.

Метод `makeMove(GameMap map)` в Herbivore и Predator содержит много общего кода. 
Например:
```java
public class Herbivore extends Creature {

  @Override
  public void makeMove(GameMap map) {
    List<Coordinate> path = BFSPathFinding.buildPathToNearest(map, getCoordinate(), Grass.class);  //Grass.class is food

    if (path == null || path.isEmpty()) {
      roamAround(map);
      return;
    }
    Coordinate nextCell = path.get(0);
    //...
  }
}

public class Predator extends Creature {
  @Override
  public void makeMove(GameMap map) {
    List<Coordinate> path = BFSPathFinding.buildPathToNearest(map, getCoordinate(), Herbivore.class);  //Herbivore.class is food

    if (path == null || path.isEmpty()) {
      roamAround(map);
      return;
    }
    Coordinate nextCell = path.get(0);
    //...
  }
}
```
Общую логику совершения хода выноси в предка- поиск пути к еде и т.д.  
В зайце и волке оставь только специфическую для них часть логики совершения хода.

**11. interface Actions**

+ 👍 Всё ок
```java
public interface Actions {
  void perform(GameMap map);
}
```

**12. class InitWorldAction implements Actions**

Нарушение DRY. Много методов, которые делают одно и то же- заселяют в карту существ определенного типа
```java
public void perform(GameMap map) {
  spawnRocks(map, STATIC_OBJECT_COUNT);
  spawnTrees(map, STATIC_OBJECT_COUNT);
  //...
}

private void spawnRocks(GameMap map, int count) {
  for (int i = 0; i < count; i++) {
    Coordinate coordinate = map.getRandomEmptyCoordinate();
    if (coordinate == null) {
      return;
    }
    map.addEntity(coordinate, new Rock(coordinate));
  }
}

private void spawnTrees(GameMap map, int count) {
  for (int i = 0; i < count; i++) {
    Coordinate coordinate = map.getRandomEmptyCoordinate();
    if (coordinate == null) {
      return;
    }
    map.addEntity(coordinate, new Tree(coordinate));
  }
}
```

Сделай один универсальный метод
```java
public void perform(GameMap map) {
  spawn(map, (c) -> new Rock(c), STATIC_OBJECT_COUNT);
  spawn(map, (c) -> new Tree(c), STATIC_OBJECT_COUNT);
  //...
}

private void spawn(GameMap gameMap, Function<Coordinates, Entity> entityMapper, int count) {
  for (int i = 0; i < count; i++) {
    Coordinate coordinate = gameMap.getRandomEmptyCoordinate();
    Entity entity = entityMapper.apply(coordinate);
    gameMap.addEntity(coordinate, entity);
  }
}
```

**13. class MoveCreaturesActions/SpawnGrassActions implements Actions**

👍 Всё ок.

**14. class Render**

+ 👍 Спрайты существ хранятся здесь, а не в самих существах. Это хорошо.

- Используй современную форму записи switch-case
```java
String result;
switch (entity) {
  case Predator p:
    result = SPRITE_PREDATOR;
    break;
  case Herbivore h:
    result = SPRITE_HERBIVORE;
    break;
  //...  
}
return result;

//ПРАВИЛЬНО:
return switch (entity) {
  case Predator -> SPRITE_PREDATOR;
  case Herbivore -> SPRITE_HERBIVORE;
  //...  
}
```
Эта форма записи switch-case используется начиная с версии java 14.

**15. class Simulation**

+ 👍 Принимает в конструктор достаточное количество зависимостей, в том числе списки действий. 
Это позволяет делать майны с разными игровыми конфигурациями, не меняя код в классе `Simulation`
```java
public Simulation(GameMap map, Render render, List<Actions> initActions, List<Actions> turnActions)
```

- Нарушение паттерна GRASP Creator.

Экземпляр рендерера нужно создавать внутри Симуляции, а не принимать в конструктор
```java
public Simulation(Render render, ...)

//ПРАВИЛЬНО:
public Simulation(...) {
  this.render = new Render();  
}
```
Паттерн Creator: создавать объекты должен тот, кто их использует.

Simulation не должен сам создавать экземпляр `GameMap` потому, что не знает, какого размера должна быть карта.  
Может быть даже несколько игровых конфигураций, где карта будет разного размера и с каждой из этих карт симуляция должна работать одинаково.    
Поэтому здесь симуляция принимает в конструктор экземпляр `GameMap` и это правильно.

Списки экшенов тоже можно принимать в конструктор.  
Благодаря этому можно создавать разные игровые конфигурации с разными экшенами, не меняя при этом код в классе Simulation.

Но `Render` это обычный класс, который не параметризируется при создании, как `GameMap`(установка размеров при создании карты).
Он не является интерфейсом, у которого могут быть разные реализации.   
Поэтому создаваться этот объект должен самой Симуляцией.

Да, теоретически можно предположить, что будет создан наследник класса `Render` и в нем будут переопределены базовые методы.
И тогда передача *такого* рендерера в конструктор симуляции имела бы практические последствия.

Но мы не будет этого предполагать.  
Потому что если бы у программиста были бы намерения в классе `Simulation` через полиморфизм использовать разные рендереры, 
он бы обозначил свои намерения тем, что принимал в конструктор симуляции рендерер в виде интерфейса или абстрактного класса.  
Но не просто как обычный класс `Render`.

**16. class Main**, содержит точку входа main

👍 Только создает и запускает Симуляцию, это хорошо.

## ВЫВОД

Каких-то критических недостатков не обнаружено. В целом, норм. 

n.119(267)  
#ревью #симуляция 