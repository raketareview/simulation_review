https://github.com/HDefender/Simulation-new  
[Bulat Ziganshin]

Есть много над чем поработать.

## НЕДОСТАТКИ РЕАЛИЗАЦИИ

1. Сделай паузу между совершениями ходов.  
Сейчас невозможно рассмотреть передвижение существ- так быстро мельтешит распечатка.

2. В программе не реализована работа с потоками и выполнение старт/стоп во время работы симуляции.  
Хотя это требование есть в ТЗ.

## ХОРОШО

+ 👍 Спрайты существ не хранятся в самих существах
+ 👍 Карта распечатывается красиво  
![pic](https://github.com/raketareview/simulation_review/blob/master/content/resources/rev-sim140/img0.png) 

## ЗАМЕЧАНИЯ

**1. Нейминг**

- Названия пакетов нужно писать стилем all lowercase 
```java
package com.example.Action;
package com.example.Entity.Objects;

//ПРАВИЛЬНО:
package com.example.action;
package com.example.entity.objects;
```

- Если в проекте есть класс `Field`, то все переменные с именем, включающим это название, должны быть экземплярами этого класса.
Если разные концепции называются одним и тем же именем, это приводит к путанице
```java
public class Field {
  private ConcurrentHashMap<Coordinates, Entity> field = new ConcurrentHashMap<>();
  //...
}

//ПРАВИЛЬНО:
public class Field {
  private ConcurrentHashMap<Coordinates, Entity> entities = new ConcurrentHashMap<>();
  //...
}
```

- Применяй стандартные названия для стандартных явлений
```java
public class Field {
  private final int horizontal;
  private final int vertical;
  //...
}

//ПРАВИЛЬНО:
public class Field {
  private final int width;
  private final int height;
  //...
}
```

- Должно быть либо "is/has", либо "contains"
```java
public boolean isContainEntity(Coordinates coordinates)

//ПРАВИЛЬНО ТАК:
public boolean containsEntity(Coordinates coordinates)

//И ТАК ПРАВИЛЬНО:
public boolean hasEntity(Coordinates coordinates)
```
Старайся давать стандартные названия по аналогии с известными названиями из стандартной библиотеки.  
Например, метод "contains" есть в `List`, `Set`, `String`.

- Аббревиатуры в названиях лучше писать так же, как и другие слова
```java
class SearchBFS

//ЛУЧШЕ:
class BfsPathFinder
```
Например, какое название понятнее: HTTPURL или HttpUrl?

- Названия методов должны быть глаголом в повелительном наклонении.  
"BFS" это название алгоритма, а не название действия
```java
public class SearchBFS {
  public LinkedList<Coordinates> bfs(...)
}

//ПРАВИЛЬНО:
public class BfsPathFinder {
  public LinkedList<Coordinates> find(...)
}
```

- Это название метода- существительное
```java
int herbivoreCounter()

//ПРАВИЛЬНО:
int countHerbivore()
```
*Oracle Java code conventions, part."Naming conventions"*  
*Мартин, "Чистый код", гл.2*  
*Ютуб, Немчинский "Как называть переменные, методы и классы?"*

**2. Форматирование строк**

Если нужно печатать или создавать строку с более, чем одним подстановочным значением или значение вставляется внутрь сообщения, используй форматирование- тогда сразу будет виден весь шаблон
```java
System.out.println("Herbivore with " + getCoordinates() + " moved to " + updated);

//ПРАВИЛЬНО:
System.out.printf("Herbivore with %s moved to %s  %n", getCoordinates(), updated);
```

**3. Используй классы через их интерфейсы**
```java
ConcurrentHashMap<Coordinates, Entity> field = new ConcurrentHashMap<>();
LinkedList<Coordinates> bfs(...)

//ПРАВИЛЬНО:
Map<Coordinates, Entity> field = new ConcurrentHashMap<>();
List<Coordinates> bfs(...)
```
Общее правило: ArrayList нужно использовать через List, HashMap- через Map и т.д.  
Это позволяет пользоваться преимуществами полиморфизма.

Да, бывают ситуации, когда, например, с LinkedList нужно работать именно как с LinkedList, а не с List. 
Но это уже нюансы.  
*"Java. Эффективное программирование", изд.3, гл.9.8*
```
"Если вы выработаете привычку использовать в качестве типов интерфейсы, ваша программа будет гораздо более гибкой" - Блох.
```

- При всех операциях с участием координаты(добавить, выдать, удалить и т.д.), нужно проверять координату на корректность. 
Если координата некорректна(находится вне пределов карты), нужно бросать исключение.

Потому что сейчас можно поместить существо на координату, которая будет выходить за пределы карты:
```java
public void put(Coordinates coordinates, Entity entity) {  <-- МОЖНО ВСТАВИТЬ СУЩЕСТВО НА КООРДИНАТУ (+100500, -100500)
  field.put(coordinates, entity);
}
```
Ближайшая аналогия- стандартные хранилища типа List и массива.  
При попытке обратитья к ним по несуществующему индексу, бросается исключение.

**4. Если в блоке if есть return(break, continue, throw, exit и т.д.), то else не пишется.**
 
В этом случае неважно, будет else или нет, так как программа будет работать одинаково, а код без `else` будет выглядеть читабельней
```java
if (coordinatesCreatureMap.get(current) instanceof Grass) {
  visited.add(current);
  goal = current;
  break;
} else {
   visited.add(current);
   queue.poll();
}

//ПРАВИЛЬНО:
if (coordinatesCreatureMap.get(current) instanceof Grass) {
  visited.add(current);
  goal = current;
  break;
} 
visited.add(current);
queue.poll();
```

**5. record Coordinates(int x, int y)**

+ 👍 Record для координаты- идеально.

- Это абсолютно лишние методы, рекорд по умолчанию имеет геттеры для своих полей
```java
int x = coordinates.getX();

public record Coordinates(int x, int y) {
  public int getX() {
    return x;
  }
  public int getY() {
    return y;
  }
}

//ПРАВИЛЬНО:
int x = coordinates.x();

public record Coordinates(int x, int y) {
}
```
Судя по всему, у тебя ошибочное представление о рекордах и ты не знаешь их возможностей.  
Ознакомься с официальной документацией на record [ТУТ](https://docs.oracle.com/en/java/javase/21/docs/api/java.base/java/lang/Record.html).

**6. class Field**

- Нарушение SRP, методы чужих ответственностей. 

Карта должна только хранить существа и обеспечить базовые операции с ними: вставить, выдать одно существо и список всех хранимых существ, удалить.  
Если какой-то метод не нужен для обеспечения этого функционала, значит он не принадлежит к ответственности карты, а принадлежит к чужой ответственности.  
Здесь методы чужих ответственностей:  
Вернуть класс существа по координате, определить что координата содержит креатуру etc.

Наверное, для проекта в целом полезно иметь метод, который определяет, что по координате находится объект типа `Creature`.  
Но этот процесс не имеет никакого отношения к единой ответственности карты- хранению существ в себе.  
Для организации хранения/удаления/выдачи существ, карте не нужно выяснять тип хранимого существа по координате.

Методы чужих ответственностей должны находиться в тех классах, в интересах которых они работают.  
Если один и тот же метод используют разные классы, то метод нужно вынести в отдельный класс, например, BoardUtils.

- Пустая клетка в карте должна обозначаться отсутствием координаты в хешмапе, а не хранением в хешмапе "земли".  
Иначе, это бесполезно забивает память и сводит на нет те преимущества, которое имеет хешмапа при хранении объектов
```java
public void updateMap(Coordinates old, Coordinates updated, Creature creature) {
  field.put(old, new Ground());
  creature.setCoordinates(updated);
  field.put(updated, creature);
}

//ПРАВИЛЬНО:
public void updateMap(Coordinates old, Coordinates updated, Creature creature) {
  field.remove(old);
  creature.setCoordinates(updated);
  field.put(updated, creature);
}
```

- Никогда не возвращай null
```java
private ConcurrentHashMap<Coordinates, Entity> field = new ConcurrentHashMap<>();

public Entity getEntity(Coordinates coordinates) {
  return field.get(coordinates);  <-- может вернуть null
}
```
Возврат null повышает риск возникновения NullPointerException в программе.  
*Мартин, "Чистый код", гл.7.7-8*  
*Ютуб, Немчинский "Почему нельзя возвращать NULL?"*

+ 👍 Хорошо, что возвращается копия мапы, а не оригинал.  
Тем самым соблюдается инкапсуляция и защищается внутреннее устройство класса
```java
public Map<Coordinates, Entity> getMap() {
  return new ConcurrentHashMap<>(field);
}
```

- Название прямо противоположно тому, что делает метод
```java
public boolean isContainEntity(Coordinates coordinates) {
  return field.get(coordinates) == null;
}

//ПРАВИЛЬНО:
public boolean isContainEntity(Coordinates coordinates) {
  return field.get(coordinates) != null;
}
```

- Метод совершения хода в карте- нарушение SRP.  
Теперь можно вызвать из карты метод хода и телепортировать зайца из одного края в другой минуя все правила игровой логистики
```java
Карта карта = new Карта(100, 100);
Заяц заяц = new Заяц();
карта.put(new Координата(0, 0), заяц);
карта.updateMap(new Координата(0, 0), new Координата (99, 99), заяц);

/* class Карта */
public void updateMap(Coordinates old, Coordinates updated, Creature creature) {
  field.put(old, new Ground());
  creature.setCoordinates(updated);
  field.put(updated, creature);
}
```
Должна ли карта учитывать логистику зайцев? Если да, то как?  
Единственно правильный ответ- карта вообще не должна иметь в себе метод телепортации.

В карте должен быть метод удаления существа по координате.  
Опционально- перегруженный метод, который удаляет существо по его объекту:
```java
public void remove(Coordinates coordinates)
public void remove(Entity entity)
```
И если клиент хочет переставить зайца из одной точки в другую, то он должен самостоятельно удалить зайца по старой координате и вставить его на новую координату.  
Тем самым, клиент будет единолично управлять передвижением существ, а не сваливать эту ответственность на карту.

**7. class SearchBFS**

- Нарушение SRP. 

Класс должен просто искать путь от точки старта до точки, соответствующей заданным условиям согласно алгоритму BFS.  
Эти условия класс должен принимать в себя и не определять эти условия самостоятельно, например путем анализа принадлежности Creature тому или иному виду существ
```java
public LinkedList<Coordinates> bfs(Map<Coordinates, Entity> coordinatesCreatureMap,
                                     Coordinates start) {
  if (coordinatesCreatureMap.get(start) instanceof Predator) {
    if (coordinatesCreatureMap.get(current) instanceof Herbivore) {...}
  }
}
//...

!(coordinatesCreatureMap.get(c) instanceof Rock) &&
!(coordinatesCreatureMap.get(c) instanceof Predator) &&
!(coordinatesCreatureMap.get(c) instanceof Tree) &&
!(coordinatesCreatureMap.get(c) instanceof Herbivore)

//ПРАВИЛЬНО:
public List<Coordinates> find(GameMap, Coordinates start, Clas<? extends Entity> target)
```
Сейчас класс выполняет не только свою прямую ответственность- поиск пути.  
Но берет дополнительную ответственность- решает за существ, какую еду они едят и учитывает это при выборе цели. 

**8. class StepCounterFromBFS**

Я так и не смог понять, что это такое и зачем.  
Одно ясно точно- это не счетчик шагов из BFS
```java
public class StepCounterFromBFS {
  public Coordinates countPathFromBfs (LinkedList<Coordinates> path, Creature creature) {...}
}
```
В существовании этого класса нет необходимости.  
Просто переделай правильно класс поиска пути BFS и грамотно его применяй.

**9. abstract class Entity**

- Содержит координату. Но координата нужна только тому существу, которое ходит.  
Поэтому entities должны хранить координату только начиная с уровня `Creature`
```java
public abstract class Entity {
  private Coordinates coordinates;
  //...
}
```

- Но прикол в том, что всё равно сейчас получить координату на уровне Entity невозможно.  
Потому что на этом уровне в объект невозможно вставить координату- она не инжектится в конструктор и не устанавливается через сеттер
```java
public abstract class Entity {
  private Coordinates coordinates;

  public Coordinates getCoordinates() {
    return coordinates;
  }
}
```

То есть, если для всех существ нужно иметь координату на уровне `Entity`, то и доступ к ней должен быть на этом уровне.  
Например:
```java
public abstract class Entity {
  private Coordinates coordinates;
  
  public Entity(Coordinates coordinates) {
    this.coordinates = coordinates;
  }

  public Coordinates getCoordinates() {...}
  public void setCoordinates(Coordinates coordinates) {...}
}
```

Хотя, повторюсь, на этом уровне вообще не должно быть координат.  
Вот таким должен быть идеальный Entity и его простые неходячие наследники в этом проекте:
```java
public abstract class Entity {
  //да, тут совсем пусто
}

public class Tree extends Entity {
}
```

**10. class Ground extends Entity**

Этого класса не должно существовать в этом проекте
```java
public class Ground extends Entity {
}
```
Пустую клетку в классе Карта нужно обозначать отсутствием координаты в ее хешмапе, а не объектом "Земля".

**11. abstract class Creature extends Entity**

- Если креатура должна принимать координату, то принимай координату, а не ее запчасти
```java
public Creature(int x, int y) {
  this.coordinates = new Coordinates(x, y);
}

//ПРАВИЛЬНо:
public Creature(Coordinates coordinates) {
  this.coordinates = coordinates;
}
```

- Все обязательные для правильного функционирования класса поля на этом уровне иерархии должны устанавливаться через конструктор. 
А не сетиться в наследниках
```java
protected Coordinates coordinates;
protected int speed;
protected int health;

public Creature(int x, int y) {
  this.coordinates = new Coordinates(x, y);
}

//ПРАВИЛЬНО:
protected Coordinates coordinates;
protected int speed;
protected int health;

public Creature(Coordinates coordinates, int speed, int health) {
  tthis.coordinates = coordinates;
  //...
}
```

- Для совершения хода, достаточно передать карту и класс поиска пути
```java
public void makeMove(Field field, SearchBFS searchBFS, StepCounterFromBFS stepCounterFromBFS)

//ПАРВИЛЬНО:
public void makeMove(Field field, SearchBFS searchBFS)
```

- Используй абстрактные методы.

Если метод должен быть обязательно перегружен в наследниках, а на текущем уровне не должен ничего делать,
то ни в коем случае не делай этот метод пустым.  
Вместо этого сделай абстрактный метод
```java
public void makeMove(Field field, SearchBFS searchBFS, StepCounterFromBFS stepCounterFromBFS) {
}

//ПРАВИЛЬНО:
public abstract void makeMove(Field field, SearchBFS searchBFS, StepCounterFromBFS stepCounterFromBFS);
```

**12. class Herbivore extends Creature**

- Нарушение дизайна ООП в наследовании. 

При создании экземпляра класса, значения обязательных полей класса должны инжектиться через конструктор предка, а не сетиться
```java
public Herbivore(int x, int y) {
  super(x, y);
  this.coordinates = new Coordinates(x, y);
  this.speed = 2;
  this.health = 10;
}

//ПРАВИЛЬНО:
private final static int SPEED = 2;
private final static int HEALTH = 10;

public Herbivore(Coordinates coordinates) {
  super(coordinates, SPEED, HEALTH);
}
```
*Эккель "Философия Java", гл.5, "Конструктор гарантирует инициализацию"* 

- Нарушение SRP, чужая ответственность, зависимость модели от представления.

Модель(а это модель) не должна ничего печатать в консоль
```java
public void makeMove(Field field, SearchBFS searchBFS, StepCounterFromBFS stepCounterFromBFS) {
  //...
  System.out.println("Herbivore with " + getCoordinates() + " moved to " + updated);
}
```
Иначе модель перестает быть универсальной и становится заточенной под конкретную среду и конкретное представление себя в этой среде- в данном случае, консоль.
В других средах нужно будет менять код в этом классе, чтобы вывод осуществлялся по правилам этой среды, то есть, это лишняя причина для изменения класса.
В других средах(напр. Андроид) эту модель нельзя будет использовать- она там просто не скомпилируется. 
Другое представление для модели(напр. если одну и ту же модель нужно в программе показвать по-разному) нельзя будет сделать, или придется делать через костыль.

Если модель должна что-то сообщить миру, она может это сделать через паттерн [CallBack](https://t.me/zhukovsd_it_chat/53243/139594).  

Вот к чему приводит зависимость модели от представления, карта рисуется в виндовый интерфейс, а модель пишет в консоль:  
https://github.com/raketareview/simulation_review/blob/master/content/rev-sim064-davidtagirov-DavidTagirov.md

Если модель должна что-то сообщить миру, она может это сделать через паттерн CallBack.  
[CallBack луноход](https://t.me/zhukovsd_it_chat/53243/139594)  
[CallBack разрушение](https://t.me/zhukovsd_it_chat/53243/184749)

Если на этом этапе разобраться с колбеком слишком сложно, то сделай хотя бы вот так
```java
public String makeMove(Field field, SearchBFS searchBFS, StepCounterFromBFS stepCounterFromBFS) {
  //...
  return "Herbivore with " + getCoordinates() + " moved to " + updated;
}
```
Это всё ещё плохо. Но не так ужасно, как печать в консоль непосредственно из модели.

- На каком основании травоядное заселяет карту травой? Оно же не агроном
```java
public void makeMove(Field field, SearchBFS searchBFS, StepCounterFromBFS stepCounterFromBFS) {
  //...  
  field.replaceGrassByGround(list.getLast());
}
```

**13. abstract class Action**

👍 Идеально
```java
public abstract class Action {
  public abstract void perform(Field field);
}
```

**14. class InitAction extends Action**

- Неправильное заполнение карты "землей". 

Карта не должна хранить объекты-землю в пустых ячейках.  
Иначе теряется  все преимущество, которое классу Карта дает HashMap для хранения сущностей
```java
for (int i = 0; i <= field.getHorizontal(); i++) {
  for (int j = 0; j <= field.getVertical(); j++) {
    if (field.isContainEntity(new Coordinates(i, j))) {
      field.put(new Coordinates(i, j), new Ground());
    }
  }
}
```
Фактически, в классе Карта её HashMap становится аналогом обычного массива, только хуже, бесполезно забивая память.  
Например, есть игровая доска 100*100 и в ней единомоментно находится общим числом 150 разных существ: камней, травы, зайцев и т.д.
Тогда при нормальном хранении этих данных в мапе с помощью ключ-значение, потребуется 150 записей.
Если в этой же мапе кроме этих значимых данных хранить еще пустые сущности, то потребуется уже 10.000 записей.

Map должна хранить только значимые записи координата-entity.  
Пустое место на карте(земля) должно кодировавиться отсутствием координаты в мапе.  
То есть, если координаты нет в мапе- там пусто, земля.

- Нарушение инкапсуляции и принципа минимального открытого интерфейса.

Публичным здесь должен быть только метод `perform(...)`.  
Остальные методы тут являются подробностями внутреннего устройства класса и должны быть private/protected.  
Клиент должен видеть только те методы, которые ему нужны для выполнения поставленной задачи.  
*Вайсфельд "Объектно-ориентированный подход", гл.5, "Минимальный открытый интерфейс"*

**15. class MoveAction extends Action**

- Хранить тут экземпляр Карты в качестве поля класса не нужно, потому что класс его не использует.  
А использует ту карту, которую передают в метод
```java
public class MoveAction extends Action {
  private Field field;  <-- Этот экземпляр Field не используется в классе
  //...

  public MoveAction(Field field) {
    this.field = field;
  }

  public void perform(Field field) {...}  <-- Этот экземпляр Field ИСПОЛЬЗУЕТСЯ в классе
}
```

- Не делай сложных вычеслений в условии if'ов, это трудно читается.  
вводи поясняющие переменные
```java
if (entry.getValue() instanceof Creature && field.isContainCreature(entry.getKey())) {
  ((Creature) entry.getValue()).makeMove(field, searchBFS, stepCounterFromBFS);
}

//ПРАВИЛЬНО:
//Лишняя ненужная проверка- field.isContainCreature(entry.getKey()
//В instanceof можешь сразу приводить к нужному типу
Entity entity = entry.getValue(); 
if (entity instanceof Creature creature) {
  creature.makeMove(field, searchBFS, stepCounterFromBFS);
}
```

**16. class Renderer**

+ 👍 Спрайты существ хранятся здесь, а не берутся из самих существ, это хорошо.

- Упрости код, сделай вспомогательный метод, который будет возвращать спрайт по существу.  
Типа такого:
```java
private String toSprite(Entity entity) {
  String name = entity.getClass().getSimpleName();
  return  switch(name) {
    case "Grass" -> GRASS;
    case "Rock" -> ROCK;
    //...
    default -> throw new IllegalArgumentException("Illegal entity: " + entity);
  };
}
```

**17. class Simulation**

+ 👍 Этот класс- не модель, поэтому печатать тут можно
```java
System.out.println("This is Simulation game!");
```

- Action'ы объявляются индивидуально
```java
private InitAction initAction;
private MoveAction moveAction;
```

На самом деле экшены нужно объявлять не индивидуально, а группой и дальше использовать через полиморфизм.  
Примерно так:
```java
private final List<Action> initActions = List.of(new UnoAction());
private final List<Action> turnActions = List.of(new DosAction(), new TresAction(), new MoveAction());

public Simulation(...) {
  //...
  executeActions(initActions);  //выполнение экшенов на старте
}

public void nextTurn() {
  //...
  executeActions(turnActions);  //выполнение экшенов на каждом ходе
}

private void executeActions(List<Action> actions) {
  for(Action a : actions) {
    a.execute(карта);
  }
}
```
Даже если сейчас есть только один init-экшен и один turn-экшен, все равно их нужно хранить группой- например, в листе.  
Это позволит при необходимости быстро добавить в эти группы новые экшены, которые так же будут использоваться через полиморфизм.

- Нарушение ТЗ, нет указанных для этого класса методов 
```java
Simulation #
Главный класс приложения, включает в себя:
...
pauseSimulation() - приостановить бесконечный цикл симуляции и рендеринга
```

**18. class Main**, содержит точку входа main

👍 Только создает и запускает Симуляцию, это хорошо.

## ВЫВОД

Доделать программу согласно ТЗ: сделать паузу/пуск во время работы программы.  
Это важно для отработки азов работы с многопоточностью.

Для лучшего понимания декомпозиции ООП посмотреть ролики Сергея про шахматы.  
Посмотреть ролики Немчинского про SOLID и Чистый код.

n.140(300)  
#ревью #симуляция 