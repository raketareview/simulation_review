https://github.com/NastyaPowerr/SimulationProject  
[Настя]

Есть над чем поработать.

## ХОРОШО

+ 👍 Спрайты существ не хранятся в самих существах
+ 👍 Координаты существ не хранятся в самих существах(мне так больше нравится)
+ 👍 Реализовано пауза/пуск во время работы
+ 👍 Перед запуском печатаются правила. Мелочь, но для юзера приятная и полезная

## ЗАМЕЧАНИЯ

**1. Нейминг**

- Не сокращай слова без необходимости
```java
for (Coordinates dir : DIRECTIONS) {...}

//ПРАВИЛЬНО:
for (Coordinates direction : DIRECTIONS) {...}
```

+ 👍 В целом, нейминг ок.

*Oracle Java code conventions, part."Naming conventions"*  
*Мартин, "Чистый код", гл.2*  
*Ютуб, Немчинский "Как называть переменные, методы и классы?"*

**2. record Coordinates(int x, int y)**

+ 👍 Нет ничего лишнего, это хорошо. Record для координаты- идеально.

- Для смещения принимай координату, а не ее составляющие
```java
public Coordinates shift(int x, int y) {
  return new Coordinates(this.x + x, this.y + y);
}

//ПРАВИЛЬНО:
public Coordinates shift(Coordinates other) {
  return new Coordinates(this.x + other.x, this.y + other.y);
}
```

**3. class SimulationMap**

- Координаты в карте должны начинаться с точки 0,0 а не 1,1
```java
public boolean isWithinBorders(Coordinates coordinates) {
  return coordinates.x() > 0 && coordinates.x() <= width && <-- РАСПОЛОЖЕНИЕ КООРДИНАТ НАЧИНАЕТСЯ С 1,1
      coordinates.y() > 0 && coordinates.y() <= height;
}
```
Википедия:
```java
В декартовой системе координат, начало координат — это точка, в которой пересекаются все оси координат. 
Это означает, что все координаты этой точки равны нулю. 
Например, на плоскости она имеет координаты (0,0)
```

+ 👍 Геттеры не возвращают null, а возвращают Optional. Это хорошо
```java
public Optional<Entity> getEntity(Coordinates coordinates) {
  return Optional.ofNullable(entities.get(coordinates));
}
```

+ 👍 Свое исключение для проблем с координатой это хорошо
```java
public void validate(Coordinates coordinates) {
  if (!isWithinBorders(coordinates)) {
    throw new InvalidCoordinateException(coordinates);
  }
}
```

+ 👍 При операциях с участием координаты выполняется ее валидация, это хорошо
```java
public void placeEntity(Coordinates coordinates, Entity entity) {
  validate(coordinates);  <-- БРОСАЕТ ИСКЛЮЧЕНИЕ ЕСЛИ КООРДИНАТА НЕ В ПРЕДЕЛАХ КАРТЫ
  entities.put(coordinates, entity);
}
```

- Здесь тоже нужно предварительно провалидировать координату
```java
public Optional<Entity> getEntity(Coordinates coordinates) {
  return Optional.ofNullable(entities.get(coordinates));
}
```

- Геттер должен называться так же, как поле, которое он возвращает.  
Но здесь правильнее назвать этот метод `toMap()`
```java
public Map<Coordinates, Entity> getMap() {
  return new HashMap<>(entities);
}

//ЛУЧШЕ:
public Map<Coordinates, Entity> toMap() {
  return new HashMap<>(entities);
}
```
Тем самым, даже на уровне нейминга, клиентам не раскрываются подробности внутреннего устройства класса `SimulationMap`.

+ 👍 С точки зрения SRP класс хороший, в нем нет лишних ответственностей.

**4. interface PathFinder**

+ 👍 Интерфейс поиска пути это хорошо. Теперь можно делать разные реализации поиска: A* и т.д.
```java
public interface PathFinder {
  List<Coordinates> getPath(SimulationMap map, Coordinates from, Coordinates to);
}
```

- Этот интерфейс подходит не для всех алгоритмов поиска.

Этот интерфейс подходит для алгоритма A* и не подходит для алгоритма BFS.  
Потому что BFS ищет не путь между точкой старта и точкой финиша.  
BFS сам ищет цель и прокладывает к ней путь, ему не нужно для начала работы знать `Coordinates to`.

Возможен общий интерфейс для A*, BFS и других алгоритмов поиска.  
Но тогда поиск будет интерпретироваться не просто как поиск пути между двумя точками.  
А как единый процесс поиска цели и прокладывания к ней пути.  
Сделать это можно примерно так:
```java
public interface PathFinder {
  List<Coordinates> find(SimulationMap map, Coordinates from, Class<? extends Entity> target);
}

public class BfsPathFinder implements PathFinder{
  @Override
  public List<Coordinates> find(SimulationMap map, Coordinates from, Class<? extends Entity> target) {
  //ищет путь на карте от точки from
  //до точки, где находится существо нужного класса(напр. target == Grass.class)
  }
}

public class AstarPathFinder implements PathFinder{
  @Override
  public List<Coordinates> find(SimulationMap map, Coordinates from, Class<? extends Entity> target) {
  //сначала находит координату с существом класса target  
  //потом ищет путь на карте от точки from до точки с целью
  }
}
```

**5. class BreadthPathFinder implements PathFinder**

Алгоритму BFS для поиска пути не нужно знать координату цели. Ему нужно знать тип цели, которую он ищет
```java
public List<Coordinates> getPath(SimulationMap map, Coordinates from, Coordinates target)

//ПРАВИЛЬНО(для BFS):
public List<Coordinates> getPath(SimulationMap map, Coordinates from, Class<? extends Entity> target)
```
Можно дискутировать, в чем с точки зрения SRP состоит единая ответственность поиска пути.  
Является ли поиск цели и прокладывание пути к ней одной ответственностью или двумя.  
Но факт состоит в том, что в своей канонической форме, алгоритм BFS самостоятельно ищет цель и одновременно прокладывает к ней путь.

Здесь у BFS забрали часть его функционала- кто-то другой предварительно ищет цель, а потом передает в BFS.

**6. abstract class Entity и его простые наследники Tree/Rock/Grass**

👍 Идеально
```java
public abstract class Entity {
}

public class Tree extends Entity {
}
```

**7. class SimulationContext**

- Божественный объект.

Я не понимаю, какую сущность из себя представляет этот класс.  
"Контекст" это не сущность, это фиговый листок, которым прикрыли божественный объект.  
Это сборник несвязанных между собой процедур.

Например, поиск случайной пустой координаты в карте, совершение хода существом, проверка того что цель закрыта etc.

Класс расформировать, методы перенести в те классы, частью ответственностей которых они являются.  
Например, `attack()` перенести в Хищника.

Поиск цели для поиска пути- часть ответственности алгоритма BFS.

Группу методов работы с картой(найти соседние координаты и т.д.), можно вынести в утилитный класс `GameMapUtils`.  
Если в этом классе сидят еще какие-то другие сущности, их тоже нужно вынести в отдельные классы.

**8. abstract class Creature extends Entity**

+ 👍 Хороший метод. Благодаря ему, существа потенциально могут есть не один тип еды, а много разных типов
```java
public abstract boolean isFood(Entity entity);
```

Тогда сигнатура поиска BFS тоже должна будет учитывать разные типы целей. Примерно, так:
```java
List<Coordinates> find(SimulationMap map, Coordinates from, Predicate<Entity> targetDetector);
```

- Для совершения хода, креатура принимает в себя объект-аргумент Context
```java
public void makeMove(SimulationContext context)
```
Поля и методы, которые хранятся в этом аргументе, нужны не всем наследникам креатуры.
Например, метод `attack()` нужен Хищнику, а Травоядному- нет.

На уровне Creature класс должен работать только с теми полями и методами, которые являются общими для всех потомков.

Для совершения хода, методу достаточно принимать карту, в рамках которой существо будет передвигаться:
```java
private final PathFinder pathFinder;
//...

public void makeMove(SimulationMap simulationMap) {
  Coordinates from = simulationMap.getCoordinates(this); 
  List<Coordinates> path = pathFinder.find(simulationMap, from, this::isFood);
  //пройти по пути path
  //и съесть еду
}
```

**9. Диалоги**

😎🤝😎

**10. interface Action**

Экшены должны производить действие над картой, а не над божественным объектом
```java
public interface Action {
  void execute(SimulationContext simulationContext);
}

//ПРАВИЛЬНО:
public interface Action {
  void execute(SimulationMap simulationMap);
}
```
ТЗ:
```java
Action - действие, совершаемое над миром. 
Каждое действие описывается отдельным классом и совершает операции над картой.
```

**11. Классы Action's**

Если убрать класс-контекст и работать только с картой, то норм.

**12. class EntityFactory**

+ 👍 Фабрика это хорошо.

- Не бросай базовый эксепшен. Конкретизируй ситуацию- бросай то исключение, которое подходит под этот конкретный случай
```java
public Entity create(Class<? extends Entity> entityClass) {
  //...
  throw new RuntimeException("Unknow entity class " + entityClass);
}
```
*Хорстманн "Java. Библиотека профессионала", т1., гл.11*
```java
"Не ограничивайтесь генерацией RuntimeException. 
Найдите подходящий подкласс или создайте собственный." - Хорстманн
```
Во многих случаях подходит исключение IllegalArgumentException. 
Здесь оно тоже подойдет.

**13. class SimulationInitializer**

Стандартным явлениям давай стандартные названия. Это фабрика симуляции
```java
public class SimulationInitializer {
  //...
  public static Simulation create() {...}
}
```

**14. class MapRenderer**

- Соблюдай требования утилитных классов.

Это утилитный класс, он содержит только статические методы.  
Утилитные классы должны быть `final` и иметь приватный конструктор.  
Не должно быть возможности унаследоваться от утилиты или сделать ее экземпляр.  
*Блох, "Java. Эффективное программирование", изд.3, гл.2.4*

+ 👍 Спрайты существ хранятся здесь, а не берутся из самих существ. Это хорошо.

**15. class Simulation**

+ 👍 Принимает в конструктор достаточное количество зависимостей, в том числе списки действий.  
Это позволяет делать майны с разными игровыми конфигурациями, не меняя код в классе `Simulation`
```java
public Simulation(SimulationMap simulationMap, PathFinder pathFinder, List<Action> initActions, List<Action> turnActions)
```

+ 👍 Всё ок.

**16. class SimulationController**

- Точку входа `main` нужно вынести из этого класса.

Этот класс не только конфигурирует и запускает симуляцию, но и управляет ее ходом.  
Поэтому точку входа `main` нужно вынести в отдельный класс, задача которого будет состоять только в том, чтобы собрать конфигурацию программы и запустить ее.  
*Мартин, "ЧК", гл.11, "Отделение конструирования системы от ее использования"*

Упражнение на понимание сути этого замечания:  
Создай два отдельный Main-класса.  
По запуску одного должна запуститься симуляция с возможностью делать старт/стоп. 
То есть так, как сейчас. 

По запуску другого- просто бесконечная симуляция без команд.

В ООП программе может быть много разных Main-классов, каждый из которых будет запускать разные конфигурации.

- Эти эмодзи нужно как-то синхронизировать с теми, что в рендерере
```java
String startSimulationMessage = """
  Добро пожаловать в симуляцию!
  \uD83D\uDC2F хищники охотятся на \uD83D\uDC07 травоядных.
  \uD83D\uDC07 травоядные ищут \uD83C\uDF31 растения для питания.
  """;
```

## ВЫВОД

В целом, норм. Но есть одно действительно важное замечание- божественный объект Context.  
С ним нужно разобраться, из-за него классы слишком тесно переплетены друг сдругом.

n.133(289)  
#ревью #симуляция 