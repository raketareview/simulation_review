https://github.com/avkalabin/simulation  
[Andrey Kalabin]

В целом, норм.

## НЕДОСТАТКИ РЕАЛИЗАЦИИ  

1. Карта распечатывается неровно  
![pic](https://github.com/raketareview/simulation_review/blob/master/content/resources/rev-sim112/img0.png)  
Нужно подобрать спрайты одинаковой ширины.

## ХОРОШО

+ 👍 Спрайты существ не хранятся в самих существах
+ 👍 Координаты существ не хранятся в самих существах(мне так больше нравится)
+ 👍 Реализовано пауза/пуск во время работы
+ 👍 Если существа не находят путь, то делают случайный ход
+ 👍 Существа имеют ограниченную область видимости
+ 🚀 Две разных игровых конфигурации

## ЗАМЕЧАНИЯ

**1. Нейминг**

- В java названия интерфейсов не пишут с префиксом "I"
```
interface IIteractionService
class InteractionService implements IIteractionService

//ПРАВИЛЬНО:
interface IteractionService
class InteractionServiceImp implements IteractionService
```
Писать названия интерфейсов через "I" требуется делать в некоторых других языках, например, в C#.  
В яве названия интерфейсов пишут либо с постфиксом "able", например Runnable. Либо просто называют существительным, например List.

- Названия переменных пишутся с маленькой
```
IAction IAction

//ПРАВИЛЬНО:
IAction action
```
*Oracle Java code conventions, part."Naming conventions"*  
*Мартин, "Чистый код", гл.2*  
*Ютуб, Немчинский "Как называть переменные, методы и классы?"*

**2. Используй классы через их интерфейсы**
```
HashMap<Coordinates, Entity> entities = new HashMap<>();

//ПРАВИЛЬНО:
Map<Coordinates, Entity> entities = new HashMap<>();
```
Общее правило: ArrayList нужно использовать через List, HashMap- через Map и т.д.  
Это позволяет пользоваться преимуществами полиморфизма.

Да, бывают ситуации, когда, например, с LinkedList нужно работать именно как с LinkedList, а не с List. 
Но это уже нюансы
```
"Если вы выработаете привычку использовать в качестве типов интерфейсы, ваша программа будет гораздо более гибкой" - Блох.
```
*"Java. Эффективное программирование" гл.9.8*

**3. Не используй статические импорты**

Становится непонятно, что используемый в классе метод или константа не его собственные, а чьи-то чужие
```
import static entity.Grass.HP_VALUE;

public class InteractionService implements IIteractionService {

  //...
  herbivore.heal(HP_VALUE);
}

//ПРАВИЛЬНО:
herbivore.heal(Grass.HP_VALUE);
```
*"ЧК", гл.17, G18*

**4. record Coordinates(int row, int column)**

👍 Нет ничего лишнего, это хорошо. Record для координаты- идеально.

**5. class WorldMap**

- Никогда не возвращай null
```
private final HashMap<Coordinates, Entity> entities = new HashMap<>();

public Entity get(Coordinates coordinates) {
  return entities.get(coordinates);  <-- может вернуть null
}
```
Возврат null повышает риск возникновения NullPointerException в программе.  
*Мартин, "Чистый код", гл.7.7-8*  
*Ютуб, Немчинский "Почему нельзя возвращать NULL?"*

- При всех операциях с участием координаты(добавить, выдать, удалить и т.д.), нужно проверять координату на корректность.

Сейчас, если у карты спросить, свободна ли ячейка с координатой (+100500, -100500), то карта скажет, что свободна
```
public boolean isEmpty(Coordinates coordinates) {
  return entities.get(coordinates) == null;
}
```
А правильный ответ- такой координаты в карте вообще нет.

Если координата некорректна(находится вне пределов карты), нужно бросать исключение.  
Ближайшая аналогия- стандартные хранилища типа List и массива. При попытке обратитья к ним по несуществующему индексу, бросается исключение.

- При попытке удалить существо по координате, где никого нет дома, нужно бросать исключение
```
public void removeEntity(Coordinates coordinates) {
  entities.remove(coordinates);
}
```
Потому что если алгоритм пытается удалить отсутствующее существо, значит алгоритм багует и это нужно выявить и устранить.  

+ 👍 В целом, с точки зрения SRP класс хороший.

**6. Пакет service**

- Термин "Service"

Пакет содержит классы, которые называются сервисами: `InteractionService`, `NavigationService` etc.  
Обычно слой "service" используется в определенных архитектурах, например MVC(S), где "S" означает сервис. Например, в Spring MVC.  
Или в других архитектурах с анемичной моделью, где модели содержат только состояние, а их поведение выносится в специальные классы-сервисы.  

В этой реализации не анемичные, а богатые модели. 
Поэтому для избежания путаницы, здесь лучше не использовать слово "Service" для названий пакетов и классов.

- Нарушение SRP, зависимость от представления

Ок, сервисы, так сервисы.  
Но классы-сервисы должны представлять из себя бизнес-логику работы с данными. 
Сервисы не должны ничего печатать в консоль или куда-то еще. 
То есть, сервисы не должны зависить от представления
```
public class InteractionService implements IIteractionService {
  @Override
  public void attack(WorldMap map, Coordinates attackerPosition, Coordinates targetPosition) {
    System.out.printf("Волк атакует! Урон: %d, HP жертвы: %d", damage, herbivore.getHp());  <-- ЗАВИСИМОСТЬ ОТ ПРЕДСТАВЛЕНИЯ
    //...
  }
}
```
Потому что бизнес-логика сервисов должна одинаково работать в любом окружении: консоль, веб, Андроид. 
И любой другой визуал, вплоть до аркадного автомата.

Сервисы(и модели) не должны ничего печатать самостоятельно.   
Если они должны что-то напечатать в окружающий мир, то они могут использовать паттерны Subscriber или Callback:  
[CallBack луноход](https://t.me/zhukovsd_it_chat/53243/139594)  
[CallBack разрушение](https://t.me/zhukovsd_it_chat/53243/184749)

**7. interface IIteractionService**

Нарушение ISP, интерфейс Зайцеволка.

функционал интерфейса относится к двум разным явлениям: Волку и Зайцу
```
public interface IIteractionService {
  void attack(WorldMap map, Coordinates attackerPosition, Coordinates targetPosition);  <-- ДЛЯ ВОЛКА
  void eatGrass(WorldMap map, Coordinates herbivorePosition, Coordinates grassPosition);  <-- ДЛЯ ЗАЙЦА
}
```
Должно быть два разных интерфейса. 
Или один интерфейс, но с единым общим методом для Волка и Зайца.  

**8. interface INavigationService**

Этот интерфейс выполняет роль поиска пути. В принципе, почему бы и нет.

Если бы просто был класс, который позиционировался, как класс поиска пути по карте, то он должен был бы содержать только один публичный метод- найти путь.
Но этот интерфейс имеет не один метод, а несколько
```
public interface INavigationService {
  Optional<Coordinates> findTarget(WorldMap map, Coordinates from, int range, Class<? extends Entity> targetType);
  List<Coordinates> findPath(WorldMap map, Coordinates start, Coordinates target, int speed);
  List<Coordinates> getAvailableMoves(WorldMap map, Coordinates from, int speed);
}
```
Учитывая, что интерфейс позиционируется не как "Поск пути", а как "Сервис навигации(для креатуры)", то ок.

- Нарушение SRP.

Поиск должен просто искать путь от точки старта до точки, соответствующей заданным условиям. 
В данном случае- до точки, в которой находится существо нужного класса.

Если поиск получает во входящие аргументы метода координаты цели, значит часть работы поиска уже выполнил кто-то другой. 
То есть, кто-то другой уже нашел цель и передал ее кординату в поиск пути
```
List<Coordinates> findPath(WorldMap map, Coordinates start, Coordinates target, int speed);
```
На самом деле поиск пути должен сам искать цель и прокладывать к ней путь. Сигнатура метода для этого должна выглядеть примерно так
```
List<Coordinate> findPath(WorldMap map, Coordinates start, Class<? extends Entity> target, int speed) {
  //ищет путь на карте от точки start
  //до точки, где находится существо нужного класса(напр. target == Grass.class)
}
```

Да, в интерфейсе описан специальный метод для поиска цели. Этот метод нужен для работы в паре с findPath(...)
```
Optional<Coordinates> findTarget(WorldMap map, Coordinates from, int range, Class<? extends Entity> targetType)
```
Но выносить этот метод в интерфейс точно не надо.

Во-первых потому, что этот метод является вспомогательным для `findTarget(...)` и представляет из себя подробности его внутренней организации.
А значит, метод `findTarget(...)` должен быть приватным и находиться в классе *конкретной реализации поиска*, а не в общем интерфейсе.

Во-вторых, не для всех алгоритмов поиска вообще нужно сначала искать координату цели, а потом прокладывать к ней путь.  
Для A* это надо. Для BFS это не надо.

Теоретически, раз существует специальный интерфейс `INavigationService`, то у него могут быть разные реализации с разными алгоритмами поиска.  
Значит, этот интерфейс должен содержать только общие абстракции, а не методы, которые описывают особенности отдельных поисковых алгоритмов. 

И тогда общий интерфейс должен выглядеть как-то так
```
public interface INavigationService {
  List<Coordinate> findPath(WorldMap map, Coordinates start, Class<? extends Entity> target, int speed);
  List<Coordinates> getAvailableMoves(WorldMap map, Coordinates from, int speed);
}
```

**9. class InteractionService implements IIteractionService**

- Нарушение SRP 
  - Реализует процесс еды Зайцеволка, проблема проистекает из имплементируемого интерфейса
  - Зависит от представления

- Мысли объектно 
```
if (herbivore.getHp() <= 0) {...}

//ЛУЧШЕ:
if (!herbivore.isAlive()) {...}
```

**10. class NavigationService implements INavigationService**

- Реализация алгоритма поиска мне кажется очень странной. 
Это не A+, но и на BFS тоже не похоже.  
Что это?

- Процедурный прием. Сразу работай с координатой как с объектом 
```
private int[][] generateMoves(int maxDistance)

//ПРАВИЛЬНО:
private List<Coordinates> generateMoves(int maxDistance)
```

И тогда вместо 
```
int[][] moves = generateMoves(speed);

for (int[] move : moves) {
  int newRow = current.row() + move[0];
  int newCol = current.column() + move[1];
  Coordinates neighbor = new Coordinates(newRow, newCol);
  //...
  result.add(neighbor);
  //...
}
```

Будет:
```
List<Coordinates> moves = generateMoves(speed);

for (Coordinate neighbor : moves) {
  //...
  result.add(neighbor);
  //...
}
```

- Мапа координата-координата обычно при реализации поиска используется как эрзац-заменитель связного списка
```
Map<Coordinates, Coordinates> parentMap = new HashMap<>();
```
Вместо мапы координата-координата сделай класс связного списка, алгоритм тогда будет выглядеть понятнее.
Например, так:
```
class Node {
  private Coordinate coordinate; 
  private Node parent;
  //...
  public Coordinate getCoordinate() {...}
  public Node getParent() {...}
}
```

**11. public abstract class Entity и его простые наследники Rock/Tree**

+ 👍 Идеально
```
public abstract class Entity {
}

public class Tree extends Entity {
}
```

**12. abstract class Creature extends Entity**

- Нарушение ООП в наследовании, нарушение OCP

Креатура содержит поле типа `IIteractionService`
```
protected final IIteractionService interactionService;
```
Но в этом интерфейсе хранится не общее поведение для всех наследников `Creature`.

Например, метод `eatGrass(...)` нужен только одному наследнику- Зайцу. 
Другой наследник, Волк, не должен знать, как есть траву.

Сейчас ситуация маскируется тем, что эти методы спрятаны в интерфейсе.
Но представь, что интерфейса нет и тогда Creature будет выглядеть так:
```
public abstract class Creature extends Entity {

  public abstract void attack(...);

  public abstract eatGrass(...);
  //...
}
```
Из этого становится очевидным, что `Creature` выступает божественным классом для своих потомков.

- Креатура должна сама искать свои текущие координаты
```
public abstract void makeMove(WorldMap worldMap, Coordinates currentPosition);

//ПРАВИЛЬНО:
public abstract void makeMove(WorldMap worldMap);
```
Креатура все равно в метод хода принимает `WorldMap`, поэтому может спросить у карты свое текущее местоположение.  

Если креатура будет принимать свое текущее местоположение извне, а не получать самостоятельно из карты, то это потенциально может привести к багам. 
Потому что креатура не может гарантировать, что пришедшая извне координата *действительно* соответствует ее текущему местоположению.  
Слепая вера клиенту тут неуместна.

**13. Общее явление в иерархии классов, но реализовано по-разному**

У травы есть жизни, которые потребляет Заяц. И у зайца на уровне `Creature` есть жизни, которые потребляет Волк.

Это одно и то же явление, но реализовано по-разному. 

У травы это просто публичная константа:
```
public class Grass extends Entity {
  public static final int HP_VALUE = 10;
}
```

У `Creature` это поле:
```
public abstract class Creature extends Entity {
  private int hp;
  //...
}
```
Но суть та же самая- это очки жизни, которые потребляются при поедании травы и зайца.  
Сделай это как-то одинаково.

**14. interface IAction**

+ 👍 Не считая названия, идеально
```
public interface IAction {
  void execute(WorldMap map);
}
```

**15. class InitIAction implements IAction**

- Магические числа

+ 👍 Универсальный метод помещения существ на случайные координаты, отлично
```
private void spawnEntity(WorldMap map, int count, Supplier<Entity> entitySupplier)
```

**16. class TurnIAction implements IAction**

+ 👍 Только инициирует движения существ, а дальше они ходят сами- отлично.

**17. class MapRenderer**

- В конце цепочки if-else нужно кидать исключение- значит в карте есть существо, спрайт которого мы не знаем. 
Потому что при добавлении нового существа в проект, можно забыть добавить его спрайт в распечатку.
И тогда существо будет жить на карте своей жизнью, но мы этого не увидим- вместо этого существа будет распечатываться дефолтный спрайт
```
return switch (entity.getClass().getSimpleName()) {
  case "Predator" -> " 🐺 ";
  //...
  default -> "  ?  ";  <-- ДЕФОЛТНЫЙ СПРАЙТ
};
```

- Не используй null в качестве корректного значения входящих аргументов
```
private String getSymbolForEntity(Entity entity) {
  if (entity == null) {
    return "  ·  ";
  }

  return switch (entity.getClass().getSimpleName()) {
    case "Predator" -> " 🐺 ";
    //...
  };
}

//ПРАВИЛЬНО:
private String toSprite(Entity entity) {
  return switch (entity.getClass().getSimpleName()) {
    case "Predator" -> " 🐺 ";
    case "Herbivore" -> "🐰 ";
    //...
  };
}
```

**18. class Simulation**

- Нужно получать уже готовую пустую карту 
```
public Simulation(int mapHeight, int mapWidth) {
  this.map = new WorldMap(mapHeight, mapWidth);
  //...
}

//ПРАВИЛЬНО:
public Simulation(WorldMap worldMap) {
  this.worldMap = worldMap;
  //...
}
```

- Дублирование кода
```
for (IAction IAction : initIActions) {
  IAction.execute(map);
}

for (IAction IAction : turnIActions) {
  IAction.execute(map);
}

//ПРАВИЛЬНО:
executeActions(initIActions);
executeActions(turnIActions);

private void executeActions(List<IAction> actions) {
  for(IAction action : actions) {
    action.execute(worldMap);
  }
}
```

**19. Разные игровые конфигурации**

В программе два майна. Один запускает программу с использованием паузы. Второй- без
```
class MainWithPause
class Main
```
Это просто замечательно 👍👍👍  

Это показывает правильное понимание того, зачем нам в ООП вообще нужен полиморфизм.

## ВЫВОД

В целом, норм.

n.112(256)  
#ревью #симуляция 