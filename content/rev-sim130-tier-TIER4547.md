https://github.com/TIER4547/Simulation  
[TIER]

Есть над чем поработать.

## НЕДОСТАТКИ РЕАЛИЗАЦИИ

1. Карта распечатывается неровно, нужно подобрать спрайты одинаковой ширины  
![pic](https://github.com/raketareview/simulation_review/blob/master/content/resources/rev-sim130/img0.png) 

2. Нет паузы/пуск во время работы. Хотя это требование есть в ТЗ.

## ХОРОШО

+ 👍 Спрайты существ не хранятся в самих существах
+ 👍 Координаты существ не хранятся в самих существах(мне так больше нравится)

## ЗАМЕЧАНИЯ

**1. Нейминг**

- Названия пакетов нужно писать стилем all lowercase 
```java
package org.example.World.Map;

//ПРАВИЛЬНО:
package org.example.world.map;
```

- Для избежания путаницы не называй пакеты, классы, интерфейсы так же, как называются классы и интерфейсы стандартной библиотеки.  
Есть стандартный интерфейс `Map`, не должно быть кастомного пакета с таким же названием
```java
package org.example.World.Map;

//ПРАВИЛЬНО:
package org.example.world.worldmap;
```

- В названии переменных не пиши тип данных, к которым они относится. И вообще не употребляй венгерскую ноттацию.  
Название переменной должно отвечать на вопрос что хранит переменная, а не как хранит
```java
Map<Coordinates, Entity> mapByCoordinates = new HashMap<>();
Map<Entity, Coordinates> mapByEntities = new HashMap<>();

//ПРАВИЛЬНО:
Map<Coordinates, Entity> coordinatesWithEntities = new HashMap<>();
Map<Entity, Coordinates> entitiesWithCoordinates = new HashMap<>();
```

- Название класса должно быть существительным. Это название- глагол
```java
class Move
```

- Метод возвращает спрайт для одного энтити
```java
String getSpriteForEntities(Entity entities)

//ПРАВИЛЬНО:
String getSpriteForEntity(Entity entities)
```

- В проекте есть класс `Move`. Поэтому геттер с таким названием должен возвращать экземпляры `Move`, либо называться иначе
```java
Set<CoordinatesShift> getCreatureMoves();

//ПРАВИЛЬНО(исходя из названия):
Set<Move> getCreatureMoves();
```
*Oracle Java code conventions, part."Naming conventions"*  
*Мартин, "Чистый код", гл.2*  
*Ютуб, Немчинский "Как называть переменные, методы и классы?"*

**2. class Coordinates**

+ 👍 Нет ничего лишнего, это хорошо.

- Все операции с хранимыми данными должны выполняться клиентами не напрямую, а через методы.  
Например, через геттеры и сеттеры. Поля не должны быть публичными
```java
public final int x;
public final int y;
```
*Блох "Java. Эффективное программирование", изд.3, гл.4.2.*
```
"Используйте в открытых классах методы доступа, а не открытые поля" - Блох.
```

- Сдвиг(shift) или складывание(add) координаты должно происходить с экземпляром своего класса.  
Заводить для этого специальный класс сдвиговой координаты не надо
```java
public Coordinates shift(CoordinatesShift shift) 

//ПРАВИЛЬНО:
public Coordinates shift(Coordinates shift) 
```
Обычную координату можно использовать как координату сдвига, например так
```java
Coordinates coordinates = new Coordinates(10, 10);
Coordinates shiftCoordinates = new Coordinates(-1, -1);  //сдвиг влево-вниз
coordinates = coordinates.shift(shiftCoordinates);
```

**3. class CoordinatesShift**

- Такие простые классы делай рекордом
```java
public class CoordinatesShift {
  public final int x;
  public final int y;

  public CoordinatesShift(int x, int y) {...}
}

//ПРАВИЛЬНО:
public record CoordinatesShift(int x, int y) {
}
```

- Этот класс здесь не нужен, потому что он означает положение точки в пространстве относительно нуля.  
А значит вместо него можно использовать обычный `class Coordinates`.

**4. class Map**

- Конфликт нейминга предметной области и среды разработки.  

Следовать ТЗ это хорошо.  
Называть свои пользовательские классы так же, как стандартные классы и интерфейсы- плохо.  

Второе приводит к постоянной путанице в коде. `Мар` это название стандартного интерфейса. 
Поэтому меньшим злом является нарушить ТЗ и дать другое название прикладному классу: Board, GameMap или что-то подобное.

Следствие конфликта нейминга предметной области и среды разработки:  
Чтобы компилятор понимал, про какой именно Map идет речь, приходится писать полный путь к стандартному интерфейсу Map
```java
java.util.Map<Coordinates, Entity> mapByCoordinates = new HashMap<>()
```

- Преждевременная оптимизация.

Для реализации функционала, Карте достаточно иметь одну хешмапу
```java
private final java.util.Map<Coordinates, Entity> mapByCoordinates = new HashMap<>();  <-- ДОСТАТОЧНО
private final java.util.Map<Entity, Coordinates> mapByEntities = new HashMap<>();  <-- ИЗБЫТОЧНО
```
Использование двух хешмап может ускорить выполнение некоторых операций, но усложняет код и делают класс более громоздким.  
В реальности, юзер не увидит разницы в скорости работы- эта программа слишком простая.  
Поэтому оптимизация скорости выполнения некоторых операций здесь в ущерб архитектуре- антипаттерн "Преждевременная оптимизация".

- Никогда не возвращай null
```java
private final java.util.Map<Coordinates, Entity> mapByCoordinates = new HashMap<>();

public Entity getEntity(Coordinates coordinates) {
  Entity entity = mapByCoordinates.get(coordinates);  <-- может вернуть null
  return entity;
}
```
Возврат null повышает риск возникновения NullPointerException в программе.  
*Мартин, "Чистый код", гл.7.7-8*  
*Ютуб, Немчинский "Почему нельзя возвращать NULL?"*

- При всех операциях с участием координаты(добавить, выдать, удалить и т.д.), нужно проверять координату на корректность.

Если координата некорректна(находится вне пределов карты), нужно бросать исключение.
Сейчас, если у карты спросить, свободна ли ячейка с координатой (+100500, -100500), то карта скажет, что свободна
```java
public boolean isCoordinateEmpty(Coordinates coordinates) {
  return !mapByCoordinates.containsKey(coordinates);
}
```
А правильный ответ- ячейки с такой координатой в карте нет вообще.

Ближайшая аналогия- стандартные хранилища типа List и массива. 
При попытке обратитья к ним по несуществующему индексу, бросается исключение.

- Не возвращай редко используемые типы данных
```java
public Collection<Entity> getEntities()

//ПРАВИЛЬНО:
public List<Entity> getEntities()
```

Все равно с полученным значением клиент работает не как с Collection, а преобразует его в List
```java
for (Entity entity : new ArrayList<>(map.getEntities())) {...}
```

- Слишком сложно
```java
public void removeEntityAndCoordinates(Coordinates coordinates, Entity entity)
```
Если нужно удалить существо из карты, для этого достаточно указать существо:
```java
public void removeEntity(Entity entity)
```
Если нужно удалять существо не по экземпляру существа, а по его координате, нужно дополнительно сделать перегруженный метод:
```java
public void removeEntity(Coordinates coordinates)
```

+ 👍 С точки зрения SRP класс хороший, в нем нет лишних ответственностей.

**5. class MapFactory**

👍 Норм. Но я бы размерность доски и количество создаваемых существ не хардкодил, а принимал в конструктор.

**6. class Move**

- Этот класс не имеет отношение к карте- он ее никак не использует.  
Поэтому класс не должен находиться в пакете `Map`.

- Класс нужно сделать record'ом.

**7. class MapConsoleRenderer**

- Рендерер карты не должен лежать в одном пакете с Картой.

Карта это модель. Рендерер карты это представление.  
Представление не должно лежать в одном пакете с моделью, по крайней мере, в этом проекте.

Представь, что ты будешь делать версию симуляции для Андроид.  
Тогда из консольной версии ты возьмешь все общие классы для консоли и андроида. 
Общими будут только классы-модели.    
И, среди прочего, пакет `Map`. Но в адроид версии, из этой папки придется удалить консольный рендерер.

- В switch-case в default нужно кидать исключение, а не возращать дефолтный спрайт. 

Потому что при добавлении нового существа в проект, можно забыть добавить его спрайт в распечатку.
И тогда существо будет жить на карте своей жизнью, но мы этого не увидим- вместо этого существа будет распечатываться дефолтный спрайт
```java
private String selectUnicodeSpriteForEntities(Entity entity) {
  switch (entity.getClass().getSimpleName()) {
    case "Wolf":
      return "\uD83D\uDC3A";
    case "Sheep":
      return "\uD83D\uDC11";
    //...  
    }
  return "";
}
```

**8. class PathFinder**

+ 👍 Сигнатура метода поиска ок. Сразу понятно, как пользоваться классом
```java
public List<Coordinates> bfs(Creature creature, Class<? extends Entity> targetType, Map map)
```

- "BFS" это название алгоритма. Это название можно вынести в название класса, например "BfsPathFinder".  
Название метода должно быть глаголом в повелительном наклонении. Например:
```java
public List<Coordinates> find(Creature creature, Class<? extends Entity> targetType, Map map)
```

- Мапа координата-координата при реализации поиска обычно используется как эрзац-заменитель связного списка
```java
java.util.Map<Coordinates, Coordinates> cameFrom = new HashMap<>();
```
Вместо мапы координата-координата сделай класс связного списка, алгоритм тогда будет выглядеть понятнее.
Например, так:
```java
class Node {
  private Coordinates coordinates; 
  private Node parent;
  //...
  public Coordinates getCoordinates() {...}
  public Node getParent() {...}
}
```

- Не возвращай null
```java
public List<Coordinates> bfs(Creature creature, Class<? extends Entity> targetType, Map map) {
  //...
  return null;
}
```

- Нарушение принципа минимального открытого интерфейса.

В классе поиска пути должен быть только один публичный метод- тот, который ищет путь
```java
public class PathFinder {
  public List<Coordinates> bfs(Creature creature, Class<? extends Entity> targetType, Map map) {...}
  public List<Coordinates> restoreFullPath(Coordinates start, Coordinates target, java.util.Map<Coordinates, Coordinates> cameFrom) {...}
}
```
Клиент должен видеть только те методы, которые нужны для выполнения поставленной задачи.  
*Вайсфельд "Объектно-ориентированный подход", гл.5, "Минимальный открытый интерфейс"*

**9. abstract class Entity и его простые наследники Tree/Rock/Grass**

👍 Идеально
```java
public abstract class Entity {
}

public class Tree extends Entity {
}
```

**10. abstract class Creature extends Entity**

- Нарушение ТЗ. Нет метода `makeMove(...)`:
```java
Creature #
Абстрактный класс, наследуется от Entity ...Имеет метод makeMove()
```

- Ходьба вынесена из этого класса в какой-то другой.  
Чисто функционально, почему бы и нет. Но это нарушение ТЗ, а нарушать ТЗ без обоснования нельзя.

**11. class Predator extends Creature**

Дублирование кода в методах
```java
public Set<CoordinatesShift> getCreatureMoves() {...}
public Set<CoordinatesShift> getCreatureAttack() {...}
```

**12. class Actions**

Идея `Action` здесь не осмыслена.

Идея состоит в том, что нужно создать семейство родственных классов, объединенных общим интерфейсом.  
Каждый из этих классов должен делать что-то свое с картой: одна акция должна заселять карту существами, другая делать ходы и т.д.  
Action'ы, изложенные в ТЗ, это вариант реализации паттерна Command. 

Сейчас `Actions` у тебя это просто сборник функций, сделанный в стиле процедурного программирования
```java
public class Actions {
  public void turnActions() {...}
  public void makeMoves() {...}
}
```

Смысл Action'ов состоит в том, что должен быть общий класс/интерфейс Action и его наследники.  
В каждом экшене должен быть только один публичный метод(не публичных может быть сколько угодно).  
Это вариация паттерна Command- экшены должны быть родственны и одинаково использоваться через полиморфизм. Примерно так
```java
interface Action{
 void execute(Карта карта);
}

class ХодитьAction реализует Action {
  void execute(Карта карта) {
    //обойти всю карту
    //найти каждую креатуру
    //и дать ей пинка чтоб побежала
  }
}

class ДатьСигаретуВсемЗайцамAction реализует Action {
  void execute(Карта карта) {
    //обойти всю карту
    //найти всех зайцев и дать им по сигарете 
  }
}

List<Action> actions = List.of(new ДатьСигаретуВсемЗайцамAction(), new ХодитьAction());
for(Action a: actions) {
  a.execute(карта);
}

/*
Результат: программа обойдет всю карту, найдет всех креатур и даст им пинка, чтобы они побежали.
А каждому зайцу предварительно даст сигарету.
*/
```
Детально рассматривать код в этом классе не вижу смысла.  
Там код плох сам по себе- божественный класс с божественными методами.  
Но главное, код в классе плох потому, что класс не соответвует концепции экшенов из ТЗ.

**13. class Simulation**

- Нарушение DI. Класс не должен инициализировать сам себя. 
В данном случае класс инициализирует сам себя тем, что сам создает карту, а не принимает ее в конструктор
```java
public class Simulation {
  //...

  public void start() {
    map = new MapFactory().spawnRandomEntities();  <-- Аргумент map должен принимать в конструктор
    //...
  }
}
```
Таким образом нельзя создать несколько игровых конфигураций с разными картами.  
*Мартин, "ЧК", гл.11, "Отделение конструирования системы от ее использования"*

- Нарушение ТЗ. В классе нет нужных методов
```java
Simulation #
Методы:
nextTurn() - просимулировать и отрендерить один ход
startSimulation() - запустить бесконечный цикл симуляции и рендеринга
pauseSimulation() - приостановить бесконечный цикл симуляции и рендеринга
```

**14. class Main**, содержит точку входа main

+ 👍 Только создает и запускает `Simulation`, это хорошо.

**15. Отсутствует работа с потоками**

Хотя в ТЗ не написано явно, но из контекста подразумевается, что одна из задач этого проекта- поработать с многопоточностью.  
В реализации нет многопоточности, из-за чего не выполняются требования ТЗ по функционалу программы.

## ВЫВОД

В целом, все шло неплохо. Но на классе Actions показалось, что автор устал.

Доделать программу по ТЗ.  
Подробнее разберись с Action'ами, посмотри ролики на ютубе про паттерн "Command", чтобы получить о нем общее представление.

n.130(285)  
#ревью #симуляция 