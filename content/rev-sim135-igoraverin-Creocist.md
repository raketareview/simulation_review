https://github.com/Creocist/SimulationProject  
[Игорь Аверин]

Есть над чем поработать.

## НЕДОСТАТКИ РЕАЛИЗАЦИИ

1. Длинные команды: start/stop. 

Ввод команд и рисование карты используют одну и ту же консоль. 
Поэтому игрок между циклами распечатки карты не всегда успевает набрать и ввести длинные команды.
Делай короткие команды, например: 0/1. 
```
To simulate 1 turn - sim1
To simulate continuously - start
To stop the simulation - stop
```

## ХОРОШО

+ 👍 Спрайты существ не хранятся в самих существах
+ 👍 Реализовано пауза/пуск во время работы
+ 👍 Дополнительные команды в меню
```
To add an entity - add
```
+ 👍 Карта распечатывается аккуратно
![pic](https://github.com/raketareview/simulation_review/blob/master/content/resources/rev-sim134/img0.png) 

## ЗАМЕЧАНИЯ

**1. Нейминг**

- Названия пакетов нужно писать стилем all lowercase 
```java
import Simulation.Coordinates;

//ПРАВИЛЬНО:
import simulation.coordinates;
```

- Избыточный контекст
```java
public class WorldMap {
  int mapWidth = 11;
  int mapLength = 11;
  //...
}

//ПРАВИЛЬНО:
public class WorldMap {
  private final int width = 11;
  private final int length = 11;
  //...
}
```

- Избыточный контекст. И так понятно, что "step" ищется в контексте "BFS"
```java
public class BFSmove {
  public static Coordinates BFSstep(WorldMap map, Coordinates start, Predicate<Entity> target) {...}
}

//ПРАВИЛЬНО:
public class BFSmove {
  public static Coordinates step(WorldMap map, Coordinates start, Predicate<Entity> target) {...}
}
```

- Название методов нужно писать стилем camelCase
```java
void SetUpEntity(Coordinates coordinates, Entity entity) 

//ПРАВИЛЬНО:
void setUpEntity(Coordinates coordinates, Entity entity) 
```

- Для полей тоже camelCase
```java
int MaxHP = 3;

//ПРАВИЛЬНО:
int maxHp = 3;
```

- Если нужно что-то установить(set), то пиши "set" а не "setup"
```java
void SetUpEntity(Coordinates coordinates, Entity entity) {
  entities.put(coordinates, entity);
}

//ПРАВИЛЬНО:
void setEntity(Coordinates coordinates, Entity entity) 
```
Но здесь это получится не совсем сеттер- в этом методе не происходит установка значения какого-то поля класса.  
Это добавление еще одного существа к уже имеющимся.  

Тут нужно не сетить, а вставить:
```java
//ПРАВИЛЬНО:
void putEntity(Coordinates coordinates, Entity entity) 
```

- Аббревиатуры в названиях лучше писать так же, как и другие слова
```java
class BFSmove

//ЛУЧШЕ:
class BfsMove
```
Например, какое название понятнее: HTTPURL или HttpUrl?

- КОНСТАНТЫ_НУЖНО_ПИСАТЬ_СТИЛЕМ_UPPER_SNAKE
```java
private static final int[][] directions

//ПРАВИЛЬНО:
private static final int[][] DIRECTIONS
```

- Сеттер должен называться так же, как и поле, которое он устанавливает + префикс "set"
```java
public void setNewCoordinates(Coordinates coordinates) {
  this.coordinates = coordinates;
}

//ПРАВИЛЬНО:
public void setCoordinates(Coordinates coordinates) {...}
```

- Это не константа, это изменяемое поле и оноНеМожетНазыватьсяКакКонстанта
```java
HP++;

//ПРАВИЛЬНО:
hp++;
```

-Допустимы только общепринятые сокращения-аббревиатуры: Ui, Url, Http etc.  
Это сокращение мне ни разу не очевидно
```java
class BgObjects
```

- Название класса должно быть существительным. Это название- глагол
```java
class AddEntity
```

- Название метода должно быть глаголом в повелительном наклонении. Это название- существительное
```java
public void command(Simulation sim)
```
*Oracle Java code conventions, part."Naming conventions"*  
*Мартин, "Чистый код", гл.2*  
*Ютуб, Немчинский "Как называть переменные, методы и классы?"*

**2. Нарушение конвенции кода**

В любой ситуации выделяй тело блока скобочками, даже если тело состоит из одной строки.  
Исключение- метод equals(), там можно после if не выделять блоки скобочками
```java
if (nx < 0 || nx >= map.mapWidth || ny < 0 || ny >= map.mapLength) continue;

//ПРАВИЛЬНО:
if (nx < 0 || nx >= map.mapWidth || ny < 0 || ny >= map.mapLength) {
  continue;
}
```
*"Oracle Java code conventions"*

**3. Нарушение DRY**, магические буквы, числа, слова. Вводи константы 
```java
System.out.println("To simulate 1 turn - sim1");
System.out.println("To simulate continuously - start");
System.out.println("To stop the simulation - stop");

case "sim1":  //...
case "start":  //...
case "stop":  //...

//ПРАВИЛЬНО:
private final static String ONE_TURN = "sim1";
private final static String START = "start";
private final static String STOP = "stop";

System.out.println("To simulate 1 turn - " + ONE_TURN);
System.out.println("To simulate continuously - " + START);
System.out.println("To stop the simulation - " + STOP);

case ONE_TURN:  //...
case START:  //...
case STOP:  //...
```
*Фаулер, "Рефакторинг", гл.8, "Замена магического числа символической константой"*  
*refactoring.guru "Замена магического числа символьной константой"*  

**4. Используй классы через их интерфейсы**
```java
HashMap<Coordinates, Entity> entities = new HashMap<>();

//ПРАВИЛЬНО:
Map<Coordinates, Entity> entities = new HashMap<>();
```
Общее правило: ArrayList нужно использовать через List, HashMap- через Map и т.д.  
Это позволяет пользоваться преимуществами полиморфизма.

Да, бывают ситуации, когда, например, с LinkedList нужно работать именно как с LinkedList, а не с List. 
Но это уже нюансы.  
*"Java. Эффективное программирование", изд.3, гл.9.8*
```
"Если вы выработаете привычку использовать в качестве типов интерфейсы, ваша программа будет гораздо более гибкой" - Блох.
```

**5. record Coordinates(Integer x, Integer y)**

+ 👍 Нет ничего лишнего, это хорошо. Record для координаты- идеально.

- При прочих равных, нужно использовать примитивный тип, а не класс-обертку
```java
record Coordinates(Integer x, Integer y)

//ПРАВИЛЬНО:
record Coordinates(int x, int y)
```
Для применения класса-обертки над примитивным типом данных, должна быть какая-то причина.  
Здесь использование обертки не дает ничего большего по сравнению с примитивом.  
*Блох "Java. Эффективное программирование", изд.3, гл.9.5*
```
"Предпочитайте примитивные типы упакованным примитивным типам" - Блох.
```

**6. class WorldMap**

- Нарушение инкапсуляции.

Всегда явно указывай область видимости и final, если переменная final
```java
int mapWidth = 11;
int mapLength = 11;

//ПРАВИЛЬНО:
private final int mapWidth = 11;
private final int mapLength = 11;
```

- Карта содержит в себе фиксированные размеры, поэтому становится неуниверсальной- нельзя создать игровые конфигурации с картами разных размеров
```java
int mapWidth = 11;
int mapLength = 11;
```
Фиксированные размеры могут быть у шахматной доски(8x8) или игры крестики-нолики(3x3).  
То есть в тех играх, где размер игрового поля жестко фиксирован правилами.  
Здесь размеры должны приниматься в конструктор, чтобы можно было создавать карту произвольных размеров.

- Начальное заселение существ в карту нужно делать на случайные координаты. Заселять на фиксированные- не интересно
```java
public void SetUpInitialScene() {
  SetUpEntity(new Coordinates(2, 3), new Herbivore(new Coordinates(2, 3)));
  //...
}
```

- Карта не должна сама себя заселять существами. 
Иначе она становится не универсальной и нельзя будет создать несколько конфигураций игры с разными начальными комбинациями существ на карте
```java
public void SetUpInitialScene() {
  SetUpEntity(new Coordinates(2, 3), new Herbivore(new Coordinates(2, 3)));
  //...
}
```
Объект не должен сам себя инициализировать.  
*Мартин, "ЧК", гл.11, "Отделение конструирования системы от ее использования"*

- Никогда не возвращай null
```java
HashMap<Coordinates, Entity> entities = new HashMap<>();

public Entity getEntity(Coordinates coordinates) {
  return entities.get(coordinates);  <-- может вернуть null
}
```
Возврат null повышает риск возникновения NullPointerException в программе.  
*Мартин, "Чистый код", гл.7.7-8*  
*Ютуб, Немчинский "Почему нельзя возвращать NULL?"*

- Нарушение инкапсуляции, класс предоставляет клиентскому коду подробности внутреннего устройства и доступ к нему
```java
public HashMap<Coordinates, Entity> getEntities() {
  return entities;
}
```
В данный момент клиентский код может выполнять любые операции с HashMap Карты напрямую, игнорируя допустимые картой способы. 
Например так
```java
Карта карта = new Карта();
<заселить карту существами>
карта.getEntities().clear(); //геноцид- удаление из карты всех существ, минуя дозволенные картой механизмы
```
Из-за этого класс является гибридом со всеми вытекающими последствиями: *"Чистый код", гл.6*

Тут есть два варианта, как сделать правильно.  
Либо вернуть не оригинал мапы, а ее копию: 
```java
public Map<Coordinates, Entity> getEntities() {
  return new HashMap(entities);
}
```
Либо возвращать существ персонально:
```java
public Entity get(Coordinates coordinates)
public Coordinates getCoordinates(Entity entity)
```
Лично мне больше нравится второй вариант, когда существ возвращают персонально.

- При всех операциях с участием координаты(добавить, выдать, удалить и т.д.), нужно проверять координату на корректность. 
Если координата некорректна(находится вне пределов карты), нужно бросать исключение.  
Сейчас можно вставить существо за пределами карты
```java
public void SetUpEntity(Coordinates coordinates, Entity entity) {
  entities.put(coordinates, entity);
}
```
Ближайшая аналогия- стандартные хранилища типа List и массива. При попытке обратиться к ним по несуществующему индексу, бросается исключение.

- Нарушение SRP.

Карта должна хранить существ и обеспечивать к ним доступ.  
Для этого, она должна иметь методы:  
Вставить, удалить, вернуть существо по координате.  

И вспомогательные им публичные методы:  
Проверить занята ли координата, вернуть координату по существу.

Если в самой Карте этих методов нет, значит клиенты выполняют такие действия со внутреннестями Карты напрямую.  
А это нарушение инкапсуляции и SRP карты.

**7. class BFSmove**

- Соблюдай требования утилитных классов.

Это утилитный класс, он содержит только статические методы. 
Утилитные классы должны быть `final` и иметь приватный конструктор.  
Не должно быть возможности унаследоваться от утилиты или сделать ее экземпляр.  
*Блох, "Java. Эффективное программирование", изд.3, гл.2.4*

- Пары x/y это координаты. Направление можно представить как координату, смещенную относительно начала движения
```java
private static final int[][] directions = {
  {1, 0}, {-1, 0}, {0, 1}, {0, -1}
};

//ПРАВИЛЬНО:
private static final Coordinates[] DIRECTIONS = {
  new Coordinates(0, 1),
  //...
};
```

- Это не класс поиска пути.

Этот класс не ищет путь.  
Потому что путь это последовательность координат от точки старта до финиша.  
Этот класс ищет следующий шаг по направлению к цели
```java
public static Coordinates BFSstep(WorldMap map, Coordinates start, Predicate<Entity> target)
```
В принципе, почему бы и нет- этот класс и не называется классом поиска пути.  
В ТЗ прямо не написано, что должен быть класс, который ищет именно путь, а не шаг на пути.  
Но все равно, мой внутренний WTF-детектор говорит, что здесь что-то не то.

- Мапа координата-координата при реализации поиска обычно используется как эрзац-заменитель связного списка
```java
Map<Coordinates, Coordinates> parent = new HashMap<>();
```
Вместо мапы координата-координата сделай класс связного списка, алгоритм тогда будет выглядеть понятнее.
Например, так:
```java
class Node {
  private Coordinates coordinates; 
  private Node parent;
  //...
  public Coordinates getCoordinates() {...}
  public Node getParent() {...}
}
```

+ 👍 Хорошо, что для поиска цели используется предикат
```java
Coordinates BFSstep(WorldMap map, Coordinates start, Predicate<Entity> target)
```
Благодаря этому, существа потенциально могут есть не один тип еды, а несколько.  
Предикат здесь используется как опредедлитель цели

**8. abstract class Entity**

- Снова нарушение инкапсуляции. 

Похоже, в проекте это системная проблема.  
Здесь это поле должно быть protected- его значение устанавливает наследник
```java
Coordinates coordinates;

//ПРАВИЛЬНО:
protected Coordinates coordinates;
```

- Содержит координату. Но координата нужна только тому существу, которое ходит.  
Поэтому entities должны хранить координату только начиная с уровня `Creature`
```java
Coordinates coordinates;
```

**9. class Creature extends Entity**

- Нарушение конвенции кода.

Конструктор должен стоять выше остальных методов.  
И это не просто формальное требование, его несоблюдение имеет практические последствия.  
Если конструктор не стоит первым, то читатель кода может подумать, что класс имеет только конструктор по умолчанию и будет исходить из этого.  
А на самом деле все осбстоит совсем наоборот.

- Нарушение ТЗ. Отсутствует метод `makeMove(...)`
```
Creature #
Абстрактный класс, наследуется от Entity. ...Имеет метод makeMove() - сделать ход.
```

Существа здесь не имеют `makeMove(...)` потому что самостоятельно они не ходят.  
Их переставляет чья-то чужая воля, как шахматист переставляет фигуры на шахматной доске. 

Чисто функционально- почему бы и нет. Но это нарушет ТЗ проекта, а это не очень хорошо.  
Нужно выполнять требования ТЗ. Нельзя на работе нарушать ТЗ просто так.

- Используй абстрактные методы.

Не делай пустые методы.  
Если метод нужно обязательно переопределить у потомка, а на этом уровне иерархии он не должен ничего делать,
то такой метод должен быть аьстрактным
```java
public void eat(WorldMap map, List<Coordinates> coordinates) {
}

//ПРАВИЛЬНО:
public abstract void eat(WorldMap map, List<Coordinates> coordinates);
```

**10. class Predator extends Creature**

- Нарушение ООП дизайна при наследовании.

Поле `speed` в Predator перекрывает одноименное поле в Creature
```java
public class Creature extends Entity {
  int speed = 1;
  //...
  public int getSpeed() {
    return speed;
  }
}

public class Predator extends Creature {
  int speed = 2;  <-- У ПРЕДКА ЕСТЬ ЭТО ПОЛЕ
  //...
  public int getSpeed() {  <-- У ПРЕДКА ЕСТЬ ЭТОТ МЕТОД
    return speed;
  }
}

//ПРАВИЛЬНО:
public class Predator extends Creature {
  private final static int SPEED = 2;
  //...
  public Creature(Coordinates coordinates) {
    super(coordinates, SPEED);
  }
}
```

- Можно сразу приводить к нужному типу
```java
if (prey instanceof Herbivore) {
  ((Herbivore) prey).HP--;
}

//ПРАВИЛЬНО:
if (prey instanceof Herbivore herbivore) {
  herbivore.HP--;
}
```

**11. Классы Action's**

Идея `Action` здесь не осмыслена. 

Идея состоит в том, что нужно создать семейство родственных классов, объединенных общим интерфейсом.  
Каждый из этих классов должен делать что-то свое с картой: одна акция должна заселять карту существами, другая делать ходы и т.д.  
Action'ы, изложенные в ТЗ, это вариант реализации паттерна Command. 

Сейчас `Actions` у тебя это группа утилитных классов, которые не родственны друг другу и не имеют ничего обзщего друг с другом, кроме нахождения в одном пакете.  
Классы имеют статические методы, а поэтому не имеют отношения не только к экшенам из ТЗ, но и к ООП в целом
```java
public class AddEntity {
  public static void addEntity(WorldMap map, String entityName) {...}
}

public class Movement {
  public static void moveCreature(WorldMap map, Creature creature) {...}
}
```

Смысл Action'ов состоит в том, что должен быть общий класс/интерфейс Action и его наследники.  
В каждом экшене должен быть только один публичный метод(не публичных может быть сколько угодно).  
Это вариация паттерна Command- экшены должны быть родственны и одинаково использоваться через полиморфизм. Примерно так
```java
interface Action{
 void execute(Карта карта);
}

class ХодитьAction реализует Action {
  void execute(Карта карта) {
    //обойти всю карту
    //найти каждую креатуру
    //и дать ей пинка чтоб побежала
  }
}

class ДатьСигаретуВсемЗайцамAction реализует Action {
  void execute(Карта карта) {
    //обойти всю карту
    //найти всех зайцев и дать им по сигарете 
  }
}

List<Action> actions = List.of(new ДатьСигаретуВсемЗайцамAction(), new ХодитьAction());
for(Action a: actions) {
  a.execute(карта);
}

/*
Результат: программа обойдет всю карту, найдет всех креатур и даст им пинка, чтобы они побежали.
А каждому зайцу предварительно даст сигарету.
*/
```

**12. class MapRenderer**

+ 👍 Спрайты существ хранятся здесь, а не берутся из вамих существ, это хорошо.

- Нарушение конвенции кода.  
Главный публичный метод должен стоять выше, чем вспомогательный ему приватный метод.

- В switch-case в default нужно кидать исключение, а не возращать дефолтный спрайт. 

Потому что при добавлении нового существа в проект, можно забыть добавить его спрайт в распечатку.
И тогда существо будет жить на карте своей жизнью, но мы этого не увидим- вместо этого существа будет распечатываться дефолтный спрайт
```java
return switch (entity.getClass().getSimpleName()) {
  case "Herbivore" -> " \uD83D\uDC07 ";
  //...
  default -> " \uD83D\uDFE9 ";  <-- ДЕФОЛТНЫЙ СПРАЙТ
};
```

**13. class Simulation extends Thread**

- Нарушение DI. Класс не должен инициализировать сам себя.  
В данном случае класс инициализирует сам себя тем, что сам создает карту, а не принимает ее в конструктор
```java
public class Simulation extends Thread {
  WorldMap map = new WorldMap();
  //...
}

//ПРАВИЛЬНО:
public class Simulation extends Thread {
  private final WorldMap map;
  //...
  public Simulation(Simulation simulation) {
    this.simulation = simulation;
  }
}
```
Таким образом нельзя создать несколько игровых конфигураций с разными картами.  
*Мартин, "ЧК", гл.11, "Отделение конструирования системы от ее использования"*

- Не используй методы-инициализаторы, используй конструкторы.

Внутри классов не должно быть методов типа `init()` и `initialize()`, которые используются для инициализации объектов вместо конструкторов
```java
public class Simulation бла-бла {
  public void initializeSim() { <-- МЕТОД ИНИЦИАЛИЗАЦИИ
    map.SetUpInitialScene();
    //...
  }
  //...
}

//ПРАВИЛЬНО:
public class Simulation бла-бла {
  public Simulation(...) {  <-- КОНСТРУКТОР
    map.SetUpInitialScene();
    //...
  }
  //...
}
```
Прямая критика инит-методов здесь: *Эккель "Философия Java", гл.5, "Конструктор гарантирует инициализацию"*.

- Нарушение ТЗ, нет всех указанных для этого класса методов 
```java
Simulation #
Главный класс приложения, включает в себя:
...
nextTurn() - просимулировать и отрендерить один ход
startSimulation() - запустить бесконечный цикл симуляции и рендеринга
pauseSimulation() - приостановить бесконечный цикл симуляции и рендеринга
```

Есть только метод `makeTurn()`. Это то же самое, что `nextTurn()`?  
Но Почему тогда называется иначе?  
Соблюдай ТЗ и читай его внимательно- не менее трех раз.

- Неправильное использование экшенов.

Из-за того, что классы экшенов сделаны неправильно, то и используются они в этом классе тоже неправильно.  

Экшены в этом проекте нужно использовать не индивидуально, а через полиморфизм.  
Примерно так:
```java
private final List<Action> initActions = List.of(new UnoAction());
private final List<Action> turnActions = List.of(new DosAction(), new TresAction(), new MoveAction());

public Simulation(...) {
  //...
  executeActions(initActions);  //выполнение экшенов на старте
}

private void nextTurn() {
  //...
  executeActions(turnActions);  //выполнение экшенов на каждом ходе
}

private void executeActions(List<Action> actions) {
  for(Action a : actions) {
    a.execute(карта);
  }
}
```

## ВЫВОД

Не видно понимания ООП на базовом уровне: зачем нужны геттеры, сеттеры, конструкторы.  
Системно нарушается инкапсуляция.

Подробнее разберись с Action'ами, посмотри ролики на ютубе про паттерн "Command", чтобы получить о нем общее представление.

Не видно понимания декомпозиции- как делить программу на классы.  
Для лучшего понимания декомпозиции ООП, посмотреть ролики Сергея про шахматы.  
Посмотреть ролики Немчинского про SOLID.

n.134(???)  
#ревью #симуляция 