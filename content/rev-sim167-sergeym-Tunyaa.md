https://github.com/Tunyaa/Simulation  
[Сергей М]

Визуал: отлично, превосходно.  
Внутреннее устройство: удручающе.

## ХОРОШО

+ 👍 Красивый Windows UI интерфейс JavaFX  
![pic](https://github.com/raketareview/simulation_review/blob/master/content/resources/rev-sim167/img0.png) 

## ЗАМЕЧАНИЯ

**1. Нейминг**

- Называй сущности именем собственным, а не перечислением того, что лежит внутри
```java
class RowColumn

//ПРАВИЛЬНО:
class Coordinates
```
Например, класс кота мы назовём "Кот", а не "Четыре лапы хвост".

- Не сокращай названий без необходимости
```java
private int len;         // Количество позиций поля 

//ПРАВИЛЬНО:
private int length; 
```

- Что за арий
```java
List<Entity>[] entitysAry 
```

- Избыточно. Из сигнатуры мы и так видим, что получаем существ по позиции
```java
List<Entity> getEntitysByPosition(int position)

//ПРАВИЛЬНО:
List<Entity> getEntities(int position)
```

- Венгерская нотация.

В названии переменных не пиши тип данных, к которому они относятся.  
И вообще не употребляй венгерскую нотацию. 
Название переменной должно отвечать на вопрос что хранит переменная, а не как хранит
```java
List<T> targetList

//ПРАВИЛЬНО:
List<T> targets
```

- Названия пакетов нужно писать стилем alllowercase 
```java
package simulation.simulation.pathFinder;

//ПРАВИЛЬНО:
package simulation.simulation.pathfinder;
```

- Название метода должно быть глаголом в повелительном наклонении
```java
void pathFinder(World world, Creature creature)

//ПРАВИЛЬНО:
void findPath(World world, Creature creature)
```

И таких методов-существительных в проекте много:
```java
void vision(World world, Creature creature)
```

*Oracle Java code conventions, part."Naming conventions"*  
*Мартин, "Чистый код", гл.2*  
*Ютуб, Немчинский "Как называть переменные, методы и классы?"*

**2. Exceptions**

- Не бросай базовый exception. Конкретизируй ситуацию- бросай то исключение, которое подходит под этот конкретный случай
```java
throw new RuntimeException("Failed to create entity", ex);
```
*Хорстманн "Java. Библиотека профессионала", т.1, гл.11*
```java
"Не ограничивайтесь генерацией RuntimeException. 
Найдите подходящий подкласс или создайте собственный." - Хорстманн
```

**3. Используй классы через их интерфейсы**
```java
ArrayList<Integer> getTargetPositions()
ArrayDeque<Integer> getPath()

//ПРАВИЛЬНО:
List<Integer> getTargetPositions()
Deque<Integer> getPath()
```
Общее правило: ArrayList нужно использовать через List, HashMap через Map, HashSet через Set и т.д.  
Это позволяет пользоваться преимуществами полиморфизма.

Да, бывают ситуации, когда, например, с LinkedList нужно работать именно как с LinkedList, а не с List. 
Но это уже нюансы.  
*"Java. Эффективное программирование", изд.3, гл.9.8*
```java
"Если вы выработаете привычку использовать в качестве типов интерфейсы, ваша программа будет гораздо более гибкой" - Блох.
```

**4. Антипаттерн "Стрела"**

Не должно быть больше 2-3 уровней вложенности.  
Если больше, это антипаттерн "Стрела", такой код очень труден для понимания
```java
public void vision(World world, Creature creature) {
  for (int r = rangeOfView; r >= 0; r--) {
    for (int i : scan) {
      if (col >= LeftExtremePoint && col <= RightExtremePoint && row >= topExtremePoint && row <= lowerExtremePoint) {
        if (e != null) {
          if (!e.isEmpty()) {
            for (Entity entity : e) {
              if (creature.getTargetClass().isInstance(entity)) {
                  //наконечник стрелы
                }
              }
            }
          }
        }
      }
    }
  }
}
```
"Стрела" значит, что метод делает несколько дел сразу и его нужно разделить на несколько вспомогательных.
```java
"Если вам нужно более трех уровней вложенности, вы все равно запутались, так что исправьте программу" - Линус Торвальдс.
```

**5. class RowColumn**

+ 👍 Нет ничего лишнего, это хорошо. Класс только хранит значения row, column. 

- Поля row,column тут должны быть final.

При прочих равных простые делай классы `immutable`.
```java
"Классы должны быть неизменяемыми, если только нет очень важной причины, чтобы сделать их изменяемыми",
"Вы всегда должны делать объекты с небольшими значениями, такими как PhoneNumber или Complex, неизменяемыми" - Блох. 
```
*Блох "Java. Эффективное программирование", изд.3, гл.4.3.*

- Класс может быть преобразован в `record` без потери функционала (за исключением того, что станет immutable).

Record'ы по умолчанию умеют правильно делать `hashCode()`, `equals()` и `toString()`.  
Про возможности рекордов почитай [тут](https://docs.oracle.com/en/java/javase/21/docs/api/java.base/java/lang/Record.html).

**6. class World**

- Нарушение SRP, божественный класс, методы чужих ответственностей. 

Карта должна только хранить существа и обеспечить базовые операции с ними:  
Вставить, вернуть существо по координате, вернуть координату по существу, удалить существо, вернуть список всех хранимых существ.  
И методы, которые напрямую не управляют размещением существ, но необходимы для этого функционала:  
Сказать ширину/высоту карты и т.д.

Если какой-то метод не нужен для обеспечения хранения существ в карте, 
значит он не принадлежит к ответственности карты, а принадлежит к чужой ответственности.  

Здесь методы чужих ответственностей:  
🔸 Начальное заполнение карты существами  
🔸 Передвигает существ на карте  
🔸 Определяет, что существо является камнем  
🔸 Размножает существ  
🔸 Считает траву  
🔸 Хранит алгоритмы поиска и зрения существ

И многое, многое другое.

Наверное, для проекта в целом полезно иметь метод, который считает количество травы.  
Но этот процесс не имеет никакого отношения к единой ответственности карты- хранению существ в себе.  
Для организации хранения/удаления/выдачи существ, карте не нужно знать общее число травы, которую карта хранит внутри себя.  

Методы чужих ответственностей должны находиться в тех классах, в интересах которых они работают.  
Если один и тот же метод используют разные классы, то метод нужно вынести в отдельный класс, например, "BoardUtils".

- Нарушение OCP. 

Карта должна работать со всеми хранимыми существами одинаково.  
И не должна работать как-то по-особому с конкретными классами-наследниками Entity
```java
public boolean isStone(int position) {...}

public int getGrassCount() {...}
```
Если карта будет знать по именам наследников Entity и иметь для них персональные методы, то класс будет открыт для изменений.  
Например, при добавлении в проект класса Птица, понадобится изменить класс Карта и добавить в него методы `isBird(...)` и `getBirdCount()`.

Карта может, не нарушая SRP, возвращать список всех существ или существ заданного ЗАПРАШИВАЕМОГО в аргументах типа:
```java
//ТАК:
public List<Entity> getAll() //и тогда пусть клиент сам отбирает отсюда существ нужного класса

//ИЛИ ТАК:
public <T extends Entity> List<T> getEntitiesBy(Class<T> type)  //вернуть существ класса, указанного клиентом 
```
И тогда пусть клиент с этой информацией делает всё, что угодно. Например, считает количество травы.

- Нарушение чеклиста ТЗ.

Существа разных типов хранятся в отдельных коллекциях
```java
private List<Entity>[] entities;              // Общий список сущностей сетки
private List<Predator> predators;        // Лист хищников
private List<Herbivore> herbivores; 
```

В ТЗ указано, что это неправильно
```java
Чеклист для самопроверки #
Проблемы и ошибки в коде:

Класс Map
Параллельные коллекции для разных типов существ (для каждого типа своя коллекция)
```

Параллельные коллекции существ нарушают OCP и просто делают код сложнее и запутаннее.  
Тот же самый функционал работы карты можно сделать без использования параллельных коллекций.

- Никогда не возвращай null
```java
public List<Entity> getEntitysByPosition(int position) {
  //...
  return null;
}
```
Возврат null повышает риск возникновения NullPointerException в программе.

*Мартин, "Чистый код", гл.7.7-8*  
*Ютуб, Немчинский "Почему нельзя возвращать NULL?"*

- Нарушение инкапсуляции, класс предоставляет клиентскому коду подробности внутреннего устройства и доступ к нему
```java
private List<Predator> predators; 

public List<Predator> getPredators() {
  return predators;
}
```

Чеклист ТЗ:
```java
Проблемы и ошибки в коде:
...
Недостаточная инкапсуляция - “протекающее” наружу внутреннее устройство класса Map (например, прямой доступ к коллекции ячеек) 
вместо набора методов с говорящими названиями (AddEntity, RemoveEntity, и так далее)
```

В данный момент клиентский код может выполнять любые операции с `List` Карты напрямую, игнорируя допустимые картой способы. 
Например так
```java
Карта карта = new Карта();
<заселить карту существами>
карта.getPredators().clear(); //геноцид- удаление из карты всех предаторов, минуя дозволенные картой механизмы
```
Из-за этого класс является гибридом со всеми вытекающими последствиями: *"Чистый код", гл.6*

В таких случаях нужно возвращать не оригинал, а копию:
```java
public List<Predator> getPredators() {
  return new ArrayList(predators);
}
```

- Магические числа: 3, 6, 10, 40 etc.

- С точки зрения ООП, этот класс не является объектом.

Это контейнер с функциями в стиле процедурного программирования.  
Код из этого класса нужно перенести в другие классы в соответствии с принципами SOLID.

В этом классе оставить только тот код, который я указал выше:
```
Карта должна только хранить существа и обеспечить базовые операции с ними:  
Вставить, вернуть существо по координате, вернуть координату по существу, удалить существо, вернуть список всех хранимых существ.  
И методы, которые напрямую не управляют размещением существ, но необходимы для этого функционала:  
Сказать ширину/высоту карты и т.д.
```

**7. class WorldGrid**

- Еще один класс, который совмещает в себе разное.

Размеры карты нужно перенести отсюда в класс карты. 

После рефакторинга класса карты и других классов, тут скорее всего не останется кода и этот класс можно будет удалить.  
Если после рефакторинга других классов здесь ещё что-то останется, то оставшийся функционал должен собой представлять что-то типа утилиты карты(WorldUtil) или её менеджера(WorldManager).

- Не делай параллельные системы координат
```java
// Возвращает позицию по ряд \ колонна
public int getPositionByRowСolumn(int row, int column) {
  return (row - 1) * width + column;
}
```

В проекте должен использоваться только один вид координат- тот класс, который содержит положение по осям x,y (row, column).  
Все операции должны выполняться только в этих единицах, а не в альтернативных интовых Position'ax, которые сворачивают 2D координату в 1D.  

Должно быть примерно так:
```java
public class World {
  //....
  public World(int width, int height) {...}

  public Entity get(Coordinates coordinates) {...}

  public Coordinates get(Entity entity) {...}

  public void remove(Coordinates coordinates) {...}

  //oth's methds
}
```

**8. interface PathFinder**

+ 👍 Интерфейс поиска пути- это хорошо. Теперь можно делать разные реализации поиска: BFS, A* и т.д.

- Этот интерфейс не ищет путь.

Поиск пути должен искать путь.  
То есть он должен найти путь и вернуть его.   
Методы этого интерфейса ничего не возвращают
```java
public interface PathFinder {
  void pathFinderToTarget(World world, Creature creature);

  void pathFinderRandom(World world, Creature creature);
}
```

Классы поиска пути должны просто искать путь от точки старта до точки, соответствующей заданным условиям согласно алгоритму BFS или A*.

В случае алгоритма BFS, поиск должен искать путь до точки, в которой находится существо нужного класса.  
Например так:
```java
public class BfsPathFinder {

  public List<Coordinates> find(Карта карта, Coordinates start, Class<? extends Entity> target) {
  //ищет путь на карте от точки start
  //до точки, где находится существо нужного класса(напр. target == Grass.class)
  }
}
```

Или поиск должен искать путь между точками старта и финиша, согласно алгоритму A*
```java
public class AstarPathFinder {

  public List<Coordinates> find(Карта карта, Coordinates from, Coordinates to) {...}
}
```

Также поиск пути по алгоритму A* может использовать ту же сигнатуру, что и BFS.  
Тогда поиск пути в нем интерпретируется не как поиск пути между двумя точками, а как поиск цели и прокладывание пути к ней.  
Например: 
```java
public class AstarPathFinder {

  public List<Coordinates> find(Карта карта, Coordinates start, Class<? extends Entity> target) {
    //сначала находит координату цели
    //потом прокладывает путь по алгоритму A* между точкой старта и точкой цели
  }
}
```
Плюсы- унификация сигнатуры разных алгоритмов поиска для полиморфизма.  
То есть, можно сделать семейство родственных классов поиска, объединенных общим интерфейсом:
```java
public interface PathFinder {
  List<Coordinates> find(Карта карта, Coordinates start, Class<? extends Entity> target);
}

public class BfsPathFinder implements PathFinder{

  @Переопределить
  public List<Coordinates> find(Карта карта, Coordinates start, Class<? extends Entity> target) {
  //ищет путь на карте от точки start
  //до точки, где находится существо нужного класса(напр. target == Grass.class)
  }
}
```

**9. class StraightPathFinder implements PathFinder**

Недостатки этого класса проистекают из его интерфейса.

**10. Интерфейсы в моделях**

Куча пар непонятных парных интерфейсов.  
Что-то про атаку:
```java
public interface Attackable {
  void takeDamage(int damage);
}

public interface Attacker {
  void attack(Attackable attackable);
}
```

Что-то про еду:
```java
public interface Eatable {
   
}

public interface Eater {
  void eat(Eatable el);
}
```
Разобраться в них мне не удалось- всё сложно.

**11. abstract class Entity**

- Содержит координату(позицию)
```java
protected int position;
```

Но координата нужна только тому существу, которое ходит.  
Поэтому entities должны хранить координату только начиная с уровня `Creature`.  
В иерархии классов все состояния и поведения должны появляться только на тех уровнях наследования, где они начинают использоваться.

- Нарушение SRP, зависимость модели от представления- существо хранит спрайт с собственным изображением
```java
protected EntityTypePng entityTypePng;
```
Модель(а это модель) не должна зависеть от представления и знать, как ее будут показывать юзеру.  
Потому что в разных средах (консоль, Swing, Android) одна и та же модель может быть показана разными способами- буквой, цифрой, пиксельной картинкой, анимацией etc.  
Спрайты всех существ должны храниться в классе, который распечатывает карту.

+ 👍 Вот таким должен быть идеальный Entity и его простые неходячие наследники в этом проекте:
```java
public abstract class Entity {
  //да, тут совсем пусто
}

public class Tree extends Entity {
}
```

**12. class Creature extends Entity**

- Креатура должна иметь возможность есть не любые классы(например, String или Exception), а только классы из семейства Entity  
```java
protected Class targetClass;

//ПРАВИЛЬНО:
protected Class<? extends Entity> targetClass;
```

- Нарушение ТЗ. Отсутствует метод `makeMove(...)`
```
Creature #
Абстрактный класс, наследуется от Entity. ...Имеет метод makeMove() - сделать ход.
```

Существа здесь не имеют `makeMove(...)` потому что самостоятельно они не ходят.  
Их переставляет чья-то чужая воля, как шахматист переставляет фигуры на шахматной доске. 

Чисто формально- почему бы и нет. Но это нарушает ТЗ проекта, а это не очень хорошо.  
Нужно выполнять требования ТЗ. Нельзя на работе нарушать ТЗ просто так.

В данном случае нарушение ТЗ ухудшило декомпозицию проекта, а не улучшило.

- Не используй экзотические классы в качестве возвращаемых значений
```java
public ArrayDeque<Integer> getPath() {...}

//ПРАВИЛЬНО:
public List<Coordinates> getPath() {...}
```

- Сложные правила пользования классом.

Вместо предусмотренного ТЗ простого управления движением через один публичный метод `makeMove(...)`, 
в классе много публичных методов, за которые нужно дёргать как за верёвочки, чтобы креатура пошла.  

Вернись к ТЗ, логику движения сгруппируй в методе `makeMove(...)`.

**13. class Herbivore extends Creature implements Eatable, Attackable**

- Нарушение дизайна ООП. 

При создании экземпляра класса, значения обязательных полей предка должны устанавливаться через конструктор, а не сетиться  
```java
public class Herbivore extends Creature implements Eatable, Attackable {

  private final PathFinder pathFinder;

  public Herbivore(PathFinder pathFinder, Vision vision) {
    setEntityTypePng(EntityTypePng.HERBIVORE);
    setHp(100);
    setRangeOfView(4);
    super.vision = vision;
    this.pathFinder = pathFinder;
    this.targetClass = Grass.class;
  }

  //...
}

//ПРАВИЛЬНО:
public class Herbivore extends Creature implements Eatable, Attackable {
  private static final int HP = 100;
  //....

  private final PathFinder pathFinder;

  public Herbivore(PathFinder pathFinder, Vision vision) {
    super(EntityTypePng.HERBIVORE, HP, VIEW_RANGE, vision, TARGET)
    this.pathFinder = pathFinder;
  }

  //...
}
```
*Эккель "Философия Java", гл.5, "Конструктор гарантирует инициализацию"*

**14. Отсутствуют указанные в ТЗ классы Action's**

Смысл Action'ов состоит в том, что должен быть общий класс/интерфейс Action и его наследники.  
В каждом экшене должен быть только один публичный метод(не публичных может быть сколько угодно).  
Это вариация паттерна Command- экшены должны быть родственны и одинаково использоваться через полиморфизм.  
Примерно так
```java
interface Action{
 void execute(Карта карта);
}

class ХодитьAction реализует Action {
  void execute(Карта карта) {
    //обойти всю карту
    //найти каждую креатуру
    //и дать ей пинка чтоб побежала
  }
}

class ДатьСигаретуВсемЗайцамAction реализует Action {
  void execute(Карта карта) {
    //обойти всю карту
    //найти всех зайцев и дать им по сигарете 
  }
}

List<Action> actions = List.of(new ДатьСигаретуВсемЗайцамAction(), new ХодитьAction());
for(Action a: actions) {
  a.execute(карта);
}

/*
Результат: программа обойдет всю карту, найдет всех креатур и даст им пинка, чтобы они побежали.
А каждому зайцу предварительно даст сигарету.
*/
```

Сейчас тот функционал, который должен быть в Action'ах, разнесен по разным классам. Например, `CreatureTurnProcessor`.  
Разбирать подробно эти классы смысла нет, их нужно удалить, а вместо них сделать Action'ы по ТЗ.

**15 class PrimaryService**

- Этот класс не соответствует названию "Service". 

Это то ли контроллер, то ли контроллер + вью.

**16. class Simulation**

Зачем тут этот класс, непонятно.  
В ТЗ он описан для консольного проекта и по ТЗ его суть- быть консольным контроллером.  
В этом проекте виндовый UI и в нем другой контроллер. Поэтому методы из этого класса нужно перенести в виндовый контроллер или куда-то ещё.

## ВЫВОД

ООП в этой реализации есть только там, где что-то осталось от декомпозиции, которую сделал Сергей и описал в ТЗ.  
Там, где ты сам что-то делал или заменил оригинальную декомпозицию, получился процедурный код. 

Сейчас ты переусложнил программу, добавил в неё много дополнительных усложнений:  
Windows UI, конфиги, кучу интерфейсов (еда + еда, атака + атака), размножение существ и тому подобное.

Из-за этого архитектура программы рухнула под своей тяжестью.

**Любые усложнения в программе повышают требования к архитектуре, а не понижают их.**

Есть и плюсы- виндовый интерфейс программы выглядит красиво 👍

Но это не стоит того, чтобы жертвовать внутренней архитектурой ради картинки.  
Для тебя, как бекендера, внутренняя архитектура должна быть важнее:
```
Чеклист для самопроверки #
Проблемы и ошибки в коде:
Интеграция библиотеки графического интерфейса ценой чистоты и понятности кода. Достаточно консольного интерфейса
```

Хорошая архитектура программы с JavaFX требует понимания архитектуры MVC.  
У тебя не видно ещё понимания ООП на базовом уровне.

Придерживайся ТЗ.  
Если в нем указано, что нужно сделать класс- делай его.  
Соблюдение ТЗ- это важный навык для работы. 

Подробнее разберись с Action'ами, посмотри ролики на ютубе про паттерн "Command", чтобы получить о нем общее представление.

Если будешь рефакторить- переделай на консольный интерфейс, чтобы сконцентрироваться на хорошей архитектуре.

Делай только указанный в ТЗ функционал.  
Дополнительные усложнения, при недостаточном умении строить архитектуру программы, приносят больше вреда, чем пользы.

Посмотри на ютубе ролики Немчинского для новичков:
```
"Правильные методы по Clean Code"
"Как называть переменные, методы и классы? Чистый код (Clean Code)"
"Принцип хорошего кода KISS"
"SOLID принципы: SRP", "SOLID принципы: OCP" и т.д. 
```

Эталонная версия Симуляции с объяснениями есть у Сергея в расширенных материалах.

n.167(361)  
#ревью #симуляция #javafx 