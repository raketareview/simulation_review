https://github.com/thatPhill/Simulation  
[Phill]

Есть над чем поработать.

## ХОРОШО

+ 👍 Реализовано пауза/пуск во время работы

## ЗАМЕЧАНИЯ

**1. Нейминг**

- В названии переменных не пиши тип данных, к которым они относится. И вообще не употребляй венгерскую ноттацию.  
Название переменной должно отвечать на вопрос что хранит переменная, а не как хранит
```java
HashMap<Coordinates, Entity> entitiesMap = new HashMap<>();

//ПРАВИЛЬНО:
HashMap<Coordinates, Entity> entities = new HashMap<>();
```

- Это одиночные экшены
```java
class InitActions
class SpawnActions

//ПРАВИЛЬНО:
class InitAction
class SpawnAction
```

- Избыточно. И так понятно, что в классе поиска пути метод поиска находит именно путь
```java
PathFinder.findPath(...)

//ЛУЧШЕ:
PathFinder.find(...)
```

*Oracle Java code conventions, part."Naming conventions"*  
*Мартин, "Чистый код", гл.2*  
*Ютуб, Немчинский "Как называть переменные, методы и классы?"*  


**2. Нарушение конвенции кода**. 

В любой ситуации выделяй тело блока скобочками, даже если тело состоит из одной строки. 
Исключение- метод equals(), там можно после if не выделять блоки скобочками
```java
if (entity instanceof Grass && mover instanceof Herbivore) return true;
if (entity instanceof Herbivore && mover instanceof Predator) return true;

//ПРАВИЛЬНО:
if (entity instanceof Grass && mover instanceof Herbivore) {
  return true;
}
if (entity instanceof Herbivore && mover instanceof Predator) {
  return true;
}
```

**3. Форматирование строк**

Если нужно печатать или создавать строку с более, чем одним подстановочным значением или значение вставляется внутрь сообщения, используй форматирование- тогда сразу будет виден весь шаблон
```java
System.out.println("За время симуляции было съедено: " + SpawnActions.countHerbivores + " " + EntityType.HERBIVORE.getEmoji());

//ПРАВИЛЬНО:
System.out.printf("За время симуляции было съедено: %d %s  \n", + SpawnActions.countHerbivores, EntityType.HERBIVORE.getEmoji());
```

**4. Нарушение DRY**, магические буквы, числа, слова. Вводи константы. 

Если к одной и той же магической штуке обращаются два разных класса, то делай ее константой в одном из этих двух классов. 
Либо перенеси ее в третий класс- эти данные должны быть синхронизированы между собой
```java
public class MessagePrinter {

  public static void printKeyForPause() {
    System.out.println("Для паузы введите букву - \"P\"");  <-- "P"
  }

  public static void printKeyForResume() {
    System.out.println("Для возобновление симуляции введите букву - \"S\"");  <-- "S"
  }
}

public class Simulation {

  private void handleInput() {
    if (input.equalsIgnoreCase("P")) {...}  <-- "P"
    if (input.equalsIgnoreCase("S")) {...}  <-- "S"
  }
}
```
*Фаулер, "Рефакторинг", гл.8, "Замена магического числа символической константой"*  
*refactoring.guru "Замена магического числа символьной константой"*  

**5. class Coordinates**

+ 👍 Нет ничего лишнего, это хорошо. Класс хранит только x/y.

- Это очень простой класс, лучше его сделать неизменяемым(immutable)  
*Блох "Java. Эффективное программирование", изд.3, гл.4.3.*
```
"Вы всегда должны делать объекты с небольшими значениями, такими как PhoneNumber или Complex, неизменяемыми" - Блох.
```

- Класс может быть преобразован в record без потери функционала.  
Правда, при этом он станет immutable, но это даже хорошо.
```
"Классы должны быть неизменяемыми, если только нет очень важной причины, чтобы сделать их изменяемыми" - Блох.
```

**6. class WorldConfig**

- Этот конфигурационный класс содержит информацию, которя относится к разным ответственностям
```Java
private int size;  <-- ДЛЯ СОЗДАНИЯ КАРТЫ
private int grass; <-- ДЛЯ ЗАПОЛНЕНИЯ КАРТЫ СУЩЕСТВАМИ
//rocks, trees...
```
Поэтому данный класс не должен использоваться в качестве объекта-аргумента и не должен в качестве такового передаваться в конструктор других классов(WorldMap).  
*"ЧК", гл.3, "Объекты как аргументы"*

Конфигурационный класс, который не является объектом-аргументом только для одого класса, имеет право на существование. 
Но другие классы должны получать не весь конфиг в конструктор, а только необходимые данные из него.  

Условный примеры:
```java
public class Settings {
  public static final int ROOM_NUMBERS = 5;
}

//ПЛОХО:
public static void main(String[] args) {
  House house = new House();
  //oth code
}

public class House {
  private final int roomNumbers;

  public House() {
    this.roomNumbers = Settings.ROOM_NUMBERS;
  }

  public int getRoomNumbers() {
    return roomNumbers;
  }
}

//ХОРОШО:
public static void main(String[] args) {
  House house = new House(Settings.ROOM_NUMBERS);
  //oth code
}

public class House {
  private final int roomNumbers;

  public House(int roomNumbers) {
    this.roomNumbers = roomNumbers;
  }

  public int getRoomNumbers() {
    return roomNumbers;
  }
}
```

- Магия
```java
if (size >= 10 && size <= 50) {...}
throw new IllegalArgumentException("Size must be between 10 and 50.");

//ПРАВИЛЬНО:
private final static int MIN_WORLD_SIZE = 10;
private final static int MAX_WORLD_SIZE = 50;
private final static String EXCEPTION_MESSAGE = "Size must be between %d and %d.".formatted(MIN_WORLD_SIZE, MAX_WORLD_SIZE);

if (size >= MIN_WORLD_SIZE && size <= MAX_WORLD_SIZE) {...}
throw new IllegalArgumentException(EXCEPTION_MESSAGE);
```

- Сообщения эксепшенов делай информативными
```java
throw new IllegalArgumentException("Too much grass.");

//ПРАВИЛЬНО:
String message = "Too much grass(>%d): %d".formatted(MAX_WORLD_SIZE, size);
throw new IllegalArgumentException(message);
```

- Сеттеры здесь не нужны, класс должен быть неизменяемым.

Кстати, я заметил, что ты в классах делаешь сеттеры, даже когда эти сеттеры не используешь в программе.  
Примеры: этот класс(поля rocks и trees), класс Coordinates.  

Видимо, ты геттеры и сеттеры для всех полей делаешь просто на всякий случай.  
Не надо так, любой метод в классе должен быть осмысленным. Сеттеры должны быть тогда, когда они реально нужны.

- Проблемы валидации.

Когда параметры устанавливаются через конструктор, проводится валидация
```java
public WorldConfig(int size, ...) {
  if (size >= 10 && size <= 50) {
    if (grass > (size / 2)) {
      throw new IllegalArgumentException("Too much grass.");
    }
  }
  //...
}  
```

Когда параметры устанавливаются через сеттеры, валидация не проводится
```java
public void setSize(int size) {
  this.size = size;
}
```

На самом деле, валидацию нужно проводить при любом способе установки значений в объекте.

**7. class WorldMap**

- Нарушение конвенции кода, поля и методы расположены вперемешку.

- Нарушение Low Coupling, неправильное использование конфигурационного класса.

Поскольку конфиг содержит информацию, которая относится к разным ответственностям, то сюда он должен передаваться не целиком.
Сюда нужно передавать только ту информацию, которая нужна для КАРТЫ
```java
public WorldMap(WorldConfig config) {
  this.config = config;
}

//ПРАВИЛЬНО:
public WorldMap(int width, int height) {
  this.width = width;
  this.height = height;
}
```

- Нарушение SRP, методы чужих ответственностей. 

Карта должна только хранить существа и обеспечить базовые операции с ними: вставить, выдать одно существо и список всех хранимых существ, удалить.
Если какой-то метод не нужен для обеспечения этого функционала, значит он не принадлежит к ответственности карты, а принадлежит к чужой ответственности.
Здесь методы чужих ответственностей:  
Производит первоначальное заселение карты существами на случайные позиции, создает список соседних координат, проверяет можно ли совершить ход в ячейку etc.

Наверное, для проекта в целом полезно иметь метод, который находит соседние координаты. 
Но этот процесс не имеет никакого отношения к единой ответственности карты- хранению существ в себе.  
Для организации хранения/удаления/выдачи существ, карте не нужно уметь создавать список соседних координаты.  

Методы чужих ответственностей должны находиться в тех классах, в интересах которых они работают.
Если один и тот же метод используют разные классы, то метод нужно вынести в отдельный класс, например, WorldMapUtils.

- Нарушение инкапсуляции, класс предоставляет клиентскому коду подробности внутреннего устройства и доступ к нему
```java
public HashMap<Coordinates, Entity> getEntitiesMap() {
  return entitiesMap;
}
```
В данный момент клиентский код может выполнять любые операции с HashMap Карты напрямую, игнорируя допустимые картой способы. 
Например так
```java
Карта карта = new Карта(...);
<заселить карту существами>
карта.getEntitiesMap().clear(); //геноцид- удаление из карты всех существ, минуя дозволенные картой механизмы
```
Из-за этого класс является гибридом со всеми вытекающими последствиями: *"Чистый код", гл.6*

Тут есть два варианта, как сделать правильно:  
Либо вернуть не оригинал мапы, а ее копию: 
```java
public HashMap<Coordinates, Entity> toMap() {
  return new HashMap(entitiesMap);
}
```

Либо возвращать существ персонально:
```java
public Entity get(Coordinate coordinate)
public Coordinates getCoordinates(Entity entity)
```
Лично мне больше нравится второй вариант, когда существ возвращают персонально. 

- При всех операциях с участием координаты(добавить, выдать, удалить и т.д.), нужно проверять координату на корректность. (1)
Если координата некорректна(находится вне пределов карты), нужно бросать исключение
```java
public void setEntity(Coordinates coordinates, Entity entity) {
  entity.setCoordinates(coordinates);
  entitiesMap.put(coordinates, entity);
}
```
Ближайшая аналогия- стандартные хранилища типа List и массива. При попытке обратитья к ним по несуществующему индексу, бросается исключение.

Сейчас можно поместить в карту существо на координату, которая находится вне её пределов.

- Нарушение OCP. 

Карта должна работать со всеми хранимыми существами одинаково и не работать как-то по-особому с конкретным классом представителей и не должна даже знать по именам наследников Entity
```java
public Creature getCreature(Coordinates coordinates) { <-- МЕТОД ДЛЯ КОНКРЕТНОГО ТИПА ENTITY
  return (Creature) entitiesMap.get(coordinates);
}
```
Если карта будет знать по именам наследников Entity и иметь для них персональные методы, то класс будет открыт для изменений.  
Например, при добавлении в проект класса Птица, понадобится изменить класс Карта и добавить в него метод `getBird(...)`.

У тебя карта уже умеет возвращать существо по координате, этого достаточно
```java
public Entity getEntity(Coordinates coordinates) {...}
```
Анализировать это существо на предмент его принадлежности к тому или иному типу существ, должен клиент.

- Нарушение DRY, дублирование кода 
```java
while (grassCount > 0 && iterator.hasNext()) {
  Coordinates pos = iterator.next();
  setEntity(pos, EntityFactory.createEntity(EntityType.GRASS, pos));
  grassCount--;
}

while (rockCount > 0 && iterator.hasNext()) {
  Coordinates pos = iterator.next();
  setEntity(pos, EntityFactory.createEntity(EntityType.ROCK, pos));
  rockCount--;
}
```
Общий код выноси во вспомогательные методы:
```java
spawn(карта, (coordinates) -> new Grass(coordinates), КОЛИЧЕСТВО_ТРАВЫ);
spawn(карта, (coordinates) -> new Rock(coordinates), КОЛИЧЕСТВО_КАМНЕЙ);
//...

private void spawn(Карта карта, Function<Coordinates, Entity> mapper, int count) {
  for (int i = 0; i < count; i++) {
    Coordinates coordinates = getRandomEmptyCoordinates(...);
    Entity entity = mapper.apply(coordinates);
    setEntity(coordinates, entity);
  }
}
```

- Пары значений x/y это сущность координата
```java
public List<Coordinates> getNeighbours(Coordinates coord, Creature mover) {
  int[][] directions = {
    {0, 1},   // right
    {1, 0},   // down
    //...
  };

  for (int[] direction : directions) {
    int newX = coord.getX() + direction[0];
    int newY = coord.getY() + direction[1];
    //... 
  }
}

//ПРАВИЛЬНО:
private static final List<Coordinates> DIRECTIONS = List.of(new Coordinates(0, 1), (new Coordinates(1, 0), ...);

public List<Coordinates> getNeighbours(Coordinates coord, Creature mover) {
  for (Coordinates direction : DIRECTIONS) {
    Coordinates neighbour = sum(coord, direction);
    //... 
  }
}
```

- Карта не должна сама себя заселять существами. 
Иначе она становится неуниверсальной и нельзя будет создать несколько конфигураций игры с разными начальными комбинациями существ на карте
```java
public void setupEntitiesPositions() {...}
```
Объект не должен сам себя инициализировать.  
*Мартин, "ЧК", гл.11, "Отделение конструирования системы от ее использования"*

- Никогда не возвращай null
```java
private final HashMap<Coordinates, Entity> entitiesMap = new HashMap<>();

public Entity getEntity(Coordinates coordinates) {
  return entitiesMap.get(coordinates);  <-- может вернуть null
}
```
Возврат null повышает риск возникновения NullPointerException в программе.  
*Мартин, "Чистый код", гл.7.7-8*  
*Ютуб, Немчинский "Почему нельзя возвращать NULL?"*

**8. interface PathFinder**

+ 👍 Интерфейс поиска пути это хорошо. Теперь можно делать разные реализации поиска: BFS, A* и т.д.

- Нарушение SRP.

Поиск должен просто искать путь от точки старта до точки, соответствующей заданным условиям. 
В данном случае- до точки, в которой находится существо нужного класса.
Если поиск получает во входящие аргументы метода конкретный экземпляр цели, значит часть работы поиска уже выполнил кто-то другой. 
То есть, кто-то другой уже нашел цель и передал ее в поиск пути
```java
List<Coordinates> findPath(Coordinates start, Coordinates goal);
```

На самом деле поиск пути должен сам искать цель и прокладывать к ней путь.  
Сигнатура метода для этого должна выглядеть примерно так
```java
List<Coordinates> findPath(WorldMap worldMap, Coordinates start, Class<? extends Entity> target) {
  //ищет путь на карте world от точки start
  //до точки, где находится существо нужного класса(напр. target == Grass.class)
}
```

**8. class BreadthFirstSearch implements PathFinder**

Мапа координата-координата обычно при реализации поиска используется как эрзац-заменитель связного списка
```java
Map<Coordinates, Coordinates> parentMap = new HashMap<>();
```
Вместо мапы координата-координата сделай класс связного списка, алгоритм тогда будет выглядеть понятнее.
Например, так:
```java
class Node {
  private Coordinates coordinates; 
  private Node parent;
  //...
  public Coordinates getCoordinates() {...}
  public Node getParent() {...}
}
```

**9. abstract class Entity**

- Содержит координату. Но координата нужна только тому существу, которое ходит. 
Поэтому entities должны хранить координату только начиная с уровня `Creature`.

- Нарушение SRP, зависимость модели от представления- существо хранит спрайт с собственным изображением
```java
private final String emoji;
```
Модель(а это модель) не должна зависеть от представления и знать, как ее будут показывать юзеру.  
Потому что в разных средах(консоль, Swing, Android) одна и та же модель может быть показана разными способами- пиксельной картинкой, анимацией etc.  
Спрайты всех существ должны храниться в классе, который распечатывает карту.

**10. abstract class Creature extends Entity**

+ 👍 Хороший метод, благодаря ему потенциально можно делать существ, которые едят не один вид существ, а несколько
```java
public abstract boolean isTarget(Entity entity);
```

Например:
```java
class Crocodil extends Creature {
  //...
  public boolean isTarget(Entity entity) {
    return entity instanceof Herbivore || entity instanceof Fish;
  }
}
```

- Нарушение SRP, класс берет на себя чужую ответственность.

Класс не должен реализовывать в себе самостоятельный поиск целиь.  
Сейчас класс частично реализует поиск, тем самым выполняет часть ответственности класса поиска пути

```java
public Coordinates findNearestTarget(WorldMap worldMap, BreadthFirstSearch pathfinder) {...}

Coordinates getNextStep(List<Coordinates> path, Entity target) {...}
```

Креатура все вопросы по поиску пути должна отдать на аутсорс, а сама должна только получать путь, примерно так:
```java
public abstract class Creature extends Entity {
  //...  
  public abstract void makeMove(BreadthFirstSearch pathfinder, WorldMap worldMap) {
    Coordinates myCoordinates = getCoordinates();
    List<Coordinates> path = pathfinder.find(worldMap, myCoordinates, en -> isTarget(en));  //findPath(..., Predicate<Entity> targetDetector)
    //пройти по пути path
  }
}
```

**11. class Herbivore/Predator extends Creature**

Нарушение DRY.  
Общий код в `void makeMove(...)` у Волка и Зайца
```java
//ЗАЯЦ
@Override
public void makeMove(BreadthFirstSearch pathfinder, WorldMap worldMap, WorldConfig worldConfig) {
  Coordinates start = getCoordinates();
  Coordinates grass = findNearestTarget(worldMap, pathfinder);  //Coordinates target = findNearestTarget(...);
  List<Coordinates> path = pathfinder.findPath(start, grass);
  if (path == null || path.isEmpty()) return;

  Coordinates next = null;
  this.setHealth(this.getHealth() - 1);

  next = getNextStep(path, worldMap.getEntity(grass));
  //...
}

//ВОЛК
@Override
public void makeMove(BreadthFirstSearch pathfinder, WorldMap worldMap, WorldConfig worldConfig) {
  Coordinates start = getCoordinates();
  Coordinates herbivore = findNearestTarget(worldMap, pathfinder);  //Coordinates target = findNearestTarget(...);
  List<Coordinates> path = pathfinder.findPath(start, herbivore); 
  if (path == null || path.isEmpty()) return;

  Coordinates next = null;

  next = getNextStep(path, worldMap.getEntity(herbivore));
  //...
}
```
Общий код выноси во вспомогательный метод предка.

**12. interface Action**

👍 Всё ок.

Действия выполняются над картой. Можно карту принимать в конструктор- так, как ты это делаешь:
```java
public interface Action {
  void execute();
}
```
Альтернативно ожно карту принимать в аргументы метода, это тоже ок:
```java
public interface Action {
  void execute(Карта карта);
}
```

**13. class InitActions implements Action**

Здесь, а не в WorldMap, должна находиться логика заселения существ на карту.

**14. class RenderAction implements Action**

В существовании этого класса не вижу смысла.  

Печатать карту нужно на каждом шаге вне зависимости от того, какие экшены есть в проекте.
Поэтому распечатка должна вызываться отдельно в классе Симуляции в методе совершения хода.

Кроме того, все остальные экшены выполняют логику, которая не зависит от представления- просто что-то делают с данными.  
А RenderAction работает с представлением данных- печатает карту в консоль. 
Это размывает концепцию экшенов.

**15. class SpawnActions implements Action**

Во многом дублирует логику заселения существ, которая уже есть в WorldMap: создает существ определенного типа в заданном количестве и помещает их в карту на случайные координаты.

**16. class TurnActions implements Action**

👍 Всё ок, только инициирует движение креатур(даёт им пинка- дальше они сами бегут).

**17. class RenderConsoleMap**

- Именно здесь, а не в `Entity`, должны храниться спрайты существ.

- Нарушение SRP.

Задача рендерера состоит в распечатке карты.  
Рендерер не имеет права вносить изменения в вызывающий его класс- ведение учета количества ходов выходи за рамки ответственности распечатки
```java
public void render(WorldMap worldMap) {
  //...
  System.out.println("Turn - " + Simulation.turnCount++);
}

//ПРАВИЛЬНО:
public void render(WorldMap worldMap, int turnCount) {
  //...
  System.out.println("Turn - " + turnCount);
}
```
Еще лучше, если рендерер будет распечатывать только саму карту, а сопроводительную информацию(номер хода) будет печатать кто-то другой.

**18. class Simulation**

- Нарушение инкапсуляции. 

Все операции с хранимыми данными должны выполняться клиентами не напрямую, а через методы.  
Например, через геттеры и сеттеры
```java
public static int turnCount = 0;
```
*Блох "Java. Эффективное программирование", изд.3, гл.4.2.*
```
"Используйте в открытых классах методы доступа, а не открытые поля" - Блох.
```

Всегда явно указывай область видимости и делай поле final, если оно не изменяется в процессе работы программы
```java
Action turnAction;

//ПРАВИЛЬНО:
private final Action turnAction;
```

- Нарушение ТЗ, нет метода `nextTurn()`.

## ВЫВОД

Замечаний меньше, чем бывает обычно.  
Для лучшего понимания декомпозиции ООП посмотреть ролики Сергея про шахматы.  
Посмотреть ролики Немчинского про SOLID.

n.120(269)  
#ревью #симуляция 