https://github.com/KhudopayMA/WorldSimulation  
[Михаил]

Есть над чем поработать.

## НЕДОСТАТКИ РЕАЛИЗАЦИИ

1. В программе реализовано пауза/пуск, но пользователь об этом не узнает- нигде на экране про это не написано.

## ХОРОШО

+ 👍 Спрайты существ не хранятся в самих существах
+ 👍 Координаты существ не хранятся в самих существах(мне так больше нравится)
+ 👍 Реализовано пауза/пуск во время работы

## ЗАМЕЧАНИЯ

**1. Нейминг**

- Название должно объяснять суть явления.  
Этот метод не просто возвращает пустую ячейку, он возвращает случайную пустую ячейку
```java
public Cell getEmtpyCell()

//ПРАВИЛЬНО:
public Cell getRandomEmtpyCell()
```

- Избыточно. Мы и так понимаем, что метод поиска в классе поиска пути ищет именно путь, а не что-то иное
```java
Pathfinder.findPath(...)

//ПРАВИЛЬНО:
Pathfinder.find(...)
```

- Название класса должно быть существительным. "Render" это действие, а "Renderer" это тот, кто выполняет это действие
```java
class Render

//ПРАВИЛЬНО:
class Renderer
```
*Oracle Java code conventions, part."Naming conventions"*  
*Мартин, "Чистый код", гл.2*  
*Ютуб, Немчинский "Как называть переменные, методы и классы?"*

**2. Нарушение конвенции кода**

В любой ситуации выделяй тело блока скобочками, даже если тело состоит из одной строки. 
Исключение- метод equals(), там можно после if не выделять блоки скобочками
```java
if (neighborEntity != null && !target.isInstance(neighborEntity)) continue;

//ПРАВИЛЬНО:
if (neighborEntity != null && !target.isInstance(neighborEntity)) {
  continue;
}
```
*Oracle Java code conventions"*

**3. Создавай вспомогательные методы**, делай программу более простой и понятной
```java
if (neighborEntity != null && !target.isInstance(neighborEntity)) {
  continue;  
}

//ПРАВИЛЬНО:
if (!isTargetEntity(neighborEntity)) {
  continue;  
}
```
Даже, если метод состоит из одной строки, но при этом делает код программы читабельней, то этот метод имеет право на жизнь.

**4. Если в блоке if есть return**(break, continue, throw, exit и т.д.), то else не пишется.
 
В этом случае неважно, будет else или нет, так как программа будет работать одинаково, а код без `else` будет выглядеть читабельней
```java
if (entity instanceof Grass) {
  return Emoji.GRASS;
} else if (entity instanceof Predator) {
  return Emoji.PREDATOR;
} 

//ПРАВИЛЬНО:
if (entity instanceof Grass) {
  return Emoji.GRASS;
} 
if (entity instanceof Predator) {
  return Emoji.PREDATOR;
} 
```

**5. class Cell**, координата

- Нарушение инкапсуляции. 

Все операции с хранимыми данными должны выполняться клиентами не напрямую, а через методы.  
Например, через геттеры и сеттеры
```java
public final int x;
public final int y;
```
*Блох "Java. Эффективное программирование", изд.3, гл.4.2.*
```
"Используйте в открытых классах методы доступа, а не открытые поля" - Блох.
```

- Не нужно хранить хэш-код. Он должен рассчитываться при вызове метода `hashCode()`
```java
private final int hash;
```

- В `equals()` не сранивают хэш-код
```java
@Override
public boolean equals(Object o) {
  if (!(o instanceof Cell cell)) return false;
  if (hash != o.hashCode()) return false;  <-- ЛИШНЕЕ
  return x == cell.x && y == cell.y;
}
```

**6. class WorldMap**

- Неправильная перегрузка конструктора
```java
public WorldMap(int width, int height) {
  this.width = width;
  this.height = height;
  this.entities = new HashMap<>();
}

public WorldMap() {
  this.width = 5;
  this.height = 5;
  this.entities = new HashMap<>();
}

//ПРАВИЛЬНО:
private final static int DEFAULT_WIDTH = 5;
private final static int DEFAULT_HEIGHT = 5;

public WorldMap() {
  this(DEFAULT_WIDTH, DEFAULT_HEIGHT);
}

public WorldMap(int width, int height) {
  this.width = width;
  this.height = height;
  this.entities = new HashMap<>();
}
```
*Эккель "Философия Java", гл.5 "Вызов конструктора из конструктора"*

- Нарушение SRP, методы чужих ответственностей. 

Карта должна только хранить существа и обеспечить базовые операции с ними: вставить, выдать одно существо и список всех хранимых существ, удалить.
Если какой-то метод не нужен для обеспечения этого функционала, значит он не принадлежит к ответственности карты, а принадлежит к чужой ответственности.  
Здесь методы чужих ответственностей: найти случайную пустую координату, посчитать количество существ определенного типа.

Наверное, для проекта в целом полезно иметь метод, который считает количество существ определенного типа. 
Но этот процесс не имеет никакого отношения к единой ответственности карты- хранению существ в себе.  
Для организации хранения/удаления/выдачи существ, карте не нужно считать количество хранимых существ.  

Методы чужих ответственностей должны находиться в тех классах, в интересах которых они работают.
Если один и тот же метод используют разные классы, то метод нужно вынести в отдельный класс, например, BoardUtils.

- Нарушение инкапсуляции, класс предоставляет клиентскому коду подробности внутреннего устройства и доступ к нему
```java
private final Map<Cell, Entity> entities;

public Map<Cell, Entity> getEntities() {
  return entities;
}
```
В данный момент клиентский код может выполнять любые операции с HashMap Карты напрямую, игнорируя допустимые картой способы. 
Например так
```java
Карта карта = new Карта(100, 100);
<заселить карту существами>
карта.getEntities().clear(); //геноцид- удаление из карты всех существ, минуя дозволенные картой механизмы
```
Из-за этого класс является гибридом со всеми вытекающими последствиями: *"Чистый код", гл.6*

Тут есть два варианта, как сделать правильно.    
Либо вернуть не оригинал мапы, а ее копию: 
```java
public Map<Cell, Entity> getEntities() {
  return new HashMap(entities);
}
```
Либо возвращать существ персонально:
```java
public Entity getEntityByCell(Cell cell)
public Cell getCell(Entity entity)
```
Лично мне больше нравится второй вариант, когда существ возвращают персонально. 

- Нарушение DRY, дублирование логики. Переиспользуй методы
```java
public Entity getEntityByCoordinates(int x, int y) {
  return entities.get(new Cell(x, y));
}

public Entity getEntityByCell(Cell cell) {
  return entities.get(cell);
}

//ПРАВИЛЬНО:
public Entity getEntityByCoordinates(int x, int y) {
  return getEntityByCell(new Cell(x, y));
}

public Entity getEntityByCell(Cell cell) {
  //тут возможно будет еще какая-то общая логика (спойлер: будет)
  return entities.get(cell);
}
```
Еще правильнее- сделать перегруженные методы
```java
public Entity getEntity(int x, int y) {...}

public Entity getEntity(Cell cell) {...}
```

- Никогда не возвращай null
```java
private final Map<Cell, Entity> entities;

public Entity getEntityByCell(Cell cell) {
  return entities.get(cell);  <-- может вернуть null
}

public Cell getEmtpyCell() {
  //...
  return null;  <-- возвращает null
}
```
Возврат null повышает риск возникновения NullPointerException в программе.  
*Мартин, "Чистый код", гл.7.7-8*  
*Ютуб, Немчинский "Почему нельзя возвращать NULL?"*

- При всех операциях с участием координаты(добавить, выдать, удалить и т.д.), нужно проверять координату на корректность.  
Если координата некорректна(находится вне пределов карты), нужно бросать исключение
```java
public Entity getEntityByCell(Cell cell) {
  //тут нужно проверить нахождение cell в пределах карты  
  return entities.get(cell);  
}
```
Ближайшая аналогия- стандартные хранилища типа List и массива. 
При попытке обратиться к ним по несуществующему индексу, бросается исключение.

**7. class Pathfinder**

- Соблюдай требования утилитных классов.

Это утилитный класс, он содержит только статические методы. 
Утилитные классы должны быть `final` и иметь приватный конструктор.  
Не должно быть возможности унаследоваться от утилиты или сделать ее экземпляр.  
*Блох, "Java. Эффективное программирование", изд.3, гл.2.4*

- Нарушение SRP.

Поиск должен просто искать путь от точки старта до точки, соответствующей заданным условиям.  
В данном случае- до точки, в которой находится существо нужного класса.  
Если поиск получает во входящие аргументы метода конкретный экземпляр цели, значит часть работы поиска уже выполнил кто-то другой.  
То есть, кто-то другой уже нашел цель и передал ее в поиск пути
```java
public static List<Cell> findPath(WorldMap worldMap, Cell entityLocation, Creature targetEntity)
```
На самом деле поиск пути должен сам искать цель и прокладывать к ней путь.  
Сигнатура метода для этого должна выглядеть примерно так
```java
public static List<Cell> findPath(WorldMap worldMap, Cell start, Class<? extends Entity> target) {
  //ищет путь на карте world от точки start
  //до точки, где находится существо нужного класса(напр. target == Grass.class)
}
```

- Пары x/y это координаты(ячейки)
```java
int[][] directions = {{1, 0}, {-1, 0}, {0, 1}, {0, -1}};
for (int[] direction : directions) {
  int x = cell.x + direction[0];
  int y = cell.y + direction[1];
  //...
  Cell neighbor = new Cell(x, y);
}

//ПРАВИЛЬНО:
private final static List<Cell> DIRECTIONS = List.of(new Cell(1, 0), ...);

for(Cell direction : DIRECTIONS) {
  //...
  Cell neighbor = sum(cell, direction); //cell.x + direction.x, ...
}
```

**8. abstract class Entity и его простые наследники Grass/Tree/Rock**

👍 Идеально
```java
public abstract class Entity {
}

public class Grass extends Entity {
}
```

**9. abstract class Creature extends Entity**

- Креатура должна сама искать свои текущие координаты
```java
public void makeMove(WorldMap worldMap, Cell currentCell, List<Cell> path) 

//ПРАВИЛЬНО:
public void makeMove(WorldMap worldMap) {
  Cell currentCell =  worldMap.getCell(this);
  List<Cell> path = Pathfinder.find(worldMap, currentCell, targetType);
  //...
}
```
Креатура все равно в метод хода принимает `WorldMap`, поэтому может спросить у карты свое текущее местоположение.  

Если креатура будет принимать свое текущее местоположение извне, а не получать самостоятельно из карты, то это потенциально может привести к багам. 
Потому что креатура не может гарантировать, что пришедшая извне координата *действительно* соответствует ее текущему местоположению.  
Слепая вера клиенту тут вредна.

Путь креатура тоже должна находить самостоятельно, а не принимать во входящие.

- Стандартные переопределения типа `hashCode()` и `equals()` ставь ниже всех остальных методов.

**10. class Predator extends Creature**

- Объясняй магию
```java
public Predator() {
  super(10, 2, Herbivore.class);
  this.attackDamage = 2;
}

//ПРАВИЛЬНО:
private final static int HP = 10;
private final static int SPEED = 2;
private final static int ATTACK_DAMAGE = 2;
private final static Class<? extends Entity> TARGET = Herbivore.class;

public Predator() {
  super(HP, SPEED, TARGET);
  this.attackDamage = ATTACK_DAMAGE;
}
```

- Не засовывай сложную логику в условие условного оператора- эта очень трудно читается
```java
if (worldMap.getEntityByCell(path.get(path.size() - 1)) instanceof Herbivore) {...}

//ПРАВИЛЬНО:
Cell cell = path.get(path.size() - 1);
Entity entity = worldMap.getEntityByCell(cell);

if (entity instanceof Herbivore) {...}
```

**11. interface Action**

👍 Идеально
```java
public interface Action {
  void execute(WorldMap worldMap);
}
```

**12. class PopulateMap implements Action**

Помещать существ на фиксированные позиции- это слабовато.  
Помещай существ на случайные координаты.

**13. class MoveEntities implements Action**

Много сложной логики перемещения креатур.  
Таким образом, единая ответственность,- выполнение хода,- разделена между двумя классами: этим и Creature(в методе `makeMove(...)`).  

Эта логика должна лежать либо в самой креатуре и быть реализована в его методе `makeMove(...)`.  
И тогда креатура интерпретируется как сущность с собственной волей.

Либо находиться в отдельном классе, например `MoveAction`, но тогда в самой креатуре не должно быть `makeMove(...)`.  
И сущность будет интерпретироваться как сущность без собственной воли, как фигура в шахматах, которую передвигает кто-то еще.

Но по ТЗ креатура должна иметь метод `makeMove(...)`, значит в этом методе и должна находиться вся логика перемещения.  
А мувер должен просто вызывать этот метод у каждой креатуры:
```java
Action - действие, совершаемое над миром. Например - сходить всеми существами. 
Это действие итерировало бы существ и вызывало каждому makeMove().
```
Итерировать- это по очереди брать креатуры и вызывать у них `makeMove(...)`.  
Далее свои действия по хождению и поеданию должны выполнять сами креатуры.

**14. Дублирование логики в class SpawnGrass и SpawnHerbivore**

Нарушение DRY.  
Два класса, которые делают одно и то же- заселяют в карту существ определенного вида в заданном количестве.  
Чеклист ТЗ:
```
Проблемы и ошибки в коде:

Иерархия классов Action’s
- Дублирование кода
```

Вместо отдельного класса на каждый вид существ, нужно сделать универсальный класс.  
Он будет принимать в конструктор количество создаваемых существ и способ их создания.  
Способ создания можно передавать через стандартный интерфейс Supplier.  
Примерно, так:
```java
public class SpawnAction extends Action {
  private final Supplier<Entity> entitySupplier;
  private final int count;

  public SpawnAction(Supplier<Entity> entitySupplier, int count) {...}

  @Override
  public void execute(Карта карта) {
    for (int i = 0; i < count; i++) {
      //получить существо из саплаера  
      //и поместить его в карту на случайную координату
    }
  }
}
```
Использование:
```java
SpawnAction заяцSpawnAction = new SpawnAction(() -> new Заяц(), КОЛИЧЕСТВО_ЗАЙЦЕВ);
SpawnAction волкSpawnAction = new SpawnAction(() -> new Волк(), КОЛИЧЕСТВО_ВОЛКОВ);
заяцSpawnAction.execute(карта); 
волкSpawnAction.execute(карта); 
```

**15. class Simulation**

- Нарушение DI. Класс не должен инициализировать сам себя.  
В данном случае класс инициализирует сам себя тем, что сам создает карту, а не принимает ее в конструктор
```java
public class Simulation {
  private WorldMap worldMap = new WorldMap();
  //...
}

//ПРАВИЛЬНО:
public class Simulation {
  private final WorldMap worldMap;
  //...
  public Simulation(WorldMap worldMap) {...}
}
```
Таким образом нельзя создать несколько игровых конфигураций с разными картами.  
*Мартин, "ЧК", гл.11, "Отделение конструирования системы от ее использования"*

- Нарушение ТЗ, нет всех указанных для этого класса методов 
```java
Simulation #
Главный класс приложения, включает в себя:
...
nextTurn() - просимулировать и отрендерить один ход
startSimulation() - запустить бесконечный цикл симуляции и рендеринга
pauseSimulation() - приостановить бесконечный цикл симуляции и рендеринга
```
Сейчас в программе есть только `startSimulation()`.

Этот класс не должен в себе реализовывать многопоточность.  
В этом классе должны быть публичные методы `startSimulation()`,`pauseSimulation()`,`nextTurn()`.  
И тот, класс, который будет реализовывать многопоточность, должен дёргать за эти методы.

## ВЫВОД

Замечаний меньше, чем обычно. Каких-то грубых ошибок архитектуры не заметил.  
Соблюдай ТЗ, перед публикацией проверяй программу по чеклисту в ТЗ.  
Посмотреть ролики Немчинского про SOLID.

n.127(279)  
#ревью #симуляция 