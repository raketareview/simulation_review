https://github.com/PronovichV1/SimulationApp  
[Valentine Pronovich]

Есть много над чем поработать.  
Используется JavaFX.

## НЕДОСТАТКИ РЕАЛИЗАЦИИ

1. Виндовс интерфейс это хорошо. Но вряд ли стоило его делать для того, чтобы показать цветные квадратики  
![pic](https://github.com/raketareview/simulation_review/blob/master/content/resources/rev-sim128/img0.png)  
Квадратики это даже по меркам консоли слабый визуал.  
В виндовс интерфейсе можно показывать существ разными красивыми картинками или анимацией. 

2. Непонятно, кто из квадратиков кто- на экране нет легенды.

## ХОРОШО

+ 👍 Координаты в существах хранятся начиная с Creature
+ 👍 Реализовано пауза/пуск во время работы
+ 👍 Windows интерфейс JavaFX

## ЗАМЕЧАНИЯ

**1. Нейминг**

- В названия не нужно вставлять частицы "Of", "The" и т.д.
Это только захламляет названия. 
Если конечно там "Of" не используется в контексте valueOf()
```java
void initTheMap()

//ПРАВИЛЬНО:
void initMap()
```

- Название обманывает. Метод возвращает не ячейку, а существо
```java
Entity getCell(int cell)

//ПРАВИЛЬНО:
Entity getEntity(int cell)
```

- Избыточно. Метод находится внутри карты. И так понятно, что получение креатур происходит из из этой карты
```java
List<Creature> getAllCreaturesFromTheMap()

//ПРАВИЛЬНО:
List<Creature> getAllCreatures()
```

- Не сокращай название без нужды, а тут ее нет, не выкидывай гласные буквы.

Что здесь значит "Qty"? Quantity, Quality, Quote To You?  
Это ребус и ответ на него неочевиден
```java
boolean isAccuratePredatorQty()
```

- Геттер должен что-то возвращать. Геттер, который ничего не возвращает- нонсенс
```java
void getCurrentPosition(int position)
```

*Oracle Java code conventions, part."Naming conventions"*  
*Мартин, "Чистый код", гл.2*  
*Ютуб, Немчинский "Как называть переменные, методы и классы?"*

**2. Используй классы через их интерфейсы**
```java
HashMap<Integer, Entity> worldMap;
ArrayList<Integer> result = new ArrayList<>();
ArrayDeque<Integer> findPath(...);

//ПРАВИЛЬНО:
Map<Integer, Entity> worldMap;
List<Integer> result = new ArrayList<>();
Deque<Integer> findPath(...);
```
Общее правило: ArrayList нужно использовать через List, HashMap- через Map и т.д.  
Это позволяет пользоваться преимуществами полиморфизма.

Да, бывают ситуации, когда, например, с LinkedList нужно работать именно как с LinkedList, а не с List. 
Но это уже нюансы.  
*"Java. Эффективное программирование", изд.3, гл.9.8*
```
"Если вы выработаете привычку использовать в качестве типов интерфейсы, ваша программа будет гораздо более гибкой" - Блох.
```

**3. Нарушение конвенции кода**

В любой ситуации выделяй тело блока скобочками, даже если тело состоит из одной строки. 
Исключение- метод equals(), там можно после if не выделять блоки скобочками
```java
if (entity instanceof Predator) return CellType.PREDATOR;
if (worldMap.getWorldMap().containsValue(creature)) currentPosition = creature.getCurrentPosition();

//ПРАВИЛЬНО:
if (entity instanceof Predator) {
  return CellType.PREDATOR;
}
if (worldMap.getWorldMap().containsValue(creature)) {
  currentPosition = creature.getCurrentPosition();
}
```

Вот тут вообще кажется, что `return` срабатывает по условию
```java
if (worldMap.getCell(next) != null) continue;
return next;

//ПРАВИЛЬНО:
if (worldMap.getCell(next) != null) {
  continue;
}
return next;
```
*Oracle Java code conventions"*

**4. В интерфейсах не пишут область видимости для полиморфных методов, она там всегда public**
```java
public interface SearchingStrategy {
  public ArrayDeque<Integer> findPath(...));
}

//ПРАВИЛЬНО:
public interface SearchingStrategy {
  ArrayDeque<Integer> findPath(...);
}
```
С версии java 9 можно делать только приватные статические методы для default методов.

**5. При прочих равных, нужно использовать примитивный тип, а не класс-обертку** 
```java
Integer nextCell = creature.getPathToFood().peek();

//ПРАВИЛЬНО:
int nextCell = creature.getPathToFood().peek();
```
Для применения класса-обертки над примитивным типом данных, должна быть какая-то причина. 
Здесь использование обертки не дает ничего большего по сравнению с примитивом.  
*Блох "Java. Эффективное программирование", изд.3, гл.9.5*
```
"Предпочитайте примитивные типы упакованным примитивным типам" - Блох.
```

**6. class WorldMap**

- Неправильный способ использования кординат.

Класс называется "Карта мира". Имеется ввиду, географическая карта.  
В географических картах положение точки в пространстве определяется пересечением двух осей и обозначается парой цифр x/y.  
В этой реализации двумерные координаты разворачиваются в одномерную плоскость и кодируются одним числом- порядковым номером.  
Это неправильный способ кодирования положения существа на карте
```java
private HashMap<Integer, Entity> worldMap;

//ПРАВИЛЬНО:
private Map<Coordinates, Entity> worldMap;
```
Нужно использовать класс координаты, который хранит значения x/y.

- Нарушение SRP, методы чужих ответственностей. 

Карта должна только хранить существа и обеспечить базовые операции с ними: вставить, выдать одно существо и список всех хранимых существ, удалить.
Если какой-то метод не нужен для обеспечения этого функционала, значит он не принадлежит к ответственности карты, а принадлежит к чужой ответственности.  
Здесь методы чужих ответственностей:  
Вернуть соседние координаты, `isAccuratePredatorQty()`, сказать тип существа по координате etc.

Наверное, для проекта в целом полезно иметь метод, который находит соседние координаты.  
Но этот процесс не имеет никакого отношения к единой ответственности карты- хранению существ в себе.  
Для организации хранения/удаления/выдачи существ, карте не нужно находить соседние координаты.  

Методы чужих ответственностей должны находиться в тех классах, в интересах которых они работают.
Если один и тот же метод используют разные классы, то метод нужно вынести в отдельный класс, например, BoardUtils.

- Карта не должна хранить запись координата(порядковый номер)-null, то есть пустые ячейки. 
Иначе теряется  все преимущество, которое дает HashMap для хранения сущностей
```java
private void initTheMap() {
  for (int i = 0; i < width * height; i++) {
    worldMap.put(i, null);
  }
}
```
Фактически, тут Map становится аналогом обычного массива, только хуже, бесполезно забивая память.
Например, есть игровая доска 100*100 и в ней единомоментно находится общим числом 150 разных существ: камней, травы, зайцев и т.д.
Тогда при нормальном хранении этих данных в мапе с помощью ключ-значение, потребуется 150 записей.
Если в этой же мапе кроме этих значимых данных хранить еще пустые сущности, то потребуется уже 10.000 записей.

Map должна хранить только значимые записи координата-entity. 
Отсутствие какой-то координаты в мапе должно означать, что такая координата не занята существом.

- Никогда не возвращай null
```java
private HashMap<Integer, Entity> worldMap;

public synchronized Entity getCell(int cell) {
  return worldMap.get(cell);  <-- может вернуть null
}
```
Возврат null повышает риск возникновения NullPointerException в программе.  
*Мартин, "Чистый код", гл.7.7-8*  
*Ютуб, Немчинский "Почему нельзя возвращать NULL?"*

- Нарушение инкапсуляции, класс предоставляет клиентскому коду подробности внутреннего устройства и доступ к нему
```java
private HashMap<Integer, Entity> worldMap;

public synchronized HashMap<Integer, Entity> getWorldMap() {
  return this.worldMap;
}
```
В данный момент клиентский код может выполнять любые операции с HashMap Карты напрямую, игнорируя допустимые картой способы. 
Например так
```java
Карта карта = new Карта(100, 100);
<заселить карту существами>
карта.getWorldMap().clear(); //геноцид- удаление из карты всех существ, минуя дозволенные картой механизмы
```
Из-за этого класс является гибридом со всеми вытекающими последствиями: *"Чистый код", гл.6*

Тут есть два варианта, как сделать правильно.  
Либо вернуть не оригинал мапы, а ее копию: 
```java
public synchronized HashMap<Integer, Entity> getWorldMap() {
  return new HashMap(this.worldMap);
}
```
Либо возвращать существ персонально:
```java
public Entity get(Coordinates coordinates)
public Coordinates getCoordinates(Entity entity)
```
Лично мне больше нравится второй вариант, когда существ возвращают персонально. 

- При всех операциях с участием координаты(добавить, выдать, удалить и т.д.), нужно проверять координату на корректность.  
Если координата некорректна(находится вне пределов карты), нужно бросать исключение
```java
public void setEntity(int cell, Entity entity) {
  worldMap.put(cell, entity);
}
```
Ближайшая аналогия- стандартные хранилища типа List и массива. 
При попытке обратиться к ним по несуществующему индексу, бросается исключение.

- Нарушение OCP. 

Карта должна работать со всеми хранимыми существами одинаково.  
И не должна работать как-то по-особому с конкретным классом представителей и не должна даже знать по именам наследников Entity
```java
public List<Creature> getAllCreaturesFromTheMap() 

//ПРАВИЛЬНО ТАК:
public List<Entity> getAll() //и тогда пусть клиент отбирает отсюда креатур

//ИЛИ ТАК:
public <T extends Entity> List<T> getEntitiesBy(Class<T> type)  //вернуть существ определенного класса 
```
Если карта будет знать по именам наследников Entity и иметь для них персональные методы, то класс будет открыт для изменений.  
Например, при добавлении в проект класса Птица, понадобится изменить класс Карта и добавить в него метод `getBirds()`.

- Почему именно "2"?  
Объясняй магию- вводи константы, которые своим именем будут объяснять значения хранимых чисел
```java
public boolean isAccuratePredatorQty() {
  //...
  return predatorCount > 2;
}
```

- Нарушение SRP.

Еще раз про ответственность карты- она должна хранить в себе существ и обеспечивать к ним доступ.  
Конвертировать существ в тип ячейки- не относится к этой ответственности
```java
public CellType getCellType(int row, int col) {
  //...
  if (entity instanceof Tree) return CellType.TREE;

  return CellType.EMPTY_CELL;
}
```

**7. interface SearchingStrategy**

+ 👍 Интерфейс поиска пути это хорошо. Теперь можно делать разные реализации поиска: BFS, A* и т.д.

- "Стратегия" это название стандартного паттерна. Я в это лм интерфейсе и его реализациях не вижу такого паттерна. 

Это просто интерфейс поиска и его реализация по алгоритмк BFS.  
Так любую группу классов бизнес логики с общим интерфейсом можно назвать стратегией.

- Предпочитай возвращать простые объекты, экзотические объекты не предпочитай
```java
ArrayDeque<Integer> findPath(...);

//ЛУЧШЕ(на мой взгляд):
List<Integer> findPath(...);
```
Ииначе ты заставляешь клиента без особой необходимости вспоминать особенности работы редко используемых типов данных.

+ 👍 Если не считать тип возвращаемого значения и использование порядкового номера ячейки вместо координаты, то сигнатура ок. 
Из нее легко понять, как пользоваться поиском пути
```java
ArrayDeque<Integer> findPath(WorldMap worldMap, int startCell, Class<? extends Entity> targetType);

//ЛУЧШЕ:
List<Coordinates> findPath(WorldMap worldMap, Coordinates from, Class<? extends Entity> targetType);
```

**8. class BfsSearchingStrategy implements SearchingStrategy**

- Нарушение SRP. 

Класс должен просто искать путь от точки старта до точки, соответствующей заданным условиям согласно алгоритму BFS.  
Класс не должен анализировать существ на их принадлежность к типам- для алгоритма BFS это просто не нужно
```java
if (entity instanceof Rock) continue;  <-- ЭТО НЕ НУЖНО ДЛЯ АЛГОРИТМА BFS
if (entity instanceof Tree) continue;
if (entity instanceof Predator) continue;
```
Если класс внутри себя зачем-то определяет принадлежность entity к какому-то типу, значит алгоритм делает что-то не то.  

Единсчтвенное сравнение, которое должно производиться над экземпляром entity, на который наткнулся поиск, это сравнение его с типом искомой цели:
```java
if (targetType.isInstance(entity)) {...}
```

- Мапа координата-координата(в данном случае порядковый_номер-порядковый_номер) при реализации поиска обычно используется как эрзац-заменитель связного списка
```java
Map<Integer, Integer> parentMap = new HashMap<>();
```
Вместо мапы координата-координата сделай класс связного списка, алгоритм тогда будет выглядеть понятнее.
Например, так:
```java
class Node {
  private Coordinates coordinates; 
  private Node parent;
  //...
  public Coordinates getCoordinates() {...}
  public Node getParent() {...}
}
```

**9. abstract class Entity и его простые потомки Rock/Tree/Grass**

👍 Идеально
```java
public abstract class Entity {
}

public class Rock extends Entity{
}
```

**10. abstract class Creature extends Entity**

- Несоблюдение ТЗ.

По ТЗ должен быть метод `makeMove(...)`. Этого метода метода здесь нет, но есть метод 
```java
public abstract void takeTurn(WorldMap worldMap);
```
Просто интересно- что улучшило переименование указанного в ТЗ названия?  
Мне кажется, стало хуже- теперь метод не "совершает ход", а "берёт ход". 
Хотя метод ничего не берет- он void.

- Сложные правила пользования классом.

В классе большое количество методов, пользоваться таким классом сложно.  
Большая часть этих методов либо не должна существовать в этом классе.  
Либо должна быть срытой от клиентов и использоваться только как подробности внутренней организации класса

Вот примерный перечень методов, которые должны быть видны клиенту:
a) Сделать ход  
b) Установить/вернуть здоровье  
с) Eстановить/вернуть координату  
в) Cказать жив или мертв

- Нарушение SRP.

В классе есть много полей и методов, которые не имеют отношения к единой ответственности сущности Creature.  
Например:
```java
protected boolean canAct = true;

public void resetAction() {
  canAct = true;
}

public void spentAction() {
  canAct = false;
}
```
Эта информация не характеризует саму сужность.  
Эта служебная информация для каких-то внешних манипуляций с сущностями.

То есть, информация, которая характеризует саму сущность:  
Очки здоровья, скорость передвижения, уровень восстановления здоровья, жив или мертв.  
Эта информация должна храниться в самом существе.

Информация, которая характеризует не сущность, а то, как её используют в чужих целях:  
Отметка о проведении с сущностью каких-то действий в текущей игровой итерации, учет готовности к новым действиям и т.д.  
Эта информация должна храниться не в самом существа, а в том классе, который проводит действия над сущностью.

Объект должен содержать ту информацию, которая характеризует сам объект, а не характеризует ситуацию, в которай этот объект используется.

Условный пример: 
Есть информационная система предприятия, в которой учтены сотрудники.  
Данные, которые описывают сотрудника: ФИО, дата рождения, должность и т.д.
Эти данные должны находиться в классе "Сотрудник".

Поступила задача вручить каждому сотруднику подарок на Новый Год и вести учёт тех, кто получил, а кто нет.  
Неправильный подход: добавить в класс "Сотрудник" поле `boolean isПолучилПодарокНовыйГод2026`.  
Правильный подход: при вручении подарка, заносить сотрудника в список "Получившие новогодний подарок сотрудники".

То есть получение/неполучение подарка не описывает сущность "Сотрудник", а описывает ситуацию, связанную с этой сущностью.

**11. Классы-стратегии для Creature**

- Мотивация.

Передвижение, поедание еды травоядным и атака хищника вынесены в интерфейсы.  
У каждого интефейса своя реализация для конкретного типа существ
```java
interface MoveStrategy
class PredatorMove implements MoveStrategy
class HerbivoreMove implements MoveStrategy
```
Зачем это сделано, мне неясно.  
Это только раздробило одну сущность, например Herbivbore, на несколько классов: `HerbivoreMove`, `HerbivoreMove` и `HerbivoreEat`.  
Код стал более громоздким и сложным.

Это точка расширения? Например, чтобы можно было делать разные стратегии атаки хищника?  
Типа, ленивый хищник, злой хищник, матерый хищник?  
Намерения и мотивация здесь мне не видны.  
Пока это выглядит, как ухудшение архитектуры, а не ее улучшение.

Если бы код стратегий лежал в самих Зайце и Волке, возможно, было бы лучше.

- Дублирование кода.

Код в статегиях для Зайца и Волка правктически идентичен. 
```java
public class PredatorMove implements MoveStrategy {
  @Override
  public void move(Creature creature, WorldMap worldMap) {
    if (!creature.getCanAct() || creature.getPathToFood().isEmpty()) return;

    Integer currentPosition = null;
    if (worldMap.getWorldMap().containsValue(creature)) currentPosition = creature.getCurrentPosition();
    if (currentPosition == null) return;
    //дальше тоже одно и то же
  }
}

public class HerbivoreMove implements MoveStrategy {
  @Override
  public void move(Creature creature, WorldMap worldMap) {
    if (!creature.getCanAct() || creature.getPathToFood().isEmpty()) return;

    Integer currentPosition = null;
    if (worldMap.getWorldMap().containsValue(creature)) currentPosition = creature.getCurrentPosition();
    if (currentPosition == null) return;
    //дальше тоже одно и то же
  }
}
```
Общий код классов выноси в их предка.

Сейчас Заяц и Волк идут к своей еде абсолютно одинаковым способом. 
Отличается только тип еды, к которой они идут.  
Не вижу смысла делать для этого разные классы- достаточно одного универсального.  

Смысл в разных стратегиях ходьбы может появиться тогда, когда разница будет не в типе искомой цели, а в способе самого перемещения.

Например:  
Ходьба по 8 или 4 направлениям(верикаль, горизонталь), ходьба только по пустым клеткам или ходьба с перепрыгиванием травы и т.д.  

**12. class Actions**

Идея `Action` здесь не осмыслена. 

Идея состоит в том, что нужно создать семейство родственных классов, объединенных общим интерфейсом.  
Каждый из этих классов должен делать что-то свое с картой: одна акция должна заселять карту существами, другая делать ходы и т.д.  
Action'ы, изложенные в ТЗ, это вариант реализации паттерна Command. 

Сейчас `Actions` у тебя это просто сборник функций, сделанный в стиле процедурного программирования
```java
public class Actions {

  public Actions(Simulation simulation) {
    this.simulation = simulation;
    initActions = new InitActions(simulation);
    turnActions = new TurnActions(simulation);
  }

  public InitActions getInitActions() {...}
  public void initAllObjectsAndEntity(WorldMap worldMap) {...}
  public void turnActionOnAllCreatures(WorldMap worldMap) {...}
}
```

Смысл Action'ов состоит в том, что должен быть общий класс/интерфейс Action и его наследники.  
В каждом экшене должен быть только один публичный метод(не публичных может быть сколько угодно).  
Это вариация паттерна Command- экшены должны быть родственны и одинаково использоваться через полиморфизм. Примерно так
```java
interface Action{
 void execute(Карта карта);
}

class ХодитьAction реализует Action {
  void execute(Карта карта) {
    //обойти всю карту
    //найти каждую креатуру
    //и дать ей пинка чтоб побежала
  }
}

class ДатьСигаретуВсемЗайцамAction реализует Action {
  void execute(Карта карта) {
    //обойти всю карту
    //найти всех зайцев и дать им по сигарете 
  }
}

List<Action> actions = List.of(new ДатьСигаретуВсемЗайцамAction(), new ХодитьAction());
for(Action a: actions) {
  a.execute(карта);
}

/*
Результат: программа обойдет всю карту, найдет всех креатур и даст им пинка, чтобы они побежали.
А каждому зайцу предварительно даст сигарету.
*/
```

В ТЗ по этому поводу написано:
```java
Action - действие, совершаемое над миром. Например - сходить всеми существами. 
...Каждое действие описывается отдельным классом и совершает операции над картой. 
```

Кроме класса `Actions`, в реализации есть еще два класса с похожим названием:  
```java
public class InitActions {
  public void addAllObjectsTo(WorldMap worldMap) {...}
  public void addGrassToMap(WorldMap worldMap, int grassSpawnQty) {...}
  public void addAllCreatureTo(WorldMap worldMap)  {...}
  public void addPredatorToMap(WorldMap worldMap, int predatorSpawnQty)  {...}
  public void addHerbivoreToMap(WorldMap worldMap, int herbivoreSpawnQty)  {...}
}

public class TurnActions {
  public void checkPathToFood(WorldMap worldMap) {...}
  public void makeMove(WorldMap worldMap) {...}
}
```
Эти классы тоже не соответствуют критериям экшенов из ТЗ и они не родственны друг другу.  
Между ними нет ничего общего, кроме названия.

Рассматривать подробно каждый из этих классов не вижу смысла.  
В них хватает своих недостатков, но самый главный из них- неправильная архитектура на уровне концепции.  
Переделай экшены в духе ТЗ и тогда их можно будет обозревать.

**13. Билдеры и фабрики существ**

В программе есть несколько классов для создания существ- фабрики и билдеры
```java
abstract class CreatureBuilder
class HerbivoreBuilder extends CreatureBuilder
class PredatorBuilder extends CreatureBuilder
class CreatureFactory
```
Потребность в таком количестве классов возникла прежде всего потому, что класс `Creature` хранит много лишнего.  
Если оставить в нем только то, что соответствует SRP класса, то можно будет обойтись только одной фабрикой, без билдеров.

**14. class CreatureFactory**

Циклическая зависимость.  
Классы взаимнозависимы друг от друга.
```java
+--> Simulation 
|        ↓
|    Actions
|        ↓
|    InitActions
|        ↓
+---CreatureFactory

public class CreatureFactory {

  public Creature createHerbivore(Simulation simulation) {
    return new HerbivoreBuilder()
      .speed(simulation.getCreatureSpeed())  <-- Зависит от класса Simulation
      .hitPoints(simulation.getHitPoints())
      //...
  }
}
```
По возможности, избегай циклических зависимостей.  
Конкретно в этом случае, циклическая зависимость появилась от общих недостатков архитектуры.  

**15. class Simulation implements Runnable**

- Всегда явно указывай область видимости. В данном случае, это protected
```java
public class Simulation implements Runnable {
  Simulation(SimulationBuilder builder) {...}
  //...
}

//ПРАВИЛЬНо:
public class Simulation implements Runnable {
  protected Simulation(SimulationBuilder builder) {...}
  //...
}
```

- Неправильное использование билдера
```java
Simulation(SimulationBuilder builder) {
  this.worldMap = builder.worldMap;
  this.creatureSpeed = builder.creatureSpeed;
  this.hitPoints = builder.hitPoints;
  this.attackPower = builder.attackPower;
  this.attackRange = builder.attackRange;
  this.herbivoreSpawnQty = builder.herbivoreSpawnQty;
  this.predatorSpawnQty = builder.predatorSpawnQty;
  this.rockSpawnQty = builder.rockSpawnQty;
  this.treeSpawnQty = builder.treeSpawnQty;
  this.grassSpawnQty = builder.grassSpawnQty;
  actions = new Actions(this);
  roundCount = 0;
}
```
Здесь билдер используется как конфигурационный класс, а это две разные концепции.

Суть паттерна "Строитель" состоит в другом- билдер Симуляции должен сам создавать экземпляр Симуляции. Так что, этот билдер-не билдер.  
Если нужно создать экземпляр Симуляции через объект-аргумент, то используй конфигурационный класс.  
Примерно, так:
```java
public class Simulation implements Runnable {
  private final Config config;

  protected Simulation(Config config) {
    this.config = config;
  }
  //...
  protected static class Config {
    protected WorldMap worldMap,
    //...
  }
}
```

И тогда вместо:
```java
public class SimulationBuilder {
  //... 
  public Simulation build() {
    if (worldMap == null) {
      throw new IllegalStateException("WorldMap must be set");
    }
    return new Simulation(this);
  }
}
```
будет:
```java
public class SimulationBuilder {
  private Simulation.Config config;
  //... 
  public Simulation build() {
    if (config.worldMap == null) {
      throw new IllegalStateException("WorldMap must be set");
    }
    return new Simulation(config);
  }
}
```

## АРХИТЕКТУРА 

Основная цель этого проекта- демонстрация принципов дизайна архитектуры приложения с помощью ООП.

Лучше это демонстрировать в консольном интерфейсе- тогда ошибки дизайна ООП архитектуры не маскируются красивыми картинками.  
Чеклист ТЗ:
```
Проблемы и ошибки в коде:
Интеграция библиотеки графического интерфейса ценой чистоты и понятности кода. 
Достаточно консольного интерфейса.
```
В этой реализации интеграция виндовс интерфейса пошла во вред архитектуре.  
И становится неясно, где ошибки архитектуры проистекают от неправильного понимания декомпозиции доменной логики. 
А где- от неправильной интеграции доменной логики с графическим интерфейсом.

## ВЫВОД

Если будешь рефакторить- переделай на консольный интерфейс, чтобы сконцентрироваться на архитектуре.  
Виндовс интерфейс для бекенда не нужен, а хорошая архитектура- нужна.

Подробнее разберись с Action'ами, посмотри ролики на ютубе про паттерн "Command", чтобы получить о нем общее представление.

Не видно и понимания декомпозиции- как делить программу на классы.  
Для лучшего понимания декомпозиции ООП посмотреть ролики Сергея про шахматы.  
Посмотреть ролики Немчинского про SOLID.

n.128(280)  
#ревью #симуляция #javafx 