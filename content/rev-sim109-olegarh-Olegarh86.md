https://github.com/Olegarh86/Simulation  
[Олегарх]

В целом, норм.

## НЕДОСТАТКИ РЕАЛИЗАЦИИ

1. Когда я выбираю дефолтную конфигурацию, происходит постепенное печатание текста с дефолтными параметрами- буквы выводятся с задержкой.
На этот визуальный эффект уходит 15 секунд. Это красиво, но ждать столько времени немного утомительно. 

## ХОРОШО

+ 👍 Красивая распечатка карты  
![pic](https://github.com/raketareview/simulation_review/blob/master/content/resources/rev-sim109/img0.png)

+ 👍 Спрайты существ не хранятся в самих существах
+ 👍 Координаты существ не хранятся в самих существах(мне так больше нравится)
+ 👍 Реализовано пауза/пуск во время работы
+ 🚀 Возможность конфигурирования настроек через меню
+ 👍 Два варианта запуска программы: через конфигурирование или с настройками по умолчанию

## ЗАМЕЧАНИЯ

**1. Нейминг**

- КОНСТАНТЫ_НУЖНО_ПИСАТЬ_СТИЛЕМ_UPPER_SNAKE
```
public final static int numberOfLines = 10;

//ПРАВИЛЬНО:
public final static int GAME_MAP_WIDTH = 10;
```

- Опечатка?
```
int randomString = random.nextInt(config.numberOfColumns);
```

- Придерживайся норм английского языка, карта мира - [world map](https://en.wikipedia.org/wiki/World_map)
```
class MapOfWorld

//ПРАВИЛЬНО:
class WorldMap
```
При прочих равных, в названия не нужно вставлять частицы "Of", "The" и т.д. 
Если конечно там "Of" не используется в контексте `valueOf()`.

+ 👍 Мне нравиться это название хешмапы
```
private final Map<Coordinate, Entity> coordinatesEntities = new HashMap<>();
```

- По конвенции, префиксы is/has должны стоять первыми
```
boolean coordinateIsValid(Coordinate coordinate)

//ПРАВИЛЬНО:
boolean IsValidСoordinate(Coordinate coordinate)
```

- Избыточно. Понятно, что в классе "Искатель пути" метод "найти" возвращает именно путь, а не что-то иное
```
PathFinder {
  List<Coordinate> findWayToTarget(...)
}

//ПРАВИЛЬНО:
PathFinder {
  List<Coordinate> find(...)
}
```

- Название должно быть в единственном числе. Класс делает одно действие, один экшен
```
interface Actions

//ПРАВИЛЬНО:
interface Action
```

- Избыточно
```
ConsoleInput.readInput()

//ПРАВИЛЬНО:
ConsoleInput.read()
```

*Oracle Java code conventions, part."Naming conventions"*  
*Мартин, "Чистый код", гл.2*  
*Ютуб, Немчинский "Как называть переменные, методы и классы?"*

**2. Вертикальное форматирование**
```
List<Coordinate> findWayToTarget(MapOfWorld map, Class<? extends Entity> target,
    List<Class<? extends Entity>> obstacles, Coordinate startCoordinate);

//ПРАВИЛЬНО:
List<Coordinate> findWayToTarget(
    MapOfWorld map, 
    Class<? extends Entity> target,
    List<Class<? extends Entity>> obstacles, 
    Coordinate startCoordinate
  );
```

**3. Нарушение DRY**, магические буквы, числа, слова. Вводи константы. А если они есть- пользуйся 
```
private final static String DEFAULT_CONFIG = "1";
private final static String USER_CONFIG = "2";
private final static String ERROR_CHANGE_CONFIG_FACTORY = "Incorrect symbol! Please, enter symbol for change " +
      "configuration of the world: '1' - for default configuration or '2' - for manual configuration";

//ПРАВИЛЬНО:
private final static String DEFAULT_CONFIG = "1";
private final static String USER_CONFIG = "2";
private final static String ERROR_CHANGE_CONFIG_FACTORY = """
    Incorrect symbol! Please, enter symbol for change 
    "configuration of the world: '%s' - for default configuration or '%s' - for manual configuration
    """.formatted(DEFAULT_CONFIG, USER_CONFIG);
```
*Фаулер, "Рефакторинг", гл.8, "Замена магического числа символической константой"*  
*refactoring.guru "Замена магического числа символьной константой"*  

**4. Используй возможности нового написания switch-case**
```
private EntityFactory getEntityFactory(String classEntity) {
  switch (classEntity) {
    case ROCK -> {
      return new RockFactory();
    }
    case TREE -> {
      return new TreeFactory();
    }
    //...  
    default -> throw new RuntimeException(ERROR);
  }
}

//ПРАВИЛЬНО:
private EntityFactory getEntityFactory(String classEntity) {
  return switch (classEntity) {
    case ROCK -> new RockFactory();
    case TREE -> new TreeFactory();
    //...  
    default -> throw new RuntimeException(ERROR);
  }
}
```

**5. record Coordinate(int line, int column)**

+ 👍 Record для координаты это хорошо.

- Эту пару обычно называют row/column
```
int line, int column
```

- Это фабричный метод, он не должен называться как геттер
```
public static Coordinate getCoordinate(int line, int column) {
  return new Coordinate(line, column);
}
```
Стандартные названия для фабричных методов: `of(...)`, `valueOf(...)` etc.

- Фабричный метод дублирует конструктор

В классе есть и конструктор(у каждого рекорда есть конструктор) и дополнительно ты сделал еще фабричный метод
```
public static Coordinate getCoordinate(int line, int column) {
  return new Coordinate(line, column);
}
```

Не делай фабричный метод, который принимает те же аргументы, что и конструктор. 
По крайней мере в *тех случаях*, когда конструктор публичный
```
Coordinate coordinate1 = new Coordinate(1, 1);  //конструктор
Coordinate coordinate2 = Coordinate.getCoordinate(1, 1); //фабричный метод
```
Есть объекты из стандартной библиотеки, которые можно создавать и через конструктор и через фабричный метод. 
Например:
```
BigInteger b1 = new BigInteger("10");  //конструктор
BigInteger b2 = BigInteger.valueOf(10);  //фабричный метод
```
Но они отличаются типом входных аргументов.

Лично я в `Coordinate` не вижу необходимости делать фабричный метод- он тут вообще ничего не дает сверх конструктора.  
Ввод любого метода в класс должен решать какую-то проблему. Фабричный метод, который просто дублирует конструктор, не решает никакой проблемы.

- Нарушение SRP

Единая ответственность координаты- хранить данные для идентификации точки в двумерном пространстве. 
В данном случае- хранить и выдавать значения осей по x и y.

Для выполнения этой ответственности координате не нужен метод, который ищет рандомную координату *где-то*
```
public static Coordinate getRandomCoordinate(Config config)
```
Класс Координата может отлично жить без этого метода. 
Определи тот класс, который не может жить без этого метода и перенеси его туда.

- Low Coupling

Класс знает про класс Конфигурации, кооторый к нему не имеет никакого отношения, а значит, зависит от него
```
getRandomCoordinate(Config config)
```
Чисто логически- в конфиге хранится много всякого: скорость Волка, пауза между ходами и т.д.  
Зачем координате знать всю эту информацию? 
Эта информация координате не нужна для выполнения ее единой ответстсвенности по идентификации точки в пространстве.

Вот такой должен быть идеальный класс Координата для этого, да и для любого другого проекта с прямоугольным плоским полем табличного вида:
```
public record Coordinate(int row, int column) {
}
```

**6. class MapOfWorld**

- Нарушение Low Coupling, неправильное использование конфигурационного класса

Карте нужно знать свои размеры, поэтому она должна просто получить и хранить свои размеры. 
А не контейнер со своими размерами и *еще чем-то*, что не имеет отношение к Карте
```
public class MapOfWorld {
  public MapOfWorld(Config config) {  <-- ЕСТЬ ЗАВИСИМОСТЬ НА КЛАСС КОНФИГУРАЦИИ
    this.config = config;
  }
  //...
}

//ПРАВИЛЬНО:
public class MapOfWorld {
  public MapOfWorld(int width, int height) {  <-- НЕТ ЗАВИСИМОСТИ НА КЛАСС КОНФИГУРАЦИИ
    this.width = width;
    this.height = height;
  }
  //...
}
```

- Особые случаи обрабатывай первыми
```
if (coordinateIsValid(coordinate)) {
  coordinatesEntities.put(coordinate, entity);
} else {
  throw new RuntimeException(ERROR_COORDINATE_NOT_VALID);
}

if (coordinateIsValid(coordinate)) {
  return Optional.ofNullable(coordinatesEntities.get(coordinate));
} else {
  throw new RuntimeException(ERROR_COORDINATE_NOT_VALID);
}

//ПРАВИЛЬНО:
if (!coordinateIsValid(coordinate)) {
  throw new RuntimeException(ERROR_COORDINATE_NOT_VALID);
} 
coordinatesEntities.put(coordinate, entity);

if (!coordinateIsValid(coordinate)) {
  throw new RuntimeException(ERROR_COORDINATE_NOT_VALID);
} 
return Optional.ofNullable(coordinatesEntities.get(coordinate));
```

- При попытке удалить существо по координате, где никого нет дома, нужно кидать исключение.  
```
void deleteEntity(Coordinate coordinate)
```
Потому что такая попытка означает баг алгоритма- надо полагать, пытаясь удалить суслика, алгоритм предполагает наличие данного суслика в данном месте.
А если суслика там нет, значит алгоритм глючит и что-то пошло не так.

+ 👍 Геттер не возвращает null, это хорошо
```
public Optional<Entity> getEntity(Coordinate coordinate)
```

+ 👍 Хорошо, что возвращается копия мапы, а не оригинал. Тем самым соблюдается инкапсуляция и защищается внутреннее устройство класса
```
public Map<Coordinate, Entity> getCoordinatesEntities() {
  return new HashMap<>(coordinatesEntities);
}
```

- Метод совершения хода в карте- нарушение SRP.
Теперь можно вызвать из карты метод хода и телепортировать зайца из одного края в другой минуя все правила игровой логистики
```
Карта карта = new Карта(100, 100);
карта.putEntity(new Координата(0, 0), new Заяц());
карта.moveEntity(new Координата(0, 0), new Координата (99, 99));

/* class Карта */
public void moveCreature(Creature currentCreature, Coordinate startCoordinate, Coordinate newCoordinate) {
  if (coordinateIsValid(startCoordinate) && coordinateIsValid(newCoordinate)) {  <-- НЕКОТОРАЯ ПРОВЕРКА КОРРЕКТНОСТИ ДВИЖЕНИЯ
    coordinatesEntities.put(newCoordinate, currentCreature);
    coordinatesEntities.remove(startCoordinate);
  } else {
    throw new RuntimeException(ERROR_COORDINATE_NOT_VALID);
  }
}
```
Должна ли карта учитывать логистику зайцев? Если да, то как? 
Карта вообще не должна задаваться этими вопросами и не должна содержать в себе метод телепортации.

+ 👍 В целом, с точки зрения SRP, критических нарушений нет.

**7. class Config**, конфигурационный класс

Конфигурационный класс в программе используется неправильно.

Другие классы получают весь конфиг в конструктор, а должны получать *только* необходимые данные из него.  
Это делает классы неуниверсальными и зависимыми от конфигурационного класса.  

Условные примеры:
```
//#1
public class Settings {
  public static final int ROOM_NUMBERS = 5;
}

//ПЛОХО:
public static void main(String[] args) {
  House house = new House();
  //oth code
}

public class House {
  private final int roomNumbers;

  public House() {
    this.roomNumbers = Settings.ROOM_NUMBERS;
  }

  public int getRoomNumbers() {
    return roomNumbers;
  }
}

//ХОРОШО:
public static void main(String[] args) {
  House house = new House(Settings.ROOM_NUMBERS);
  //oth code
}

public class House {
  private final int roomNumbers;

  public House(int roomNumbers) {
    this.roomNumbers = roomNumbers;
  }

  public int getRoomNumbers() {
    return roomNumbers;
  }
}
```

```
#2:
public class Settings {
  private final static int ROOM_NUMBERS = 5;

  public getRoomNumbers() {
    return ROOM_NUMBERS;
  }
}

//ПЛОХО:
public static void main(String[] args) {
  House house = new House(new Settings());
  //oth code
}

public class House {
  private final int roomNumbers;

  public House(Settings settings) {
    this.roomNumbers = settings.getRoomNumbers();
  }

  public int getRoomNumbers() {
    return roomNumbers;
  }
}

//ХОРОШО:
public static void main(String[] args) {
  Settings settings = new Settings();
  House house = new House(settings.getRoomNumbers());
  //oth code
}

public class House {
  private final int roomNumbers;

  public House(int roomNumbers) {
    this.roomNumbers = roomNumbers;
  }

  public int getRoomNumbers() {
    return roomNumbers;
  }
}
```
Про классы констант, конфигурации и их использование я писал [ТУТ](https://t.me/zhukovsd_it_chat/53243/176984)

Да, класс может получать в конструктор не отдельные поля, а целый объект с набором этих полей. 
Но там должна быть только нужная классу информация.  
*"ЧК", гл.3, "Объекты как аргументы"*

**8. interface PathFinder**

+ 👍 Интерфейс поиска пути это хорошо. Теперь можно делать разные реализации поиска: BFS, AStar и т.д.

+ 👍 Сигнатура метода поиска ок
```
List<Coordinate> findWayToTarget(MapOfWorld map, Class<? extends Entity> target,
    List<Class<? extends Entity>> obstacles, Coordinate startCoordinate);
```

+ 👍 Идея разных препятствий для разных существ это ок.

**9. class Nodes** связный список для использования в поиске пути BFS

+ 👍 Сделал вспомогательный класс для поиска пути, а не ограничился мапой координата-координата, это хорошо.

- Какая-то избыточная реализация связного списка
```
public class Nodes {
  private Node head;

  static class Node {
    private final Coordinate coordinate;
    private Node next;
    //...
  }
}
```
Алгоритму BFS не нужно знать голову(начальную точку списка). 
Для этого алгориртма достаточного одного класса `Node` без `Nodes`.

В реализации алгоритма BFS весь прикол в том, что когда мы наконец нашли цель, то идя в однонапрвленном списке назад, мы восстановим этот путь.
Да, потом этот путь нужно "перевернуть", но само по себе в этом нет ничего страшного.

Я так понимаю, дополнительный класс "Nodes" и его голова, связь в `Node` с другими точками не назад а вперед- все это для того, чтобы соптимизировать процесс поиска пути.
Оптимизация здесь состоит в том, что не нужно переворачивать найденный путь. 

Идея ясна. Её плюсы: поиск происходит чуть-чуть быстрее. Минусы: более сложный класс.  
Выгода по скорости будет совсем ничтожная и принципиально ни на что не повляет.

- Код вложенного класса должен располагаться ниже кода хозяйского класса. 
Здесь код вложенного класса находится даже выше, чем конструктор хозяйского класса- вообще нонсенс.

**10. class BFSPathFinder implements PathFinder**

- Все равно происходит переворот пути. 
Мотивация существования класса `Nodes` для меня становится все более непонятной
```
List<Coordinate> wayToTarget = new ArrayList<>();
//заполнение wayToTarget координатами из nodes
Collections.reverse(wayToTarget);
```

- Алгоритм реализован неправильно. Он работает, но внутренее устройство его какое-то странное

Сначала происходит поиск цели, результатом поиска является координата, где была найдена цель-существо нужного класса (#1)  
Далее вызывается метод, который по этой координате как-то хитро находит путь (#2)
```
public List<Coordinate> findWayToTarget(...) {
  List<Nodes> allNodes = new ArrayList<>();
  Coordinate targetCoordinate = findTarget(map, target, obstacles, startCoordinate, allNodes); <---#1
  if (targetIsAvailable(startCoordinate, targetCoordinate)) {
    return routeConstruction(startCoordinate, targetCoordinate, allNodes);  <---#2
  }
  return List.of();
}
```
Промежуточный результатом поиска цели должна быть не координата, где лежит цель.  
А нода, которая в себе хранит эту координату *и еще* весь путь от точки старта.  
Должно быть как-то так:
```
public List<Coordinate> findWayToTarget(...) {
  Node targetNode = findTarget(map, target, obstacles, startCoordinate, allNodes); <-- НАШЛИ НОДУ С КООРДИНАТОЙ ЦЕЛИ
  List<Nodes> path = toList(targetNode);  <-- ПРЕОБРАЗОВАЛИ НОДУ В СПИСОК КООРДИНАТ ОТ КОНЕЧНОЙ ДО НАЧАЛЬНОЙ ТОЧКИ
  return Collections.reverse(path); <-- ВЕРНУЛИ ПУТЬ В ВИДЕ СПИСКА КООРДИНАТ ОТ НАЧАЛЬНОЙ ДО КОНЕЧНОЙ ТОЧКИ
}
```

**11. abstract class Creature extends Entity**

👍 В целом, норм.

**12. class Herbivore/Predator extends Creature**

Из кода получается, что препятствием является всё, что не еда
```
//ВОЛК
private static final Class<? extends Entity> TARGET = Herbivore.class;
private static final List<Class<? extends Entity>> OBSTACLES = List.of(Rock.class, Tree.class, Grass.class, Predator.class);

//ЗАЯЦ
private static final Class<? extends Entity> TARGET = Grass.class;
private static final List<Class<? extends Entity>> OBSTACLES = List.of(Rock.class, Tree.class, Herbivore.class, Predator.class);
```
Вполне логично считать препятствием всё, что не еда.  
Но зачем делать специальный список с перечислением всех, кто не еда- не ясно.
В таком случае проще сделать алгоритм поиска, который будет считать препятствием любое существо, кроме класса-цели(то есть еды).

А сейчас при такой реализации эти классы нарушают принцип открытости/закрытости- сейчас они открыты для изменений.  
Если в проект добавится новое ентити, например еще одно препятствие в виде класса Картус, 
то придется изменять код в классах Herbivore/Predator- добавить Кактус в список припятствий.

Список препятствий имеет смысл, если для разных существ есть разные проходимые и непроходимые поверхности. 
Например, если Волк может ходить по траве, а Заяц нет; Заяц может ходить по болоту, а Волк нет.

Хотя тогда должен быть не список "Препятствия", а список "Проходимые поверхности". 
Препятствием должно являться все, что не еда и не проходимая поверхность.

**13. Интерфейс Actions и его реализации**

👍 В целом, норм.

**14. class OutputActions implements Actions**

Отдельный экшен для распечатки карты. Вынесение распечатки в экшен мне кажется спорным.  
Ведь вне зависимости от того, сколько в программе будет экшенов и каких именно, все равно нужно каждый ход распечатывать карту.

**15. Пакет view**

👍 Отдельные интерфейсы и классы, через которые происходит ввод/вывод информации для юзера. 
Тем самым логика отделяется от представления, это отлично.

**16. interface Renderer**

+ 👍 Интерфейс рендерера это хорошо. Теперь можно делать разные рендереры для разных визуальных сред(консоль, интерфейс виндовс, http, матричный принтер etc)
и разного отображения информации(цветной, черно-белый и пр.)

- Абстракции и подробности.

Интерфейс должен декларировать "абстрактное" действие, здесь это абстрактное действие- распечатать карту.
Интерфейс должен говорить, *что делать*, а не как делать.  

Но сейчас интерфейс еще объявляет действие, которое является подробностью выполнения команды "распечатать (что-то)".  
Еще хуже, что эта подробность касается распечатывания в конкретной среде- консоли
```
public interface Renderer {
  void draw(MapOfWorld world, Config config);
  String getSprite(Object entity);
}
```
Потому что только в консоли спрайт существа будет в виде строки. 
Ну может еще при распечатке на матричном принтере.

Но в других средах спрайт может быть не обязательно строкой. 
Например, в программе на Swing спрайт существа может быть пиксельной картинкой или какой-то анимацией.
И условный класс `SwingRenderer implements Renderer` будет печатать существ пиксельными картинками и анимацией.

А при выполнении на аркадном автомате(через соответствующую имплементацию `ArcadeMachineRenderer`), существо будет вообще обозначаться свечением лампочки.

Правильный интерфейс должен быть без таких подробностей:
```
public interface Renderer {
  void draw(MapOfWorld world, Config config);
}
```

**17. class BaseSimulationRenderer implements Renderer**

+ 👍 Спрайты хранятся здесь, а не в самих существах, это хорошо.

- Рендерер должен работать не с Object, а конкретно с существами
```
public String getSprite(Object entity)

//ПРАВИЛЬНО:
public String toSprite(Entity entity)
```

- Лишняя кастомизация, она тут ни в чем не участвует
```
case Tree t -> TREE_SPRITE;

//ПРАВИЛЬНО:
case Tree -> TREE_SPRITE;
```

- Плохо читается: несколько(четыре) инструкций, написанных в одну строку. 

Вводи поясняющие переменные. 
Все равно при чтении кода приходится мысленно разворачивать это на несколько строк
```
stringBuilder.append(getSprite(world.getEntity(Coordinate.getCoordinate(i, j)).orElse(null)));
```
*Фаулер, "Рефакторинг", гл.6, "Введение поясняющей переменной"* 

**18. Фабрика фабрик**

Не стоит доходить до создания фабрик фабрик, это заметно усложняет архитектуру. 
Потом еще захочется сделать фабрику фабрики фабрик и т.д.
```
public class FactoryEntityFactories {  <-- ФАБРИКА ФАБРИКИ СУЩЕСТВ
  private EntityFactory getEntityFactory(String classEntity) {...}
  //...
}
```
Как конкретно этого избежать- нужно отдельно смотреть в каждом конкретном случае. Пока просто тезис.

**19. class Simulation**

👍 Принимает в конструктор достаточное количество зависимостей, в том числе списки действий. 
Это позволяет делать майны с разными игровыми конфигурациями, не меняя код в классе `Simulation`
```
public Simulation(Input input, Config config, MapOfWorld world, List<Actions> initActions, List<Actions> turnActions) 
```

**20. class Main**, содержит точку входа main

👍 Только создает и запускает Симуляцию, это хорошо.

## ВЫВОД

Несмотря на некоторое количество замечаний, в целом норм. 
В большистве случаев код читается без особых усилий, мысль автора понятна.

n.109(248)  
#ревью #симуляция 