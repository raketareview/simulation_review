https://github.com/aneG200229/simulation  
[aneG]

Есть над чем поработать.

## НЕДОСТАТКИ РЕАЛИЗАЦИИ

1. Нет защиты от дурака
```
=== МЕНЮ СИМУЛЯЦИИ ===
1. Запустить бесконечную симуляцию
2. Сделать один ход
3. Выход
ssss
Exception in thread "main" java.util.InputMismatchException
	at java.base/java.util.Scanner.throwFor(Scanner.java:947)
```

## ХОРОШО

+ 👍 Реализовано пауза/пуск во время работы

## ЗАМЕЧАНИЯ

**0. Мусор в репозитории**

В репозитории закомичены лишние файлы и папки.  
Разберись, что нужно сохранять в репозитории, а что нет.

**1. Нейминг**

- Возврат значения константы это не повод нарушать camelCase
```java
public int getMAX_Y() {
  return MAX_X;
}

//ПРАВИЛЬНО:
public int getMaxY()
```

- Название должно как можно лучше объяснять суть явления.

Этот класс не содержит путь. Этот класс ищет путь
```java
public class Path 

//ПРАВИЛЬНО:
public class PathFinder
```
Аналогии: FileReader, а не File; GameMapRenderer, а не GameMap.

- Если в проекте есть класс `GameMap`, то все переменные с именем, включающим это название, должны быть экземплярами этого класса.
Если разные концепции называются одним и тем же именем, это приводит к путанице
```java
public class GameMap {
  private final HashMap<Coordinates, Entity> gameMap = new HashMap<>();
  //...
}

public class Path {
  ArrayList<Coordinates> path = new ArrayList<>();
  //...
}

//ПРАВИЛЬНО:
public class GameMap {
  private final HashMap<Coordinates, Entity> entities = new HashMap<>();
  //...
}

public class PathFinder {
  ArrayList<Coordinates> path = new ArrayList<>();
  //...
}
```

- Это множественное число
```java
public Collection<Entity> getAllEntity()

//ПРАВИЛЬНО:
public Collection<Entity> getAllEntities()
```

- Для избежания путаницы не называй пакеты, классы, интерфейсы так же, как называются классы и интерфейсы стандартной библиотеки.  
Есть стандартный интерфейс `Map`, не должно быть кастомного пакета с таким же названием
```java
package map;

//ПРАВИЛЬНО:
package gamemap;
```

- В названии переменных не пиши тип данных, к которым они относится. И вообще не употребляй венгерскую ноттацию
```java
List<Coordinates> findPath(..., Class<?> tClass)

//ПРАВИЛЬНО:
List<Coordinates> findPath(..., Class<?> target)
```
*Oracle Java code conventions, part."Naming conventions"*  
*Мартин, "Чистый код", гл.2*  
*Ютуб, Немчинский "Как называть переменные, методы и классы?"*

**2. Используй классы через их интерфейсы**
```java
HashMap<Coordinates, Entity> gameMap = new HashMap<>();
ArrayList<Coordinates> path = new ArrayList<>();

//ПРАВИЛЬНО:
Map<Coordinates, Entity> gameMap = new HashMap<>();
List<Coordinates> path = new ArrayList<>();
```
Общее правило: ArrayList нужно использовать через List, HashMap- через Map и т.д.  
Это позволяет пользоваться преимуществами полиморфизма.

Да, бывают ситуации, когда, например, с LinkedList нужно работать именно как с LinkedList, а не с List. 
Но это уже нюансы.  
*"Java. Эффективное программирование", изд.3, гл.9.8*
```
"Если вы выработаете привычку использовать в качестве типов интерфейсы, ваша программа будет гораздо более гибкой" - Блох.
```

**3. Если в блоке if есть return**(break, continue, throw, exit и т.д.), то else не пишется.
 
В этом случае неважно, будет else или нет, так как программа будет работать одинаково, а код без `else` будет выглядеть читабельней
```java
if (x <= gameMap.getMAX_X() && x >= gameMap.getMIN_X() && y <= gameMap.getMAX_Y() && y >= gameMap.getMIN_Y()) {
  return true;
} else {
  return false;
}

//ПРАВИЛЬНО:
if (x <= gameMap.getMAX_X() && x >= gameMap.getMIN_X() && y <= gameMap.getMAX_Y() && y >= gameMap.getMIN_Y()) {
  return true;
} 
return false;
```

**4. Форматирование строк**

Если нужно печатать или создавать строку с более, чем одним подстановочным значением или значение вставляется внутрь сообщения, используй форматирование- тогда сразу будет виден весь шаблон
```java
System.out.println("\uD83D\uDC3A на " + oldCoords + " атаковал \uD83D\uDC30 на " + herbivoreCoords);
System.out.println("=== ХОД " + turnCounter + " ===");

//ПРАВИЛЬНО:
System.out.printf("%s на %s атаковал %s на %s  \n", PREDATOR_SPRITE, HERBIVORE_SPRITE, oldCoords, herbivoreCoords);
System.out.printf("=== ХОД %d ===  \n", turnCounter);
```

**5. Нарушение DRY**, магические буквы, числа, слова. Вводи константы 
```java
private void showRunningMenu() {
  System.out.println("=== БЕСКОНЕЧНАЯ СИМУЛЯЦИЯ ЗАПУЩЕНА ===");
  System.out.println("1. Пауза");
  System.out.println("2. Выход");
}

private void handleRunningMenu(int choice) {
  switch (choice) {
    case 1 -> simulation.pauseSimulation();
    case 2 -> exit();
    default -> System.out.println("Неверный выбор");
  }
}

//ПРАВИЛЬНО:
private final static int PAUSE = 1;
private final static int QUIT = 2;

private void showRunningMenu() {
  System.out.println("=== БЕСКОНЕЧНАЯ СИМУЛЯЦИЯ ЗАПУЩЕНА ===");
  System.out.println(PAUSE + ". Пауза");
  System.out.println(QUIT + ". Выход");
}

private void handleRunningMenu(int choice) {
  switch (choice) {
    case PAUSE -> simulation.pauseSimulation();
    case QUIT -> exit();
    default -> System.out.println("Неверный выбор");
  }
}
```
*Фаулер, "Рефакторинг", гл.8, "Замена магического числа символической константой"*  
*refactoring.guru "Замена магического числа символьной константой"*  

**6. class Coordinates**

+ 👍 Нет ничего лишнего, это хорошо.

- Класс может быть преобразован в record без потери функционала.

**7. class GameMap**

- Нарушение конвенции кода, константы должны стоять первыми.

- Карта содержит в себе фиксированные размеры, поэтому становится неуниверсальной- сейчас нельзя создать игровые конфигурации с картами разных размеров
```java
public class GameMap {
  private final int MAX_X = 6;
  private final int MIN_X = 0;
  private final int MAX_Y = 6;
  private final int MIN_Y = 0;
  //...
}
```
Фиксированные размеры могут быть у шахматной доски(8x8) или игры крестики-нолики(3x3).  
Здесь размеры должны приниматься в конструктор, чтобы можно было создавать карту произвольных размеров.

- Никогда не возвращай null
```java
private final HashMap<Coordinates, Entity> gameMap = new HashMap<>();

public Entity getEntity(Coordinates coordinates) {
  return gameMap.get(coordinates);  <-- может вернуть null
}
```
Возврат null повышает риск возникновения NullPointerException в программе.  
*Мартин, "Чистый код", гл.7.7-8*  
*Ютуб, Немчинский "Почему нельзя возвращать NULL?"*

- При всех операциях с участием координаты(добавить, выдать, удалить и т.д.), нужно проверять координату на корректность. 

Если координата некорректна(находится вне пределов карты), нужно бросать исключение.
Сейчас, если у карты спросить, свободна ли ячейка с координатой (+100500, -100500), то карта скажет, что свободна
```java
public boolean isCellEmpty(Coordinates coordinates) {
  return !gameMap.containsKey(coordinates);
}
```
А правильный ответ- ячейки с такой координатой в карте нет вообще.

Ближайшая аналогия- стандартные хранилища типа List и массива. 
При попытке обратитья к ним по несуществующему индексу, бросается исключение.

- Метод совершения хода в карте- нарушение SRP.
Теперь можно вызвать из карты метод хода и телепортировать зайца из одного края в другой минуя все правила игровой логистики
```java
Карта карта = new Карта();
карта.addEntity(new Координата(0, 0), new Заяц());
карта.moveEntity(new Координата(0, 0), new Координата (99, 99));

/* class Карта */
public void moveEntity(Координата from, Координата to){
  Entity entity = getEntity(from);
  removeEntity(from);
  putEntity(to, entity);
}
```
Должна ли карта учитывать логистику зайцев? Если да, то как?  
Единственно правильный ответ- карта вообще не должна иметь в себе метод телепортации
```java
public void moveEntity(Coordinates from, Coordinates to) {
  //6 строк кода  
}
```

+ 👍 В целом, с точки зрения SRP класс хороший.

**8. class Path**

+ 👍 Хорошая сигнатура метода поиска- сразу понятно, как пользоваться классом 
```java
public List<Coordinates> findPath(Coordinates start, Class<?> tClass, GameMap gameMap)
```

- Мапа координата-координата обычно при реализации поиска используется как эрзац-заменитель связного списка
```java
HashMap<Coordinates, Coordinates> cameFrom = new HashMap<>();
```
Вместо мапы координата-координата сделай класс связного списка, алгоритм тогда будет выглядеть понятнее.
Например, так:
```java
class Node {
  private Coordinates coordinates; 
  private Node parent;
  //...
  public Coordinates getCoordinates() {...}
  public Node getParent() {...}
}
```

- Создавай вспомогательные методы, делай программу более простой и понятной
```java
if (x <= gameMap.getMAX_X() && x >= gameMap.getMIN_X() && y <= gameMap.getMAX_Y() && y >= gameMap.getMIN_Y()) {...}

//ПРАВИЛЬНО:
if (isInsideGameMap(x, y)) {...}

private boolean isInsideGameMap(int x, int y) {
  return x <= gameMap.getMAX_X() && x >= gameMap.getMIN_X() && y <= gameMap.getMAX_Y() && y >= gameMap.getMIN_Y();
}
```

- Здесь напрашивается такой алгоритм
```java 
public List<Coordinates> getNeighbors(Coordinates coord) {
  List<Coordinates> neighbors = new ArrayList<>();
  neighbors.add(new Coordinates(coord.getX() - 1, coord.getY()));
  neighbors.add(new Coordinates(coord.getX() + 1, coord.getY()));
  neighbors.add(new Coordinates(coord.getX(), coord.getY() - 1));
  neighbors.add(new Coordinates(coord.getX(), coord.getY() + 1));
  return neighbors;
}

//ЛУЧШЕ:
private final static List<Coordinates> SHIFT_COORDINATES = List.of(new Coordinates(0, 1), ....);

public List<Coordinates> getNeighbors(Coordinates coord) {
  List<Coordinates> neighbors = new ArrayList<>();

  for(Coordinates shift : SHIFT_COORDINATES) {
    Coordinates neighbor = sum(coord, shift);
    neighbors.add(neighbor);
  }
  return neighbors;
}
```

**9. abstract class Entity**

- Содержит координату. Но координата нужна только тому существу, которое ходит. 
Поэтому entities должны хранить координату только начиная с уровня `Creature`.

- Нарушение SRP, зависимость модели от представления- существо хранит спрайт с собственным изображением
```java
private String emoji;
```
Модель(а это модель) не должна зависеть от представления и знать, как ее будут показывать юзеру.  
Потому что в разных средах(консоль, Swing, Android) одна и та же модель может быть показана разными способами- пиксельной картинкой, анимацией etc.  
Спрайты всех существ должны храниться в классе, который распечатывает карту.

**10. abstract class Creature extends Entity**

Это значение не изменяется во время работы программы, оно должно быть final
```java
private int speed;

//ПРАВИЛЬНО:
private final int speed;
```

**11. class Herbivore/Predator extends Creature**

- Нарушение SRP, чужая ответственность, зависимость модели от представления.

Модель(а это модель) не должна ничего печатать в консоль
```java
public void makeMove(GameMap gameMap, Path path) {
  System.out.println("\uD83D\uDC3A на " + oldCoords + " атаковал \uD83D\uDC30 на " + herbivoreCoords);
  //...
  System.out.println("\uD83D\uDC30 убит на координатах " + herbivoreCoords);
  //...
  System.out.println("\uD83D\uDC3A переместился с " + oldCoords + " на " + newCoords);
}
```
Иначе модель перестает быть универсальной и становится заточенной под конкретную среду и конкретное представление себя в этой среде- в данном случае, консоль.
В других средах нужно будет менять код в этом классе, чтобы вывод осуществлялся по правилам этой среды, то есть, это лишняя причина для изменения класса.
В других средах(напр. Андроид) эту модель нельзя будет использовать- она там просто не скомпилируется. 
Другое представление для модели(напр. если одну и ту же модель нужно в программе показвать по-разному) нельзя будет сделать, или придется делать через костыль.

Если модель должна что-то сообщить миру, она может это сделать через паттерн [CallBack](https://t.me/zhukovsd_it_chat/53243/139594)

Вот к чему приводит зависимость модели от представления, карта рисуется в виндовый интерфейс, 
а модель [пишет в консоль](https://github.com/raketareview/simulation_review/blob/master/content/rev-sim064-davidtagirov-DavidTagirov.md)  


Если модель должна что-то сообщить миру, она может это сделать через паттерн CallBack.  
[CallBack луноход](https://t.me/zhukovsd_it_chat/53243/139594)  
[CallBack разрушение](https://t.me/zhukovsd_it_chat/53243/184749)

- Нарушение DRY, дублирование логики в `makeMove(...)`.

Этот код общий в двух классах:
```java
public void makeMove(GameMap gameMap, Path path) {
  List<Coordinates> pathCoordinates = path.findPath(this.getCoordinates(), Grass.class, gameMap);
  if (pathCoordinates == null || pathCoordinates.isEmpty()) {
    return;
  }

  Coordinates oldCoords = this.getCoordinates();
  //...
}
```

Замени условие на `path.findPath(..., getTarget(), ...)` и вынеси общий код во вспомогательный метод предка.

- Магия
```java 
public Predator(Coordinates coordinates) {
  super(coordinates, "\uD83D\uDC3A", 2, 20);
  this.damage = 5;
}

//ПРАВИЛЬНО:
private final static String SPRITE = "\uD83D\uDC3A";
private final static int SPEED = 2;
private final static int HP = 20;
private final static int DAMAGE = 5;

public Predator(Coordinates coordinates) {
  super(coordinates, SPRITE, SPEED, HP);
  this.damage = DAMAGE;
}
```

**12. abstract class Action**

- Нарушение OCP и принципов создания иерархии классов.

Метод поиска случайной координаты нужен не всем потомкам Action.  
Классу `MoveCreaturesAction` этот метод не нужен
```java
public Coordinates getRandomCoordinates(GameMap map)
```
Функционал должен появляться только на тех уровнях иерархии, где этот функционал нужен.

Это такая же ошибка, как в классе Entity. Он там хранит координату на уровне базового `Entity`, а должен хранить начиная с `Creature`.

**13. Общие замечания по Action'ам**

Идея Action'ов состоит в том, что нужно создать семейство родственных классов, объединенных общим интерфейсом.  
Каждый из этих классов должен делать что-то свое с картой: одна акция должна заселять карту существами, другая делать ходы и т.д.  
Action'ы, изложенные в ТЗ, это вариант реализации паттерна Command. 

Должен быть общий класс/интерфейс Action и его наследники.  
В каждом экшене должен быть **только один публичный метод**(не публичных может быть сколько угодно).  
Это вариация паттерна Command- экшены должны быть родственны и одинаково использоваться через полиморфизм. 

Сейчас в экшенах минимум два публичных метода: `perform(GameMap map)` и `getRandomCoordinates(GameMap map)`.  
В некоторых имеется еще и третий метод. Например, в `InitGrassAction` есть `public static int getMaxGrass()`.

Нужно переделать экшены так, чтобы в них был только один публичный метод и экшены использовались в стиле паттерна Command.

**14. Группа классов class InitGrass/Tree/...Action extends Action**

Множество классов, которые делают одно то же- заселяют карту определенным видом существ.  
Код в них дублирует друг друга, это нарушает чеклист ТЗ:
```
Проблемы и ошибки в коде:
Иерархия классов Action’s - Дублирование кода
```

Вместо отдельного класса на каждый вид существ, нужно сделать универсальный класс, который будет принимать в конструктор количество создаваемых существ и способ их создания.
Способ создания можно передавать через стандартный интерфейс Function
```java
public class SpawnAction extends Action {
  private final Function<Coordinates, Entity> entityCreator;
  private final int count;

  public SpawnAction(Function<Coordinates, Entity> entityCreator, int count) {
    this.entityCreator = entityCreator;
    this.amount = count;
  }

  @Override
  public void execute(Карта карта) {
    for (int i = 0; i < count; i++) {
      Coordinates coordinates = getRandomEmptyCoordinates(карта);
      Entity entity = entityCreator.apply(coordinates);
      карта.addEntity(entity, coordinates);
    }
  }

  private Coordinates getRandomEmptyCoordinates(Карта карта) {
    //находит и возвращает случайную пустую координату
  }
}
```
Использование:
```java
SpawnAction траваSpawnAction = new SpawnAction((c) -> new Трава(c), КОЛИЧЕСТВО_ТРАВЫ);
SpawnAction каменьSpawnAction = new SpawnAction((c) -> new Камень(c), КОЛИЧЕСТВО_КАМНЕЙ);
траваSpawnAction.execute(карта); 
каменьSpawnAction.execute(карта); 
```

**15. class SpawnGrassAction/SpawnHerbivoreAction extends Action**

Такое же дублирование, как и в предыдущем случае.

**16. class MoveCreaturesAction extends Action**

+ 👍 Тут все ок.

**17. class Renderer**

Спрайты существ должны храниться здесь, а не браться из самих существ.

**18. class Menu**

Меню в процедурном стиле. Для простой программы- почему бы и нет.  
Про меню в ООП стиле я писал [ТУТ](https://t.me/zhukovsd_it_chat/53243/114908).

**19. class Main**, содержит точку входа main

👍 Только создает и запускает Симуляцию, в данном случае, через Меню. Это хорошо.

## ВЫВОД

Замечаний, особенно важных, меньше, чем обычно.  
Разобраться с массированным дублированием кода в экшенах.  

n.122(271)  
#ревью #симуляция 