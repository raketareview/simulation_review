https://github.com/Frytes/Simulation  
[Frytes]

Есть над чем поработать.

## НЕДОСТАТКИ РЕАЛИЗАЦИИ

1. Нет паузы/пуск во время работы. Хотя это требование есть в ТЗ.

## ХОРОШО

+ 👍 Спрайты существ не хранятся в самих существах
+ 👍 Алгоритм поиска A*

## ЗАМЕЧАНИЯ

**1. Нейминг**

Есть стандартный интерфейс `Map`, не должно быть кастомного пакета с таким же названием
```java
package frytes.simulation.map;

//ПРАВИЛЬНО:
package frytes.simulation.gamemap;
```

- Избыточно. Мы и так понимаем, что метод поиска в классе поиска пути ищет именно путь, а не что-то иное
```java
public class PathFinder {
  public List<Coordinates> findPath(...)
  //...
}

//ПРАВИЛЬНО:
public class PathFinder {
  public List<Coordinates> find(...)
  //...
}
```

- Название класса должно быть существительным. "Render" это действие, а "Renderer"- тот, кто выполняет это действие
```java
class RenderMap

//ПРАВИЛЬНО:
class GameMapRenderer
```

- Геттер должен называться так же, как возвращаемое им поле + префикс "get"
```java
private final HashMap<Coordinates, Entity> worldMap = new HashMap<>();

public java.util.Map<Coordinates, Entity> getEntities() {
  return worldMap;
}

//ПРАВИЛЬНО:
private final HashMap<Coordinates, Entity> entities = new HashMap<>();

public java.util.Map<Coordinates, Entity> getEntities() {
  return entities;
}
```
*Oracle Java code conventions, part."Naming conventions"*  
*Мартин, "Чистый код", гл.2*  
*Ютуб, Немчинский "Как называть переменные, методы и классы?"*

**1.1 Нарушение конвенции кода**

В любой ситуации выделяй тело блока скобочками, даже если тело состоит из одной строки.  
Исключение- метод equals(), там можно после if не выделять блоки скобочками
```java
if (!Map.isValidCoordinates(neighborCoord)) continue;
if (!isWalkable) continue;

//ПРАВИЛЬНО:
if (!Map.isValidCoordinates(neighborCoord)) {
  continue;
}
if (!isWalkable) {
  continue;
}
```
*"Oracle Java code conventions"*  

**2. Используй классы через их интерфейсы**
```java
HashMap<Coordinates, Entity> worldMap = new HashMap<>();
HashMap<Coordinates, Coordinates> cameFrom = new HashMap<>();

//ПРАВИЛЬНО:
Map<Coordinates, Entity> worldMap = new HashMap<>();
Map<Coordinates, Coordinates> cameFrom = new HashMap<>();
```
Общее правило: ArrayList нужно использовать через List, HashMap- через Map и т.д.  
Это позволяет пользоваться преимуществами полиморфизма.

Да, бывают ситуации, когда, например, с LinkedList нужно работать именно как с LinkedList, а не с List. 
Но это уже нюансы.  
*"Java. Эффективное программирование", изд.3, гл.9.8*
```
"Если вы выработаете привычку использовать в качестве типов интерфейсы, ваша программа будет гораздо более гибкой" - Блох.
```

**3. Форматирование строк**

Если нужно печатать или создавать строку с более, чем одним подстановочным значением или значение вставляется внутрь сообщения, используй форматирование- тогда сразу будет виден весь шаблон
```java
System.err.println("Ошибка создания " + entityClass.getSimpleName() +
    " на (" + coordinates.x() + "," + coordinates.y() + "): " + e.getMessage());

//ПРАВИЛЬНО:
System.err.printf("Ошибка создания %s на (%d,%d): %s  %n", entityClass.getSimpleName(), coordinates.x(), coordinates.y(), e.getMessage());
```

**4. record Coordinates(int x, int y)**

+ 👍 Record для координаты- идеально.

- Не используй статические импорты- становится непонятно, что используемый в классе метод или константа не его собственные, а чьи-то чужие
```java
int x = (random.nextInt(MAP_WIDTH));
int y = (random.nextInt(MAP_HEIGHT));

//ПРАВИЛЬНО:
int x = (random.nextInt(Simulation.MAP_WIDTH));
int y = (random.nextInt(Simulation.MAP_HEIGHT));
```
*"ЧК", гл.17, G18*

- Нарушение SRP.

Это должен быть простейший класс, который должен содержать только два числа, обозначающие положение точки на плоскости: x,y или row,column.  
Здесь же класс отвечает не только за хранение координаты, но выполняет другие действия, тем самым нарушая SRP: создает случайную координату
```java
public static Coordinates getRandomCoordinated()
```
Для идентификации точки на плоскости, этому классу не нужно находить случайную координату.  
А значит, это действие принадлежит чужой ответственности. 

**6. class Map**

- Конфликт нейминга предметной области и среды разработки.

Следовать ТЗ это хорошо.  
Называть свои пользовательские классы так же, как стандартные классы и интерфейсы- плохо.  

Второе приводит к постоянной путанице в коде. `Мар` это название стандартного интерфейса.  
Поэтому меньшим злом является нарушить ТЗ и дать другое название прикладному классу: Board, GameMap или что-то подобное.

- Карта создается с фиксированными размерами, которые берутся из констант.  
Поэтому карта становится неуниверсальной- нельзя создать игровые конфигурации с картами разных размеров
```java
public static boolean isValidCoordinates(Coordinates coordinates) {
  return coordinates.x() >= 0
    && coordinates.y() >= 0
    && coordinates.x() < MAP_WIDTH  <-- РАЗМЕР ИЗ КОНСТАНТЫ
    && coordinates.y() < MAP_HEIGHT; <-- РАЗМЕР ИЗ КОНСТАНТЫ
  }
```
Фиксированные размеры могут быть у шахматной доски(8x8) или игры крестики-нолики(3x3).  
То есть в тех играх, где размер игрового поля жестко фиксирован правилами.  
Здесь размеры должны приниматься в конструктор, чтобы можно было создавать карту произвольных размеров.

- Никогда не возвращай null 
```java
private final HashMap<Coordinates, Entity> worldMap = new HashMap<>();

public Entity getEntity(Coordinates coordinates) {
  return worldMap.get(coordinates);  <-- может вернуть null
}
```
Возврат null повышает риск возникновения NullPointerException в программе.  
*Мартин, "Чистый код", гл.7.7-8*  
*Ютуб, Немчинский "Почему нельзя возвращать NULL?"*

- Нарушение инкапсуляции, класс предоставляет клиентскому коду подробности внутреннего устройства и доступ к нему
```java
private final HashMap<Coordinates, Entity> worldMap = new HashMap<>();

public java.util.Map<Coordinates, Entity> getEntities() {
  return worldMap;  
}
```
В данный момент клиентский код может выполнять любые операции с HashMap Карты напрямую, игнорируя допустимые картой способы. 
Например так
```java
Карта карта = new Карта();
<заселить карту существами>
карта.getEntities().clear(); //геноцид- удаление из карты всех существ, минуя дозволенные картой механизмы
```
Из-за этого класс является гибридом со всеми вытекающими последствиями: *"Чистый код", гл.6*

Тут есть два варианта, как сделать правильно.  
Либо вернуть не оригинал мапы, а ее копию: 
```java
public Map<Coordinates, Entity> getEntities() {
  return new HashMap(worldMap);
}
```
Либо возвращать существ персонально:
```java
public Entity get(Coordinates coordinates)
public Coordinates getCoordinates(Entity entity)
```
Лично мне больше нравится второй вариант, когда существ возвращают персонально.

- При всех операциях с участием координаты(добавить, выдать, удалить и т.д.), нужно проверять координату на корректность.

Если координата некорректна(находится вне пределов карты), нужно бросать исключение.  
Потому что сейчас можно поместить существо на координату, которая будет выходить за пределы карты:
```java
  public void setEntity(Coordinates coordinates, Entity entity) {  <-- можно вставить координату (+100500,-100500)
    entity.setCoordinates(coordinates);
    worldMap.put(coordinates, entity);
  }
```
Ближайшая аналогия- стандартные хранилища типа List и массива.  
При попытке обратитья к ним по несуществующему индексу, бросается исключение.

+ 👍 Хороший метод
```java
public <T extends Entity> List<T> getEntitiesByType(Class<T> type)
```

- Нарушение SRP, чужая ответственность, зависимость модели от представления.  
Модель(а это модель) не должна ничего печатать в консоль
```java
  System.out.println("Нельзя передвинуть. Место занято другим");
```
Иначе модель перестает быть универсальной и становится заточенной под конкретную среду и конкретное представление себя в этой среде- в данном случае, консоль.
В других средах(напр. Андроид) модель нельзя будет использовать или придется это делать через костыль.

В данном случае, если место занято другим, нужно кинуть исключение.  
И что с этим потом делать, пусть решает клиент карты.

- Метод совершения хода в карте- нарушение SRP.  
Теперь можно вызвать из карты метод хода и телепортировать зайца из одного края в другой минуя все правила игровой логистики
```java
Карта карта = new Карта(100, 100);
Заяц заяц = new Заяц();
карта.setEntity(new Координата(0, 0), заяц);
карта.moveEntity(new Координата(99, 99), заяц);

/* class Карта */
public void moveEntity(Coordinates coordinates, Entity entity) {...}
```
Должна ли карта учитывать логистику зайцев? Если да, то как?  
Единственно правильный ответ- карта вообще не должна иметь в себе метод телепортации.

**7. class PathFinder**

- В методе поиска сбивает с толку аргумент `Map map`, кажется, что это тип стандартной мапы
```java
public List<Coordinates> findPath(Map map, ...)
```
Это одно из последствий неправильного нейминга карты игры.    
Вот так было бы лучше:
```java
public List<Coordinates> findPath(GameMap gameMap, ...)
```

- Из сигнатуры неясно, как пользоваться поиском
```java
public List<Coordinates> findPath(Coordinates start, Coordinates target, Map map, Class<? extends Entity> targetType)
```
Если поиск выполняется по алгоритму A*, то этот алгоритм должен искать путь между двумя известными точками.  
И тогда сигнатура должна быть такой:
```java
public List<Coordinates> find(GameMap gameMap, Coordinates from, Coordinates to)
```
Если поиск выполняется по алгоритму BFS, то этот алгоритм должен искать путь между точкой старта и ближайшей найденной точкой с едой.  
И тогда сигнатура должна быть такой:
```java
public List<Coordinates> find(GameMap gameMap, Coordinates from,  Class<? extends Entity> target)
```
Можно сделать поиск A* с сигнатурой, как у BFS- это полезно, если есть желание использовать их через общий интерфейс.  
Например:
```java
public interface PathFinder {
  List<Coordinates> find(GameMap gameMap, Coordinates from, Class<? extends Entity> target)  
}

public class BfsPathFinder implements PathFinder{...}

public class AstarPathFinder implements PathFinder{
  @Override
  public List<Coordinates> find(GameMap gameMap, Coordinates from, Class<? extends Entity> target) {
    Coordinates to = findКоординатаЦели(gameMap, from, target);
    //найти и вернуть путь от from до to 
  }

  private Coordinates findКоординатаЦели(...) {...}
}
```

+ 👍 Хороший вспомогательный класс для процесса поиска
```java
private record Node(Coordinates coordinates, int priority) implements Comparable<Node>
```

**8. abstract class Entity**

- Не используй Lombok в этом проекте.  

Этот проект нужен для отработки умений писать в коре.  
Поэтому все методы тут ты должен писать осмысленно, а не через магию ломбоковских аннотаций
```java
@Setter
@Getter
@AllArgsConstructor
public abstract class Entity
```

- Содержит координату. Но координата нужна только тому существу, которое ходит.  
Поэтому entities должны хранить координату только начиная с уровня `Creature`
```java
Coordinates coordinates;
```

+ Вот такими должны быть идеальный ентити и его простые неходячие наследники:
```java
public abstract class Entity {
}

public class Rock extends Entity {
}
```

**9. class House extends Entity**

Название класса противоречит его toString() 
```java
public class House extends Entity {
  public House(Coordinates coordinates) {
    super(coordinates);
  }

  @Override
  public String toString() {
    return "Rock";
  }
}
```

**10. abstract class Creature extends Entity implements Damageable**

- Всегда явно указывай область видимости и `final`, если значения поля не меняется.  
Да, при использовании ломбока- тоже
```java
@Getter
@Setter
public abstract class Creature extends Entity implements Damageable {
  Integer moveSpeed;  <-- УКАЖИ ЯВНО ОБЛАСТЬ ВИДИМОСТИ
  int hp;  <-- УКАЖИ ЯВНО ОБЛАСТЬ ВИДИМОСТИ
  //...
}
```

- При прочих равных, нужно использовать примитивный тип, а не класс-обертку
```java
Integer moveSpeed;

//ПРАВИЛЬНО:
int moveSpeed;
```
Для применения класса-обертки над примитивным типом данных, должна быть какая-то причина.  
Здесь использование обертки не дает ничего большего по сравнению с примитивом.  
*Блох "Java. Эффективное программирование", изд.3, гл.9.5*
```
"Предпочитайте примитивные типы упакованным примитивным типам" - Блох.
```

- Тип цели и атаку тоже нужно хранить на уровне Creature как поле со скоростью, например.  
Иначе непонятно, почему скорость это поле, а еда определяется через вызов метода
```java
Integer moveSpeed;
int hp;

protected abstract int getAttackDamage();
protected abstract Class<? extends Entity> getTargetType();

//ПРАВИЛЬНО:
private final int speed;
private final int attackDamage;
private final Class<? extends Entity> food;
private int hp;
```

- Магические числа объясняй через константы
```java
this.hp += 5;
```

**11. class Herbivore extends Creature**

- Нарушение дизайна ООП в наследовании. 

При создании экземпляра класса, значения обязательных полей класса должны устанавливаться через конструктор, а не сетиться
```java
public class Herbivore extends Creature {
  public Herbivore(Coordinates coordinates, int hp, int moveSpeed) {
    super(coordinates);
    this.hp = hp;
    this.moveSpeed = moveSpeed;
  }
  //...
}

//ПРАВИЛЬНО:
public class Herbivore extends Creature {
  private static final Class<? extends Entity> FOOD = Grass.class;
  private static final int ATTACK_DAMAGE = ...

  public Herbivore(Coordinates coordinates, int hp, int speed) {
    super(coordinates, hp, speed, FOOD, ATTACK_DAMAGE);
  }
  //...
}
```
*Эккель "Философия Java", гл.5, "Конструктор гарантирует инициализацию"* 

- Нарушение LSP, неправильное распределение данных в иерархии наследования.

Этот метод возвращает ноль. Это чисто заглушка, бесполезный метод-костыль
```java
@Override
protected int getAttackDamage() {
  return 0;
}
```
Фактически, Заяц, как и Волк, делает кусь. Но кусь зайца из-за нулевой атаки безвреден.  
Немедленно вспоминается история про боевых свиней:  
![свиньи-убийцы](https://github.com/raketareview/simulation_review/blob/master/content/resources/rev-sim138/pigs-story.png) 

Эта заглушка находится здесь потому что в Creature есть этот абстрактный метод и все наследники, в том числе Травоядное, должны его переопределить.

Если только Predator должен уметь реально атаковать, то уровень атаки должен храниться только в Predator'e.  
И выполнение действий атаки- там же.  
А в Creature и Herbivore этого быть не должно.

Но тогда алгоритм ходьбы Herbivore и Predator будет отличаться?

Да, будет.  
Общий код ходьбы нужно будет оставить в креатуре, а различия перенести в переопределенные методы `makeMove(...)` классов Herbivore и Predator.  
Да, сложнее. А шо делать.

- Неправильный toString(). 

toString() должен быть стандартным(как делает IDE по Alt+Ins) и использоваться только для отладки.  
toString() должен содержать значение всех значимых полей класса.  
toString() не должен использоваться для представления, за исключением предельно простых классов, вроде PhoneNumber
```java
@Override
public String toString() {
  return "Herbivore";
}

//ПРАВИЛЬНО:
@Override
public String toString() {
  return "Herbivore{" +
    "moveSpeed=" + moveSpeed +
    ", hp=" + hp +
    ", turnToMove=" + turnToMove +
    '}';
}
```
*Блох, "Java. Эффективное программирование", изд.3, гл.3.3*

**12. abstract class Actions**

- Должен называться в единственном числе
```java
abstract class Actions

//ПРАВИЛЬНО:
abstract class Action
```

- Неправильный базовый `Action`, божественный класс. 

Сейчас `Actions` у тебя это божественный класс, который в себе содержит методы, которые нужны не всем своим наследникам
```java
public abstract class Actions {
  protected final Map map;

  public Actions(Map map) {
    this.map = map;
  }

  public abstract void perform();
  protected void generateEntity(Class<? extends Entity> entityClass, int count)  {...}
  protected void createPredator(int count)  {...}
  protected void createHerbivore(int count) {...}
}
```
Например, для экшена совершения хода не нужны методы создания существ.  
Но он их получает по наследству от базового Actions.

Базовый экшен должен быть примерно такой:
```java
public abstract class Action {
  protected final GameMap gameMap;

  public Action(GameMap gameMap) {
    this.gameMap = gameMap;
  }
  public abstract void perform();
}
```
Еще лучше- такой:
```java
public interface Action {
  void perform(GameMap gameMap);
}
```

- Магические числа
```java
if (timesCount == 99)
```

- Зависимость модели от представления.

Экшены это тоже модели- они содержат бизнес логику, которая не должна зависеть от представления.  
Поэтому экшены не должны ничего печатать в UI, особенно- напрямую в консоль
```java
System.out.println("Не удалось найти пустое место, Все ячейки заняты");
```
Если не удалось найти пустое место, бросай исключение.

- Нарушение DRY, дублирование кода.

Методы делают одно и то же- помещают в карту существ определенного типа
```java
protected void createPredator(int count) 
protected void createHerbivore(int count)
```
Сделай один универсальный метод. Например:
```java
spawn(gameMap, ()-> new Заяц(), КОЛИЧЕСТВО_ЗАЙЦЕВ);
spawn(gameMap, ()-> new Волк(), КОЛИЧЕСТВО_ВОЛКОВ);
//...


protected void spawn(GameMap gameMap, Supplier<Entity> entitySupplier, int count) {
  for(....) {
    Coordinates coordinates  = getRandomEmptyCoordinates(gameMap);
    Entity entity = entitySupplier.get();
    gameMap.setEntity(coordinates, entity);
  }  
}
```

- Если сделать унверсальный метод, то отпадет необходимость и в этом ужасе:
```java
protected void generateEntity(Class<? extends Entity> entityClass, int count) {
  //ужас
  //ужас
  //ужас
}
```

**13. class TurnActions extends Actions**

- В единственном числе!
```java
class TurnActions extends Actions

//ПРАВИЛЬНО:
class TurnAction extends Action
```

- Делай ходы на уровне Creature's 
```java
List<Herbivore> herbivores = map.getEntitiesByType(Herbivore.class);
List<Predator> predators = map.getEntitiesByType(Predator.class);

for (Herbivore h : herbivores){
  h.makeMove(map);
}
for (Predator p : predators){
  p.makeMove(map);
}

//ПРАВИЛЬНО:
List<Creature> creatures = gameMap.getEntitiesByType(Creature.class);
for(Creature creature : creatures) {
  creature.makeMove(gameMap);
}
```

**14. class RenderMap**

+ 👍 Спрайты существ хранятся здесь, а не берутся из самих существ, это хорошо.

- Если пришло неизвестное существо, то нужно бросать исключение, а не рисовать дефолтный спрайт. 

Потому что при добавлении нового существа в проект, можно забыть добавить его спрайт в распечатку.  
И тогда существо будет жить на карте своей жизнью, но это будет не сразу выявлено- вместо этого существа будет распечатываться дефолтный спрайт
```java
if (sprite == null) sprite = " ❓ ";
```

**15. class Simulation**

- Нарушение DI. Класс не должен инициализировать сам себя.  
В данном случае класс инициализирует сам себя тем, что сам создает карту, а не принимает ее в конструктор
```java
public Simulation() {
  this.map = new Map();
  //...
}

//ПРАВИЛЬНО:
public Simulation(GameMap gameMap) {
  this.gameMap = gameMap;
  //...
}
```
Таким образом нельзя создать несколько игровых конфигураций с разными картами.  
*Мартин, "ЧК", гл.11, "Отделение конструирования системы от ее использования"*

- Нарушение конвенции кода, константы должны стоять выше остальных полей.

- Action'ы объявляются индивидуально
```java
private final Actions turnActions;
Actions initActions = new InitActions(map);
```

На самом деле экшены нужно объявлять не индивидуально, а группой и дальше использовать через полиморфизм.  
Примерно так:
```java
private final List<Action> initActions = List.of(new UnoAction());
private final List<Action> turnActions = List.of(new DosAction(), new TresAction(), new MoveAction());

public Simulation(...) {
  //...
  executeActions(initActions);  //выполнение экшенов на старте
}

public void nextTurn() {
  //...
  executeActions(turnActions);  //выполнение экшенов на каждом ходе
}

private void executeActions(List<Action> actions) {
  for(Action a : actions) {
    a.execute(карта);
  }
}
```
Даже если сейчас есть только один init-экшен и один turn-экшен, все равно их нужно хранить группой- например, в листе.  
Это позволит при необходимости быстро добавить в эти группы новые экшены, которые так же будут использоваться через полиморфизм.

- Нарушение конвенции кода.  
Метод-точка входа `main()` должен стоять первым методом.  
Здесь он стоит последним.

- Создание и запуск системы делай в отдельном классе.  
В ООП программе точка входа `main()` должна находиться в отдельном классе.  
*Мартин, "ЧК", гл.11, "Отделение конструирования системы от ее использования"*

- Нарушение ТЗ, нет указанных для этого класса методов 
```java
Simulation #
Главный класс приложения, включает в себя:
...
pauseSimulation() - приостановить бесконечный цикл симуляции и рендеринга
```

## ВЫВОД

Убрать Ломбок, для него еще рано.

Доделать программу согласно ТЗ- сделать паузу/пуск во время работы программы.  
Это важно для отработки азов работы с многопоточностью.

Не видно понимания ООП на базовом уровне: зачем нужны геттеры, сеттеры, конструкторы, инкапсуляция.  
Какие поля на каких уровнях иерархии нужно располагать и почему- наследование и полиморфизм.  
Разберись с этими темами.

Для лучшего понимания декомпозиции ООП посмотреть ролики Сергея про шахматы.  
Посмотреть ролики Немчинского про SOLID.

n.138(298)  
#ревью #симуляция 