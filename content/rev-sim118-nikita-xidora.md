https://github.com/NikitaPopovsky/Simulation  
[Никита]

Есть очень много, над чем нужно вдумчиво поработать.

## НЕДОСТАТКИ РЕАЛИЗАЦИИ

1. Карта распечатывается неровно, нужно подобрать спрайты одинаковой ширины  
![pic](https://github.com/raketareview/simulation_review/blob/master/content/resources/rev-sim118/img0.png) 

## ХОРОШО

+ 👍 Спрайты существ не хранятся в самих существах
+ 👍 Координаты существ не хранятся в самих существах(мне так больше нравится)
+ 👍 Реализовано пауза/пуск во время работы

## ЗАМЕЧАНИЯ

**1. Нейминг**

- Если метод называется "проверка", значит он должен что-то проверить и вернуть результат проверки в виде `boolean`.
Если метод `void`, он не должен называться "check"
```java
void checkValidationPosition(Set<Integer> positions, int parent)
```

- Некоторые названия здесь не соответствуют сущности, которую описывают. 
Например, координата- не координата, карта- не карта. 
Но об этом ниже.

- Если возвращаешь по value, то и возвращай по value
```java
Commands getByValue (String commandString)

//ПРАВИЛЬНО:
Commands getByValue (String value)
```

- Соблюдай единообразие, одной концепции- одно имя. Методы печати должны называться или "print" или "draw"
```java
public static void printMap(GameMap gameMap, int countStep) 
private static void drawPreliminaryData(int countStep)
```

*Oracle Java code conventions, part."Naming conventions"*  
*Мартин, "Чистый код", гл.2*  
*Ютуб, Немчинский "Как называть переменные, методы и классы?"*

**2. Если в блоке if есть return**(break, continue, throw, exit и т.д.), то else не пишется.
 
В этом случае неважно, будет else или нет, так как программа будет работать одинаково, а код без `else` будет выглядеть читабельней
```java
if (numPosition % widthMap == 0) {
  return widthMap;
} else {
  return numPosition % widthMap;
}

//ПРАВИЛЬНО:
if (numPosition % widthMap == 0) {
  return widthMap;
} 
return numPosition % widthMap;
```

**3. Комментарии**

В проекте много комментариев.
Иногда они констатируют очевидное- пересказывают название метода
```java
//Проверка валидации
private void checkValidationPosition(Set<Integer> positions, int parent) {...}
```

Иногда пересказывают названия классов
```java
//Класс представляет траву
public class Grass extends Entity 
```

Иногда призваны компенсировать плохой код и объяснить, что делает метод
```java
  //Когда мы нашли конечную позицию, нам нужно понять по каким клеткам мы сюда дошли
  //для этого перебираем коллекцию позиций с родителями (клетка с которой перешли)
  public Deque<Integer> positionsForNextStep() {...}
```
Когда в проекте много каментов, это плохо- пользы от них практически нет, они только забивают пространство и мешают читать код.  
В идеале, комментариев вообще не должно быть, код должен объяснять сам себя через правильный нейминг и лаконичный код.  
*Мартин, "ЧК", гл.4*


**4. Нарушение DRY**, магические буквы, числа, слова. Вводи константы. А если они есть- пользуйся
```java
public enum Commands {
  START("s"),
  PAUSE("p"),
  EXIT("e"),
  //...
}

public class Render {
  public static void showCommands() {
    System.out.println("Доступные команды : Старт симуляции - S, пауза - P, выход - E");
  }
}

//ПРАВИЛЬНО:
public class Render {
  public static void showCommands() {
    System.out.printf("Доступные команды : Старт симуляции - %s, пауза - %s, выход - %s  \n",
      Commands.START.getValue(),
      Commands.PAUSE.getValue(),
      Commands.EXIT.getValue()
    );
  }
}
```
*Фаулер, "Рефакторинг", гл.8, "Замена магического числа символической константой"*  
*refactoring.guru "Замена магического числа символьной константой"*  

**5. class Coordinate**

- Координата заявлена как DTO, но координата им не является
```java 
//ДТО хранящий позиции сущностей на карте
```
В DTO ключевое слово "Transfer"- эти объекты предназначены для передачи данных между слоями программы.  
Класс координаты похож на DTO своей простотой, но он принципиально отличается своим назначением.  
Это простой класс "общего назначения".

- Нарушение SRP

Это должен быть простейший класс, который должен содержать только два числа, обозначающие положение точки на плоскости: x,y или row,column.
Лучше даже, если этот класс будет record'ом.

Определение:
```
В элементарной геометрии координаты — величины, определяющие положение точки на плоскости и в пространстве.
```

Здесь же класс не только хранит положение точки в пространстве, но еще много других полей, которые к этому не имеют отношения:
```java
//имеют отношение к координате:
private int x;  
private int y;

//не имеют отношение к координате:
private final Entity entity;
private int position;
private boolean busy;
```
Это ячейка, ящик, бокс, но не координата.

Непоняток добавляет двойная система учета: по координате и по позиции.  
Или имеется ввиду, что учет коробок на складе ведется по позиции, а внутри коробки хранятся две разные штуки- 
координаты, которые характеризуют положение существа, и само существо?  

В общем, концепция класса неясна, но одно точно понятно- это не координатный класс.

- Непонятное поле. 

Без контекста использования класса в проекте непонятно, что означает флаг "занято"
```java
private boolean busy;

public boolean isBusy() {
  return busy;
}

public void setBusy(boolean busy) {
  this.busy = busy;
}
```

Занято, это когда в коробке есть существо? Тогда нужен не флаг, а метод:
```java
public boolean isBusy() {
  return entity != null;
}
```
Или это означает что-то иное?  
Например, статус коробки на складе, типа "занято, не трогать"?  
Тогда учет занятости нужно вести не в этом классе, а там, где занятость коробки что-то означает для происходящих бизнес процессов.  
Где ты комментарий, когда ты так нужен...

Если рассматривать этот класс как сферического коня в вакууме, то поле `boolean busy` в текущем виде ему вообще не нужно.

- Вот идеальная координата для этого проекта и для любого другого, который работает с двумерными координатами:
```java
public record Coordinate (int x, int y) {
  //да, тут совсем пусто  
}
```
Такой `record` полностью соотвествует сущности координаты: обеспечивает идентификацию точки в пространстве.  
При этом не делает ничего лишнего, то есть, не забирает на себя чужие ответственности.

**6. enum Constants**, хранит константы конфигурации приложения

- Константный класс в программе используется неправильно.

Другие классы здесь напрямую читают свои конфигурационные данные из класса констант.    
Это делает классы неуниверсальными и зависимыми от конфигурационного класса.  

Эти данные классы должны получать в свой конструктор.

Условные примеры:
```java
public final class Settings {
  public static final int ROOM_NUMBERS = 5;
}

//ПЛОХО:
public static void main(String[] args) {
  House house = new House();
  //oth code
}

public class House {
  private final int roomNumbers;

  public House() {
    this.roomNumbers = Settings.ROOM_NUMBERS;
  }

  public int getRoomNumbers() {
    return roomNumbers;
  }
}

//ХОРОШО:
public static void main(String[] args) {
  House house = new House(Settings.ROOM_NUMBERS);
  //oth code
}

public class House {
  private final int roomNumbers;

  public House(int roomNumbers) {
    this.roomNumbers = roomNumbers;
  }

  public int getRoomNumbers() {
    return roomNumbers;
  }
}
```
Про классы констант, конфигурации и их использование я писал [ТУТ](https://t.me/zhukovsd_it_chat/53243/176984)

**7. class GameMap**

- Нарушение конвенции кода. Конструктор должен стоять выше остальных методов.

- Если класс синглтон, то конструктор должен быть private
```java
public static GameMap getInstance() {...}
public GameMap() {...}
```

- Синглтон это антипаттерн.

Нет никаких оснований, чтобы этот класс был сиглетоном.  
Например, здесь это имеет практические последствия- нельзя создать несколько конфигураций программы с разными размерами карты.  
И дело тут не в том, планируется ли в этом проекте иметь несколько конфигураций с картами разного размера- 
нет никаких оснований для самого факта таких ограничний.

Синглтоны сейчас считаются антипаттерном. 
Для этого есть специальный термин- "Синглетонизм".

- Неправильное использование константного класса.
```java
public GameMap() {
  this.width = Constants.WIDTH_MAP.getValue();
  this.height = Constants.HEIGHT_MAP.getValue();
  this.coordinates = new HashSet<>();
}

//ПРАВИЛЬНО:
public GameMap(int width, int height) {
  this.width = width;
  this.height = height;
  this.coordinates = new HashSet<>();
}
```

- Вводящее в заблуждение название.

Сейчас для хранения существ используется такая коллекция:
```java
public GameMap() {
  private Set<Coordinate> coordinates;
  //...
}
```

Из этого невозможно понять, как в карте хранятся существа.

Только после изучения класса `Coordinate` становится понятно, что на самом деле это не координата, а нечто другое.  
Правильный нейминг не улучшил бы код, но повысил бы его понятность:
```java
public GameMap() {
  private Set<Box> boxes;
  //...
}
```
Теперь ясно, где хранятся существа.

- Нарушение инкапсуляции, класс предоставляет клиентскому коду подробности внутреннего устройства и доступ к нему
```java
private Set<Coordinate> coordinates;

public Set<Coordinate> getCoordinates() {
  return coordinates;
}
```
В данный момент клиентский код может выполнять любые операции с `Set` карты напрямую, игнорируя допустимые картой способы.  
Например так
```java
Карта карта = new Карта();
<заселить карту существами>
карта.getCoordinates().clear(); //уничтожить все ячейки в карте
```
Из-за этого класс является гибридом со всеми вытекающими последствиями: *"Чистый код", гл.6*

Про это есть замечание в чеклисте ТЗ проекта:
```
Недостаточная инкапсуляция - “протекающее” наружу внутреннее устройство класса Map (например, прямой доступ к коллекции ячеек) 
вместо набора методов с говорящими названиями (AddEntity, RemoveEntity, и так далее)
```

- Последствия неправильной архитектуры класса Coordinate.

Из-за плохой архитектуры класса координат(которые по сути являются не координатой, а коробкой), становится сложным выполнить простейшие действия.

Например, чтобы удалить существо из карты, нужно перебрать все коробки и если в какой-то из них будет сидеть существо, то эта коробка удаляется.  
Реализовано это так:
```java
public void removeEntity(int position) {
  Coordinate removeCoordinate = null;
  for (Coordinate coordinate : coordinates) {
    if (coordinate.getPosition() == position) {
      removeCoordinate = coordinate;
    }
  }

  if (removeCoordinate != null) {
    coordinates.remove(removeCoordinate);
  }
}
```
Есть и другие последствия, которые в проекте накапливаются как снежный ком.

**Поэтому всю архитектуру нужно переделать.** 

Класс координаты сделать простым, как я указал выше.  
Существа в карте хранить в такой коллекции:
```java
private final Map<Coordinates, Entity> entities = new HashMap<>();
```

Доступ клиенского кода извне к существам карты сделать согласно чеклиста ТЗ через методы `addEntity(...)`, `getEntity(...)` и так далее.

- Еще раз про сущность карты

Что такое карта, как сущность? Имеется ввиду, не конкретно эта реализация, а вообще.

Возьмем пример из реального мира: географическая карта.  
Имея две координаты, можно найти на карте какой-то объект. Например, Эльбрус.  
Возможна и обратная ситуация: найдя на карте Эльбрус, по его положению на карте можно найти его координаты.

Теперь посмотрим, что делает твоя карта.  
Она не находит объект по координате. Она не находит координату по объекту.  
Она просто по запросу выкатывает весь набор коробок- нате, разбирайтесь сами
```java
private Set<Coordinate> coordinates;

public Set<Coordinate> getCoordinates() {
  return coordinates;
}
```

И потом точно так же принимает этот набор обратно через сеттер
```java
public void setCoordinates(Set<Coordinate> coordinates) {
  this.coordinates = coordinates;
}
```

Таким образом, этот класс не являет собой воплощение сущности Карты, это что-то другое.  
Например, склад, где туда-сюда ездят фуры с коробками.  

Чтобы превратить этот склад в карту, нужно убрать из него методы `get/setCoordinates()` и вместо них создать такие методы:
```java
public void putEntity(Entity entity, Coordinate coordinate)
public Entity getEntity(Coordinate coordinate)
public Coordinate getCoordinate(Entity entity)
public void removeEntity(Coordinate coordinate)
public List<Entity> getAllEntities()
```
И вспомогательные методы: сказать ширину и высоту карты, занята ли координата.

**8. class BreadthFirstSearch**

- Находится в пакете "utils", но утилитой не является.

Утилитные классы должны быть `final` и иметь приватный конструктор.  
Не должно быть возможности унаследоваться от утилиты или сделать ее экземпляр.  
*Блох, "Java. Эффективное программирование", изд.3, гл.2.4*

- Нарушение конвенции кода. Публичные методы должны стоять выше вспомогательных приватных. 

- Сложные правила пользования классом

Сейчас в классе есть несколько публичных методов
```java
public class BreadthFirstSearch {
  //...
  public static Set<Integer> getNeighborsPosition(int curPosition) {...}
  public void addStaticEntityToVisited(Set<Integer> staticEntity) {...}
  public void searchEndPosition() {...}
  public Deque<Integer> positionsForNextStep() {...}
}
```
Зачем в классе поиска пути столько публичных методов и как ими должен пользоваться клиент- совершенно неясно.

Нет никаких оснований, чтобы класс был таким сложным для пользования.  
Сделай только один публичный метод, который предназначен для использования клиентом.

Публичный метод поиска должен просто искать путь от точки старта до точки, соответствующей заданным условиям.  
В данном случае- до точки, в которой находится существо нужного класса. 
Например, так:
```java
public List<Coordinate> findPath(GameMap gameMap, Coordinate start, Class<? extends Entity> target) {
  //ищет путь на карте от точки start
  //до точки, где находится существо нужного класса(напр. target == Grass.class)
}
```
Здесь предполагается, что класс `Coordinate` и `GameMap` сделаны так, как я рекомендовал выше.

*Вайсфельд "Объектно-ориентированный подход", гл.5, "Минимальный открытый интерфейс"*

- Нарушение SRP.

Название класса говорит о том, что это поиск пути по алгоритму BFS. Но кроме этого, здесь есть публичный метод, который создает набор соседних координат(позиций)
```java
public static Set<Integer> getNeighborsPosition(int curPosition)
```
Создание набора соседних координат это не поиск пути между двумя точками, тем более по алгоритму BFS.

Если для работы алгоритма BFS нужно искать соседние точки, то `getNeighborsPosition(...)` должен быть приватным.  
Если этот метод нужен для пользования клиента, то этот метод нужно вынести из этого класса в клиентский класс.  
Если этот метод нужен и для алгоритма `BreadthFirstSearch` и для клиента, значит, его нужно вынести в третий класс. 
Например, в `GameMapUtils`.

**9. abstract class Entity**

- Дженерики используй правильно. 

Здесь имеется ввиду, что в метод должен прийти не любой класс(например `GameMap.class`), а только класс-наследник Entity
```java
Entity createEntity(Class<?> entityClass)

//ПРАВИЛЬНО:
Entity createEntity(Class<? extends Entity> entityClass)
```

- Фабричный метод, который создает потомков
```java
public static Entity createEntity(Class<?> entityClass) {
  if (entityClass == Rock.class) {
    return new Rock();
  } else if (entityClass == Tree.class) {
    return new Tree();
  } else ...
}
```
С точки зрения OCP это сомнительно, но ок- такие прецеденты есть и в стандартной библиотеке.

Я бы советовал сделать чище и просто создать отдельную фабрику:
```java
public final class EntityFactory {
  //приватный конструктор
  public static Entity get(Class<? extends Entity> entityClass) {...}
}
```

Тогда Entity стал бы таким и это идеально:
```java
public abstract class Entity {
}
```

**10. Классы сущностей**

Все классы сущностей не содержат в себе никаких состояний и поведений:
```java
public abstract class Creature extends Entity {
}

public class Herbivore extends Creature {
}
```
Таким образом, все существа интерпритируется как сущности без своей воли, что-то типа знаков крестик и нолик в одноименной игре.

О правильности такой трактовки можно спорить, но я просто напомню ТЗ:
```
Creature #
Абстрактный класс, наследуется от Entity. Существо, имеет скорость (сколько клеток может пройти за 1 ход), 
количество HP. Имеет метод makeMove() - сделать ход.
```
И для всех наследников Creature в ТЗ тоже есть свои требования.

Пока ты не умеешь декомпозировать программы лучше Сергея, придерживайся его ТЗ.  
Вообще, писать программы по ТЗ это тоже необходимый навык, который нужно тренировать.  
На работе нельзя будет игнорировать требования ТЗ только потому что "я художник, я так вижу".

**11. abstract class Action**

- Идея `Action` здесь не осмыслена. 

Идея состоит в том, что нужно создать семейство родственных классов, объединенных общим интерфейсом.  
Каждый из этих классов должен делать что-то свое с картой: одна акция должна заселять карту существами, другая делать ходы и т.д.  
Action'ы, изложенные в ТЗ, это вариант реализации паттерна Command. 

Сейчас `Actions` у тебя это просто божественный класс, делающий разное и сборник функций, сделанный в стиле процедурного программирования
```java
public abstract class Action {

    //Выполняем действие
    public abstract void make ();

    //Добавляем первоначальные действия
    public static void addInitActions(Queue<Action> Actions)  {...}

    //Добавляем действия, повторяющиеся каждый ход
    public static void addTurnActions (Queue<Action> Actions)  {...}

    //Получаем текущие координаты
    protected Set<Coordinate> getCoordinates()  {...}

    //Устанавливаем новые координаты после действия
    protected void setCoordinates(Set<Coordinate> coordinates)  {...}

    //Получаем цели для действия
    //Пример: для травоядного - трава; для хищника - травоядное
    protected Set<Integer> getTargets(Entity entity, Set<Coordinate> coordinates) {...}
}
```

Смысл Action'ов состоит в том, что должен быть общий класс/интерфейс Action и его наследники.  
В каждом экшене должен быть только один публичный метод(не публичных может быть сколько угодно).  
Это вариация паттерна Command- экшены должны быть родственны и одинаково использоваться через полиморфизм. Примерно так
```java
interface Action{
 void execute(Карта карта);
}

class ХодитьAction реализует Action {
  void execute(Карта карта) {
    //обойти всю карту
    //найти каждую креатуру
    //и дать ей пинка чтоб побежала
  }
}

class ДатьСигаретуВсемЗайцамAction реализует Action {
  void execute(Карта карта) {
    //обойти всю карту
    //найти всех зайцев и дать им по сигарете 
  }
}

List<Action> actions = List.of(new ДатьСигаретуВсемЗайцамAction(), new ХодитьAction());
for(Action a: actions) {
  a.execute(карта);
}

/*
Результат: программа обойдет всю карту, найдет всех креатур и даст им пинка, чтобы они побежали.
А каждому зайцу предварительно даст сигарету.
*/
```

Подробно рассматривать здесь классы-наследники `Action` я смысла не вижу.  
Они плохи не потому, что они плохи сами по себе, хотя местами они плохи сами по себе.

Они плохи прежде всего потому, потому пали жертвой плохой архитектуры, на которую опираются: 
хранение существ в коробках и недостатки базового класса `Action`.  
Сначала нужно устранить эти недостатки, а потом уже можно будет смотреть, хороши или нет конкретные классы-экшены.

**12. Синглетонизм в экшенах**

Все классы-экшены поражены синглетонизмом
```java
public class ClearBusyStatus extends Action {
  private static ClearBusyStatus instance;
  //...
}

public class CreationEntity extends Action {
  private static CreationEntity instance;
  //...
}
```

**13. class Render**

Печатает не только карту, но и разные тексты. 
В принципе, почему бы и нет.

- Нарушение SRP, DRY, знает больше, чем положено.

На каком основании рендерер хранит в себе эту информацию? 
Если карту переделают и ее размеры поменяются, как рендерер об этом узнает?
```java
public static void showStartMessage() {
  System.out.println("Размер поля 10х10. Количество каждого существа на карте - 5 шт.");
}

//ПРАВИЛЬНО:
public void showStartMessage() {
  int entitiesCount = getEntitiesCount(gameMap);
  System.out.printf("Размер поля %dх%d. Количество каждого существа на карте - %d шт.  \n", gameMap.getWidth(), gameMap.getHeight(), entitiesCount);
}
```
Эту информацию рендерер должен брать из самой карты.

- Избыточно
```java
case Herbivore _ -> DrawEntity.HERBIVORE.getValue();

//ПРАВИЛЬНО:
case Herbivore -> DrawEntity.HERBIVORE.getValue();
```

- В switch-case в default нужно кидать исключение, а не возращать дефолтный спрайт. 

Потому что при добавлении нового существа в проект, можно забыть добавить его спрайт в распечатку.
И тогда существо будет жить на карте своей жизнью, но мы этого не увидим- вместо этого существа будет распечатываться дефолтный спрайт
```java
private static String getDrawEntity(Entity entity) {
  return switch (entity) {
    case Herbivore _ -> DrawEntity.HERBIVORE.getValue();
    //...
    case null, default -> "";
  };
}
```

**14. class Simulation**

- Синглетонизм.

- Не нужно на каждом ходе заново создавать экшены. 

Просто- зачем? Экшены не должны хранить в себе состояния, которые нужно каждый раз сбрасывать.  
Поэтому экшены нужно создать один раз и потом все время ими пользоваться
```java
private void addAction() {
  if (countStep == 0) {
    Action.addInitActions(actions);
    return;
  }
  Action.addTurnActions(actions);
}

public void makeStep() {
  addAction();
  //...
}

//ПРАВИЛЬНО:
private final List<Action> initActions = List.of(new UnoAction());
private final List<Action> turnActions = List.of(new DosAction(), new TresAction(), new MoveAction());

public Simulation(...) {
  //...
  executeActions(initActions);  //выполнение экшенов на старте
}

private void nextTurn() {
  //...
  executeActions(turnActions);  //выполнение экшенов на каждом ходе
}

private void executeActions(List<Action> actions) {
  for(Action a : actions) {
    a.execute(board);
  }
}
```

**15. class Main**, содержит точку входа main

👍 Только создает и запускает Симуляцию, это хорошо.

## ВЫВОД

Это тот случай, когда от неправильного камушка в фундаменте перекосило все здание.  
Здесь этот камушек- класс `Coordiante`.

Переделать программу по моим рекомендациям и согласно ТЗ.  
ТЗ и чеклисту в нем уделить особое внимание.

Посмотреть на ютубе ролики про паттерн "Команда"- это аналог класса Action из этого проекта.

Для лучшего понимания декомпозиции ООП посмотреть ролики Сергея про шахматы.  
Посмотреть ролики Немчинского про SOLID.

n.118(266)  
#ревью #симуляция 