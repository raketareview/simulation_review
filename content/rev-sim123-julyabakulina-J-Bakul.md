https://github.com/J-Bakuli/Simulation_Bakuli  
[Юля Бакулина]

Есть над чем поработать.

## НЕДОСТАТКИ РЕАЛИЗАЦИИ

1. На старте распечатываются правила, это хорошо.  
Но юзер не успевает эти правила даже заметить- сразу же начинает работать симуляция.
Нужно запускать процесс симуляции по команде юзера.

2. Карта распечатывается неровно, нужно подобрать спрайты одинаковой ширины  
![pic](https://github.com/raketareview/simulation_review/blob/master/content/resources/rev-sim123/img0.png) 

## ХОРОШО

+ 👍 Спрайты существ не хранятся в самих существах
+ 👍 Координаты существ не хранятся в самих существах(мне так больше нравится)
+ 👍 Реализовано пауза/пуск во время работы
+ 👍 Цветовая индикация здоровья существ на карте

## ЗАМЕЧАНИЯ

**1. Нейминг**

- Названия пакетов нужно писать стилем all lowercase 
```java
package simulation.pathFinding;

//ПРАВИЛЬНО:
package simulation.pathfinding;
```

- Стандартным явлениям давай стандартные имена. Это валидация координаты
```java
public void ensureInBounds(Coordinate coordinate)

//ПРАВИЛЬНО:
public void validate(Coordinate coordinate)
```

- Избыточно
```java
InitMessages.printMessages()

//ПРАВИЛЬНО:
InitMessages.print()
```

- Избыточно. Мы и так понимаем, что фабрика существ делает существ
```java
EntityFactory.createEntity(EntityType entityType)

//ПРАВИЛЬНО:
EntityFactory.create(EntityType entityType)
```

- Константы должны быть final static, сейчас это не константа
```java
private final int HP_RESTORED_PER_GRASS_EATEN = 1;
```

- Это не константы- константы не могут быть объявлены внутри метода. Поэтому данная переменная не может быть названа UPPER_SNAKE
```java
public static void runUserControlCommands(Simulation simulation) {
  final String ERROR_INPUT = "Input error: please enter a number (1–3).";
  //...
}

//ПРАВИЛЬНО:
public static void runUserControlCommands(Simulation simulation) {
  final String errorInput = "Input error: please enter a number (1–3).";
  //...
}
```

- Если метод что-то проверяет(чекает), то он должен возвращать результат проверки в виде boolean.  
Если метод void, значит он не должен называться "check"
```java
public void checkHealth(WorldMap worldMap)
```

- Не называй метод "run" если он не переопределяет `Runnable.run()`- название этого метода ассоциируется с этим стандартным интерфейсом и применением потоков. 
Здесь этот метод не имеет отношение к `Runnable.run()`
```java
public class SimulationRunner {
  public static void run() {...}
  //...
}

//ПРАВИЛЬНО:
start() {...} //go(), execute(), perform(), поехали() 
```

*Oracle Java code conventions, part."Naming conventions"*  
*Мартин, "Чистый код", гл.2*  
*Ютуб, Немчинский "Как называть переменные, методы и классы?"*

**2. Если в блоке if есть return**(break, continue, throw, exit и т.д.), то else не пишется.
 
В этом случае неважно, будет else или нет, так как программа будет работать одинаково, а код без `else` будет выглядеть читабельней
```java
if (coordinate == null) {
  return Optional.empty();
} else {
  return Optional.ofNullable(entities.get(coordinate));
}

//ПРАВИЛЬНО:
if (coordinate == null) {
  return Optional.empty();
} 
return Optional.ofNullable(entities.get(coordinate));
```

**2. Текст в exception** всегда должен быть на английском языке
```java
throw new IllegalArgumentException("newEntities не может быть null");
```
Исключение это не просто телеграмма, которая летит сквозь слои. 
У exception особое назначение- если исключение вылетит и не будет перехвачено внутри программы, то аварийно прекратит выполнение программы.
Тогда на экране будет распечатано сообщение эксепшена, и это сообщение должно быть понятно сисадмину в любой точке планеты. 
А значит, сообщение должно быть на английском. 
Интерпритация исключения и перевод его на локальный язык должно происходить там, где это соответствует архитектуре программы или не происходить вовсе, если исключение не планируется перехватывать.

**3. Форматирование**

Не нужно несколько инструкций всегда стремиться сводить в одну. 
Иногда для простоты и более красивого форматирования кода, лучше писать по одной инструкции в строке
```java
throw new InvalidCoordinateException(
  String.format("Coordinates (%d, %d) are out of bounds (%d×%d)", x, y, width, height)
);

//ЛУЧШЕ:
String message = "Coordinates (%d, %d) are out of bounds (%d×%d)".formatted(x, y, width, height);
throw new InvalidCoordinateException(message);
```

**4. Используй многострочные строки**
```java
private static final String INIT_MESSAGES = "Добро пожаловать в игру Симуляция. \n" +
    "Вы увидете пошаговую симуляцию 2D мира, населённого травоядными и хищниками. \n" +
    "Кроме существ, мир содержит ресурсы (траву), которыми питаются травоядные, и статичные объекты, " +
    "камни  и деревья. %n%n";

//ПРАВИЛЬНО:
private static final String INIT_MESSAGES = """
    Добро пожаловать в игру Симуляция. 
    Вы увидете пошаговую симуляцию 2D мира, населённого травоядными и хищниками. 
    Кроме существ, мир содержит ресурсы (траву), которыми питаются травоядные, и статичные объекты, камни  и деревья. 
    

    """;
```
Многострочные строки(текстовые блоки) имеются в java начиная с 13 версии.

**5. Нарушение DRY**, магические буквы, числа, слова. Вводи константы. А если они уже есть- пользуйся ими
```java
public class SimulationRunner {
    public static final int PAUSE_SIMULATION_COMMAND = 1;
    public static final int CONTINUE_SIMULATION_COMMAND = 2;
    public static final int EXIT_SIMULATION_COMMAND = 3;
    //...
}

public class Simulation {
  //...

  private void printUserControlInfoMenu() {
    System.out.println("[MENU] → 1: Pause | → 2: Continue | → 3: Exit \n");
  }
}
```
*Фаулер, "Рефакторинг", гл.8, "Замена магического числа символической константой"*  
*refactoring.guru "Замена магического числа символьной константой"*  

**6. record Coordinate(int x, int y)**

👍 Нет ничего лишнего, это хорошо. Record для координаты- идеально.

**7. class WorldMap**

+ 👍 Геттеры не возвращают null, это хорошо- возвращается Optional
```java
public Optional<Entity> getEntity(Coordinate coordinate) 
``` 

+ 👍 Хорошо, что возвращается копия хешмапы, а не ее оригинал. 
Это защищает внутреннее устройство класса WorldMap
```java
public Map<Coordinate, Entity> getEntities() {
  return new HashMap<>(entities);
}
```

+ 👍 При всех операциях, где участвует координата, выполняется валидация. 
Если координата находится вне пределов карты, бросается исключение, это хорошо
```java
public void setEntity(Coordinate coordinate, Entity entity) {
  ensureInBounds(coordinate);  <-- ВАЛИДАЦИЯ
  entities.put(coordinate, entity);
}
```

- Очень сильно сомневаюсь в корректности существования этого метода
```java
public void setEntities(HashMap<Coordinate, Entity> newEntities) 
```
Во-первых, зачем? Уже есть стандартный метод для вставки существ- `setEntity(Coordinate coordinate, Entity entity)`.  
Все равно, чтобы передвать хешмапу с существами в Карту, эту хешмапу нужно сначала сформировать.
Не вижу причин, почему бы вместо такого "промежуточного" формирования хешмапы и вставки ее в Карту, не вставлять сразу существ в Карту.  
Вставка существ в карту через промежуточное формирование хешмапы это усложнение правил пользования этим классом без видимых выгод.

Во-вторых, при вставке хешмапы, возникают несколько проблем.  
Например, откуда мы знаем, что прилетевшая в сеттер хешмапа соответствует условиям корректности класса WorldMap?  
Сейчас нам никто не мешает сформировать хешмапу с некорректными координатами типа (+100500, -100500) и вставить ее в Карту размером 10x10.  
Валидация координаты выполняется в `setEntity(Coordinate coordinate, Entity entity)`, а в `setEntities(HashMap<Coordinate, Entity> newEntities)`- нет.

- Нарушение принципа минимально открытого интерфейса.

Методы, которые нужны только внутри своего класса, являются подробностями внутренней реализации класса и не должны быть публичными.
Метод валидации координаты должен быть private
```java
public void ensureInBounds(Coordinate coordinate) {  <-- используется только внутри класса, должен быть private
  //валидация: бросает исключение, если координата некорректна  
}

public boolean isInBounds(Coordinate coordinate)  <-- тут public правильно, используется и внутри класса и вне его
```
*Вайсфельд "Объектно-ориентированный подход", гл.5, "Минимальный открытый интерфейс"*

- Избыточное защитное программирование, Over Validation.

В методах проводится валидация входящих аргументов. В общем случае, это хорошо.
Например, при вставке существа на координату мы должны проверить корректность этой координаты- находится ли она в пределах карты.

Но бывают ситуации, когда отсутствие проверки лучше ее наличия.  

Например, здесь координата проверяется на null
```java
public boolean isInBounds(Coordinate coordinate) {
  if (coordinate == null) {
    return false;
  }

  int x = coordinate.x();
  int y = coordinate.y();

  return x >= 0 && x < width && y >= 0 && y < height;
}
```
Казалось бы, это хорошо- метод выглядит более устойчивым.  
Но по факту, метод нарушает свой контракт. 

В метод передают null вместо координаты и спрашивают: эта координата находится в пределах границ карты(is in bounds)?
Метод отвечает: нет, это кордината не находится в пределах границ карты.  
А правильный ответ должен быть таким: это не находится в карте, это не находится за ее пределами, это вообще не координата.

То есть, нужно кинуть исключение.  
Если мы просто уберем отсюда проверку на null, все будет работать так, как надо- в случае прихода null, вылетит NullPointerException.

Еще пример.  
Если убрать проверку на null, все равно в случае прихода null вылетит исключение. 
Только не IllegalArgumentException, а NPE. Но тип конкретного эксепшена здесь не принципиален
```java
public void setEntities(HashMap<Coordinate, Entity> newEntities) {
  if (newEntities == null) {
    throw new IllegalArgumentException("newEntities не может быть null");
  }
  this.entities.clear();
  this.entities.putAll(newEntities);
}
```
Без избыточных проверок код будет выглядеть чище:
```java
public void setEntities(HashMap<Coordinate, Entity> newEntities) {
  this.entities.clear();
  this.entities.putAll(newEntities);  <-- если newEntities == null, то вылетит NPE
}
```

Не нужно по умолчанию все входящие в метод аргументы проверять на null, это Over Validation.  
Избыточные проверки захламляют код.

Если входящий null вызовет исключение в методе, то пусть это сделает.  
Благодаря быстрому падению ты сразу узнаешь, что в алгоритме есть баг: код по какой-то причине передает в методы null вместо корректных значений. 
Выявив баг, ты быстро устранишь его.

- Дублирование логики.

Два метода, которые делают одно и то же- помещают существо в карту
```java
public void setEntity(Coordinate coordinate, Entity entity) {
  //...
  entities.put(coordinate, entity);
}

public boolean addEntity(Coordinate coordinate, Entity entity) {
  //...
  entities.put(coordinate, entity);
}
```

- Метод должен ответить на простой вопрос: есть существо по координате, или нет.  
Если на этот вопрос нельзя внятно ответить по причине некорретных входных данных, метод должен кинуть исключение
```java
public boolean hasEntity(Coordinate coordinate) {
  return coordinate != null && entities.containsKey(coordinate);
}

//ПРАВИЛЬНО:
public boolean hasEntity(Coordinate coordinate) {
  validate(coordinate);  <-- бросает исключение, если координата вне карты или null
  return entities.containsKey(coordinate);
}
```
Ближайшая аналогия- стандартные хранилища типа List и массива. 
При попытке обратитья к ним по несуществующему индексу, бросается исключение.

- Избыточно сложная для пользования методом сигнатура. Сделай по аналогии с `hasEntity(...)` 
```java
public void removeEntity(Coordinate coordinate, Entity entity)

//ПРАВИЛЬНО:
public void removeEntity(Coordinate coordinate)  <-- хочешь, так удаляй
public void removeEntity(Entity entity)          <-- а хочешь, вот так
```

+ 👍 В целом, с точки зрения SRP, класс хороший.

**8. class WorldMapUtils**

Этот метод должен возвращать коллекцию не просто соседних позиций. 
Этот метод должен возвращать коллекцию соседних позиций которые *находятся в пределах карты*
```java
  public static Set<Coordinate> getNeighbouringCells(Coordinate pos) {
    Set<Coordinate> neighbours = new HashSet<>(8);
    
    neighbours.add(new Coordinate(pos.x(), pos.y() + 1));
    neighbours.add(new Coordinate(pos.x(), pos.y() - 1));
    //еще 6 подобных строк
    //...

    return Collections.unmodifiableSet(neighbours);
  }
```

Например, если позиция будет находиться в центре карты, то у нее будет 8 соседних ячеек.  
Если позиция будет в верхнем левом углу карты(0,0), то соседей будет 3.  
Если позиция будет в точке (1,0), то соседей будет 5.

**9. interface PathFinder**

+ 👍 Интерфейс поиска пути это хорошо. Теперь можно (было бы) делать разные реализации поиска: BFS, A* и т.д.

- Этот интерфейс не ищет путь.

Задача поиска пути- искать путь. 
Путь это последовательность координат от старта до цели.  
Сейчас интерфейс не ищет путь, а делает что-то непонятное
```java
public interface PathFinder {
  Optional<Coordinate> findNearestMealViaBfs(...);

  Optional<Direction> findNextStepTowardMeal(...);
}
```

- Нарушение концепции интерфейсов как таковых.

Интерфейсы по определению могут иметь разные реализации.  
Наличие интерфейса поиска подразумевает, что могут быть реализации с разными видами поиска.  
Например, BFS, A* или "Искать вправо-влево как глупый зомби".

Здесь же этот интерфейс уже подразумевает один конкретный вид поиска- в названии метода есть указание на поиск BFS
```java
Optional<Coordinate> findNearestMealViaBfs(...);  
```
Интерфейс должен быть организован таким образом, чтобы егомогли реализовывать разные поиски, а не только BFS.

- Интерфейс должен быть простым и понятным для пользования.

Поиск должен просто искать путь от точки старта до точки, соответствующей заданным условиям. 
В данном случае- до точки, в которой находится существо нужного класса.

В интерфейсе должен быть только один метод, который выполняет кантракт класса. То есть, ищет путь.  
Например, так:
```java
public interface PathFinder {
  //ищет путь на карте от точки start
  //до точки, где находится существо нужного класса(напр. target == Grass.class)
  List<Coordinate> find(Карта карта, Coordinate start, Class<? extends Entity> target); 
}
```
Что потом с этим путём будет делать клиент- не должно быть заботой PathFinder'a.

**10. class BreadthFirstSearchPathFinder implements PathFinder**

Недостатки этого класса являются следствием недостатков интерфейса, который он реализовывает.  
Этот класс не ищет путь. Вернее, результатом его работы не является найденный путь.

**11. class InitMessages**

Этот класс содержит только статический метод, значит это утилитный класс. 
Такие классы должны быть `final` и иметь приватный конструктор.  
Не должно быть возможности унаследоваться от утилиты или сделать ее экземпляр
```java
public class InitMessages {
  public static void printMessages() {...}
}

//ПРАВИЛЬНО:
public final class InitMessages {
  private InitMessages() {
  }

  public static void printMessages() {...}
}
```
*Блох, "Java. Эффективное программирование", изд.3, гл.2.4*

**12. enum EntityType**

Енам существует для дополнительной внешней типизации классов
```java
public enum EntityType {
  GRASS, ROCK, TREE, HERBIVORE, PREDATOR;
}
```
Не используй дополнительную типизацию классов, потому что это дублирует стандартные способы.  
Используй стандарные способы определять принадлежность объектов к классам:
```java
return switch (entityType) {
  case GRASS -> new Grass();
  case ROCK -> new Rock();
  //...
}

//ПРАВИЛЬНО:
String name = entity.getClass().getSimpleName();

switch (name) {
  case "Grass" -> //...
  case "Rock" ->   //...
  //...
}
```

**13. abstract class Entity**

- Хранит енам с дополнительной типизацией, которая дублирует стандартную.  
Енам убрать, идентифицировать существ через `getClass()`.

- Если все наследники должны хранить общую для них всех информацию, то храни эту информацию в их предке
```java 
public abstract class Entity {
  public abstract EntityType getType();
}

//ПРАВИЛЬНО:
public abstract class Entity {
  private final EntityType type;

  public Entity(EntityType type) {
    this.type = type;
  }

  public EntityType getType() {
    return type;
  }
}
```
Как я уже писал, хранить EntityType в энтити это вообще неправильно. 
Но я сейчас говорю о другом- о принципах хранения общей информации в наследниках.

Вот идеальный энтити:
```java 
public abstract class Entity {
  //да, тут совсем пусто  
}
```

**14. abstract class Creature extends Entity**

- Тоже овервалидация. Если карта будет null, то и без этой валидации выкинет исключение
```java 
public void checkHealth(WorldMap worldMap) {
  if (worldMap == null) {
    throw new InvalidWorldMapException("WorldMap cannot be null.");
  }
  //...
  worldMap.getEntityCoordinate(this);  <-- вылетит NPE при worldMap == null
}
```

- При попытке двинуть мертвое существо нужно кидать исключение.  
Мертвый негр не идет играть в баскетбол и попытка алгоритма сделать это означает баг, который нужно выявить и устранить
```java
public void makeMove(WorldMap worldMap, PathFinder pathFinder) {
  if (!this.isAlive()) {
    return;  <-- тут кинуть исключение
  }
  //...
}
```

- Много сложного кода, который отвечает за выполнение движения.  
Когда переделаешь интерфейс поиска, использующий поиск пути код в этом классе тоже станет понятнее.

- Нарушение OCP.

Класс не должен знать по именам своих наследников и реализовывать в себе логику, которая касается не всех наследников, а только кого-то из них
```java
if (!this.isAlive() && !(this instanceof Predator)) {...}
```
Логика, которая касается только предатора, должна находиться только в предаторе.

**15. class Predator extends Creature**

- Нарушение SRP, чужая ответственность, зависимость модели от представления.
Модель(а это модель) не должна ничего печатать в консоль
```java
public void checkHealth(WorldMap worldMap) {
  System.out.printf("[PREDATOR_CHECK] %s HP: %d (ignored)%n", this, this.getHp());
}
```
Иначе модель перестает быть универсальной и становится заточенной под конкретную среду и конкретное представление себя в этой среде- в данном случае, консоль.
В других средах(напр. Андроид) модель нельзя будет использовать. 
Другое представление для модели(напр. если одну и ту же модель нужно в программе показвать по-разному) нельзя будет сделать, или придется делать через костыль.

Вот к чему приводит зависимость модели от представления, карта рисуется в виндовый интерфейс, а модель [пишет в консоль](https://github.com/raketareview/simulation_review/blob/master/content/rev-sim064-davidtagirov-DavidTagirov.md)

Если модель должна что-то сообщить миру, она может это сделать через паттерн `CallBack`.  
[CallBack луноход](https://t.me/zhukovsd_it_chat/53243/139594)  
[CallBack разрушение](https://t.me/zhukovsd_it_chat/53243/184749)  

Логгирование в `Creature` я бы по той же причине тоже делал через callback, а не напрямую.

**16. class SimulationConfig**

- Экземпляр класса создается через фабричный метод
```java
public static SimulationConfig defaultConfig()
```
Но при этом в проекте есть фабрика конфигов- `SimulationConfigFactory`. Зачем нужна фабрика, если есть фабричный метод?

Я бы советовал сделать наоборот:  
В конфиге убрать фабричный метод, а конструктор открыть.  
Сделать `DefaultSimulationConfigFactory` и туда перенести из `SimulationConfig` все константы и логику создания дефолтного конфига.

Это бы очень разгрузило и так тяжелый класс `SimulationConfig`.

- Нет оснований ограничивать конфиг только дефолтом.

Из-за фабричного метода, можно сделать конфиг только с одними значениями- дефолтными.
Для таких ограничений не оснований. Нужно дать возможность делать разные конфиги.

- Конфигурационный класс в программе используется неправильно.

Другие классы получают весь конфиг в конструктор, а должны получать *только* необходимые данные из него.  
Это делает классы неуниверсальными и зависимыми от конфигурационного класса.  

Условные примеры:
```java
public class Settings {
  private final static int ROOM_NUMBERS = 5;
  public getRoomNumbers() {
    return ROOM_NUMBERS;
  }
}

//ПЛОХО:
public static void main(String[] args) {
  House house = new House(new Settings());
  //oth code
}

public class House {
  private final int roomNumbers;

  public House(Settings settings) {
    this.roomNumbers = settings.getRoomNumbers();
  }

  public int getRoomNumbers() {
    return roomNumbers;
  }
}

//ХОРОШО:
public static void main(String[] args) {
  Settings settings = new Settings();
  House house = new House(settings.getRoomNumbers());
  //oth code
}

public class House {
  private final int roomNumbers;

  public House(int roomNumbers) {
    this.roomNumbers = roomNumbers;
  }

  public int getRoomNumbers() {
    return roomNumbers;
  }
}
```
Про классы констант, конфигурации и их использование я писал [ТУТ](https://t.me/zhukovsd_it_chat/53243/176984)

Да, класс может получать в конструктор не отдельные поля, а целый объект с набором этих полей. 
Но там должна быть только нужная классу информация.  
*"ЧК", гл.3, "Объекты как аргументы"*

**17. class SimulationConfigFactory**

- Неправильное использование фабрики.

Антипаттерн «Полтергейст»: бесполезный класс-посредник
```java
public class SimulationConfigFactory {
  public static SimulationConfig createDefaultSimulationConfig() {
    return SimulationConfig.defaultConfig();
  }
}
```
Фабрику нужно применять тогда, когда есть сложные правила создание объекта. 
Вызов фабричного метода из самого объекта- это не сложно, для этого не нужен отдельный класс-фабрика.

Как я писал выше, хорошо бы в эту фабрику перенести логику создания конфига из его фабричного метода.  
Я бы вообще усилил накал ООП и сделал что-то типа какого:
```java
public interface SimulationConfigFactory {
  SimulationConfig create();
}

public class DefaultSimulationConfigFactory implements SimulationConfigFactory {
  public static final int DEFAULT_HERBIVORE_SPEED = 1;
  public static final int DEFAULT_HERBIVORE_MAX_HP = 5;
  //...

  @Override
  public SimulationConfig create() {...}
}
```
Это было бы точкой расширения, которая давала бы возможность в перспективе делать `SmallSimulationConfigFactory`, `LargeSimulationConfigFactory` etc.

**18. class EntityFactory**

+ 👍 Фабрика существ это хорошо.

- Нарушение OCP.

Фабрика хранит в себе конфиг всей программы. Но в этом конфиге только часть информации нужна фабрике, остальная информация к фабрике не имеет отношение.  
Таким образом, фабрика знает много лишнего и зависит от вещей, от которых не должна зависеть
```java
public class EntityFactory {
  private final SimulationConfig simulationConfig;
  //...
  public EntityFactory(SimulationConfig simulationConfig) {...}
}
```
Допустим, мы решили сделать версию этой же симуляции, но для Swing'а.  
Для этого из консольной версии возьмем как можно больше классов, в том числе, фабрику существ.  
Но очевидно, что для свинговой версии придется делать свой класс-конфиг, который будет хранить специфическую инфу для виндового интерфейса новой программы.  
Например, размеры кнопок и картинок.

И вот, у нас будет новый класс-конфиг, с новыми полями и методами.  
И наоборот, часть инфы для консольного конфига окажется неактульной и мы их удалим из нового конфига.  
Теперь этот конфиг будет отличаться от консольного конфига. 
Поэтому и свинговый EntityFactory станет несовместим с исходным консольным EntityFactory- они будут приниамть в себя разные классы.

Чтобы не было таких эффектов, `EntityFactory` должен в себя принимать только такой объект-аргумент, который содержит только ту информацию, которая имеет отношение к фабрике.

И дело тут не в том, планируешь ли ты на самом деле делать разные версии симуляции для разных платформ.  
Дело в принципе построения классов, при котором класс не должен изменяться из-за причин, которые не имеют к нему отношения.

**19. interface Action**

👍 Идеально
```java
public interface Action {
  void execute(WorldMap worldMap);
}
```

**20. class MoveCreaturesAction implements Action**

+ 👍 Только инициирует движение существ(дает им пинка, дальше они бегут сами), это хорошо.

- Неправильное использование конфига.

Класс должен принимать в себя не весь конфиг, а только нужную информацию из него
```java
public class MoveCreaturesAction implements Action {

  public MoveCreaturesAction(SimulationConfig simulationConfig) {...}

  @Override
  public void execute(WorldMap worldMap) {
    //...
    creature.makeMove(worldMap, simulationConfig.getPathFinder());
  }
}

//ПРАВИЛЬНО:
public class MoveCreaturesAction implements Action {

  public MoveCreaturesAction(PathFinder pathFinder) {...}

  @Override
  public void execute(WorldMap worldMap) {
    //...
    creature.makeMove(worldMap, pathFinder);
  }
}
```

**21. interface WorldMapRenderer**

+ 👍 Интерфейс рендерера это хорошо. 

Теперь можно делать разные рендереры для разных визуальных сред(консоль, интерфейс виндовс, http, матричный принтер etc).
И разного отображения информации(цветной, черно-белый и пр.)

**22. class ConsoleEmojiWorldMapRenderer implements WorldMapRenderer**

+ 👍 Спрайты хранятся здесь, а не берутся из самого существа. Это хорошо.

- Избыточно сложный алгоритм
```java
private enum SpriteType {
  PREDATOR("\uD83D\uDC3A"),
  HERBIVORE("\uD83D\uDC04"),
  //...
  public String getSprite() {
    return sprite;
  }
}

public static String getSprite(EntityType entityType) {
  return switch (entityType) {
    case HERBIVORE -> SpriteType.HERBIVORE.getSprite();
    case PREDATOR -> SpriteType.PREDATOR.getSprite();
    //...
  }
}

//ЛУЧШЕ:
private enum SpriteType {...}

private String toSprite(Entity entity) {
  String name = entity.getClass().getSimpleName().toUpperCase();
  SpriteType type = SpriteType.valueOf(name);
  return type.getSprite();
}
```

- Не передавай Optional во входящие
```java
private String getCellContent(Optional<Entity> entityOpt)

//ПРАВИЛЬНО:
private String getCellContent(Entity entity)
```
Метод должен получать уже подготовленные данные, а не выколупывать их из Optional.

**23. class Simulation**

- Нарушение конфенции кода. Константы должны стоять выше остальных полей.

- Обработка исключений.

Какое именно исключение ожидается здесь перехватить?
```java
try {
  action.execute(worldMap);
} catch (Exception e) {
  System.err.println("Error executing action: " + e.getMessage());
}
```
Нельзя перехватывать просто "на всякий случай" базовое исключение `Exception`. 
Нужно перехватывать и соответствующим образом обрабатывать те исключения, которые мы ожидаем получить.

Какие конкретно исключения мы ожидаем получить при вызове `action.execute(worldMap)`?

Ну ок. Перехватываем базовое исключение. Но делаем мы это тут чтобы что? 

Есть три основных способа реагировать на ошибку: игнорирование, исправление, быстрое падение.
Сейчас выбрано игнорирование- при перехвате исключения, просто печатается его сообщение
```java
catch (Exception e) {
  System.err.println("Error executing action: " + e.getMessage());
}
```
То есть, программу будет глючить, она будет кидать исключения, мы будем их перехватывать и печатать юзеру "Error executing action: ...".  
Но дело в том, что заметать под ковер исключения это плохая практика- мы не устраняем причины багов и программа продолжает работать некорректно.

Я смоделировал одну из возможных ошибок и вот как при этом ведет себя программа:
```java
Error executing action: WorldMap cannot be null.
Step: 13
Current count: herbivores — 10, predators — 4.

⬛⬛⬛⬛⬛⬛⬛⬛⬛⬛⬛⬛⬛⛰⬛⬛⬛⬛
⛰⬛⬛⬛⬛⬛⬛⬛⬛⬛⬛⬛🐄⛰⬛⬛⬛⬛
⬛⬛🐄⬛⬛⬛⬛⬛⬛⬛⬛⬛⬛⬛⬛🌳⬛⬛
⬛⬛⬛⛰⛰⬛🌾⛰⬛⬛⬛⬛🌾⬛⬛⬛🌳⬛
⬛⬛⬛⛰🌳⬛⬛⬛⬛🐄⬛🐄⬛⬛⬛⬛⬛⬛
⬛🌳⬛🌾⬛⬛⬛⬛⛰⬛🐺⬛⬛⬛⬛⬛⛰⬛

Error executing action: WorldMap cannot be null.
Step: 14
Current count: herbivores — 10, predators — 4.

⬛⬛⬛⬛⬛⬛⬛⬛⬛⬛⬛⬛⬛⛰⬛⬛⬛⬛
⛰⬛⬛⬛⬛⬛⬛⬛⬛⬛⬛⬛🐄⛰⬛⬛⬛⬛
⬛⬛🐄⬛⬛⬛⬛⬛⬛⬛⬛⬛⬛⬛⬛🌳⬛⬛
⬛⬛⬛⛰⛰⬛🌾⛰⬛⬛⬛⬛🌾⬛⬛⬛🌳⬛
⬛⬛⬛⛰🌳⬛⬛⬛⬛🐄⬛🐄⬛⬛⬛⬛⬛⬛
⬛🌳⬛🌾⬛⬛⬛⬛⛰⬛🐺⬛⬛⬛⬛⬛⛰⬛

Error executing action: WorldMap cannot be null.
Step: 15
Current count: herbivores — 10, predators — 4.

⬛⬛⬛⬛⬛⬛⬛⬛⬛⬛⬛⬛⬛⛰⬛⬛⬛⬛
⛰⬛⬛⬛⬛⬛⬛⬛⬛⬛⬛⬛🐄⛰⬛⬛⬛⬛
⬛⬛🐄⬛⬛⬛⬛⬛⬛⬛⬛⬛⬛⬛⬛🌳⬛⬛
⬛⬛⬛⛰⛰⬛🌾⛰⬛⬛⬛⬛🌾⬛⬛⬛🌳⬛
⬛⬛⬛⛰🌳⬛⬛⬛⬛🐄⬛🐄⬛⬛⬛⬛⬛⬛
⬛🌳⬛🌾⬛⬛⬛⬛⛰⬛🐺⬛⬛⬛⬛⬛⛰⬛
```
Как видно, из-за ошибки "WorldMap cannot be null" на карте не происходит никакого движения, но программа продолжает бессмысленно работать.  
Наблюдающий эту ситуацию юзер будет в недоумении.

Если мы перехватываем исключение и не знаем, как исправить причину его возникновения, надо просто сообщить юзеру, что возникла критическая ошибка и программа будет остановлена. 

**24. class SimulationRunner**

Если есть константы- пользуйся
```java
public static final int PAUSE_SIMULATION_COMMAND = 1;
public static final int CONTINUE_SIMULATION_COMMAND = 2;
public static final int EXIT_SIMULATION_COMMAND = 3;

public static void runUserControlCommands(Simulation simulation) {
  final String ERROR_INPUT = "Input error: please enter a number (1–3).";
  //...
}

//ПРАВИЛЬНО:
public static final int PAUSE_SIMULATION_COMMAND = 1;
public static final int CONTINUE_SIMULATION_COMMAND = 2;
public static final int EXIT_SIMULATION_COMMAND = 3;
public static final String ERROR_INPUT = "Input error: please enter a number (%d–%d).".formatted(PAUSE_SIMULATION_COMMAND, EXIT_SIMULATION_COMMAND);
```

- Не используй секцию static. Это не дает ничего, кроме сложного для чтения кода 
```java
private static final SimulationConfig simulationConfig;

static {
  simulationConfig = SimulationConfigFactory.createDefaultSimulationConfig();
}

//ПРАВИЛЬНО:
private static final SimulationConfig simulationConfig = SimulationConfigFactory.createDefaultSimulationConfig();
```

- Нарушение инкапсуляции. Публичным здесь должен быть только `run()`- это единственный метод, который предназначен для использования клиентами.

**25. class Main**, содержит точку входа main

+ 👍 Только создает и запускает Симуляцию, в данном случае через `SimulationRunner`, это хорошо.

+ 👍 Вот здесь делать перехват исключений это норм
```java
public static void main(String[] args) {
  try {
    SimulationRunner.run();
  } catch (SimulationException e) {
    System.out.println(e.getMessage());
  }
}
```
Когда в программе возникают аварийные ситуации, которые мы не можем корректно исправить, то кинуть исключение и перехватить его на самом верху, то есть здесь, это хорошее решение.  
Цель такого перехвата чисто косметическая- сделать так, чтобы программа не просто аварийно вылетила с непонятными красными эксепшенами в консоль.  
А чтобы програма написала сообщение юзеру и корректно закрылась. 

Только сообщение для юзера должно быть более осмысленным.  
Нужно написать, что возникла такая-то ошибка и работа программа будет прекращена.

Перехваты большинства остальных исключений в недрах других классов, например в Simulation, нужно убрать.  
Все равно реакцией на большинство из них должно быть прекращение работы программы.  

## АРХИТЕКТУРА

Местами код сложный из-за использования конфигурационных классов, работу с которыми не получилось сделать простой.  

Не стремись сделать больше, чем указано в ТЗ.  
Во-первых, делать лишнюю работу это плохая практика.   
Во-вторых, приоритет здесь это тренировка в написание хорошей архитектуры. 
Но при усложнении реализации, неизбежно ухудшается архитектура, поэтому нужно соблюсти баланс.

Удели внимание правильной работе с исключениями. 
В программе они часто используются неправильно:  
Иногда исключения не выбрасываются там, где надо, иногда игнорируются тогда, когда не должны игнорироваться.

Вот типичный пример неправильной работы с исключениями в программе:
```java
public static Set<Coordinate> getNeighbouringCells(Coordinate pos) {
  //...  
  Set<Coordinate> neighbours = new HashSet<>(8);

  try {
    neighbours.add(new Coordinate(pos.x(), pos.y() + 1));
    neighbours.add(new Coordinate(pos.x(), pos.y() - 1));
    //еще 6 подобных строк
  } catch (ArithmeticException e) {
    throw new ArithmeticException(
    "Coordinate overflow at pos=(" + pos.x() + "," + pos.y() + ")");
  }
}
```
Здесь нет необходимости в перехвате `ArithmeticException`, потому что такого исключения здесь не вылетит никогда. 

При создании `new Coordinate(...)` не могут возникнуть эксепшены.  
При добавлении значения в сет через `add(...)` могут возникнуть эксепшены- 
перечень возможных при этом эксепшенов есть в комментарии к этому методу в классе Set.java  
Но среди них тоже нет `ArithmeticException`.

Вот там же обратный пример- недостаточное использование исключений
```java
public static Set<Coordinate> getNeighbouringCells(Coordinate pos) {

  if (pos.x() < 0 || pos.y() < 0) {
    throw new IllegalArgumentException(
       "Coordinates must be non-negative: x=" + pos.x() + ", y=" + pos.y());
  }
  //...
}
```
Здесь входящая координата проверяется на корректнось и это правильно.
Если коордиата будет некорректно, то метод не сможет выполнить свой контракт, а значит, должен кинуть исключение.

Но здесь проверяется только нижняя граница коректных значений, а верхняя- нет.  
Правильно должно быть так:
```java
public static Set<Coordinate> getNeighbouringCells(WorldMap worldMap, Coordinate pos) {

  if (!worldMap.isInBounds(pos)) {
    throw new IllegalArgumentException(
       "Coordinates must be non-negative: x=" + pos.x() + ", y=" + pos.y());
  }
  //...
}
```

**Быстрое падение это нормально.**

Исключения это наши друзья.  
Они во время отладки вызывают быстрое падение программы и тем самым показывают, где у нас в алгоритме баг.  
Появление исключений это всегда следствие некорректного алгоритма. 
Эксепшены не могут возникнуть из ниоткуда, об этом не стоит тревожиться.

Поэтому помещать банальные операции с коллекциями, типа `set.add(...)` в try-catch не стоит, это быстро захламит код без всякой пользы.

Правильная реакция на исключения: выявить в алгоритме причину их возникновения и устранить эту причину.  
Неправильные реакции: игнорировать исключения, перехватывать исключения "на всякий случай".

В остальном, с классами в программе все довольно неплохо.

## ВЫВОД

Почитай про использование исключений, начни с соответствующих глав в книгах классиков: Эккеля и Блоха.

В целом, с классами все более-менее ок, вопиющих нарушений SOLID не обнаружил.  
Где-то есть неудачные решения, где-то есть неправильно понятая идея, как в Поиске Пути.  
Но в общем норм.

n.123(272)  
#ревью #симуляция 