https://github.com/KittieFoxxy/simulation  
[Cat Fox]

Есть над чем поработать.

## НЕДОСТАТКИ РЕАЛИЗАЦИИ

1. Почему стоит такое ограничение, минимум 20x20?  
Я бы хотел посмотреть работу программы на более мелкой карте, например, 6x6 или 10x10 
```
Введите размеры симуляции (ширина-высота) через пробел (например '20 20')
Размеры не должны быть меньше 20 и больше 50
20 20
```

2. Каждый раз вводит настройки для запуска- утомительно. 

Можно было бы сделать несколько Main'ов: один пусть запускает программу через ввод настроек, а другой- с настройками по умоляанию.  
Возможность запускать разные конфигурации программы, не меняя при этом код в основных классах- это важная черта ООП, полиморфное поведение.

3. Мы можем показывать работу симуляции более выразительными средствами, чем цветные квадратики.

Неудобно смотреть на меняющиеся картинки и каждый раз пересчитывать в голове соответствие квадратика существу.  
В этом смысле, даже простые буквы были бы лучше: H(herbivore), r(rock) etc  
![pic](https://github.com/raketareview/simulation_review/blob/master/content/resources/rev-sim129/img0.png)  
Почему бы не использовать красивые эмодзи, за которыми можно будет легко следить. Например: 🐇 🐯 🌴 🍀

4. Карта распечатывается неправильно- с поворотом на 180°
```java
public class Test {
  public static void main(String[] args) {
    SimulationMap simulationMap = new SimulationMap(4, 4);
    final int x = 3;
    final int y = 0;
    Coordinate coordinate = new Coordinate(x, y);
    simulationMap.addEntity(coordinate, new Grass());

    SimulationView view = new SimulationView();
    view.drawWorld(simulationMap);
  }
}

public record Coordinate(int x, int y) {}

//РЕЗУЛЬТАТ:
 ⬜ ⬜ ⬜ ⬜
 ⬜ ⬜ ⬜ ⬜
 ⬜ ⬜ ⬜ ⬜
 🟩 ⬜ ⬜ ⬜
————————————
Обозначения: 🟩 Трава  🟫 Дерево  ⬛ Камень  🟨 Травоядное  🟥 Хищник
```

## ХОРОШО
+ 👍 Спрайты существ не хранятся в самих существах
+ 👍 Координаты в существах хранятся начиная с Creature
+ 👍 Реализовано пауза/пуск во время работы
+ 👍 Возможность конфигурирования карты через меню
+ 🚀 Используется агоритм Jump Point Search (JPS) — оптимизация алгоритма A*

## ЗАМЕЧАНИЯ

**1. Нейминг**

- Для избежания путаницы не называй пакеты, классы, интерфейсы так же, как называются классы и интерфейсы стандартной библиотеки.  
Есть стандартный интерфейс `Map`, не должно быть кастомного пакета с таким же названием
```java
package com.pot.simulation.domain.map;

//ПРАВИЛЬНО:
package com.pot.simulation.domain.simulationmap;
```

- Придерживайся единообразия
```java
public Optional<Entity> getEntity(Coordinate coordinate) 
public boolean isWithinBounds(Coordinate c)

//ПРАВИЛЬНО:
public Optional<Entity> getEntity(Coordinate coordinate) 
public boolean isWithinBounds(Coordinate coordinate)
```

- Хуже венгерской ноттации только венгерская ноттация, которая обманывает.  
Здесь в названии фигурирует слово "Set", но эта переменная не принадлежи к типу Set, это PriorityQueue
```java
PriorityQueue<Node> openSet
```

- Не увлекайся однобуквенными названиями. Название аргументов "g" и "h" это термины из алгоритма A*/JSP. 
Но остальные аргументы здесь можно назвать осмысленно
```java
Node(Coordinate c, Node p, int g, int h) 

//ПРАВИЛЬНО
Node(Coordinate coordinate, Node parent, int g, int h) 
```

- Название обманывает. Метод не инициализирует контроллер симуляции. Метод управляет процессом симуляции
```java
void initSimulationController()
```

- Перечни в виде листов, сетов и т.п. должны называться во множественном числе
```java
List<Action> configureInitAction()

//ПРАВИЛЬНО
List<Action> configureInitActions()
```

*Oracle Java code conventions, part."Naming conventions"*  
*Мартин, "Чистый код", гл.2*  
*Ютуб, Немчинский "Как называть переменные, методы и классы?"*

**2. Нарушение конвенции кода**

В любой ситуации выделяй тело блока скобочками, даже если тело состоит из одной строки.  
Исключение- метод equals(), там можно после if не выделять блоки скобочками
```java
if (current.coord.equals(target)) return reconstructPath(current);

//ПРАВИЛЬНО:
if (current.coord.equals(target)) {
  return reconstructPath(current);
}
```
*"Oracle Java code conventions"*

**3. Используй классы через их интерфейсы**
```java
PriorityQueue<Node> openSet = new PriorityQueue<>(Comparator.comparingInt(n -> n.f));

//ПРАВИЛЬНО:
Queue<Node> openSet = new PriorityQueue<>(Comparator.comparingInt(n -> n.f));
```
Общее правило: ArrayList нужно использовать через List, HashMap- через Map и т.д.  
Это позволяет пользоваться преимуществами полиморфизма.

Да, бывают ситуации, когда, например, с LinkedList нужно работать именно как с LinkedList, а не с List. 
Но это уже нюансы.  
*"Java. Эффективное программирование", изд.3, гл.9.8*
```
"Если вы выработаете привычку использовать в качестве типов интерфейсы, ваша программа будет гораздо более гибкой" - Блох.
```

**4. record Coordinate(int x, int y)**

👍 Нет ничего лишнего, это хорошо. Record для координаты- идеально.

**5. class SimulationMap**

- Нарушение SRP, методы чужих ответственностей. 

Карта должна только хранить существа и обеспечить базовые операции с ними: вставить, выдать одно существо и список всех хранимых существ, удалить.
Если какой-то метод не нужен для обеспечения этого функционала, значит он не принадлежит к ответственности карты, а принадлежит к чужой ответственности.
Здесь методы чужих ответственностей: создать список пустых координат.
  
Наверное, для проекта в целом полезно иметь метод, который создает список пустых координат. 
Но этот процесс не имеет никакого отношения к единой ответственности карты- хранению существ в себе.  
Для организации хранения/удаления/выдачи существ, карте не нужно искать пустые координаты.  

Методы чужих ответственностей должны находиться в тех классах, в интересах которых они работают.
Если один и тот же метод используют разные классы, то метод нужно вынести в отдельный класс, например, BoardUtils.

+ 👍 Геттеры не возвращают null, это хорошо. Они здесь возвращают Optional.

+ 👍 Хороший метод
```java
public <T extends Entity> List<T> findEntities(Class<T> type)
```

- Не возвращай экзотические типы данных из своих методов. Клиенту будет непонятно, как пользоваться таким методом
```java
public Stream<Coordinate> findEntityCoordinates(Class<? extends Entity> type) 

//ПРАВИЛЬНО:
public List<Coordinate> findEntityCoordinates(Class<? extends Entity> type) 
```
В данном случае возвращать `Stream` некорректно еще и потому, что это не коллекция, а поток.

- При всех операциях с участием координаты(добавить, выдать, удалить и т.д.), нужно проверять координату на корректность.  
Если координата некорректна(находится вне пределов карты), нужно бросать исключение
```java
public void addEntity(Coordinate coordinate, Entity entity) 
public void removeEntity(Coordinate coordinate, Entity entity)
```
Ближайшая аналогия- стандартные хранилища типа List и массива. При попытке обратиться к ним по несуществующему индексу, бросается исключение.


**6. Пакет util**

Классы в этом пакете- не утилитные. Уилиты это классы без изменяемых состояний и только со статическими методами.

**7. interface PathFinder**

+ 👍 Интерфейс поиска пути это хорошо. Теперь можно делать разные реализации поиска: BFS, A*, JSP и т.д.

- Нарушение SRP.

Поиск должен просто искать путь от точки старта до точки, соответствующей заданным условиям. 
В данном случае- до точки, в которой находится существо нужного класса.

Если поиск получает во входящие аргументы метода координаты цели, значит часть работы поиска уже выполнил кто-то другой. 
То есть, кто-то другой уже нашел цель и передал ее кординату в поиск пути
```java
List<Coordinate> find(Coordinate start, Coordinate end);
```
На самом деле поиск пути должен сам искать цель и прокладывать к ней путь. Сигнатура метода для этого должна выглядеть примерно так
```java
//ищет путь на карте от точки start
//до точки, где находится существо нужного класса(напр. target == Grass.class)
List<Coordinate> find(Coordinate start, Class<? extends Entity> target);
```
Это метод интерфейса. А значит, сигнатура метода должна одинаково работать с разными алгоритмами поиска и быть общей для всех.  
Например, для алгоритма BFS- для его работы не нужно заранее находить координату цели.

Если алгоритму JSP для создания пути нужно предварительно указать координату цели, то он предварительно должен сам найти эту координату, а не запрашивать ее у клиента.

**8. record JpsPathFinder(SimulationMap map) implements PathFinder**

- Некорректное использование record'a.

Это должен быть обычный класс.  
Record'ы применяются просто для хранения данных и не должны иметь развитого поведения
```java
record JpsPathFinder(SimulationMap map) implements PathFinder {...}

//ПРАВИЛЬНО:
public class JpsPathFinder implements PathFinder {
  private final SimulationMap map;

  public JpsPathFinder(SimulationMap map) {...}
  //...
}
```

- Поиск должен сам предварительно найти координату цели, к которой нужно проложить путь
```java
@Override
public List<Coordinate> find(Coordinate start, Coordinate target) {
  //...
}

//ПРАВИЛЬНО:
@Override
public List<Coordinate> find(Coordinate start, Class<? extends Entity> target) {
  Coordinate targetCoordinate = getTargetCoordinate(target);
  //...
}
```

- Не передавай null в методы. Делай перегрузку методов таким образом, чобы не пришлось передавать null
```java
Node startNode = new Node(start, null, 0, getDistance(start, target));
  
private static class Node {
  Node(Coordinate c, Node p, int g, int h) {...}
}  

//ПРАИВЛЬНО:
Node startNode = new Node(start, 0, getDistance(start, target));
  
private static class Node {
  Node(Coordinate coordinate, Node parent, int g, int h) {
    this(coordinate, g, h);
    this.parent = parent;
  }

  Node(Coordinate coordinate, int g, int h) {...}
}  
```
*Мартин "ЧК", гл.7*
```java
"Возвращать null из методов плохо, но передавать null при вызове еще хуже" - Мартин.
```

**9. abstract class Entity и его простые наследники Tree/Rock/Grass**

👍 Идеально
```java
public abstract class Entity {
}

public class Grass extends Entity {
}
```

**10. class Carnivore/Herbivore extends Creature**

- Нарушение ТЗ. Непонятна мотивация переименования класса "Predator", указанного в ТЗ, на "Carnivore".

- Дублирование кода.

Код в `makeMove(...)` в двух классах частично совпадает. Общий код выноси в предка- Creature.  
Чеклист ТЗ:
```
Проблемы и ошибки в коде:
Дублирование кода между классами Herbivore и Predator
```

**11. class MakeMoveAction implements Action**

👍 Идеально

**12. Остальные Action'ы**

Все хорошо, но они не должны быть record'ами.

**13. record ActionConfig(SimulationMap map)**

- По своему функционалу, это не конфигурационный класс, а фабрика экшенов.

- Этот класс не должен быть рекордом. По крайней мере не в этом виде- когда он имеет сложное поведение
```java
public record ActionConfig(SimulationMap map) {
  public List<Action> configureInitAction() {...}  <--+-- Слишком сложное поведение для record'a
  public List<Action> configureTurnAction() {...}  <--+
}
```

- Не используй тестовую конфигурацию методом комментирования/раскомментирования кода 
```java
public List<Action> configureInitAction() {
  int mapSize = map.height() * map.width();
  return List.of(
     new SpawnAction(map, Rock.class, (int) (0.15 * mapSize)),
     new SpawnAction(map, Carnivore.class, (int) (0.1 * mapSize))
    // для тестов
    // new SpawnAction(map, Rock.class, (int) (0.1 * mapSize)),
    // new SpawnAction(map, Tree.class, (int) (0.1 * mapSize)),
```
В таких случаях, используй полиморфизм. Сделай рабочую и тестовую реализации.  
Примерно, так:
```java
public abstract class ActionFactory {
   //...
   public abstract List<Action> getInitActions();
   public abstract List<Action> getTurnActions() 
}

public class RegularActionFactory extends ActionFactory {...}
public class TestActionFactory extends ActionFactory {...}
```

**14. class SimulationMapConfig**

Это константный класс
```java
public class SimulationMapConfig {
  public final static int MIN_SIZE = 20;
  public final static int MAX_SIZE = 50;
}
```
Он должен быть final и иметь приватный конструктор.  
Не должно быть возможности унаследоваться от константного класса или сделать его экземпляр.

**15. class InputHandler**

- Класс в процедурном стиле- отдельные методы для ввода данных от юзера. 

Эти методы фактически делают одно и то же- ведут с юзером диалог пока он не введет требуемые данные.  
Код при этом дублируется- вечный цикл, печать сообщений и т.д.

Для простой программы- почему бы и нет.  
Более удобная ООП концепция диалогов для консоли изложена [ТУТ](https://github.com/raketareview/simulation_review/blob/master/content/rev-sim076-ivan-LlqWst.md), п."Архитектура".  
Подробнее в моем [стриме](https://t.me/zhukovsd_it_chat/53243/176204) начиная с 6:28. 

- В таких случаях не возвращай массивы. Возвращай объекты, которые означают нужную концепцию
```java
public int[] handleSettingsInput() <-- [0] - ширина карты, [1] - высота карты

//ПРАВИЛЬНО:
public SimulationMapSize handleSettingsInput()

public record SimulationMapSize(int width, int height) {
};
```
*Фаулер "Рефакторинг", гл.3, "Одержимость элементарными типами"*

**16. class SimulationView**

- Не стремись несколько инструкций сводить в одну. Иногда код читается значительно легче, если есть поясняющие переменные
```java
sb.append("Старт/Продолжить - %s; Пауза - %s; Выход - %s;".formatted(InputHandler.START_SIMULATION, InputHandler.PAUSE_SIMULATION, InputHandler.STOP_SIMULATION));

//ПРАВИЛЬНО:
String message = "Старт/Продолжить - %s; Пауза - %s; Выход - %s;".formatted(InputHandler.START_SIMULATION, InputHandler.PAUSE_SIMULATION, InputHandler.STOP_SIMULATION);
sb.append(message);
```

- Эти данные не изменяются, нет смысла каждый раз пересоздавать легенду при очередном вызове метода. 
Сделай её константой
```java
public class SimulationView {
  public void drawWorld(SimulationMap map) {
    // ....
    sb.append("Обозначения: ")
        .append("🟩 Трава  ")
        .append("🟫 Дерево  ")
        .append("⬛ Камень  ")
        .append("🟨 Травоядное  ")
        .append("🟥 Хищник\n");
    sb.append("Старт/Продолжить - %s; Пауза - %s; Выход - %s;".formatted(InputHandler.START_SIMULATION, InputHandler.PAUSE_SIMULATION, InputHandler.STOP_SIMULATION));
    System.out.print(sb);
  }
}
```

- Стандартные имена индексов- i, j, это нормально
```java
for (int i = 0; i < map.height(); i++) {
  for (int j = 0; j < map.width(); j++) {
    Coordinate c = new Coordinate(i, j);
    //печать
  }
}
```

Но иногда лучше использовать другие имена
```java
for (int y = 0; y < map.height(); y++) {
  for (int x = 0; x < map.width(); x++) {
    Coordinate c = new Coordinate(y, x);
    //печать
  }
}
```
И тогда становится понятно, почему карта распечатывается неправильно.  
Это хороший пример того, как нейминг может повлиять на процесс разработки.

- Не бросай базовый эксепшен. Конкретизируй ситуацию- бросай то исключение, которое подходит под этот конкретный случай
```java
try {
  new ProcessBuilder("cmd", "/c", "cls").inheritIO().start().waitFor();
} catch (Exception e) {
  throw new RuntimeException(e);
}
```
*Хорстманн "Java. Библиотека профессионала", т1., гл.11*
```java
"Не ограничивайтесь генерацией RuntimeException. 
Найдите подходящий подкласс или создайте собственный." - Хорстманн.
```

**17. class Simulation**

+ 👍 Принимает в конструктор достаточное количество зависимостей, в том числе списки действий.  
Это позволяет делать майны с разными игровыми конфигурациями, не меняя код в классе `Simulation`
```java
public Simulation(SimulationMap map, SimulationView view, List<Action> initActions, List<Action> turnActions)
```

- Нарушение паттерна GRASP "Creator"(Создатель)

Здесь класс принимает в конструктор зависимость, которую должен создавать самостоятельно:
```java
public Simulation(
  SimulationMap map, <-- Правильно, должен принимать в конструктор
  SimulationView view, <-- Неправильно, должен сам создавать
  ...) {...}
```
Creator гласит, что создавать объект должен тот, кто его использует. 

Рассмотрим, что это значит.

В данном случае `Simulation` принимает в конструктор `SimulationMap`- это правильно.  
Потому что `Simulation` не должен знать, какого размера нужно делать `SimulationMap`, а значит не должен самостоятельно создавать этот объект. 

Но принимать в конструктор SimulationView- уже неправильно.  
Потому что SimulationView это конкретный класс, а не интерфейс и этот класс не параметризируется. 

Наример, экземпляр SimulationMap можно параметризировать, то есть установить ему произвольную ширину и высоту.  
А экземпляр SimulationView параметризировать нельзя- он всегда стандартный.  
Поэтому, согласно паттерна Creator, объект SimulationView *здесь* должен создавать сам класс Simulation.

Технически, можно создать наследника SimulationView, переопределить в нем методы и передавать в конструктор SimulationMap.  
Но если программист хочет использовать в своем классе разные вью через полиморфизм, то он должен обозначить свои намерения более явно.  
Напимер, передавать в конструктор интерфейс или абстрактный класс.

**18. class Main**, содержит точку входа main

+ 👍 Только создает и запускает Симуляцию, в данном случае, через лаунчер. Это хорошо.

## АРХИТЕКТУРА

Единственное уместное использование `record` в этом проекте- Координата.  
Мотивацию record'a изучи [ТУТ](https://openjdk.org/jeps/384)

## ВЫВОД

Есть досадные детские ошибки- неправильная распечатка массива.  
В целом, по архитектуре- норм, замечаний меньше, чем обычно.
 
n.129(282)  
#ревью #симуляция 