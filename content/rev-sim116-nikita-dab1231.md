https://github.com/dab1231/task2  
[никита]

Есть над чем поработать.

## НЕДОСТАТКИ РЕАЛИЗАЦИИ

1. Длинные команды: start/stop
```
Доступные команды: [start], [pause], [exit]
```
Ввод команд и рисование карты используют одну и ту же консоль. 
Поэтому игрок между циклами распечатки карты не всегда успевает набрать и ввести длинные команды.
Делай короткие команды, например: 0/1 или s/p. 

## ХОРОШО

+ 👍 Спрайты существ не хранятся в самих существах
+ 👍 Координаты существ не хранятся в самих существах(мне так больше нравится)
+ 👍 Реализовано пауза/пуск во время работы

## ЗАМЕЧАНИЯ

**О. Лишние файлы в репозитории**

Изучи, какие файлы и папки нужно помещать в репозиторий.

**1. Нейминг**

- Избыточно. Все мы знаем, что означает x/y в координате
```java
public class Coordinate {
  private int xPos;
  private int yPos;
  //...
}

//ПРАВИЛЬНО:
public class Coordinate {
  private int x;
  private int y;
  //...
}
```

- Для избежания путаницы не называй пакеты, классы, интерфейсы так же, как называются классы и интерфейсы стандартной библиотеки.
Есть стандартный интерфейс `Map`, не должно быть кастомного пакета с таким же названием
```java
public class Map

//ПРАВИЛЬНО:
public class GameMap
```

*Oracle Java code conventions, part."Naming conventions"*  
*Мартин, "Чистый код", гл.2*  
*Ютуб, Немчинский "Как называть переменные, методы и классы?"*

**2. Используй классы через их интерфейсы**
```java
private HashMap<Coordinate, Entity> entities;

//ПРАВИЛЬНО:
private Map<Coordinate, Entity> entities;
```
Общее правило: ArrayList нужно использовать через List, HashMap- через Map и т.д.  
Это позволяет пользоваться преимуществами полиморфизма.

Да, бывают ситуации, когда, например, с LinkedList нужно работать именно как с LinkedList, а не с List. 
Но это уже нюансы.  
*"Java. Эффективное программирование", изд.3, гл.9.8*
```
"Если вы выработаете привычку использовать в качестве типов интерфейсы, ваша программа будет гораздо более гибкой" - Блох.
```

**3. Нарушение DRY**, магические буквы, числа, слова. Вводи константы 
```java
System.out.println("Симуляция готова. Введите 'start', 'pause' или 'exit'.");

case "start":  //...
case "pause":  //...
case "exit":   //...

//ПРАВИЛЬНО:
private final static String START = "start";
private final static String PAUSE = "pause";
private final static String EXIT = "exit";

System.out.printf("Симуляция готова. Введите '%s', '%s' или '%s'.  \n", START, PAUSE, EXIT);

case START:  //...
case PAUSE:  //...
case EXIT:   //...
```
*Фаулер, "Рефакторинг", гл.8, "Замена магического числа символической константой"*  
*refactoring.guru "Замена магического числа символьной константой"*  

**4. Разделение по пакетам**

Сейчас в проекте 19 классов и все они лежат в одном пакете, поэтому проект плохо структурирован.  
Если в проекте больше 4-5 классов, то эти классы нужно группировать по пакетам.  
Например, здесь все классы, принадлежащие к типу Entity, нужно вынести в пакет "entities", экшены- в "actions" и т.д.

**5. class Coordinate**

+ 👍 Нет ничего лишнего, это хорошо. 

- Класс может быть преобразован в record без потери функционала.

**6. class Map**

- Конфликт имён.

Следовать ТЗ это хорошо. Называть свои пользовательские классы так же, как стандартные классы и интерфейсы- плохо. 
Второе перевешивает, потому что это приводит к постоянной путанице в коде
```java
class Map

//ПРАВИЛЬНО:
class GameMap
```

Вот к чему приводит называние своих класов так же, как стандартные классы/интерфейсы.  
Приходится прописывать полный путь к стандартным классам, чье имя дублируется.  
Хотя здесь написание полного пути к `HashMap` необязательно, потому что в этом проекте дублируется имя "Map", а не "HashMap"😁:
```java
java.util.HashMap<Coordinate, Creature> creatures = new java.util.HashMap<>();

//ПРАВИЛЬНО:
Map<Coordinate, Creature> creatures = new HashMap<>();
```

- Эти поля не меняются после создаия объекта, а потому должны быть неизменяемыми:
```java
private int width;
private int height;
private HashMap<Coordinate, Entity> entities;

//ПАРВИЛЬНО:
private final int width;
private final int height;
private final HashMap<Coordinate, Entity> entities;
```

- Никогда не возвращай null
```java
private HashMap<Coordinate, Entity> entities;

public Entity getEntity(Coordinate coordinate) {
  return entities.get(coordinate);  <-- может вернуть null
}
```
Возврат null повышает риск возникновения NullPointerException в программе.  
*Мартин, "Чистый код", гл.7.7-8*  
*Ютуб, Немчинский "Почему нельзя возвращать NULL?"*

- При всех операциях с участием координаты(добавить, выдать, удалить и т.д.), нужно проверять координату на корректность. 
Если координата некорректна(находится вне пределов карты), нужно бросать исключение
```java
public boolean isOccupied(Coordinate coordinate) {
  return entities.containsKey(coordinate);
}
```
Сейчас, если у карты спросить, занята ли ячейка с координатой (+100500, -100500), то карта скажет, что свободна. 
А правильный ответ- ячейки с такой координатой в карте нет вообще.

- Нарушение DRY. 

Методы делают одно и то же- считают количество существ заданного класса, при этом дублирую код подсчета
```java
public int getGrassCount() {...}
public int getHerbivoreCount() {...}
```

- Это же за одно является нарушением OCP. 

При добавлении новых типов сущест и необходимости их подсчета, например Птиц, нужно будет изменять класс Карты и добавить в него метод `getBirdCount()`.

- Нарушение SRP. 

Карта должна работать со всеми хранимыми существами одинаково и не работать как-то по-особому с конкретным классом представителей и не должна даже знать по именам наследников Entity
```java
  public java.util.HashMap<Coordinate, Creature> getCreatures() {...}

  public int getGrassCount() {...}

  public int getHerbivoreCount() {...}

//ПРАВИЛЬНО ТАК:
public List<Entity> getAll() //и тогда пусть клиент отбирает отсюда существ нужного класса и/или считает их количество

//ИЛИ ТАК:
public List<? extends Entity> getAllBy(Class<? extends Entity> clazz)  //вернуть существ определенного класса 
```

- Метод совершения хода в карте- нарушение SRP.

Теперь можно вызвать из карты метод хода и телепортировать зайца из одного края в другой минуя все правила игровой логистики
```java
Карта карта = new Карта(100, 100);
карта.addEntity(new Координата(0, 0), new Заяц());
карта.moveEntity(new Координата(0, 0), new Координата (99, 99));

/* class Карта */
  public void moveEntity(Coordinate from, Coordinate to) {
    Entity entity = entities.get(from);
    if (entity == null) {  <-- ПРАВИЛА ДЛЯ ХОДА
      throw new IllegalArgumentException("No entity at the specified coordinate");
    }
    if (to.getX() < 0 || to.getX() >= width
        || to.getY() < 0 || to.getY() >= height) {  <-- ЕЩЁ ПРАВИЛА ДЛЯ ХОДА
      throw new IllegalArgumentException("Target coordinate is occupied ");
    }
    entities.remove(from);
    entities.remove(to);
    entities.put(to, entity);
  }
```
Должна ли карта учитывать логистику зайцев? Если да, то как?  
Здесь метод телепортации частично пытается ответить на этот вопрос. 
Тем самым Карта, фактически, берет на себя часть общеигровой логики- хранит часть правил совершения хода.

Но единственно правильный ответ в этой ситуации- карта вообще не должна содержать в себе метод телепортации.

**7. class PathFinder**

- Сложные правила пользования классом

Сейчас в классе есть несколько публичных методов
```java
public class PathFinder {

  public List<Coordinate> reconstructPath(Coordinate start, Coordinate goal, java.util.HashMap<Coordinate, Coordinate> cameFrom) {...}
  public List<Coordinate> findPath(Coordinate start, Coordinate goal, Map map) {...}
  public Coordinate findNearestTarget(Map map, Coordinate start, Class<?> targetClass) {...}
}
```
Для использования этого класса, и выполнения простого действия поиска пути, нужно вызвать эти методы друг за другом в опредленном порядке.

Нет никаких оснований, чтобы класс был таким сложным для пользования.  
Сделай только один публичный метод, который предназначен для пользования клиентом.  
Например, такой:
```java
public List<Coordinate> findPath( Map map, Coordinate start, Class<? extends Entity> target) {
  //ищет путь на карте от точки start
  //до точки, где находится существо нужного класса(напр. target == Grass.class)
}
```
Если для работы этого метода понадобится предварительно найти координату цели, 
то пусть метод поиска координаты цели будет приватным.  
И вызывать его будет не клиент, а `List<Coordinates> findPath(...)`.

*Вайсфельд "Объектно-ориентированный подход", гл.5, "Минимальный открытый интерфейс"*

- Никогда не возвращай null
```java
public Coordinate findNearestTarget(Map map, Coordinate start, Class<?> targetClass) {
  Coordinate coordinateMinPath = null;
  //...
  return coordinateMinPath;
}
```

- Мапа координата-координата обычно при реализации поиска используется как эрзац-заменитель связного списка
```java
private java.util.HashMap<Coordinate, Coordinate> cameFrom;
```
Вместо мапы координата-координата сделай класс связного списка, алгоритм тогда будет выглядеть понятнее.
Например, так:
```java
class Node {
  private Coordinate coordinate; 
  private Node parent;
  //...
  public Coordinates getCoordinates() {...}
  public Node getParent() {...}
}
```

**8. abstract class Entity и его простые наследники Grass/Rock/Tree**

+ 👍 Идеально
```java
public abstract class Entity {
}

public class Grass extends Entity {
}
```

**9. abstract class Creature extends Entity**

- Обзязательные поля класса должны инициализироваться через конструктор, а не сетиться. 
Конкретно эти поля здесь должны быть final- их значение не должно меняться после создания объекта
```java
public abstract class Creature extends Entity {
  private int speed;
  private int health;

  public void setHealth(int health) {...}
  public void setSpeed(int speed) {...}
  //...
}

//ПРАВИЛЬНО:
public abstract class Creature extends Entity {
  private final int speed;
  private final int health;

  public Creature(int speed, int health) {
    this.speed = speed;
    this.health = health;
  }
}
```

- Креатура должна сама искать свои текущие координаты
```java
public void makeMove(Map map, Coordinate currentPos) {...}

//ПРАВИЛЬНО:
public void makeMove(GameMap gameMap) {
  Coordinate currentPos = gameMap.getCoordinate(this);
  //...
}
```
Креатура все равно в метод хода принимает карту, поэтому может спросить у нее свое текущее местоположение.  

Если креатура будет принимать свое текущее местоположение извне, а не получать самостоятельно из карты, то это потенциально может привести к багам. 
Потому что креатура не может гарантировать, что пришедшая извне координата *действительно* соответствует ее текущему местоположению.  
Слепая вера клиенту тут вредна.

- Не делай сложных вычислений в условиях в циклов, это сильно ухудшает читабельность
```java
for (int i = 1; i < Math.min(getSpeed() + 1, path.size()); i++) {...}

//ПРАВИЛЬНО:
int steeps = Math.min(getSpeed() + 1, path.size());
for (int i = 1; i < steeps; i++) {...}
```


**10. class Herbivore/Predator extends Creature**

- Неправильная перегрузка конструктора
```java
public Herbivore() {
  this.setHealth(10);
  this.setSpeed(1);
}

public Herbivore(int health, int speed) {
  this.setHealth(health);
  this.setSpeed(speed);
}

//ПРАВИЛЬНО:
public Herbivore() {
  this(10, 1);
}

public Herbivore(int health, int speed) {
  this.setHealth(health);
  this.setSpeed(speed);
}
```
*Эккель "Философия Java", гл.5 "Вызов конструктора из конструктора"*

- Нарушение дизайна ООП. 

При создании экземпляра класса, значения обязательных полей предка должны устанавливаться через конструктор, а не сетиться  
```java
public class Herbivore extends Creature {

  public Herbivore() {
    this.setHealth(10);
    this.setSpeed(1);
  }
  //...
}

//ПРАВИЛЬНО:
public class Herbivore extends Creature {
  private static final int HEALTH = 10;
  private static final int SPEED = 1;

  public Herbivore() {
    super(HEALTH, SPEED);
  }
  //...
}
```
*Эккель "Философия Java", гл.5, "Конструктор гарантирует инициализацию"*

- Используй константы- это поможет делать однотипные классы с минимальными усилиями
```java
public class Herbivore extends Creature {

  public Herbivore() {
    this.setHealth(10);
    this.setSpeed(1);
  }

  @Override
  protected Class<?> getTargetType() {
    return Grass.class;
  }
  //...
}

public class Predator extends Creature {

  public Predator() {
    setHealth(10);
    setSpeed(2);
  }

  @Override
  protected Class<?> getTargetType() {
    return Herbivore.class;
  }
  //...
}

//ПРАВИЛЬНО:
public class Herbivore extends Creature {

  private static final int HEALTH = 10;
  private static final int SPEED = 1;
  private static final Class<?> FOOD = Grass.class;

  public Herbivore() {
    super(HEALTH, SPEED)
  }

  @Override
  protected Class<?> getTargetType() {
    return FOOD;
  }
  //...
}

public class Predator extends Creature {

  private static final int HEALTH = 10;
  private static final int SPEED = 2;
  private static final Class<?> FOOD = Herbivore.class;

  public Predator() {
    super(HEALTH, SPEED);
  }

  @Override
  protected Class<?> getTargetType() {
    return FOOD;
  }
  //...
}
```

**11. interface Action**

👍 Идеально
```java
public interface Action {
  void execute(Map map);
}
```

**12. class InitMapActions implements Action**

- Нарушение DRY, дублирование логики
```java
public void execute(Map map) {
  for (int i = 0; i < herbivoreCount; i++) {
    Herbivore herbivore = new Herbivore();
    addRandomEntity(map, herbivore);
  }

  for (int i = 0; i < predatorCount; i++) {
    Predator predator = new Predator();
    addRandomEntity(map, predator);
  }
  //...
}

//ПРАВИЛЬНО:

public void execute(Map map) {
  spawn(map, herbivoreCount, () -> new Herbivore());
  spawn(map, predatorCount, () -> new Predator());
  //...
}

private void spawn(Карта карта, int count, Supplier<Entity> entitySupplier) {
  for (int i = 0; i < count; i++) {
    Entity entity = entitySupplier.get();
    addRandomEntity(карта, entity);
  }
}
```

**13. class MoveCreaturesAction implements Action**

👍 Все ок, только свое положение существо должно само спрашивать у карты
```java
creature.makeMove(map, position);

//ПРАВИЛЬНО:
creature.makeMove(map);
```

**14. class SpawnHerbivoresActions/SpawnGrassAction implements Action**

Нарушение DRY. Два класса, которые делают одно и то же- поддерживают популяцию конкретного вида существ на карте
```java
public class SpawnGrassAction implements Action {

  @Override
  public void execute(Map map) {
    int count = map.getGrassCount();
    if (count <= 2) {
      for (int i = 0; i < 3; i++) {
        Coordinate coordinate = InitMapActions.getRandomFreeCoordinate(map);
        map.addEntity(coordinate, new Grass());
      }
    }
  }
}
```

Вместо отдельных классов под каждый вид существ, нужно сделать один универсальный
```java
public class SpawnAction<T> implements Action {

  private final Class<T> target;
  private final int limit;
  private final Supplier<T> entitySupplier;

  public SpawnAction(Class<T> target, int limit, Supplier<T> entitySupplier) {...}
  
  @Override
  public void execute(Map map) {
    //подсчитать в карте существ класса target
    //если их меньше limit,
    //то восстановить численность с помощью entitySupplier
  }
}
```

Использование:
```java
SpawnAction<Grass> grassSpawnAction = new SpawnAction<>(Grass.class, GRASS_NUM_LIMIT, Grass::new);
```

**15. class Simulation**

+ 👍 Правильное использование экшенов в виде двух списков. Только они тут должны быть final
```java
private List<Action> initActions;
private List<Action> turnActions;
```

- Нарушение конвенции кода, конструктор должен стоять первым из методов.

- Класс не должен сам создавать карту. Пустую карту он должен принимать в конструктор
```java
public Simulation(int width, int height, ....)

//ПРАВИЛЬНО:
public Simulation(GameMap gameMap, ....)
```

- Длинный список аргументов. 

Возможно, количество существ нужно сгрупировать в отдельный record и передавать его в конструктор `Simulation` в качестве объекта-аргумента
```java
public Simulation(int width, int height, int herbivoreCount, int predatorCount,
                    int grassCount, int rockCount, int treeCount)
```
*"ЧК", гл.3, "Объекты как аргументы".*

- В этом классе не должно быть точки входа main
```java
public class Simulation {
  
  public static void main(String[] args) {
    Simulation simulation = new Simulation(10, 10, 5, 2, 5, 5, 5);

    SimulationController controller = new SimulationController(simulation);
    controller.startControlLoop();
  }
}

//ПРАВИЛЬНО:
public class Main {
  
  public static void main(String[] args) {
    Simulation simulation = new Simulation(...);
    //...
  }
}
```

Тем более, тут складывается циклическая зависимость: Simulation хранит SimulationController, а тот в свою очередь хранит Simulation
```java
public class Simulation {
  
  public static void main(String[] args) {
    //...
    SimulationController controller = new SimulationController(simulation);  <-- ЦИКЛИЧЕСКАЯ ЗАВИСИМОСТЬ
  }
}
```
Отделяй создание системы от ее использования.  
*"ЧК", гл.11, "Отделение конструирования системы от ее использования".*

## ВЫВОД

Есть над чем поработать, но в целом замечаний меньше, чем обычно.

n.116(264)  
#ревью #симуляция 