https://github.com/j0797/simulation-clean  
[J.]

Есть над чем поработать.

## НЕДОСТАТКИ РЕАЛИЗАЦИИ

1. Карта распечатывается неровно, нужно подобрать спрайты одинаковой ширины  
![pic](https://github.com/raketareview/simulation_review/blob/master/content/resources/rev-sim126/img0.png) 

2. Нет паузы/пуск во время работы. Хотя это требование есть в ТЗ.

## ХОРОШО

+ 👍 Спрайты существ не хранятся в самих существах
+ 👍 Координаты существ не хранятся в самих существах(мне так больше нравится)

## ЗАМЕЧАНИЯ

**1. Нейминг**

- Во избежание путаницы не называй пакеты, классы, интерфейсы так же, как называются классы и интерфейсы стандартной библиотеки.  
Есть стандартный интерфейс `Map`, не должно быть кастомного пакета с таким же названием
```java
package map;

//ПРАВИЛЬНО:
package worldmap;
```

- Перечни в виде массивов, сетов, листов и мап, должны назваться во множественном числе
```java
Map<Class<? extends Entity>, Set<Coordinates>> entityIndex

//ПРАВИЛЬНО:
Map<Class<? extends Entity>, Set<Coordinates>> entityIndexes
```

- Это экшен в единственном числе
```java
class InitActions implements Action 

//ПРАВИЛЬНО:
class InitAction implements Action 
```
*Oracle Java code conventions, part."Naming conventions"*  
*Мартин, "Чистый код", гл.2*  
*Ютуб, Немчинский "Как называть переменные, методы и классы?"*

**2. Нарушение конвенции кода**

В любой ситуации выделяй тело блока скобочками, даже если тело состоит из одной строки. 
Исключение- метод equals(), там можно после if не выделять блоки скобочками
```java
if (!isValidCoordinate(from) || !isValidCoordinate(to)) return false;
if (from.equals(to)) return false;

//ПРАВИЛЬНО:
if (!isValidCoordinate(from) || !isValidCoordinate(to)) {
  return false;
}
if (from.equals(to)) {
  return false;
}
```

**3. Форматирование строк**

Если нужно печатать или создавать строку с более, чем одним подстановочным значением или значение вставляется внутрь сообщения, используй форматирование- тогда сразу будет виден весь шаблон
```java
System.out.println("Добавлено: " + herbivoresAdded + " травоядных, " + predatorsAdded + " хищников");

//ПРАВИЛЬНО:
System.out.printf("Добавлено: %d травоядных, %d хищников  %n", herbivoresAdded, predatorsAdded);
```

**4. record Coordinates(int row, int column)**

+ 👍 Нет ничего лишнего, это хорошо. Record для координаты- идеально.

+ 👍 Хорошо, что поля координаты называются row/column а не y/x. Мне так больше нравится.  
В этом случае нет, как часто бывает, путаницы с порядком расположения в массивах: координата(x,y), а в массивах наоборот- массив[y, x].

**5. class WorldMap**

- Сложный, избыточный способ хранения существ с использованием двух коллекций
```java
private final Map<Coordinates, Entity> entities = new HashMap<>();  <-- ЭТОГО ДОСТАТОЧНО 
private final Map<Class<? extends Entity>, Set<Coordinates>> entityIndex = new HashMap<>();  <-- ЭТО ЛИШНЕЕ
```
Такой способ хранения приводит к тому, что все простые операции с существами выполняются очень сложно и мудрёно.  
Например, вот так сейчас выглядит вставка существа в карту:
```java
public boolean placeEntity(Coordinates coordinates, Entity entity) {
  Entity existing = entities.get(coordinates);
  if (existing != null) {
    removeEntityFromIndex(existing.getClass(), coordinates);
  }
  entities.put(coordinates, entity);
  entityIndex.computeIfAbsent(entity.getClass(), k -> new HashSet<>())
      .add(coordinates);
  return true;
}
```
Нужно долго думать, чтобы понять, что здесь происходит.

При правильном хранении существ только в одной коллекции, этот метод выглядит очень просто:
```java
public vpod putEntity(Coordinates coordinates, Entity entity) {
  validate(coordinates); //если координата вне пределов карты- бросает исключение
  entities.put(coordinates, entity);
}
```

Зачем вообще нужна эта коллекция?
```java
private final Map<Class<? extends Entity>, Set<Coordinates>> entityIndex = new HashMap<>();
```
Она позволяет быстро получать перечень координат существ определенного класса.  

Но самой карте для выполнения ее единой ответственности по хранинию существ и обеспечении к им доступа, не нужно уметь создавать перечень существ определенного класса.  
И тем более не нужно для этого держать специальную информационную структуру в виде сета.

Здесь достаточно иметь методы 
```java
public List<Entity> getAllEntities() //возвращает список всех хранимых entity
public List<Entity> getCoordinates(Entity entity)
```
Тогда клиентский код, которому надо для выполнения своей единой отвественности координаты всех существ определенного класса(напр. класса Creature),
может получить из карты список всех существ, отфильтровать существ нужных классов и создать список их координат.

Твой способ с `Map<...> entityIndex` будет работать быстрее? Безусловно.  
Но у нас здесь приоритет состоит не в написании более быстрых алгоритмов.  
Для нас приоритет- простая, понятная, легко масштабируемая архитектура.

Если приходится выбирать между простым более медленным алгоритмом и быстрым, но громоздким, нужно выбирать простой.  
Оптимизацию по скорости нужно делать только в том случае, если готовая программа будет тормозить и вызывать тем самым неудовольствие юзера.  
Нам здесь это не грозит- программа в любом случае будет очень легкая. 

Преждевременная оптимизация это антипаттерн.

+ 👍 Это хороший метод, имеет здесь право на жизнь
```java
public <T extends Entity> List<T> getEntitiesOfType(Class<T> type)
```
Это более удобная альтернатива использованию `getAllEntities()` + `getCoordinates()` из примера выше.  
Но формировать список существ определенного типа нужно не из `entityIndex`, а сразу из `entities`.

- Нарушение SRP, методы чужих ответственностей. 

Карта должна только хранить существа и обеспечить базовые операции с ними: вставить, выдать одно существо и список всех хранимых существ, удалить.  
Если какой-то метод не нужен для обеспечения этого функционала, значит он не принадлежит к ответственности карты, а принадлежит к чужой ответственности.  
Здесь методы чужих ответственностей: вернуть соседние координаты, вернуть соседние пустые координаты, вернуть случайную пустую координату и т.д.

Наверное, для проекта в целом полезно иметь метод, который находит и возвращает список соседних пустых координат. 
Но этот процесс не имеет никакого отношения к единой ответственности карты- хранению существ в себе.  
Для организации хранения/удаления/выдачи существ, карте не нужно искать соседние пустые координаты.  

Методы чужих ответственностей должны находиться в тех классах, в интересах которых они работают.
Если один и тот же метод используют разные классы, то метод нужно вынести в отдельный класс, например, BoardUtils.

- Никогда не возвращай null
```java
private final Map<Coordinates, Entity> entities = new HashMap<>();

public Entity getEntity(Coordinates coordinates) {
  return entities.get(coordinates);  <-- может вернуть null
}
```
Возврат null повышает риск возникновения NullPointerException в программе.  
*Мартин, "Чистый код", гл.7.7-8*  
*Ютуб, Немчинский "Почему нельзя возвращать NULL?"*

- При всех операциях с участием координаты(добавить, выдать, удалить и т.д.), нужно проверять координату на корректность.  
Если координата некорректна(не находится в пределах карты), нужно бросать исключение
```java
public void removeEntity(Coordinates coordinates) {
  Entity entity = entities.remove(coordinates);  <-- ПЕРЕД УДАЛЕНИЕМ СУЩЕСТВА НУЖНО ПРОВАЛИДИРОВАТЬ КООРДИНАТУ
  if (entity != null) {
    removeEntityFromIndex(entity.getClass(), coordinates);
  }
}
```
Ближайшая аналогия- стандартные хранилища типа List и массива. При попытке обратиться к ним по несуществующему индексу, бросается исключение.

- Метод совершения хода в карте- нарушение SRP.
Теперь можно вызвать из карты метод хода и телепортировать зайца из одного края в другой минуя все правила игровой логистики
```java
Карта карта = new Карта(100, 100);
карта.placeEntity(new Координата(0, 0), new Заяц());
карта.moveEntity(new Координата(0, 0), new Координата (99, 99));

/* class Карта */
public boolean moveEntity(Coordinates from, Coordinates to) {
  //18 строк логики  
}
```
Должна ли карта учитывать логистику зайцев? Если да, то как?  
Единственно правильный ответ- карта вообще не должна иметь в себе метод телепортации.

**6. class BreadthFirstSearch**

- Нарушение SRP.

BFS это алгоритм поиска пути. Путь это последовательность координат до цели.  
В этом классе должен быть только один публичный метод- тот, который ищет путь.

Поиск следующего шага к цели или поиск ближайшей координаты с едой- это всё что угодно, но не поиск пути 
```java
public Optional<Coordinates> getNextStepToTarget(...) 
public Optional<Coordinates> findNearestGrass(WorldMap map, Coordinates start) 
public Optional<Coordinates> findNearestHerbivore(WorldMap map, Coordinates start)
```

Поиск должен просто искать путь от точки старта до точки, соответствующей заданным условиям.  
В данном случае- до точки, в которой находится существо нужного класса.  
Сигнатура метода для этого должна выглядеть примерно так
```java
public List<Coordinates> find(Карта карта, Coordinates start, Class<? extends Entity> target) {
  //ищет путь на карте от точки start
  //до точки, где находится существо нужного класса(напр. target == Grass.class)
}
```
Это должен быть *единственный* публичный метод в классе.  
Что дальше будут делать клиенты с найденным путем и как конкретно по нему будут идти существа- не должно быть заботой класса `BreadthFirstSearch`.

**8. abstract class Entity и его простые наследники Grass/Tree/Rock**

👍 Идеально
```java
public abstract class Entity {
}

public class Grass extends Entity {
}
```

**7. abstract class Creature extends Entity**

Нарушение ТЗ. Не вижу метода `makeMove(...)`
```
Creature #
Абстрактный класс, наследуется от Entity. ...Имеет метод makeMove() - сделать ход.
```

Существа здесь не имеют `makeMove(...)` потому что самостоятельно не ходят.  
Их переставляет чья-то чужая воля, как шахматист переставляет фигуры на шахматной доске. 

Чисто функционально- почему бы и нет. Но это нарушет ТЗ проекта, а это не очень хорошо.  
Нужно выполнять требования ТЗ. Нельзя на работе нарушать ТЗ просто так.  

**9. class Predator extends Creature**

Неправильная перегрузка конструктора
```java
public Predator() {
  super(2, 30, 15);
  this.attackPower = 10;
}

public Predator(int speed, int maxHunger, int healthpoints, int attackPower) {
  super(speed, maxHunger, healthpoints);
  this.attackPower = attackPower;
}

//ПРАВИЛЬНО:
public Predator() {
  this(DEFAULT_SPEED, DEFAULT_MAX_HUNGER, DEFAULT_HP, DEFAULT_ATTACK_POWER);
}

public Predator(int speed, int maxHunger, int healthpoints, int attackPower) {
  super(speed, maxHunger, healthpoints);
  this.attackPower = attackPower;
}
```
*Эккель "Философия Java", гл.5 "Вызов конструктора из конструктора"*

**10. interface Action**

👍 Идеально
```java
public interface Action {
  void perform(WorldMap map);
}
```

**11. class InitActions implements Action**

Нарушение SRP, чужая ответственность, зависимость модели от представления.

Модель(а бизнес логика экшенов это модель) не должна ничего печатать в консоль
```java
private void printInitialStats(WorldMap map) {
  System.out.println("Начальная статистика:");
  System.out.println("   Размер мира: " + map.getWidth() + "x" + map.getHeight());
  //...
}
```
Иначе модель перестает быть универсальной и становится заточенной под конкретную среду и конкретное представление себя в этой среде- в данном случае, консоль.
В других средах нужно будет менять код в этом классе, чтобы вывод осуществлялся по правилам этой среды, то есть, это лишняя причина для изменения класса.
В других средах(напр. Андроид) эту модель нельзя будет использовать- она там просто не скомпилируется. 
Другое представление для модели(напр. если одну и ту же модель нужно в программе показвать по-разному) нельзя будет сделать, или придется делать через костыль.

Вот к чему приводит зависимость модели от представления, карта рисуется в виндовый интерфейс, а модель пишет в консоль:  
https://github.com/raketareview/simulation_review/blob/master/content/rev-sim064-davidtagirov-DavidTagirov.md

Если модель должна что-то сообщить миру, она может это сделать через паттерн CallBack.  
[CallBack луноход](https://t.me/zhukovsd_it_chat/53243/139594)  
[CallBack разрушение](https://t.me/zhukovsd_it_chat/53243/184749)

Но в данном случае, распечатать инфу со статистикой карты можно в `Simulation` сразу после вызова initAction'ов.

**12. class TurnManager implements Action**

- Почему это `Manager`, если это `Action`? Гугли, что в java понимается под классами с именем `Manager`.

- Нарушение SRP.

Из-за того, что креатуры самостоятельно не управляют своим движением, код в этом классе сложен и загадочен
```java
public TurnManager() {
  this.turnActions = Arrays.asList(
    new CreatureStateUpdate(),
    new CreatureMovement(),
    new CreatureFeeding(),
    new DeathProcessing(),
    new GrassRegrowth(),
    new PopulationControl(),
    new StatisticsCollection()
  );
}

@Override
public void perform(WorldMap map) {
  for (Action action : turnActions) {
    action.perform(map);
  }
}
```
Здесь я вижу выполнение нескольких разных ответственностей:  
Совершение креатурами хода, что включает в себя перемещение и поедание.  
Восстановление популяции существ.  
Печать статистики.

Один экшен должен выполнять одно дело, как указано в ТЗ
```
Action - действие, совершаемое над миром. Например - сходить всеми существами. 
Это действие итерировало бы существ и вызывало каждому makeMove(). 
Каждое действие описывается отдельным классом и совершает операции над картой. 
```
Причем, печать статистики это вообще не имеет никакого отношения к экшенам.  
Action'ы не должны работать с представлением(печать в консоль), они должны содержать только бизнес логику работы с картой.

Не должно быть экшена, который содержит кучу разных других экшенов.

**13. Action'ы в пакете steps**

Куча классов с кучей логики. 
Большое количество кода, причина которого- несоблюдение ТЗ, а местами неправильное понимание идеи экшенов.

**14. Идея классов Action как варинта паттерна Command**

Идея Action'ов как таковых состоит в том, что нужно создать семейство родственных классов, объединенных общим интерфейсом.  
Каждый из этих классов должен делать что-то свое с картой: одна акция должна заселять карту существами, другая делать ходы и т.д.  

Смысл Action'ов состоит в том, что должен быть общий класс/интерфейс Action и его наследники.  
В каждом экшене должен быть только один публичный метод(не публичных может быть сколько угодно).  
Это вариация паттерна Command- экшены должны быть родственны и одинаково использоваться через полиморфизм.  
Примерно так
```java
interface Action{
 void execute(Карта карта);
}

class ХодитьAction реализует Action {
  void execute(Карта карта) {
    //обойти всю карту
    //найти каждую креатуру
    //и дать ей пинка чтоб побежала
  }
}

class ДатьСигаретуВсемЗайцамAction реализует Action {
  void execute(Карта карта) {
    //обойти всю карту
    //найти всех зайцев и дать им по сигарете 
  }
}

List<Action> actions = List.of(new ДатьСигаретуВсемЗайцамAction(), new ХодитьAction());
for(Action a: actions) {
  a.execute(карта);
}

/*
Результат: программа обойдет всю карту, найдет всех креатур и даст им пинка, чтобы они побежали.
А каждому зайцу предварительно даст сигарету.
*/
```

При этом экшен движения должен не самостоятельно передвигать ноги каждой креатуре, а только инициировать её движение с помощью пинка.
То есть, вызвать в каждой креатуре метод `makeMove(...)`.  
Передвигаться креатуры должны самостоятельно.  
Выглядеть это должно примерно так
```java
class Movection реализует Action {

  @Override
  public void execute(Карта карта) {
    List<Creature> creatures = getCreatures(карта);
    for(Creature creature: creatures) {
      creature.makeMove(карта);  //даёт пинка
    }
  }

  private static List<Creature> getCreatures(Карта карта) {
    //найти и вернуть все креатуры из карты
  }
}
```

**15. class Simulation**

- Класс должен принимать в конструктор уже созданный экземпляр пустой карты, а не создавать ее самостоятельно
```java
public Simulation(int width, int height, long turnDelayMs) 

//ПРАВИЛЬНО:
public Simulation(WorldMap worldMap, long turnDelayMs) 
```

- Неправильное использование экшенов
```java
private final TurnManager turnManager;
```

Как указано в ТЗ, должо быть две группы экшенов:
```
Симуляция содержит 2 массива действий:
initActions - действия, совершаемые перед стартом симуляции...
turnActions - действия, совершаемые каждый ход....
```

Нужно сделать их так, чтобы можно было легко изменять состав экшенов в этих группах.  
А сами экшены при этом должны одинаково работать через полиморфизм.  
Примерно так:
```java
private final List<Action> initActions = List.of(new UnoAction());
private final List<Action> turnActions = List.of(new DosAction(), new TresAction(), new MoveAction());

public Simulation(...) {
  //...
  executeActions(initActions);  //выполнение экшенов на старте
}

private void nextTurn() {
  //...
  executeActions(turnActions);  //выполнение экшенов на каждом ходе
  renderer.render(worldMap);
}

private void executeActions(List<Action> actions) {
  for(Action a : actions) {
    a.execute(board);
  }
}
```

- Класс не должен возвращать свою карту. С какой целью он это делает?
```java
private final WorldMap map;

public WorldMap getMap() {
  return map;
}
```

- Магические числа: 40, 50, 10, 2 etc.

- Распечатку статистики я бы вынес в отдельный класс. Только не делай такой класс экшеном- это другое.

**16. class Main**, содержит точку входа main

+ 👍 Только создает и запускает Симуляцию, это хорошо.

## ВЫВОД

Доделать функционал по ТЗ.  
Подробнее разберись с Action'ами, посмотри ролики на ютубе про паттерн "Command", чтобы получить о нем общее представление.

Для лучшего понимания декомпозиции ООП, посмотреть ролики Сергея про шахматы.  
Посмотреть ролики Немчинского про SOLID

n.126(277)  
#ревью #симуляция 