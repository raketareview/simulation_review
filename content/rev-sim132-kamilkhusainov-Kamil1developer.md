https://github.com/Kamil1developer/Simulation  
[Kamil KHusainov]

Есть очень много, над чем нужно поработать.

## НЕДОСТАТКИ РЕАЛИЗАЦИИ

1. Нажать на цифры недостаточно. Их нужно ввести
```
Для паузы игры нажмите на 0
Для окончания игры нажмите на 2 
```

## ХОРОШО

+ 👍 Реализовано пауза/пуск во время работы
+ 👍 Алгоритм поиска A*

## ЗАМЕЧАНИЯ

**1. Нейминг**

- Для избежания путаницы не называй пакеты, классы, интерфейсы так же, как называются классы и интерфейсы стандартной библиотеки.  
Есть стандартный интерфейс `Map`, не должно быть кастомного пакета с таким же названием
```java
package map;

//ПРАВИЛЬНО:
package gamemap;
```

- Избыточно. Не вижу, что полезного здесь дает уточнение "index"
```java
record Position(int rowIndex,int columnIndex)

//ПРАВИЛЬНО:
record Position(int row,int column)
```

- Название путает.  

"Canvas" означает "холст"- поверхность, на которой рисуют. Так в графических библиотеках называют специальный объект, на который выводят изображение.
Здесь это не холст, а карта игры
```java
class MapCanvas

//ПРАВИЛЬНО:
class GameMap
```

- Если в проекте есть класс `MapCanvas`, то все переменные с именем, включающим это название, должны быть экземплярами этого класса.
Если разные концепции называются одним и тем же именем, это приводит к путанице
```java
public class MapCanvas {
  private Map<Position, Entity> mapCanvas = new HashMap<>();
  //...
}

//ПРАВИЛЬНО:
public class MapCanvas {
  private Map<Position, Entity> positionWithEntities = new HashMap<>();
  //...
}
```

- Однобуквенные названия, основном, корректны только в качестве названий индексов в циклах.  
Эти однобуквенные названия некорректны- непонятно, что они означают
```java
private final int n, m;
```

- Однобуквенное название "e" принято дпапть только экземплярам Excrption'ов
```java
void setMapGrass(Entity e)

//ПРАВИЛЬНО:
void setMapGrass(Entity entity)
```

- Метод называется сеттером травы. Поэтому и сетить он должен траву, а не что-то иное
```java
void setMapGrass(Entity e)

//ПРАИВЛЬНО:
void setMapGrass(Grass grass)
```

- Это не сеттер.

Сеттеры должны устанавливать укакое-то значение какого-то поля.  
Здесь данные не устанавливаются, а добавляются к уже имеющимся 
```java
public void setMapGrass(Entity e) {
  mapGrass.put(e.getPosition(), e);
}

//ПРАВИЛЬНО:
public void putGrass(Grass grass) {...}
```

- Геттер должен называться так же, как возвращаемое им поле + префикс "get"
```java
public Map<Position, Entity> getMap() {
  return mapCanvas;
}

//ПРАВИЛЬНО:
public Map<Position, Entity> getMapCanvas() {
  return mapCanvas;
}
```

- КОНСТАНТЫ_НУЖНО_ПИСАТЬ_СТИЛЕМ_UPPER_SNAKE
```java
private static final String emoji = "\uD83C\uDF3F";

//ПРАВИЛЬНО:
private static final String EMOJI = "\uD83C\uDF3F";
```

- Константы должны быть static
```java
private final double BASE_PERCENT = 0.1;
private final int COMPLETE_SIMULATION = 2;

//ПРАВИЛЬНО:
private final static double BASE_PERCENT =
private final static int COMPLETE_SIMULATION = 2; 0.1;
```
*Oracle Java code conventions, part."Naming conventions"*  
*Мартин, "Чистый код", гл.2*  
*Ютуб, Немчинский "Как называть переменные, методы и классы?"*

**2. Нарушение DRY**, магические буквы, числа, слова. Вводи константы. А если они уже есть- пользуйся 
```java
private final int COMPLETE_SIMULATION = 2;
private final int PAUSE_SIMULATION = 0;
private final int CONTINUE_SIMULATION = 1

System.out.println("Введите 1 или 2 " + "\n");
System.out.println("Введите 0 или 2 " + "\n");
System.out.println("Для начала игры нажмите на 1 \n");

//ПРАВИЛЬНО:
System.out.printf("Введите %d или %d  %n", CONTINUE_SIMULATION, COMPLETE_SIMULATION);
System.out.println("Введите %d или %d  %n", PAUSE_SIMULATION, COMPLETE_SIMULATION);
System.out.printf("Для начала игры нажмите на %d  %n", CONTINUE_SIMULATION);
```
*Фаулер, "Рефакторинг", гл.8, "Замена магического числа символической константой"*  
*refactoring.guru "Замена магического числа символьной константой"*

**3. Нарушение DRY**, магические буквы, числа, слова. Вводи константы. 

Если к одной и той же магической штуке обращаются два разных класса, то делай ее константой в одном из этих двух классов. 
Либо перенеси ее в третий класс- эти данные должны быть синхронизированы между собой
```java
class Task implements Runnable {
  Simulation simulation;
  private final int COMPLETE_SIMULATION = 2;
  private final int PAUSE_SIMULATION = 0;
  private final int CONTINUE_SIMULATION = 1;
  //...
}

public class Simulation {
  public void startSimulation() throws InterruptedException {
    System.out.println("Для начала Симуляции нажмите на 1 ");  <-- ЭТИ КОНСТАНТЫ ХРАНЯТСЯ В class Task
    //...
  }

  private void processSimulation(ExecutorService executorService) throws InterruptedException {
    System.out.println("Для паузы игры нажмите на 0");
    System.out.println("Для окончания игры нажмите на 2 \n");
    //...
  }

  public synchronized void pauseSimulation() {
    System.out.println("Нажмите 1, чтобы продолжить ");
    System.out.println("Нажмите 2, чтобы закончить ");
    //...
  }

private int startCommand(Scanner sc) {
    System.out.println("Введите 1 для начала Симуляции! ");
    //...
  }
}
```

**4. record Position(int rowIndex,int columnIndex)**

👍 Нет ничего лишнего, это хорошо. Record для координаты- идеально
```java
public record Position(int rowIndex,int columnIndex) {
}
```

**5. class MapCanvas**, карта игры

- Лишние поля. Достаточно только одной мапы `Map<Position, Entity>` 
```java
private Map<Position, Entity> mapCanvas = new HashMap<>();  <-- ЭТОГО ДОСТАТОЧНО
private Map<Position, Entity> mapGrass = new HashMap<>();  <-- ЛИШНЕЕ
private Map<Position, Entity> mapHerbivore = new HashMap<>();  <-- ЛИШНЕЕ
```
В классе должна быть только одна мапа, которая хранит записи координата-существо.  
Этого будет достаточно для всех действий, которые должна выполнять Карта.

Несколько коллекций для хранения существ нарушают чеклист ТЗ:
```java
Проблемы и ошибки в коде:
Класс Map
параллельные коллекции для разных типов существ (для каждого типа своя коллекция)
```

- Нарушение OCP.

Карта должна работать со всеми хранимыми существами одинаково и не работать как-то по-особому с конкретными классами-наследниками Entity.  
Здесь карта имеет отдельные методы для работы с каждым наследником Entity
```java
public void setMapGrass(Entity e) {
  mapGrass.put(e.getPosition(), e);
}

public void setMapHerbivore(Entity e) {
  mapHerbivore.put(e.getPosition(), e);
}

//ПРАВИЛЬНО:
public void putEntity(Entity entity) {
  mapGrass.put(e.getPosition(), e);
}
```
Да, по факту, это одинаковые методы и отличаются они только названием. 
Но подразумевается, что каждый из этих методов должен работать с конкретным типом существ.

- Нарушение конвенции кода 
```java
private final int n, m;

//ПРАВИЛЬНО:
private final int n;
private final int m;
```

- Неправильный тип возвращаемого значения.

Не возвращай результат в виде массива или коллекций тогда, когда возвращаемое значение означает концепцию, состояющую из фиксированного количество значений.  
А не просто последовательность каких-то значений произвольной длины
```java
public List<Integer> getMapSize() {
  return List.of(n, m);
}

//ПРАВИЛЬНО ТАК:
public Size getMapSize() {
  return new Size(width, height);
}

public record Size(int width, int height){
}

//ТАК ТОЖЕ ПРАВИЛЬНО:
public getWidth() {
  return width;
}

public getHeight() {
  return height;
}
```

- Нарушение инкапсуляции, класс предоставляет клиентскому коду подробности внутреннего устройства и доступ к нему
```java
private Map<Position, Entity> mapCanvas = new HashMap<>();

public Map<Position, Entity> getMap() {
  return mapCanvas;
}
```
В данный момент клиентский код может выполнять любые операции с HashMap Карты напрямую, игнорируя допустимые картой способы. 
Например так
```java
Карта карта = new Карта(100, 100);
<заселить карту существами>
карта.getMap().clear(); //геноцид- удаление из карты всех существ, минуя дозволенные картой механизмы
```
Из-за этого класс является гибридом со всеми вытекающими последствиями: *"Чистый код", гл.6*

Тут есть два варианта, как сделать правильно.  
Либо вернуть не оригинал мапы, а ее копию: 
```java
public Map<Position, Entity> getMap() {
  return new HashMap(mapCanvas);
}
```
Либо возвращать существ персонально:
```java
public Entity get(Position position)
public Position getPosition(Entity entity)
```
Лично мне больше нравится второй вариант, когда существ возвращают персонально.

- Проверяй короткие имена классов, а не полные
```java
switch (entry.getValue().getClass().getName()) {
  case "main.entities.creature.Herbivore": return Herbivore.class;
  //...
}

//ПРАВИЛЬНО:
String name = entry.getValue().getClass().getSimpleName();
switch (name) {
  case "Herbivore": return Herbivore.class;
  //...
}
```

- Метод не выполняет свой контракт. 

Обещает вернуть класс типа Entity, но может вернуть и Optional.class
```java
public Class<?> getTypeEntity(int rowIndex, int columnIndex) {
  //...  
  return Optional.class;
}
```

- При всех операциях с участием координаты(добавить, выдать, удалить и т.д.), нужно проверять координату на корректность.

Если координата некорректна(находится вне пределов карты), нужно бросать исключение.  
Потому что сейчас можно поместить существо на координату, которая будет выходить за пределы карты:
```java
public void setMapGrass(Entity e) {
  mapGrass.put(e.getPosition(), e);
}
```
Ближайшая аналогия- стандартные хранилища типа List и массива.  
При попытке поместить данные по несуществующему индексу, бросается исключение.

- Нарушение SRP и инкапсуляции.

Для пользования картой, в нее нужно вставлять, получать и удалять существо.  
В этом классе я вижу только метод добавления существа.  
Значит, сторонние классы читают и удаляют существ из Карты с помощью каких-то обходных путей.  
А должны это делать только через методы самой карты.


**6. class PathFinder**

- Слишком большой размер.

Просто какой-то гигантский класс на 400 строк.  
Уже по размеру понятно, что с классом что-то не так.    
Класс поиска пути должен быть минимум в 4 раза короче.

- Этот класс не ищет путь.

Путь это последовательность точек от точки старта до точки финиша.  
Этот класс не ищет путь. Его единственный публичный метод возвращает какую-то координату
```java
public Position findPath(MapCanvas mapCanvas, Creature creature)
```
Как с помощью этого класса искать путь, совершенно неясно.

Поиск пути должен просто искать путь от точки старта до точки, соответствующей заданным условиям согласно алгоритма BFS.  
В данном случае- до точки, в которой находится существо нужного класса
```java
public List<Coordinates> find(World world, Coordinates start, Class<? extends Entity> target) {
  //ищет путь на карте world от точки start
  //до точки, где находится существо нужного класса(напр. target == Grass.class)
}
```

Или поиск должен искать путь между точками старта и финиша, согласно алгоритма A*
```java
public List<Coordinates> find(World world, Coordinates from, Coordinates to) {...}
```

Так же, поиск пути по алгоритму A* может использовать ту же сигнатуру, что и BFS.  
Тогда поиск пути в нем интерпритируется не как поиск пути между двумя точками, а как поиск цели и прокладывание пути к ней.  
Например: 
```java
public List<Coordinates> find(World world, Coordinates start, Class<? extends Entity> target) {
  //ищет координату цели
  //ищет путь по алгоритму A* между точкой стартой и точкой цели
}
```
Плюсы- унификация сигнатуры разных алгоритмов поиска для полиморфизма.  
То есть, можно сделать семейство родственных классов поиска, объединенных общим интерфейсом.

- Дублирование кода.

Одна из причин раздутого кода в этом классе- дублирование. Например:
```java
private List<Position> findFoodForHerbivore(MapCanvas mapCanvas, Creature creature) throws NoSuchElementException {
  for (Map.Entry<Position, Entity> m : mapCanvas.getMapGrass().entrySet()) {
    findHScoreForCreature(m.getKey(), m.getValue(), creature);
  }
  //остальное то же самое
}

private List<Position> findFoodForPredator(MapCanvas mapCanvas, Creature creature) {
  for (Map.Entry<Position, Entity> m : mapCanvas.getMapHerbivore().entrySet()) {
    findHScoreForCreature(m.getKey(), m.getValue(), creature);
  }
  //остальное то же самое
}
```
Общий код выноси во вспомогательные методы.

- Нарушение SRP, OCP
```java
private boolean isObstacle(Class<?> object, Object eater) {
  if ((object == Grass.class) && (eater instanceof Herbivore)) {
    return false;
  }
  if ((object == Herbivore.class) && (eater instanceof Predator)) {
    return false;
  }
  //...
}
```
Нарушение SRP: Поиск пути не должен в себе хранить правила, кто для кого препятствие или кто для кого еда.  
Эти правила должны храниться в самих Entity. 
И если эта информация нужна для процесса поиска, топоиск должен ее получать из самого Entity

Нарушение OCP: Если поиск хранит эту информацию в себе, то при добавлении новых типов существ, придется добавлять правила этих существ в класс поиска.  
Например:
```java
private boolean isObstacle(Class<?> object, Object eater) {
  if ((object == Bag.class) && (eater instanceof Bird)) {
    return false;
  }
  //...
}
```
То есть, класс открыт для изменения.

- Ищи еду, а не препятствия.

Сейчас ты ищешь препятствия. Например, для Травоядного все препятствие, кроме травы
```java
private boolean isObstacle(Class<?> object, Object eater) {
  if ((object == Grass.class) && (eater instanceof Herbivore)) {
    return false;
  }
  //...
}
```
Нужно поменять концепцию. Ищи не препятствия, а цель.  

Все объекты, которые встречаются на карте, нужно считать препятствием, кроме еды.  
Искать нужно еду, это цель, а препятствия нужно обходить.  

Этот тип еды(цель) нужно передавать в сигнатуру поиска, как я указывал выше:
```java
public List<Coordinates> find(..., Class<? extends Entity> target) {
  //target- класс цели, которую нужно найти
  //все остальные объекты на карте- препятствия
}
```

**7. abstract class Entity**

- Содержит координату. Но координата нужна только тому существу, которое ходит.  
Поэтому entities должны хранить координату только начиная с уровня `Creature`
```java
private Position position;
```

- Нарушение SRP, зависимость модели от представления- существо хранит спрайт с собственным изображением
```java
private String emoji;
```
Модель(а это модель) не должна зависеть от представления и знать, как ее будут показывать юзеру.  
Потому что в разных средах(консоль, Swing, Android) одна и та же модель может быть показана разными способами- пиксельной картинкой, анимацией etc.  
Спрайты всех существ должны храниться в классе, который распечатывает карту.

+ Вот каким должен быть идеальный Entity для этого проекта:
```java
public abstract class Entity {
  //да, тут совсем пусто
}
```

**8. abstract class Creature extends Entity**

- Методы не должны быть пустыми. Если какой-то метод предназначен для переопределения, он должен быть абстрактным:
```java
public void makeMove(Position tile, MapCanvas mapCanvas) {
}

//ПРАВИЛЬНО:
public abstract void makeMove(Position tile, MapCanvas mapCanvas);
```

**9. interface Action**

Экшен должен выполнять дейсвия с Картой, но не возвращать Карту
```java
public interface Action {
  MapCanvas perform();
}

//ПРАВИЛЬНО:
public interface Action {
  void perform(MapCanvas mapCanvas);
}
```


**10. class SpawnEntitiesAction implements Action**

- Идея `Action` здесь не осмыслена. 

Идея состоит в том, что нужно создать семейство родственных классов, объединенных общим интерфейсом.  
Каждый из этих классов должен делать что-то свое с картой: одна акция должна заселять карту существами, другая делать ходы и т.д.  
Action'ы, изложенные в ТЗ, это вариант реализации паттерна Command. 

Сейчас `SpawnEntitiesAction` у тебя это просто сборник функций, сделанный в стиле процедурного программирования
```java
public class SpawnEntitiesAction implements Action {
  //...
  public void checkTypeFood(Entity e) {...}
  public <T extends Entity> void generateEntity(Supplier<T> e, double percent) {...}
  public void initializePlacement() {...}
  public MapCanvas perform() {...}
}
```

Смысл Action'ов состоит в том, что должен быть общий класс/интерфейс Action и его наследники.  
В каждом экшене должен быть только один публичный метод(не публичных может быть сколько угодно).  
Это вариация паттерна Command- экшены должны быть родственны и одинаково использоваться через полиморфизм. Примерно так
```java
interface Action{
 void execute(Карта карта);
}

class ХодитьAction реализует Action {
  void execute(Карта карта) {
    //обойти всю карту
    //найти каждую креатуру
    //и дать ей пинка чтоб побежала
  }
}

class ДатьСигаретуВсемЗайцамAction реализует Action {
  void execute(Карта карта) {
    //обойти всю карту
    //найти всех зайцев и дать им по сигарете 
  }
}

List<Action> actions = List.of(new ДатьСигаретуВсемЗайцамAction(), new ХодитьAction());
for(Action a: actions) {
  a.execute(карта);
}

/*
Результат: программа обойдет всю карту, найдет всех креатур и даст им пинка, чтобы они побежали.
А каждому зайцу предварительно даст сигарету.
*/
```

Делает ли класс `SpawnEntitiesAction` только одно дело- заселение карты существами?  
Не знаю и не хочу разбираться. Мне достаточно того, что он большой и в нем куча публичных методов- а значит, он не соответствует идее экшенов из ТЗ.

**11. Дублирование кода в AddGrasses и AddHerbivores**

Два класса, которые делают одно и то же- заселя.т в карту существ определенного типа.  
Код в классах дублируется, это нарушение чеклиста ТЗ:
```java
Проблемы и ошибки в коде:
Иерархия классов Action’s: Дублирование кода
```

**12. class Renderer**

- Спрайты существ должны храниться здесь, а не браться из самих существ.

- Стандартные имена индексов в цикле- i,j и это нормально.  
Но иногда лучше использовать более подходящие к случаю имена
```java
for (int i = 0; i < mapCanvas.getMapSize().get(0); i++) {
  for (int j = 0; j < mapCanvas.getMapSize().get(1); j++) {
    Position pair = new Position(i, j);
  }
}

//ЛУЧШЕ:
for (int row = 0; row < mapCanvas.getMapSize().get(0); row++) {
  for (int column = 0; column < mapCanvas.getMapSize().get(1); column++) {
    Position pair = new Position(row, column);
  }
}
```

**13. class Simulation**

- Нарушение DI. Класс не должен инициализировать сам себя.  
В данном случае класс инициализирует сам себя тем, что сам создает карту, а не принимает ее в конструктор
```java
public class Simulation {
  private MapCanvas mapCanvas = new MapCanvas(10, 10);
  //...
}

//ПРАВИЛЬНО:
public class Simulation {
  private final MapCanvas mapCanvas;

  public Simulation(MapCanvas mapCanvas, ...) {
    this.mapCanvas = mapCanvas;
    //...
  }
}
```
Таким образом нельзя создать несколько игровых конфигураций с разными картами.  
*Мартин, "ЧК", гл.11, "Отделение конструирования системы от ее использования"*

- В таких случаях просто называй перехваченое исключеине "ignored"
```java
try {
  executorService.execute(inputTask);
} catch (RejectedExecutionException e) {
  //игнор, ничего страшного
}


//ПРАВИЛЬНО:
try {
  executorService.execute(inputTask);
} catch (RejectedExecutionException ignored) {
}
```

**14. class Main**, содержит точку входа main

- Класс Main отсутствует.  

Есть просто файл "Main.java", в которром нет класса Main, а есть только метод main:
```java
import main.Simulation;

void main() throws InterruptedException {
  Simulation simulation = new Simulation();
  simulation.startSimulation();
}
```
В java нет "глобальных процедур", как в других языках.  
Все методы являются поведением какого-то класса.  
Поэтому должны находиться в классах:
```java
public class Main {
  public static void main(String[] args) throws InterruptedException {
    Simulation simulation = new Simulation();
    simulation.startSimulation();
  }
}
```

+ 👍 Метод main только создает и запускает Симуляцию, это хорошо.

- Не бросай исключения в космос.

Метод main- последняя точка в программе, после которой исключение аварийно закрывает работу программы.
Если ты знаешь, что в программе могут возникнуть исключения, то обработай их внутри программы
```java
public static void main(String[] args) throws InterruptedException
```
Хотя бы просто перехвати, напечатай сообщение "Критическая ошибка, работа программы будет прекращена" и корректно закрой программу.
Сообщение без конкретики- не самый лучший вариант. Но это лучше, чем если программа просто вылетит с красными эксепшенами и непонятными сообщениями.

## ВЫВОД

Хуже всего дела обстоят в классах Карта и Поиск пути.

Подробнее разберись с Action'ами, посмотри ролики на ютубе про паттерн "Command", чтобы получить о нем общее представление.

Для лучшего понимания декомпозиции ООП посмотреть ролики Сергея про шахматы.  
Посмотреть ролики Немчинского про SOLID.

n.132(288)  
#ревью #симуляция 