https://github.com/NikitaPopovsky/Simulation  
[Никита]

После первого рефакторинга. Стало значительно лучше.

## НЕДОСТАТКИ РЕАЛИЗАЦИИ

1. Распечаталось имя енама("COUNT_ENTITY"), а не значение хранимого в нем параметра
```
Размер поля 10х10. Количество каждого существа на карте - COUNT_ENTITY шт. 
```

2. Почему спрайты печатаются через запятые? Надо печатать не как проще, а как красивее. Красивее- без запятых  
![pic](https://github.com/raketareview/simulation_review/blob/master/content/resources/rev-sim124/img0.png) 

## ХОРОШО

+ 👍 Спрайты существ не хранятся в самих существах
+ 👍 Координаты существ не хранятся в самих существах(мне так больше нравится)
+ 👍 Реализовано пауза/пуск во время работы

## ЗАМЕЧАНИЯ

**1. Нейминг**

- Название должно как можно лучше объяснять суть явления. Этот метод возвращает не просто пустую координату, он возвращает пустую случайнуюю координату
```java
Coordinate getEmptyCoordinates()

//ПРАВИЛЬНО:
Coordinate getEmptyRandomCoordinates()  
```

- Предпочитай стандартные шаблоны названий
```java
boolean checkValidation(Node node, GameMap gameMap)
boolean checkTarget(Node current, Set<Coordinate> targets)

//ПРАВИЛЬНО:
boolean isValid(Node node, GameMap gameMap)
boolean isTarget(Node current, Set<Coordinate> targets)
```

- Метод возвращает не одно существо(entity) а много(entities)
```java
public Map<Coordinate, Entity> getEntityByClass(Class<? extends Entity> entityClass)

//ПРАВИЛЬНО:
public Map<Coordinate, Entity> getEntitiesByClass(Class<? extends Entity> entityClass)
```
*Oracle Java code conventions, part."Naming conventions"*  
*Мартин, "Чистый код", гл.2*  
*Ютуб, Немчинский "Как называть переменные, методы и классы?"*

**2. Если в блоке if есть return**(break, continue, throw, exit и т.д.), то else не пишется
```java
if (entityClass == Rock.class) {
  return new Rock();
} else if (entityClass == Tree.class) {
  return new Tree();
}

//ПРАВИЛЬНО:
if (entityClass == Rock.class) {
  return new Rock();
} 
if (entityClass == Tree.class) {
  return new Tree();
}
```

**3. record Coordinate(int x, int y)**

👍 Нет ничего лишнего, это хорошо. Record для координаты- идеально.

**4. class GameMap**

- Нарушение SRP, методы чужих ответственностей. 

Карта должна только хранить существа и обеспечить базовые операции с ними: вставить, выдать одно существо и список всех хранимых существ, удалить.
Если какой-то метод не нужен для обеспечения этого функционала, значит он не принадлежит к ответственности карты, а принадлежит к чужой ответственности.
Здесь методы чужих ответственностей: найти случайную пустую координату, посчитать существ определенного класса etc.

Наверное, для проекта в целом полезно иметь метод, который находит случайную пустую координату. 
Но этот процесс не имеет никакого отношения к единой ответственности карты- хранению существ в себе.  
Для организации хранения/удаления/выдачи существ, карте не нужно находить случайную пустую координату.  

Методы чужих ответственностей должны находиться в тех классах, в интересах которых они работают.
Если один и тот же метод используют разные классы, то метод нужно вынести в отдельный класс, например, BoardUtils.

То, что метод нахождения случайной пустой координаты не относится к отвевтвенности карты, доказать просто.  
Сейчас искать пустую координату нужно для того, чобы разместить на карте существ в случайном порядке.  
Представь, что существа размещаются не в случайном порядке, а на конкретных фиксированных местах.  
В этом случае вообще отпадет необходимость в таком методе. А карта будет все так же хранить существ и обеспечивать к ним доступ.  
Следовательно, этот метод существует не в интересах единой ответственности карты, а в интересах кого-то другого.  
Этот метод сейчас нужен только одному классу- `ActionUtil`, вот там он и должен находиться.

+ 👍 Хороший метод, но мне кажется, он несколько сложноват для использования клиентом
```java
public Map<Coordinate, Entity> getEntityByClass(Class<? extends Entity> entityClass)
```
Вместо него я бы использовал два простых метода:
```java 
public List<? extends Entity> getAllBy(Class<? extends Entity> clazz)  //вернуть существ определенного класса 
public Coordinates getCoordinates(Entity entity)
```
Использование:
```java
//СЕЙЧАС ТАК:
Map<Coordinate, Entity> entities = gameMap.getEntityByClass(Creature.class);

for (Map.Entry<Coordinate, Entity> entity : entities.entrySet()) {
  Creature creature = (Creature) entity.getValue();
  Map<Coordinate, Entity> targets = gameMap.getEntityByClass(creature.getTargetClass());
  for (Coordinate neighbor : GameMapUtil.getNeighbors(entity.getKey())) {...}
  //...
}

//БУДЕТ:
List<Creatures> creatures = gameMap.getAllBy(Creature.class);

for(Creature creature : creatures) {
  Coordinate coordinate = gameMap.getCoordinate(creature);
  List<Coordinate> neighbors = GameMapUtil.getNeighbors(coordinate);
  //...
}
```

- Никогда не возвращай null
```java
private final HashMap<Coordinate, Entity> entities;

public Entity getEntity(Coordinate coordinate) {
  return entities.get(coordinate);  <-- может вернуть null
}
```
Возврат null повышает риск возникновения NullPointerException в программе.  
*Мартин, "Чистый код", гл.7.7-8*  
*Ютуб, Немчинский "Почему нельзя возвращать NULL?"*

- При всех операциях с участием координаты(добавить, выдать, удалить и т.д.), нужно проверять координату на корректность. 
Если координата некорректна(находится вне пределов карты), нужно бросать исключение
```java
public void removeEntity(Coordinate coordinate) {
  entities.remove(coordinate);
}
```
Ближайшая аналогия- стандартные хранилища типа List и массива. При попытке обратитья к ним по несуществующему индексу, бросается исключение.

**5. Пакет utils**

Некоторые классы в пакете не являются утилитными. Например, class Node.  
Утилитные классы содержат только статические методы и не имеют состяояний(то есть, изменяемых полей). 

+ 👍 В целом, с точки зрения SRP, в классе мало нарушений. Те, которые есть- не слишком принципиальные.

**6. class GameMapUtil**

- Это утилитный класс, у него должен быть приватный конструктор.  
Не должно быть возможности сделать экземпляр утилиты.  
*Блох, "Java. Эффективное программирование", изд.3, гл.2.4*

- Сюда можно вынести метод нахождения случайной пустой координаты и метод подсчета количества существ определенного класса.

- Нужно искать не все соседние координаты, а все соседние координаты, находящиеся в пределах карты
```java
public static Set<Coordinate> getNeighbors(Coordinate current) {
  //ищет все соседние координаты  
}

//ПРАВИЛЬНО:
public static Set<Coordinate> getNeighbors(GameMap gameMap, Coordinate current) {
  //ищет все соседние координаты, которые находятся в пределах gameMap
}
```
Если координата находится в центре карты, у нее будет четыре корректных соседних координаты(по вертикали и горизонтали).  
Если в левом верхнем углу- только два. 

Почему нужно искать именно соседние координаты в пределах карты? Потому что класс называется GameMapUtil, а не CoordinatesUtil. 
А значит работа с координатами проводится в контексте(конкретной) карты.

**7. record Node (Coordinate coordinate, Node parent)**

👍 Идеально.

**8. class BreadthFirstSearch**

- Предпочитай возвращать простые объекты, экзотические объекты не предпочитай
```java
public static Deque<Coordinate> find(...)

//ЛУЧШЕ(на мой взгляд):
public static List<Coordinate> find(...)
```
Ииначе ты заставляешь клиента без особой необходимости вспоминать особенности работы редко используемых типов данных.

Тем более, что ты во факту и так возвращаешь List:
```java
public static void main(String[] args) {
  GameMap gameMap = new GameMap(10, 10);
  gameMap.addEntity(new Coordinate(0, 0), new Herbivore(1, 1));
  gameMap.addEntity(new Coordinate(9, 0), new Grass());
  Object path = BreadthFirstSearch.find(new Coordinate(0,0), Set.of(new Coordinate(9, 0)), gameMap);
  System.out.println(path.getClass());
}

//РЕЗУЛЬТАТ:
class java.util.LinkedList
```

- Нарушение SRP.

Поиск должен просто искать путь от точки старта до точки, соответствующей заданным условиям. 
В данном случае- до точки, в которой находится существо нужного класса.
Если поиск получает во входящие аргументы метода конкретный экземпляр цели, или перечень целей как в данном случае, 
значит часть работы поиска уже выполнил кто-то другой. 

То есть, кто-то другой уже нашел цель и передал ее в поиск пути
```java
public static Deque<Coordinate> find(Coordinate start, Set<Coordinate> targets, GameMap gameMap) 
```
На самом деле поиск пути должен сам искать цель и прокладывать к ней путь. Сигнатура метода для этого должна выглядеть примерно так
```java
public List<Coordinates> find(GameMap gameMap, Coordinates start, Class<? extends Entity> target) {
  //ищет путь на карте от точки start
  //до точки, где находится существо нужного класса(напр. target == Grass.class)
}
```
Если для поиска пути нужно предварительно найти координаты цели(целей), пусть это делает сам класс поиска пути.

**9. abstract class Entity**

👍 Идеально
```java
public abstract class Entity {
}
```

**10. abstract class Creature extends Entity**

- Этот параметр не относится к SRP класса `Creature` и не должен тут быть
```java
private boolean isBusy;
```
Объект должен содержать ту информацию, которая характеризует сам объект, а не характеризует ситуацию, в которай этот объект используется.

Условный пример: 
Есть информационная система предприятия, в которой учтены сотрудники.  
Данные, которые описывают сотрудника: ФИО, дата рождения, должность и т.д.
Эти данные должны находиться в классе "Сотрудник".

Поступила задача вручить каждому сотруднику подарок на Новый Год и вести учёт тех, кто получил, а кто нет.  
Неправильный подход: добавить в класс "Сотрудник" поле `boolean isПолучилПодарокНовыйГод2026`.  
Правильный подход: при вручении подарка, заносить сотрудника в список "Получившие новогодний подарок сотрудники".

То есть получение/неполучение подарка не описывает сущность "Сотрудник", а описывает ситуацию, связанную с этой сущностью.

- Не возвращай null
```java
public Class<? extends Entity> getTargetClass() {
  //...
  return null;
}
```

- Нарушение OCP.

Класс не должен знать по именам своих потомков и реализовывать специфическую логику потомков.  
Иначе, при появлении новых потомков, например Bird, придется переделывать код в этом классе
```java
public Class<? extends Entity> getTargetClass() {
  if (this instanceof Herbivore) {
    return Grass.class;
  } else if (this instanceof Predator) {
    return Herbivore.class;
  }
  return null;
}

//ПРАВИЛЬНО:
private final Class<? extends Entity> target;

public Creature(Class<? extends Entity> target, ...) {...}

public Class<? extends Entity> getTarget() {
  return target;
}
```

**11. class Predator extends Creature**

Нарушение конвенции кода. Конструктор должен стоять первым среди методов.

**12. interface Action**

👍 Идеально.
```java
public interface Action {
  void make(GameMap gameMap);
}
```

**13. class MovingToTargetAction implements Action**

Осуществляет передвижение creatures по карте, как шахматист передвигает фигуры на доске.  
Чисто функционально- почему бы и нет. Но это нарушет ТЗ проекта, а это не очень хорошо
```
Creature ...Имеет метод makeMove() - сделать ход.
```

Во-первых, нужно выполнять требования ТЗ. Нельзя на работе нарушать ТЗ просто потому что "я художник, я так вижу".  
Во-вторых, пока ты не умеешь декомпозировать программы лучше Сергея, лучше придерживаться его декомпозиции.

Согласно ТЗ, осуществлять свое передвиженое должна сама креатура в методе `makeMove()`, который сейчас в ней отсутсвует.

Я бы советовал написать этот метод так:
```java
class Creature бла-бла {
  //...
  public void makeMove(GameMap gamemap) {
    //получить из Карты свою координату
    //получить из Поиска пути путь к еде
    //идти к еде, пока не кончится запас хода
    //если дошел к еде- съесть её
  }
}
```

**14. Дробление**

Антипаттерн дробление- разделение одного действия на несколько классов.  
Сейчас есть несколько классов, которые в сумме выполняют одну задачу- осуществляют передвижение существа в целях его пропитания
```java 
class MovingToTargetAction implements Action <-- Выполнение движения к еде
class InteractToTargetAction implements Action <-- Выполнение поедания
```
Рекомендация та же- сделать по ТЗ.

**15. enum Constants**

Константный класс в программе используется неправильно.  

Другие классы обращаются напрямую к константному классу, а должны получать необходимые данные в свой конструктор.  
Это делает классы неуниверсальными и зависимыми от константного класса.  

Условный пример: 
```java
public class Settings {
  public static final int ROOM_NUMBERS = 5;
}
```

Варианты использования:
```java
public static void main(String[] args) {
  House house = new House();
  //oth code
}

public class House {
  private final int roomNumbers;

  public House() {
    this.roomNumbers = Settings.ROOM_NUMBERS;
  }

  public int getRoomNumbers() {
    return roomNumbers;
  }
}

//ХОРОШО:
public static void main(String[] args) {
  House house = new House(Settings.ROOM_NUMBERS);
  //oth code
}

public class House {
  private final int roomNumbers;

  public House(int roomNumbers) {
    this.roomNumbers = roomNumbers;
  }

  public int getRoomNumbers() {
    return roomNumbers;
  }
}
```
Про классы констант, конфигурации и их использование я писал [ТУТ](https://t.me/zhukovsd_it_chat/53243/176984)

**16. class InitializationCreaturesAction implements Action**

Неправильное использование константного класса.  
Необходимые данные класс должен получать в конструктор, а не самостоятельно загружать из константного класса
```java
public class InitializationCreaturesAction implements Action {
  //...
  @Override
  public void make(GameMap gameMap) {
    int countEachEntity = Constants.COUNT_ENTITY.getValue();
    ActionUtil.spawn(Predator.class, gameMap, countEachEntity);
    //...
  }
}

//ПРАВИЛЬНО:
public class InitializationCreaturesAction implements Action {
  //...
   public InitializationCreaturesAction(int count) {
    this.count = count;
  }

  @Override
  public void make(GameMap gameMap) {
    ActionUtil.spawn(Predator.class, gameMap, count);
    //...
  }
}
```

**17. class CreationEntityAction implements Action**

Нарушение DRY, дублирование кода
```java
int difference = MinCountEntity - gameMap.getCountEntityByClass(Grass.class);
if (difference != 0) {
  ActionUtil.spawn(Grass.class, gameMap, difference);
}

difference = MinCountEntity - gameMap.getCountEntityByClass(Herbivore.class);
if (difference != 0) {
  ActionUtil.spawn(Herbivore.class, gameMap, difference);
}

//ПРАВИЛЬНО:
ActionUtil.spawn(Herbivore.class, gameMap, gameMap.getCountEntityByClass(Grass.class)) ;
ActionUtil.spawn(Herbivore.class, gameMap, gameMap.getCountEntityByClass(Herbivore.class)) ;
```

**18. final class ActionFactory**

Почему бы и нет.

**19. enum Commands**

Если пытаются получить енам по ключу, которого нет, нужно кидать исключение- такого значения нет, запрос некорректен
```java
private final String value;

public static Commands getByValue(String value) {
  for (Commands com : Commands.values()) {
    if (com.getValue().equals(value)) {
      return com;
    }
  }
  return Commands.EMPTY;
}

//ПРАВИЛЬНО:
public static Commands getByValue(String value) {
  for (Commands com : Commands.values()) {...}
  throw new IllegalArgumentException("Illegal value: " + value);
}
```

**20. class RenderMap**

- В switch-case в default нужно кидать исключение, а не возращать дефолтный спрайт. 

Потому что при добавлении нового существа в проект, можно забыть добавить его спрайт в распечатку.
И тогда существо будет жить на карте своей жизнью, но мы этого не увидим- вместо этого существа будет распечатываться дефолтный спрайт
```java
private static String getDrawEntity(Entity entity) {
  return switch (entity) {
    case Herbivore _ -> DrawEntity.HERBIVORE.getValue();
    case Predator _ -> DrawEntity.PREDATOR.getValue();
    //...
    case null, default -> "";
  };
}
```

- Нет никакой необходимости предварительно преобразовывать карту в массив изображений. Печатай карту сразу
```java
private static String[][] gameMapToString() {...}

//ПРАВИЛЬНО:
public static void printMap(GameMap gameMap, int countStep) {
  for(int y = 0; y < gameMap.getHeight(); y++ ) {
    for(int x = 0; x < gameMap.getWidth(); x++ ) {
      Coordinates coordinates = new Coordinates(x, y);  
      //если по координате пусто- распечатать спрайт земли
      //если по координате есть существо, 
      //распечатать спрайт существа,
  } 
  System.out.println();  //переход на новую строку
}
```
Представим, что у нас есть карта 100x100.  
Тогда каждый раз при распечатке нужно будеть формировать промежуточный массив изображений на 10.000 спрайтов, это избыточно.

- Избыточно 
```java
private static String getDrawEntity(Entity entity) {
  return switch (entity) {
    case Herbivore _ -> DrawEntity.HERBIVORE.getValue();
    case Predator _ -> DrawEntity.PREDATOR.getValue();
    //еще 4 строки
    case null, default -> "";
  };
}

//ЛУЧШЕ:
private String toSprite(Entity entity) {
  String name = entity.getClass().getSimpleName().toUpperCase();
  DrawEntity type = DrawEntity.valueOf(name);
  return type.getValue();
}
```

**21. class Simulation**

+ 👍 Принимает в конструктор достаточное количество зависимостей, в том числе списки действий. 
Это позволяет делать майны с разными игровыми конфигурациями, не меняя код в классе `Simulation`
```java
public Simulation(GameMap gameMap, List<Action> initActions, List<Action> turnActions) 
```

- На каждом ходе нужно выполнять только турн-экшены. Инициализационные екшены нужно выполнять в конструкторе
```java
public void nextTurn() {
  List<Action> actions = null;
  if (countStep == 0) {
    actions = initActions;
  } else {
    actions = turnActions;
  }
  for (Action action : actions) {
    action.make(gameMap);
  }
  //...
}

//ПРАВИЛЬНО:
public Simulation(...)  {
  //...
  executeActions(initActions);  
}

public void nextTurn() {
  executeActions(turnActions);  
  //...
}

private void executeActions(List<Action> actions) {
  for (Action action : actions) {
    action.make(gameMap);
  }
}
```

**22. class Main**, содержит точку входа main

+ 👍 Только создает и запускает Симуляцию, это хорошо.

## ВЫВОД

После рефакторинга архитектура программа стала значительно лучше.  
Есть важные с точки зрения архитектуры вещи, которые стоило бы переделать: поиск пути, метод движения в самой креатуре.  
В целом, норм, можно переходить к следующему проекту.

n.124(273)  
#ревью #симуляция 